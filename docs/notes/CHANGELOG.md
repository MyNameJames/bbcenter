# Changelog — BBCenter V2

> ประวัติ phase/refactor ทั้งหมด — ย้ายออกจากหัว INDEX.md + CLAUDE.md (2026-06-02) เพื่อลด token budget ของ entry docs
> แต่ละ bullet = 1 phase entry เรียงใหม่→เก่า (ล่าสุดอยู่บน). ไม่ต้องโหลดทุก turn — เปิดเฉพาะตอนสืบประวัติ phase

---

## INDEX.md — symbol / template / CSS-JS history

_44 entries · ล่าสุดอยู่บน_

- 2026-07-27 (**mileage: ลบ dead code รอบ 2 — booker filter + per-vehicle breakdown** — `views/vehicle/vehicle_mileage.py` ไฟล์เดียว, ไม่แตะ template/JS): ต่อจาก cost-filter cleanup วันเดียวกัน (entry ถัดไป) — grep ตอน verify แล้วเจอ dead อีก 2 ชุด **คนละเวลาคนละเหตุ**. **(A) booker filter** ตายพร้อม `bb_filter` 2026-07-22 (CHANGELOG จดเองว่า "เพิ่มระหว่าง session แล้วถอดออกทั้งหมด สุทธิ = ไม่มี filter ผู้จอง") แต่ backend ค้าง: route ยังยิง 2 query ต่อการโหลดหน้า (`SELECT DISTINCT user_id` + `User.filter(id.in_(...))`) ส่ง `bookers_all` เข้า template ที่ไม่มีที่ render + `_query_mileage_bookings()` ยังมี branch `JOIN User ... ILIKE(full_name|username)` ที่ไม่มีทางเข้าถึง → ลบ `bookers_all`/`booker_ids`, ตัด param `f_booker`, `_parse_mileage_filters()` เลิกอ่าน `booker_q` (tuple **10→9 ค่า**), `f{}` ตัด key. **(B) per-vehicle monthly breakdown** — `_build_vehicle_breakdown()` + kwargs `breakdown`/`breakdown_totals`/`curr_year`/`curr_month` เคยป้อน panel **"รายละเอียดค่าน้ำมัน · ปี xxxx"** (ตาราง รถทุกคัน × 12 เดือน + คอลัมน์/แถวรวม + คลิกตัวเลขเพื่อกรองตารางล่าง + ปุ่ม toggle "ซ่อนรายเดือน" + mobile pager ทีละเดือน) ที่หายไปตอน rewrite template `mileage_admin.html` ยกไฟล์ใน commit `5e0e7dd` "edit achitect" (2026-05-15) — **dead มา 2 เดือนกว่า ไม่เกี่ยวกับ redesign รอบ ก.ค. เลย**, ต้นทุน = query booking approved ทั้งปีปัจจุบัน + วน `_compute_mileage_cost()` ต่อใบ ทุก request. **ตัดสินใจ:** 2026-07-21 เคยจดว่า "ยังไม่ตัดสินใจตัด/ทำ chart ต่อ" → เคาะ **ลบ** เพราะเก็บไว้ = จ่าย query ให้ feature ที่ไม่มีอยู่ ส่วนถ้าจะทำ chart จริงรูปข้อมูลคงไม่ใช่แบบนี้พอดี และเขียนใหม่ ~18 บรรทัด (git history หาเจอ). **Import กำพร้าที่ตามมา** ลบด้วย: `User`, `or_` (จาก A), `TH_MONTHS` (จาก B — template นิยาม `{% set TH_MONTHS %}` ของตัวเองอยู่แล้ว ไม่กระทบ). **Verify:** grep 8 ชื่อ (`bookers_all|booker_ids|booker_q|f_booker|breakdown_totals|_build_vehicle_breakdown|curr_year|curr_month`) ทั่ว `app/`+`tests/` = 0 hit · pytest 100 ผ่านครบ. ⚠️ URL เก่า `?booker_q=..` จะถูกเมิน (ไม่ error). **พบแต่ยังไม่แก้ (นอก scope รอบนี้ กำพร้ามาก่อนอยู่แล้ว):** import ที่ไม่ถูกใช้ในไฟล์นี้อีก 6 ตัว — `_fmt_date_th`, `_lookup_budget_for_booking`, `current_app`, `adminfleet_bp`, `admincost_bp`, `driver_bp`)

- 2026-07-27 (**mileage: ลบ dead code cost-filter (`cost_ceiling`/`cost_min`/`cost_max`)** — `views/vehicle/vehicle_mileage.py` ไฟล์เดียว, ไม่แตะ template/JS): ปิด follow-up ที่ค้างจาก 2026-07-22 (ถอด `bb_filter` ออกจากหน้า mileage แล้ว cost range-slider หายไปด้วย แต่ backend ยังคำนวณอยู่). **ลบ `_calc_cost_ceiling()` ทั้งฟังก์ชัน** — เคย query `VehicleBooking` status=approved **ทุกใบไม่จำกัด date filter** แล้ววน `_compute_mileage_cost()` ต่อใบเพื่อหา max fuel_cost มาปัดขึ้นหลักพันเป็น `max` ของ slider (ตั้งใจไม่จำกัด date ตั้งแต่ 2026-07-02 เพื่อแก้ bug ceiling ยุบเป็น 0 เมื่อช่วงวันที่ที่เลือกไม่มี booking) = query หนักสุดของหน้าและโตตามอายุระบบ → ลบแล้วได้ perf คืนตรงๆ ไม่ใช่แค่ความสะอาด. **Dashboard:** `_parse_mileage_filters()` เลิกอ่าน `cost_min`/`cost_max` (คืน tuple **12→10 ค่า**) · `_build_mileage_rows()` ตัด 2 param + 2 if · `f{}` dict ตัด 2 key · `render_template` ตัด `cost_ceiling=`. **Export:** `_filter_and_calc_mileage_rows(bookings, f_status)` ตัด 2 param + 2 if · `mileage_export()` เลิกอ่าน query 2 ตัว (export link sync มาจาก `#filterForm` ซึ่งไม่มี input พวกนี้แล้ว → dead เส้นเดียวกัน). **Verify:** grep `cost_min|cost_max|cost_ceiling` ทั่ว `app/` + `tests/` = 0 hit · pytest 100 ผ่านครบ (ไม่มี test ตัวไหนเคยแตะฟังก์ชันที่ลบ). ⚠️ **breaking เฉพาะ URL เก่าที่ bookmark ไว้** — `?cost_min=..&cost_max=..` จะถูกเมิน (ไม่ error) ไม่กรองให้แล้ว. ถ้าอยากได้ filter ช่วงค่าน้ำมันคืน = ทำ `ue_chip` ตัวใหม่ ไม่ใช่ revert commit นี้)

- 2026-07-22 (**mileage admin: layout flatten + filter→ue_chip migrate + shared combo/daterange portal+mutual-exclusion fix** — `vehicle_mileage.{html,js}`, `components.css`, `ue.css`, `bb-components.js`, `bb-daterange.js`, `_components/bb/daterange.html`): Zone 1b (4 KPI action cards, added 2026-07-21) **removed entirely** + desktop table `.bb-card` wrapper removed (flush, no boxed frame) — ghost-KPI summary strip → UE-style title+description text block reusing existing `sumAll*`/`sel*` element ids (no JS logic change); fixed legacy `#sumAllCost` hardcoded 26px/800-weight override in `ue.css` left over from old KPI-pill days. **Filter chips migrated**: vehicle/driver/budget_type/budget_sub จาก page-local `bb_ml_dd` macro (custom `.bb-select`/`.bb-menu` dropdown) → canonical `ue_chip_dd`/`ue_chip_opt` (radio single-select, `_components/bb/ue_chip.html`); ทั้ง 4 ย้ายออกจาก `bb_filter` flyout เข้า toolbar chip แถวตัวเอง → **`bb_filter` (cost-range slider + "ล้างการเลือก") ถอดออกจากหน้านี้ทั้งก้อน** — ⚠️ `views/vehicle/vehicle_mileage.py` ยังคำนวณ `cost_ceiling`/อ่าน `f.cost_min`/`f.cost_max` = dead/unreachable code path ตอนนี้ (ไม่ได้แก้ backend — ค้าง follow-up). Booker filter (`bb_combo`) เพิ่มระหว่าง session แล้วถอดออกทั้งหมดพร้อม JS wiring (สุทธิ = ไม่มี filter ผู้จองในหน้านี้). **Shared component behavior (กระทบหน้าอื่นด้วย ไม่ใช่แค่หน้านี้):** `.ue-chip` base restyle (flat gray `n50`/`n700`, ตัด color-on-selected behavior ทิ้ง); `bb_combo`(`initCombo`, bb-components.js) + `bb_daterange`(bb-daterange.js) popover **portal ไป `document.body` (`position:fixed`)** ตอนเปิด (เดิม `position:absolute` ใน parent) + **cross-widget popover mutual-exclusion** ผ่าน convention ใหม่ `window.__bbActivePopoverClose` (เปิด popover ใหม่ปิดตัวที่เปิดค้างอัตโนมัติ); `bb_daterange` footer ปุ่ม "ยกเลิก"→"ปิด" ทุกหน้า (แก้ latent bug — เดิมกดแล้วแค่ล้าง draft date ไม่ได้ปิด popover จริง). New token `--bb-n700: #4A4A4A` (components.css `:root`) เติมช่องว่างใน neutral scale `n50/100/200/300/500`. **Row bug fixes** (`vehicle_mileage.js`): `clearSelection()` no-op บน custom `<span>` checkbox (`.checked=false` ไม่มีผล) → ใช้ `setCheck()`; row-click handler exclude แค่ `button, a` ไม่รวม `[role="button"]` → เมนู "⋮" (`<span role="button">`) เคย toggle เลือกแถวไปด้วย → แก้ selector; `openMileage()` เคย crash ที่ `querySelector('svg, i').style` (`ms-icons.js` แปลง `<i data-lucide>`→`<span data-lucide>` ไม่เหลือ svg/i) → แก้ selector + null guard. Empty-state icon hardcode Material Symbols `note_stack` → convention เดิม `<i data-lucide="note-stack">` + `.bb-empty-icon .material-symbols-outlined{font-size:1.75rem}` (Material Symbols คุมขนาดด้วย font-size ไม่ใช่ width/height).)

- 2026-07-21 (**design v2.1 "ink คือโครง เขียวคือสัญญาณ" — component redesign Batch 0-3** — `core/css/components.css`, `core/css/ue.css`, `_components/bb/{badge,button,table,drawer}.html` (drawer ใหม่), `_components/render/{_status,_button,_desc,_section,_drawer}.html`, `components/{button,badge,table,drawer,__init__}.py` (drawer.py ใหม่), `app.py` (gallery), `dev/components.html`, `vehicle_admin.js`, `vehicle_mileage.{html,js}` + 4 mockup ใน `static/core/`): กลับทิศจาก v2.0 หลังลองทำ component ชุดจริง — **v2.0 ให้เขียวทำทุกอย่าง (ปุ่ม+active+link+tint) → หน้าเดียวมีเขียว 6-7 จุด เขียวเลิกเป็นสัญญาณ**. v2.1 เอา **ฐานสี monochrome จาก `ubereats.design.md`** (ink `#000000` · canvas · surface-1 `#F3F3F3` · surface-2 `#E8E8E8`) แล้วให้ **เขียวเหลือ 2 หน้าที่: พื้น tint `#EAFBF2` + ลิงก์/ghost `#0B7A3E`** — ปุ่มหลัก/tab active/pagination active = **ink**. คง ramp เทา (`mut #757575`/`n500`/`n400`) แม้ spec ให้ `ink-muted = #000000` เพราะ dashboard มี label/meta/cell รอง ถ้าดำหมดตารางอ่านไม่ออก. **Batch 0 (token):** `:root` เหลือที่เดียวที่ `components.css` — **ลบ token block ใน `ue.css`** (ปิด §14 P2 "2 `:root` ซ้อน") · เพิ่ม `--bb-ink`/`--bb-ink-h`/`--bb-n400`/`--bb-n500`/`--bb-body`/`--bb-r-surface`/`-pill`/`-flush` · `--bb-accent-i`/`--bb-chip-*`/`--bb-r-xs..xl` เหลือเป็น alias deprecated · เงาดำ 2 ระดับ · ring ดำ. **Batch 1 (control):** Button radius pill + 15px + สูง 40/32 + **ตัดเงาทิ้ง** + `is-danger-sec` (ขาว+ขอบ+ตัวแดง) + `is-block` + disabled เป็นพื้นเทาแทน opacity (opacity ทำตัวหนังสือจางจนอ่านไม่ออก) · Search border 2px→1px + focus ink+shadow-sm · Tabs active ink + count active ink-fill · Dropdown trigger 1px h40, active = ink fill, menu radius 12→8 + `.bb-menu-item.is-locked`/`.bb-menu-note`/`.bb-menu-foot` ใหม่. **Batch 2 (data):** **ตารางเปลือย** — ตัดกรอบ/radius เหลือ border-top+bottom + hairline คั่นแถว, thead ไม่มีพื้นเทา, **row py 24** (ที่ว่างเป็นตัวแบ่งแทนเส้น), หัวตาราง accent→`mut` · เพิ่ม `.bb-row-group`(+`.is-accent`)/`.bb-row-locked`/`.bb-cell-sub`/`.bb-cell-link`/`tr.is-sel` + row key `_group`/`_locked`/`_sel` + `Column(sub=)` ใน `bb_table_v2` · Status เป็น **badge radius 8 pad 6/10 + icon เสมอ — ลบ `.bb-dot` ทิ้งทั้งระบบ** (9 จุดใน js/template แทนด้วย icon ประจำ tone; `ok` คงเป็นเขียวตามความตั้งใจ แก้ปัญหาชนเขียว accent ด้วย icon+label แทนการย้ายเฉด) · Pagination pill 36px + active ink + `.bb-pag-size`. **Batch 3 (drawer):** ★ `.bb-drawer` (แผงขวา 420px · flex 3 ชั้น head นิ่ง/body scroll/foot นิ่ง — ใช้แทน Modal เมื่อต้องเห็น list เบื้องหลัง P2 Workspace/Queue) + `.bb-section` + `.bb-desc` (grid 2 คอลัมน์ ไม่ใช่ `<table>`) → object `Drawer`/`Section`/`DescList` · Callout `is-info` เดิม `accent-bg`+`accent-h` = **3.4:1 ตก AA** แยกเป็น info น้ำเงิน / ok เขียว. **Bug ที่เจอระหว่างทาง:** `.bb-table-actions` มี `display:flex` แต่ถูกใส่บน `<td>` โดยตรง → cell หลุดจาก table layout ไม่ร่วมคำนวณความสูงแถว **เส้นคั่นแถวเหลื่อม** → แก้เป็น `text-align:right` + `inline-flex` ที่ตัวปุ่ม. **Timeline ไม่แตะ** (user เลือกคงของเดิม). ปิด §14 P1 (a11y) + P2 (token) + P5 gap `Button` (object รับ `href`/`target`/`title`/`mobile_icon` ครบ). ⚠️ **หน้าเดิมทุกหน้าที่ใช้ `.bb-*` เปลี่ยนหน้าตาทันที** (ปุ่มเขียว→ดำ · ตารางเปลือย · status badge) — ต้องไล่ดูด้วยตาทีละหน้า. mockup เทียบก่อน/หลัง: `mockup-bb-v2.html` (token) + `-batch1` (control) + `-batch2` (data) + `-batch3` (drawer))

- 2026-07-21 (**design_guideline v2.0 "เขียวคือของจริง"** — doc-only: `design_guideline.md` 313→472 บรรทัด, `CLAUDE.md`, `INDEX_ui.md`; **ไม่แตะ code สักไฟล์**): guideline เดิมเขียน accent น้ำเงิน `#4081EC` แต่ `ue.css` override เป็นเขียว `#06C167` มาตั้งแต่ 2026-07-11 → หน้า mileage ที่ user ใช้จริงเป็นเขียว **doc ขัดโค้ดมา 10 วัน**. v2.0 ตัดสินให้ **โค้ดชนะ** แล้วเขียน doc ตาม + merge doctrine จาก `ubereats.design.md` (UE marketing) เข้ามา 4 ข้อ. **9 การตัดสินใจ** (§11): (1) accent = เขียว `#06C167` **fill-only** + `#0B7A3E` สำหรับ text/border — คำนวณ WCAG พบ `#06C167` บนขาว = **2.38:1 ตกแม้เกณฑ์ UI 3:1** จึงต้องแยก 2 token, `#0B7A3E` = 5.43:1 ผ่าน AA, ตัวขาวบนเขียว = 8.83:1 (2) neutral เทาอมฟ้า cool → **เทากลาง** 4 step (ยืม `#F3F3F3`/`#E8E8E8` จาก UE marketing เติมช่องที่ `ue.css` ยุบซ้ำ) (3) **rem → px** + ตัด root font scaling ทิ้ง (ไม่เคย implement จริง) cap width 1400 แทน (4) **ยกเลิกข้อห้าม weight 700/800** → allowlist 400/500/600/700/800 + type 2 ชั้นแบ่งด้วย weight (≥700 display / ≤600 interface) (5) **radius binary** 5 step (4/6/8/12/16) → เหลือ `8` (พื้น) / `pill` (กดได้) / `0` (flush) — กฎเดียว "กดได้ไหม" (6) เงา cool-tint 3 ระดับ → **ดำ 2 ระดับ** (7) **`_base_ue.html` = Z0 App Shell target** (เดิม page-opt-in "ไม่มี timeline unify") — migrate ทีละหน้า ไม่มี deadline (8) UE marketing = ยืมแค่ doctrine (binary radius · type 2 ชั้น · gray 2 step · shape discipline) **ห้ามลอกสี/ฟอนต์/ความโปร่ง** เพราะปาเลตต์ ink+white ของมันพึ่งรูปถ่ายซึ่ง dashboard ไม่มี (9) Stripe ลดจาก "สีหลัก" → craft only, UE Manager ขึ้นเป็น ref หลัก. **§14 drift ledger ใหม่** — P1 a11y (`.bb-btn.is-ghost` + `.bb-input:focus` ใช้เขียว 2.38:1 = bug จริงบนหน้า mileage ตอนนี้) · P2 token (2 `:root` ซ้อน) · P3 binary radius 7 จุด · P4 เล็กน้อย (letter-spacing -0.5px โดนไทย, icon size) · **ค้างเคาะ: เฉด `success` `#16A34A` ชนเขียว accent** ชั่วคราวบังคับ icon+label คู่. เพิ่ม mockup อ้างอิง `static/core/mockup-ubereats-marketing.html` (render จาก `ubereats.design.md` — reference only ห้ามลอกสี).)

- 2026-06-14 (**Zendenta redesign → prod: blue rebrand + vehicle.html theme** — `tokens.css`, `design-system.css`, `vehicle.css`, `vehicle_zendenta.css` (ใหม่), `vehicle.html`): **(1) Global blue token** — `--vc-primary` + `--vc-accent` navy/indigo → **น้ำเงิน `#0046FF`** (hover `#003AD6`, dark `#00318F`, light `#E6EDFF`, border `#B9CCFF`, ring `rgba(0,70,255,.18)`); `--vc-fg` คงเป็น navy `#1C2E4A` = สี text. มีผลทุกหน้า (เหตุผล: user ขอน้ำเงินเป็นสีหลัก, Zendenta-inspired). **(2)** `design-system.css` เพิ่ม util `.text-header` (Sukhumvit light/300) + `.text-header-bold` (700). **(3) vehicle.html** — page head เป็น **fuel-header pattern** (fuel-title/-subtitle/-header-actions + ปุ่ม จองรถ vc-btn-primary) + **`.cal-toolbar`** (grid 1fr/auto/1fr ให้ `#currentMonthLabel` กลางเสมอ); weekday header ชื่อเต็มบน desktop (`d-none d-md-inline`/`d-md-none`); `<body class="zd">` + โหลด `vehicle_zendenta.css`. **(4) vehicle_zendenta.css (ใหม่, scope `body.zd`, vehicle only)** — icon ใหญ่ (sidebar 21/header 22) + calendar restyle "ภาพ 2": grid-head พื้นใส, date-number มุมขวาบน, today=เลขปกติ, selected=วงกลมน้ำเงินทึบ (วงกลมเดียว), other-month opacity .5, event pill. **(5) vehicle.css** ลบ dead block page-head (`.vrc-phead-*/.vrc-nav-*/.vrc-today-btn/.vrc-cta-btn`); `.event-card` → pill base.)
- 2026-06-12 (**LINE notification channel (ช่องทางที่ 3)** — `core/line_service.py`, `core/broadcast.py`, `core/line_webhook.py` ใหม่ + `notification_service.py`, `models/user.py`, `app.py`, 8 vehicle controllers): เพิ่ม LINE Messaging API (Official Account) — **LINE Notify ตายแล้ว เม.ย. 2025 ใช้ Messaging API เท่านั้น**. **สถาปัตยกรรม:** (a) `line_service.py` channel impl — `_push_group`/`_push_user`/`reply` + notify_* 5 ตัว (ชื่อตรงกับ telegram_service, plain text); (b) `broadcast.py` group dispatcher — รวม Telegram+LINE → 8 vehicle controllers เปลี่ยน import จาก `telegram_service` เป็น `broadcast` (call site เดิมไม่แตะ); (c) per-user DM hook ใน `notification_service._create()` — mirror ทุก in-app event ให้ user ที่มี `line_user_id` (graceful skip ไม่ rollback); (d) ผูกบัญชี `line_webhook.py` (blueprint `core_bp`) — `POST /line/webhook` verify X-Line-Signature (HMAC-SHA256) + โค้ด 6 หลัก, `GET /line/link` หน้าโค้ด (template `core/line_link.html`). **DB:** `User.line_user_id` (unique) + `line_link_code` (migration `2026-06-12_user-line-id.sql`). **ENV (.env):** `LINE_CHANNEL_ACCESS_TOKEN`/`LINE_CHANNEL_SECRET`/`LINE_GROUP_ID` (graceful skip ถ้าไม่มี). **Telegram:** เปลี่ยน token = แก้ `.env` + restart (ไม่แตะ code). ⚠️ webhook ต้อง public HTTPS reachable.)
- 2026-06-12 (**Phase 5 #15: booking state machine กลาง** — `vehicle_workflow.py` ใหม่, `vehicle_admin.py`, `vehicle_common.py`, `tests/test_booking_workflow.py`): สร้าง `vehicle_workflow.py` — `ALLOWED_TRANSITIONS` dict (5 status × frozenset ของ to_status ที่ allowed), `guard_budget(booking)` (เช็ค active budget ก่อน approve; personal/None → skip), `apply_transition(booking, to_status, actor_id)` (validate transition + set status + updated_by; ไม่ commit). เพิ่ม `require_vehicle_admin` decorator ใน `vehicle_common.py` (ใช้กับ route ใหม่ในอนาคต). **Fix gap:** `admin_assign` approve path ไม่เคยเช็ค budget active → เพิ่ม `guard_budget()` call ก่อน approve; ถ้าไม่มีงบ active → 400 `{'ok': False, 'msg': ...}` (ป้องกัน booking ค้างอนุมัติโดยไม่มีงบรองรับ). Tests: `test_booking_workflow.py` 13 tests (unit ALLOWED_TRANSITIONS/apply_transition/guard_budget + route admin_assign approve/reject ทุก path))
- 2026-06-12 (**Phase 2: auto-reject overdue bookings cron** — `notification_cron.py`, `notification_service.py`): เพิ่ม cron job `auto_reject_overdue_bookings` ทำงาน 08:10 BKK ทุกวัน — ค้นหา booking ที่ status ∈ {pending, waiting_approver} และ `start_datetime < now` แล้ว set `status='rejected'`, `reject_reason='เลยกำหนดเดินทาง — ระบบยกเลิกอัตโนมัติ'`, `updated_by=None` + notify owner 1 ใบ (in-app เท่านั้น ไม่ส่ง Telegram); idempotent (รันซ้ำไม่มีผล); registered ใน `init_scheduler` ด้วย id=`auto_reject_overdue`; **ไม่แตะ approved** (ทริปเกิดจริงแล้ว รอปิดทริปได้). Notify: `notify_auto_rejected()` Event #17 (ntype=warning, icon=rejected). Tests: `tests/test_auto_reject_cron.py` 6 tests; `conftest.py app` fixture เพิ่ม StaticPool (cron เปิด app_context ตัวเอง).)
- 2026-06-12 (**Phase 1: cancel-refund rework + guards** — `vehicle_booking.py`, `vehicle_admin.py`, `vehicle_budget.py`, `vehicle_budget_service.py`): **ลบ refund system** ออกจาก cancel/reject/delete flow — งบถูกหักเฉพาะตอนปิดทริป (mileage) ไม่ใช่ตอน approve; `refund_for_mileage`/`rededuct_for_mileage` ยังอยู่ (ใช้โดย `override_fuel`). **Guards ใหม่:** (a) `cancel_booking` — owner: status ∈ {pending, waiting_approver} เท่านั้น (ไม่มี approved อีกต่อไป); admin: +approved; ถอด refund call + ยุบ flash เป็น branch เดียว (b) `delete_booking` — block ทุกคนรวม admin ถ้ามี `mileage.budget_deducted_at` เพื่อกัน ledger orphan; ถอด refund call (c) `admin_revert_booking` — guard: ห้ามถ้ามี deduct; source ∈ {approved, waiting_approver, rejected}; set `updated_by` + เคลียร์ `reject_reason`; fix `submitRevert()` JS เช็ค `res.ok`/`data.ok` ก่อน patch UI (d) `budget_manage` action `refund_booking` → **`cancel_booking`** (ไม่ refund, แค่ flip status; hidden input + handler rename); ถอด refund calls จาก `approve_booking` admin/approver reject path + `admin_assign` reject path. **ลบ** `refund_for_booking()` จาก `vehicle_budget_service.py` + test ที่เกี่ยวข้อง. **UI:** `canCancel` Jinja แก้ owner guard; confirm text ตัด "งบจะถูกคืน". **Tests:** เพิ่ม `route_app`/`client`/`login` fixtures ใน `conftest.py` (StaticPool) + `tests/test_booking_cancel_guards.py` 10 tests.)
- 2026-06-02 (**vehicle_admin sectionBefore polish + admin edit-any-status**: **Spacing** (`vehicle_admin.css`) — `#sectionBefore .va-list { padding:0 space-5 space-5 }` (rows align filter-tabs gutter + bottom breathing room; `#sectionAfter`/tripList ไม่กระทบ); `.va-filter-tabs` margin bottom `space-4`→`space-3`; `.bl-content` gap `2px`→`space-1`; mobile `@max-width:575.98px` gutter `space-3`. **buildRowActions** (`pages/vehicle-admin.js`) — pending approve ลบคำ "อนุมัติ" → **pencil icon** (`vc-btn-primary vc-btn-icon`, title คงเดิม); pending reject ลบคำ "ปฏิเสธ" → **circle-x icon** (`vc-btn-secondary vc-btn-icon`); `waiting_approver`/`forwarded` ลบ `.bl-status-text` "รอ Approver" → **pencil edit** (`openAssignModal(id,'edit')`) = admin แก้ assignment ได้ทุกสถานะ แม้ส่ง approver/อนุมัติแล้ว (backend `admin_assign` vehicle_view.py:1838 เช็กแค่ `is_vehicle_admin()` ไม่มี status guard; submit map `edit`→`assign_action='approve'`); `rejected` ยังว่าง; status โชว์ผ่าน title badge. **STATUS_ICON** approved `check-circle` (lucide normalize → `lucide-circle-check-big` ตัวหนา) → `circle-check` (`lucide-circle-check` ตัวเบา ตรงกับ collapsed bar/modal/js). **`.bl-card--group`** ลบ `background:green-bg` → green **outline** อย่างเดียว (border `green-border`, hover `green`; `.bl-icon--group` คงสีเขียว); group expand smooth — `.bl-grp-collapse.collapsing { transition:height var(--vc-dur-base) var(--vc-ease-out) }` override Bootstrap `height .35s ease`. **Modal backdrop** (4 modals scoped) `inset:0 + width:100% + height:100%/100dvh` override BS5.3 `width:100vw;height:100vh` (กัน scrollbar-overshoot / ขอบจอไม่โดนคลุม → overlay เต็มจอ; chrome ทุกตัว z-index < backdrop 1050 ไม่ใช่ปัญหา z-index).)

- 2026-05-24 (**vehicle_admin booking row Phase B** (sectionBefore selection mode): เพิ่ม visible checkbox บน selectable rows. **CSS** `.va-list--selecting` parent class (toggle ผ่าน `toggleGroupMode/cancelGroupMode/toggleNotifyMode/cancelNotifyMode`) → grid `22px 40px 1fr auto` ทุก card; `.bl-sel-check` 18×18 (border-radius 4, accent-color fg; modifier `--notify` accent-color blue) + animation `va-slide-in-left 220ms`; placeholder `.bl-sel-slot` (18×18 ว่าง) สำหรับ row ที่ select ไม่ได้ — รักษา alignment; reduce-motion guard เพิ่ม `.bl-sel-check`. **JS** `pages/vehicle-admin.js` — `renderSingleRow` เพิ่ม `checkboxSlot` (pending+groupMode→checkbox; approved+notifyMode→checkbox.notify; mode-but-not-selectable→empty slot); `renderGroupRow` แยก `titleBadge` ("N งานรวม") ออกจาก leftmost `grpCheckboxSlot` (notifyMode + approved members); `toggleGroupMode/toggleNotifyMode` `add` class, `cancelGroupMode/cancelNotifyMode` `remove`. **Functional logic** เดิม (`groupSel/notifySel`, `updateMergeBtn`, `confirmMerge`→assign modal `group_new`) คงเดิม.)
- 2026-05-24 prev (**vehicle_admin booking row Phase A** (sectionBefore visual revamp): `vehicle_admin.css` section 10 rewrite — `.bl-card` revert จาก Phase 5 flat-rail (`grid 3px 1fr auto` + `border-bottom`) กลับเป็น **bordered card** (`border 1px + radius-md + grid 40px 1fr auto`); gap ผ่าน `.bl-card + .bl-card { margin-top:space-2 }`. ลบ `.bl-rail` / `.bl-dept-tag` / `.bl-time`; แทนด้วย **`.bl-icon` circle 40px** modifier `--{pending|approver|approved|rejected|group}` → `--vc-{amber|blue|green|red|green}-{bg/border}` + Lucide 18px (`clock/send/check-circle-2/x-circle/git-merge`). Title row = dest + inline `.bl-status .vc-badge ${STATUS_BADGE.cls}`. Meta single-line `pax · HH:MM[-HH:MM] · booker` (Phase 5 = 2-line). Right zone action-only: pending = `[อนุมัติ primary][ปฏิเสธ secondary.bl-action-secondary]`; approved = `[✎][↻.bl-action-secondary]`; waiting_approver = "รอ Approver" italic. `@media (max-width:639.98px)` ซ่อน `.bl-action-secondary`. **Group row** `.bl-card--group` = `bg:green-bg + border:green-border` (hover border:green); circle `.bl-icon--group git-merge`; tag "N งานรวม" คงเดิม. Sub-rows: light green border-top, **ลบ ungroup ต่อ row** (header เท่านั้น) — `.bl-group-sub` block + sub-name 14/600 + meta `pax · time · booker`. **JS** `pages/vehicle-admin.js` — `STATUS_RAIL` → `STATUS_ICON` map; `renderSingleRow` swap (title=dest, booker→meta) + reject button เรียก `openAssignModal(id,'reject')` ที่มีอยู่แล้ว; `renderGroupRow` sub-items ลบ per-row shuffle. Selection `.bl-selected/.bl-notify-selected` ใช้ `border-color` swap (เลิกพึ่ง rail). **Phase B (future)**: checkbox + "รวมงาน" toggle bar + bulk merge — แยก phase per UX choice.)
- 2026-05-24 prev (**vehicle_admin redesign Phase 6-8** (2026-05-24): **Phase 6** vehicle col-4 polish — `vehicle_admin.css` section 7 rewrite: `.vs-row` padding tightened `--vc-space-3 --vc-space-5`, `.vs-icon` 36→32 + border 1px frame (active = `--vc-fg`, maintenance = `--vc-amber-bg/--vc-amber/--vc-amber-border` แทน red); `.vs-name` typography swap to `var(--vc-font-mono, ui-monospace, SFMono-Regular, monospace)` + sm/500/tracking 0.01em (จาก 13.5px/700) เพื่อ plate scan-friendly; `.vs-name-sub` xs/400; `.vs-detail` xs muted flex gap; **status detail variants** `.vs-detail-available/-maintenance` uppercase + tracking 0.06em + size 10.5px + dot pseudo `::before` 6×6 round bg `--vc-{green\|amber}` + hide inline Lucide icon (`> [data-lucide] { display: none }`) — uniform dot+text status pattern; `.va-vehicle-card` animation `va-fade-up base ease-out 180ms backwards`. **Phase 7** page entrance sequencing — เพิ่ม `va-fade-up` ทุก top-level block: `.va-page-header` (no delay = first), `#sectionBefore` (120ms delay), `#sectionAfter` (240ms delay), `.va-vehicle-card` (180ms — set ใน Phase 6); reduce-motion `@media` block ขยายครอบ `.va-page-header`/`.va-vehicle-card`/`#sectionBefore`/`#sectionAfter`/`.bl-card` + Phase 3 entries — animation: none !important. **Phase 8** modal polish — 4 modals `#assignModal/#swapModal/#repairModal/#revertModal` dialog enter: `translateY(16px)` → `translateY(8px)`, duration `--vc-dur-slow` → `--vc-dur-base` (220ms snappy แทน 400ms+), ease unchanged; เพิ่ม sibling selector `#{modal}.fade ~ .modal-backdrop.fade { transition: opacity --vc-dur-fast --vc-ease-out }` (backdrop fade-in 140ms ตรง designer §8). reduce-motion block แต่เดิมครอบ modal `.modal-dialog` อยู่แล้ว — no change. **Doc**: เลขฟิลด์ Phase 1-5 → Phase 1-8 complete; roadmap ปิด.)
- 2026-05-24 (**vehicle_admin redesign Phase 5** (2026-05-24): booking rows 3-zone flat layout. **CSS** `vehicle_admin.css` section 10 rewrite — `.bl-card` เปลี่ยนจาก card-with-border `.vc-radius-md` + 40px circle icon → flat row (border:0 + border-radius:0 + border-bottom 1px `--vc-border` แยก row, last-child no border); grid `3px minmax(0,1fr) auto` col-gap `--vc-space-4` padding `--vc-space-4 --vc-space-5`; `.bl-rail` 3px vertical bar (`align-self:stretch; border-radius:999px`) แทน `.bl-licon` circle — 5 modifiers `--{pending\|approver\|approved\|rejected\|group}` map `--vc-{amber\|blue\|green\|red\|green}` ตามลำดับ; `.bl-content` 2-line (`.bl-title` text-base/500/letter-spacing -0.005em truncate + `.bl-dept-tag` pill xs muted bg-subtle radius 999px); `.bl-meta` flex-wrap sm/muted + `.bl-meta-text` truncate + `.bl-meta-dot` opacity 0.6; **new** `.bl-right` flex-column align-end gap-2 stacked + `.bl-time` mono/sm/500/tabular-nums (ใช้ `--vc-font-mono` หรือ ui-monospace fallback) + `.bl-actions` (icon-only ghost cluster); hover `background: var(--vc-bg-subtle)` (เบากว่า Phase 2 `transform translateY + border-color:fg`); selection states `.bl-selected`/`.bl-notify-selected` simplified — ลบ transform + box-shadow → ใช้ bg + rail color override แทน (`.bl-selected .bl-rail { background:--vc-fg !important; }`); stagger reveal — `animation: va-fade-up base ease-out backwards + animation-delay: calc(min(var(--va-i,0), 8) * 30ms)`. **JS** `pages/vehicle-admin.js` — `STATUS_LICON` → `STATUS_RAIL` map (status → rail color class); rewrite `renderSingleRow(b, idx=0)` + `renderGroupRow(grpName, members, idx=0)` ใหม่: 3-zone markup (`<span.bl-rail>` + `<div.bl-content>` + `<div.bl-right>`) + dept tag `<span.bl-dept-tag>` (จาก `b.dept`) + time block `${b.start}–${b.end}` mono + actions ใน `.bl-actions`; group row ใช้ ghost icon-only buttons (ungroup/edit/chevron) แทน secondary; `style="--va-i:${idx}"` ทุก row สำหรับ stagger; `renderBefore()` map callback pass `(item, i) =>` idx; populate `#beforeCount` "${cnt.all} รายการ" ใต้ booking section title. `.va-list` ลบ padding/gap (rows flush + border-bottom แยก). Trip card hover lost border-color change (informational §6). **Template** `vehicle_admin.html` — booking section title "รายละเอียด" → "**รายการจอง**" + `<span.vc-card-head-meta#beforeCount>` placeholder.)
- 2026-05-24 (**vehicle_admin redesign Phase 4** (2026-05-24): filter tabs rewrite — pattern lifted จาก `mileage_admin.css .mlg-status-chips/.mlg-chip` (user preference over designer's underline recommendation). **CSS** `vehicle_admin.css` section 4 rewrite — `.va-filter-tabs` เปลี่ยนจาก strip + border-bottom เป็น **pill container** (`display:inline-flex`, gap 4px, padding 3px, margin `--vc-space-4 --vc-space-5`, border 1px `--vc-border`, radius 8px, bg `--vc-bg`, max-width compute เผื่อ margin, overflow-x auto + scrollbar hidden); `.ftab` เปลี่ยนเป็น chip (padding 5px 12px, border-radius 6px, font-size sm/500, color `--vc-fg-muted`, transition bg+color fast); hover = `bg-subtle + fg`; **active = `--vc-fg` bg + `--vc-bg` text + weight 600** (dark fill — Vercel/Linear opposite of designer's underline); focus-visible 2px accent ring; `.ftab-cnt` tabular-nums + opacity 0.6 (active 0.85 — chip text inherit color, count blend in); Phase 2 polish underline block (`.va-filter-tabs .ftab::after scaleX` + `.ftab-cnt` box pill) ลบทิ้งทั้งหมด — comment header แก้ให้ระบุว่า Phase 4 ลบ underline. Template/JS ไม่ต้องแก้ (class names `va-filter-tabs/ftab/ftab.active/ftab-cnt` เดิม + JS `setFilter()` toggle `.active` class เหมือนเดิม).)
- 2026-05-24 (**vehicle_admin redesign Phase 3** (2026-05-24): week navigator hero refactor. **Template** `vehicle_admin.html` — เพิ่ม `.va-week-meta` row บน `.va-week-bar` (left: `<span.va-week-eyebrow#weekRangeText>` (DD–DD MMM YYYY) + `<h2.va-week-selected#selDateHeading>` (วันX_ที่ DD MMM YYYY); right: `<button.va-week-today#weekTodayBtn>วันนี้</button>` + `<div.va-week-picker>` wrapper containing `<button.va-week-picker-btn#weekPickerBtn aria-expanded aria-haspopup="dialog">` (calendar icon + label + chevron-down `.va-week-picker-caret`) + `<div.va-week-picker-pop#weekPickerPop[hidden] role="dialog">` (label + `<input type="date".va-week-picker-input#weekPickerInput>`); ลบ `.va-week-heading` block เก่า (h2 ย้ายขึ้น meta — selDateHeading id ยังอยู่). **CSS** `vehicle_admin.css` — section 2 rewrite: `.va-week-card` overflow `hidden → visible` (ให้ popover escape) + animation `va-fade-up 60ms backwards`; เพิ่ม `.va-week-meta/-left/-right` (flex space-between, padding space-5 space-6, border-bottom), `.va-week-eyebrow` (xs/500/uppercase/tracking 0.08em/tabular-nums), `.va-week-selected` (h2 lg/600/letter-spacing -0.014em), `.va-week-today-tag` (xs/uppercase pill bg-subtle ใน h2 inline เมื่อ today), `.va-week-today` ghost btn (sm/500 muted → hover fg + bg-hover), `.va-week-picker` (relative anchor), `.va-week-picker-btn` (secondary look + caret rotate via `[aria-expanded="true"]`), `.va-week-picker-pop` (absolute top:100%+6px right:0, min-width 240px, border + radius-md, padding, z-index `--vc-z-dropdown`, animation `va-pop-in base ease-out`, `transform-origin: top right`); `.va-week-bar` ปรับ padding `--vc-space-3 --vc-space-4` (ลบ `border-bottom` — meta แทนแล้ว); เพิ่ม `@keyframes va-fade-up/-pop-in/-week-cell-in` + class `.va-week-strip--switching .va-week-day` apply `va-week-cell-in` 30ms stagger via `--va-i`; เพิ่ม mobile (≤767px) media — meta wrap full-width + popover flip ซ้าย (`right:auto; left:0; transform-origin: top left`); reduce-motion block ขยายครอบ `.va-week-card`/`.va-week-picker-pop`/strip-switching + caret. **JS** `pages/vehicle-admin.js` — เพิ่ม `renderWeekMeta()` (smart range text compression: same-month/year vs cross-month vs cross-year; h2 day line + today tag); เพิ่ม `_alignWeekStartTo(date)` helper (Monday-based snap); `openWeekPicker()`/`closeWeekPicker()` (toggle hidden + aria-expanded + auto-focus input); `bindWeekControls()` ครั้งเดียวตอน init bind: `#weekTodayBtn` (reset selDate=today + align weekStart + close pop + renderAll), `#weekPickerBtn` (toggle popover), `#weekPickerInput` change (parse YYYY-MM-DD → selDate + align week + close + renderAll), document click outside (close), Esc (close + return focus to btn); `renderWeekNav()` cells get `el.style.setProperty('--va-i', i)` สำหรับ stagger; `shiftWeek(dir)` เรียก `renderWeekMeta()` + force-reflow + add `.va-week-strip--switching` class (skip ถ้า reduce-motion); `renderAll()` แทน inline `#selDateHeading` set ด้วย `renderWeekMeta()` call; เรียก `bindWeekControls()` ก่อน first `renderAll()`.)
- 2026-05-24 (**vehicle_admin redesign Phase 1-2** (2026-05-24): cleanup + header refresh. **Template** `vehicle_admin.html` — ลบ `.vc-card va-kpi-card` block ทั้งหมด (4 cells: รออนุมัติ/ส่ง Approver/อนุมัติ/ปฏิเสธ); refactor `.va-page-header` (drop BS utility `d-flex align-items-end justify-content-between flex-wrap gap-3 mb-4` → CSS-only + Bootstrap `mb-5 pt-3`); eyebrow text "Admin · Approval Queue" → "Admin / Approval queue"; เพิ่ม `<div class="va-page-header-stat">` 3-item pill ใน `.va-page-header-actions` slot — แต่ละ item = `<span.va-stat-dot.va-stat-dot--{amber\|blue\|green}>` + `<span.va-stat-num#stat{Pending\|Approver\|Approved}>` + `<span.va-stat-lbl>` + `<span.va-stat-sep>·</span>` separators; `aria-live="polite"` สำหรับ count update. **CSS** `vehicle_admin.css` — ลบ section "2. KPI STRIP — 4 cells override" (`.va-kpi-card .vc-kpi-group.va-kpi-4` grid + ≤991/575px media queries) + Phase 2 polish KPI rules (`.va-kpi-card .vc-kpi-cell::before` hover rail + `.va-kpi-cell:nth-child(N):hover .vc-icon-sm` semantic color amber/blue/green/red + `.va-kpi-label` tighten) + KPI entry ใน `prefers-reduced-motion` block; ปรับ `.va-page-header` (gap `--vc-space-4 → --vc-space-6`, padding-bottom `--vc-space-5 → --vc-space-6`, ลบ margin-bottom — ให้ template control); `.va-page-title` font-size `--vc-text-xl (24px) → clamp(26px, 3.4vw, 32px)` + letter-spacing `--vc-tracking-tight → -0.022em`; eyebrow font-size `11px → --vc-text-xs (12px)` + weight `600 → 500`; เพิ่ม block `.va-page-header-stat` (pill — `display:inline-flex`, gap `--vc-space-2`, padding `--vc-space-2 --vc-space-4`, border `--vc-border`, radius 999px, font-size `--vc-text-sm`, tabular-nums, white-space:nowrap) + `.va-stat-item/.va-stat-num/.va-stat-lbl/.va-stat-sep` + `.va-stat-dot` (6×6 round) + 3 dot color modifiers (`--amber/--blue/--green`). **JS** `pages/vehicle-admin.js` — rename helper `setKpi`/`_kpiRaf` → `setStat`/`_statRaf` (count-up animation logic ยังเดิม 600ms ease-out cubic + reduced-motion check); 4 call sites ใน `renderBefore()` `setKpi('kpiPending'/'kpiApprover'/'kpiApproved'/'kpiRejected', cnt.*)` → 3 call sites `setStat('statPending'/'statApprover'/'statApproved', cnt.*)` (ตัด rejected ออกเพราะ inline stat แสดงแค่ 3 ตัว). **Doc sync**: design_system.md §9.7 KPI Cell snippet ลบ `.va-kpi-card`/`.va-kpi-4` wrappers (เหลือแค่ `.vc-kpi-group` + `.vc-kpi-cell` primitive). **Roadmap**: Phase 3-8 ยังเหลือ — week navigator + date picker popover (hero), filter tabs underline (`mlg-status-chips` pattern), list rows 3-zone redesign, vehicle col-4 vertical list, animation system + stagger reveal, modal polish.)
- 2026-05-23 (**vehicle.html mobile redesign Phase A+B** (2026-05-23): mobile-first premium redesign — Phase A=CSS only (compact calendar grid `aspect-ratio:1` square cells, today filled-black inverted text, selected `box-shadow` inset ring, press scale `:active{transform:scale(.94)}`, `--vc-dur-fast --vc-ease-snap` transition, drop card chrome on mobile), Phase B=hero+daybar markup (new `.vrc-m-hero d-md-none` block ใส่ title 20px/600 + month picker with dual buttons that share JS handlers via `forEach` dual-bind, `.vrc-m-daybar` sticky bar `position:sticky; top:0; z-index:--vc-z-sticky` แทน old `.mobile-list-header`, new `#mobileDaybarCount` populated by `updateMobileList()`). Desktop toolbar 2 blocks ได้ `d-none d-md-flex` ซ่อนบน mobile. Animation `vrc-m-rise/-fade` + reduced-motion guard. ปุ่ม "จองรถ" inline ยังอยู่ (รอ Phase D = round icon FAB). Phase F (multi-dot indicator 1=dot/2-3=short bar/4+=long bar) ยังไม่ทำ)
- 2026-05-23 (**vehicle.html mobile redesign Phase C** (2026-05-23): premium event-card refactor (`updateMobileList()` + new `.vrc-m-evt*` CSS block). **Visual**: ตัด Bootstrap `card/card-body` nesting → flat `.vrc-m-evt` (border + radius-md, press scale `:active{transform:scale(.985)}`, hover `border-color:--vc-fg-subtle`); 8px colored dot (`--{approved\|pending\|approver\|rejected\|group\|completed}`) optical-aligned กับ first text line (`margin-top:7px`); title 14px/500/tabular-nums เป็น "{time}–{timeEnd} · {dest}" สำหรับ single, "{carLabel}" สำหรับ group; meta plain text + `.sep` (`--vc-fg-disabled`) แทน Lucide icons; status pill 22px ขวาเสมอ (bg + matching `--vc-*-border` like `bk-detail-status` premium pattern). **Group card**: `.vrc-m-evt--group` + purple dot + status pill "{N} งานรวม"; collapse trigger ฝัง `.vrc-m-evt-toggle` (28px chevron, rotate 180° เมื่อ open); sub-rows `.vrc-m-evt-sub` (bg-subtle indented container) + `.vrc-m-evt-sub-row` button (time prefix 42px + booker + pax/dest meta) — ลบ pattern nested card-in-card. **Single card**: ลบปุ่ม edit pencil inline (tap ทั้งใบ → openEventDetail, edit อยู่ใน detail modal). **Empty state**: `.vrc-m-empty` 56px circle icon + title 14px + sub 12.5px ("ไม่มีการจองในวันนี้" + "แตะปุ่ม 'จองรถ' ด้านบนเพื่อเพิ่มการจองใหม่"). Helpers `dotKey(s)/statusKey(s)` map `'completed'→'completed'` variant (gray neutral))
- 2026-05-23 (**vehicle.html mobile redesign Phase D** (2026-05-23): round icon-only FAB (user Q2 choice). **Template**: ลบ `#mobileDateCountBtn` inline button จาก `.vrc-m-daybar` (daybar เหลือแค่ label + count); เพิ่ม `<button class="vrc-m-fab d-md-none" id="mobileFabBook">` + Lucide plus icon ก่อน `</main>` (fixed bottom-right, outside scroll container). **CSS** `.vrc-m-fab`: 56×56 round, `--vc-fg` bg + `--vc-on-primary` icon (22px plus, stroke-width:2), `position:fixed; right:16px; bottom:max(20px, env(safe-area-inset-bottom))` (iOS safe area), `z-index:--vc-z-raised`, `:active{transform:scale(.92)}`. **Shadow exception** — `box-shadow: 0 6px 16px rgba(0,0,0,.18), 0 2px 6px rgba(0,0,0,.10)` (ละเมิด no-shadow rule, justified = ลอยเหนือ scroll plane ต้อง depth cue, Material/Apple pattern). Spring entry animation `vrc-m-fab-in` (scale .7→1) duration slow easing spring delay .2s. **JS** `updateMobileList()` retarget past-date disable จาก `#mobileDateCountBtn` → `#mobileFabBook` ด้วย `classList.toggle('is-disabled', isPastDate)` + `aria-disabled` attr (Q4 silent disable — `.is-disabled` rule = opacity .35 + pointer-events:none))
- 2026-05-23 (**vehicle.html mobile redesign Phase E** (2026-05-23): stagger reveal animation. **CSS** keyframe `vrc-m-item-in` (`opacity 0 + translateY(8px) → 1+none`) + apply to `.vrc-m-evt` (duration `--vc-dur-base` 220ms, easing `--vc-ease-out`, `animation-delay: calc(var(--i,0) * 30ms)`) + `.vrc-m-empty` (slow, no delay); calendar grid `#calendarBody` ใน `@media (max-width:767px)` ได้ `vrc-m-fade` slow delay .05s — fires ทุกครั้ง renderCalendar (month nav + day pick = reactive feel); ทั้งหมด wrap `@media (prefers-reduced-motion: no-preference)` guard. **JS** `updateMobileList()` เพิ่ม `let _i = 0` ก่อน group/single loops; emit `style="--i:${_i++}"` บนแต่ละ `.vrc-m-evt` (running counter ข้าม both loops — order: groups first then singles); ทำให้ stagger เริ่มจาก 0ms ใบแรกเรียงเป็น cascade 30ms/ใบ)
- 2026-05-23 (**vehicle.html mobile redesign Phase F** (2026-05-23): multi-dot indicator ในช่องปฏิทิน — volume hint โดยไม่ใช้สี (mono restraint). **CSS** `.mobile-indicator` default 4×4 round dot (1 booking) + 2 modifiers ใหม่ `.mobile-indicator--bar-md` 14×3 pill (2-3 bookings) และ `--bar-lg` 22×3 pill (4+ bookings) ใช้ `border-radius:999px` + `height:3px` override (อิง `--vc-fg-subtle` ปกติ, today cell auto-invert ผ่าน existing rule `.calendar-cell.today .mobile-indicator{background:--vc-on-primary;opacity:.7}`). **JS** `createCell()` (L368-370) refactor — ลบ inline `style="width:25px;border-radius:4px"` / `style="width:15px;border-radius:4px"` 2 จุด → ใช้ class modifier `.mobile-indicator--bar-lg` / `--bar-md` (matches user spec: 1=dot, 2-3=short bar, 4+=long bar))
- 2026-05-23 (**vehicle.html mobile fix bundle** (2026-05-23): (a) today selected+today CSS bug — `.calendar-cell.selected.today` ขาด `background: var(--vc-fg)` re-assert → today auto-selected on first load กลายเป็น white-on-white invisible (fix: append `background: var(--vc-fg)` ก่อน `box-shadow: none`); (b) `.vrc-m-daybar` ลบ `position:sticky` + `z-index:--vc-z-sticky` (leak ขึ้นใต้ notification panel + UX value น้อย) — daybar scroll ตาม content ปกติ; (c) FAB Phase D revert ตาม user request — ลบ `<button class="vrc-m-fab" id="mobileFabBook">` จาก `vehicle.html` (ก่อน `</main>`), ลบ `.vrc-m-fab` block + `@keyframes vrc-m-fab-in` จาก `vehicle.css`, re-add inline `<button class="vc-btn vc-btn-primary vc-btn-sm" id="mobileDateCountBtn">` ใน `.vrc-m-daybar` (กลับไป Phase B layout), `updateMobileList()` ใน `pages/vehicle.js` retarget past-date disable กลับเป็น `#mobileDateCountBtn` (revert Phase D — disable ผ่าน `.disabled` + inline style opacity .5))
- 2026-05-23 (**vehicle.js hide cancelled bookings** (2026-05-23): user-facing vehicle page ซ่อน booking ที่ status='cancelled' — `const mockEvents = (window.BOOKINGS || []).filter(e => e.status !== 'cancelled')` L82 (filter at source) → กระทบ calendar cell + dots/bars + mobile list + popover "+N รายการ" + count badges ทั้งหมด. Rejected ยังขึ้น (user เลือก hide cancelled อย่างเดียว). admin pages (vehicle_admin/budget_manage/notification) ยังเห็น cancelled ตามปกติ (Phase 9 audit). Edge: deep-link `?pay=<cancelled_id>` หรือ `openEventDetail(cancelled_id)` จะ fail silent — acceptable (cancelled = ไม่ active แล้ว))
- 2026-05-23 (**_sidebar v3** (2026-05-23): collapsible admin group by domain ("จัดการรถ" parent + 6 sub items อนุมัติรถ/เลขไมล์/รถและทีม/งบประมาณ/ค่าล่วงเวลา/ค่าน้ำมัน) — new `.sb-group/.sb-group-toggle/.sb-item--sub` + grid-row height anim 280ms + stagger reveal 30ms/item + indent guide line scaleY anim + localStorage `bb-sb-groups` persist; section-label ตัด UPPERCASE + letter-spacing, weight 500→400, +border-top คั่นระหว่าง section; sub active = neutral `--vc-bg-hover` (no accent bar); badge dark indigo → soft gray pill; fix `.sb-group-toggle` `font: inherit` → `font-family: inherit` (เคย override `.sb-item` font-size ทำให้ parent ตัวใหญ่); new context processor `inject_admin_pending_tomorrow()` ใน `app.py` — `pending_count` = pending bookings start = พรุ่งนี้ (BKK time `get_bkk_time() + 1d`), เฉพาะ vehicle admin/superadmin)
- 2026-05-22 (**budget_manage redesign Phase 9** (2026-05-22): C1 cancel-after-approve flow (User + Admin) — soft cancel pattern (status='cancelled', row kept), refund งบ idempotent, multi-recipient notify w/ priority cascade dedup. **Backend** `vehicle_view.py` — เพิ่ม route `POST /vehicle/cancel/<booking_id>` (vehicle_bp.cancel_booking) หลัง delete_booking. Guards: (a) permission = owner OR `is_vehicle_admin()` (b) status ∈ {pending, waiting_approver, approved} (c) time guard `now < booking.start_datetime` (block หลัง trip start). Flow: `budget_svc.refund_for_booking()` (5th call site) → build 5 recipient sets (owner/admin/approver/driver/mate) with priority cascade `already_notified` set (ทุก user ได้รับ notify เพียง 1 ใบ, role_label = highest priority) → loop `_n_user_cancelled()` 5 ครั้ง → flip `status='cancelled'` + `updated_by` → `tg_notify_cancelled()` (pattern: delete_old + send + save_id, no try/except wrap = match approve_booking convention) → commit. `index()` รับ kwargs `now=datetime.now()` + `is_vehicle_admin=is_vehicle_admin()` เพิ่มเติม. **Notification** `notification_service.py` — เพิ่ม `notify_user_cancelled(*, user_id, booking, cancelled_by, role_label)` Event #16 รองรับ 5 role_labels (owner/admin/approver/driver/mate) — message text แตก by role; icon ใช้ `ICON['deleted']` (trash). **Telegram** `telegram_service.py` — เพิ่ม `notify_cancelled(booking, cancelled_by)` — 🚫 ยกเลิกการจอง + standard fields. **Template** `vehicle.html` — BOOKINGS data รับ `cancelUrl` + `canCancel` ใหม่ (`canCancel = b.status ∈ cancellable AND b.start_datetime > now AND (owner OR admin)`). **JS** `pages/vehicle.js` — `detailActions` block ใช้ `e.canCancel` แทน `(e.isOwner && e.isPending)`; ปุ่ม "ยกเลิก" POST ไป `e.cancelUrl` (เปลี่ยนจาก `deleteUrl`); confirm dialog ขยายเป็น "งบจะถูกคืน + แจ้ง Admin/Approver/Driver/ผู้ร่วมเดินทาง"; ปุ่ม "แก้ไข" ยังจำกัด `e.isOwner && e.isPending` (approved booking แก้เองไม่ได้). **Models** `models.py` L184 — comment update เพิ่ม `cancelled` enum value (DB-level ไม่มี migration — column เป็น `String(20)` ไม่มี CHECK constraint, `'cancelled'` ใช้อยู่แล้วตั้งแต่ 2026-05-18 admin refund_booking). **schema.md** L144 + Version Timeline v2.12 + prose section sync'd by db-helper. **notifee audit** (cancel_booking() pre-deploy): 2 FAILs found + fixed (duplicate notify when user is admin+approver/admin+mate; owner+admin overlap → priority cascade dedup); 1 WARN aligned (Telegram try/except wrap → ลบให้ตรง project convention))
- 2026-05-22 (**budget_manage redesign Phase 8** (2026-05-22): dead purple CSS cleanup (markup zero usage since Phase 4). `budget_manage.css` §7 — ลบ `.vc-btn-purple` + `.vc-btn-purple:hover` + `.vc-badge-purple` blocks (10 lines → 3-line retired comment pointer). `components/kpi.css` — ลบ `.vc-kpi-value--purple { color: var(--vc-purple); }` (L80) + header doc comment line. Tokens `--vc-purple` + `--vc-purple-bg` + `--vc-purple-border` ใน `tokens.css` **เก็บไว้** — `design_system_reference.html` ยังใช้ใน swatch demo + hover link (functional CSS file zero references). Phase 8 closes purple retirement loop ที่เริ่มใน Phase 4)
- 2026-05-22 (**budget_manage redesign Phase 7** (2026-05-22): Pivot section — fiscal year (มี.ค.→ก.พ.) × แผนก, 2 collapsed cards (central + dept) ระหว่าง personal-line กับ "งบประมาณส่วนกลาง" section. **Backend** `vehicle_view.py` — เพิ่ม helper `_build_budget_pivot(fiscal_year_start_ad)` (สลับ AD-year mapping: m3-12 → start, m1-2 → start+1; filter `is_active=True`; return dict `{central, central_labels, central_max, dept, dept_labels, dept_max, fiscal_months: [(m,y)×12]}`); `budget_manage()` compute `fiscal_year_start_ad = sel_year if sel_month>=3 else sel_year-1` + call helper + pass `pivot`/`fiscal_year_start_ad`/`TH_MONTHS` ไป template. **Template** `budget_manage.html` แทรก 2 `<details class="vc-card vc-pivot-wrap budget-pivot-card">` (collapsed default) ใช้ `vc-pivot-summary` + `grid-3x3` lucide + `vc-collapse-chevron`; `<thead>` sticky col "แผนก"/"กอง" + 12 month cols (TH_MONTHS[m]) + total col; `<tbody>` heat-tinted cells (`--cell-heat = cv/max*100`) + drill-down `<a href="url_for('adminfleet.budget_manage', year=y, month=m)">` (fiscal year mapping → click cell จะไปเดือนนั้นในปี y ถูกต้อง); row total + `<tfoot>` col totals + grand total; empty state `.budget-pivot-empty`. **CSS architecture** — extract `.vc-pivot-*` block (~165 lines) จาก `fuel_admin.css` §22 → `app/static/css/components/pivot.css` (shared component); `design-system.css` เพิ่ม `@import url('./components/pivot.css')`; `fuel_admin.css` §22 เหลือแค่ comment pointer. `budget_manage.css` §12.5 เพิ่ม page-local tweaks: `.budget-pivot-card` margin-bottom space-5, `.budget-pivot-summary-content` flex/gap-2, `.budget-pivot-empty` padding space-5. **future_features.md** เพิ่ม row #14 "Personal pivot deferred" (BudgetType ไม่มี 'personal' — ต้อง query VehicleMileage แทน — scope ใหญ่กว่า))
- 2026-05-22 (**budget_manage redesign Phase 6** (2026-05-22): modal alert boxes → meta lines. Template — 3 alert blocks ใน modals: (a) setBudget L559 `ds-alert ds-alert-info budget-modal-alert--flush` → `<p class="budget-modal-notice">` + `info` lucide icon + wrapped span (notice ที่ end of modal-body, ก่อน footer); (b) topUp L593 `ds-alert ds-alert-info budget-modal-alert` (`<strong>dept</strong> · เพดานปัจจุบัน: N ฿`) → `<dl class="budget-modal-meta">` 2-row `<div class="budget-modal-meta-row">` × (`<dt>แผนก</dt><dd data-bind="dept_label">`, `<dt>เพดานปัจจุบัน</dt><dd class="budget-modal-mono"><span data-bind="current_cap">0</span> ฿</dd>`); (c) adjust L641 same pattern (`current_used` + label "ใช้ไปปัจจุบัน"); `data-bind` attrs ย้ายไป `<dd>` (JS `[data-bind="X"]` querySelector ยังทำงาน — ไม่ต้องแก้ JS). CSS §11 rewrite — ลบ `.budget-modal-alert(--flush)`; เพิ่ม `.budget-modal-meta` (flex column, gap space-2, `border-block: 1px solid` Stripe-style horizontal divider top+bottom) + `.budget-modal-meta-row` (flex baseline space-between gap-3) + `<dt>` (SMALL CAPS 11px/500 uppercase tracking-wide muted) + `<dd>` (sm/600/fg, letter-spacing -0.01em); เพิ่ม `.budget-modal-notice` (flex flex-start, gap space-2, border-top space-3, fg-muted sm, lucide icon `--vc-fg-muted`, `<strong>` → fg). Goal: ลด visual weight box → focus กลับไปที่ form input. Reusable principle: **Stripe-style `<dl>` meta block** สำหรับ modal context info (label-value pairs) แทน alert box. Flash `ds-alert` ที่ L37 ไม่ใช่ scope (global pattern))
- 2026-05-22 (**budget_manage redesign Phase 5** (2026-05-22): personal row card → inline line. Template `.budget-personal-row`→`.budget-personal-line` (1 site L153). CSS rewrite block §10 — drop `border`/`background`/`border-radius`/`padding` (card chrome) → ใช้ `padding-block: --vc-space-3` + `border-bottom: 1px solid --vc-border` (subtle separator); icon 20px→16px; `.meta` flex column → **inline `display:flex; align-items:baseline; gap:--vc-space-2`** (label + value อยู่บรรทัดเดียวกัน); `.label` xs/uppercase/tracking-wide → `--vc-text-sm` lowercase (พอแล้วเพราะ value mono อยู่ติด); `.value` md→sm (ลด weight, value ไม่ใช่ hero ของ section); `.meta-end` `margin-left:auto` push ขวา. Icon ยัง `--vc-green` (warmth signal). ลด visual weight: personal section ไม่แข่ง prominence กับ card sections ถัดไป)
- 2026-05-21 (**budget_manage redesign Phase 4** (2026-05-21): premium polish + animations + purple retirement (full). **4a Purple cleanup:** template — dept section header drop `is-purple`, `.vc-bcard-name.is-purple` drop, dept `bar_cls` Jinja fallback `'is-purple'`→`''`, refund modal `vc-badge-purple`→`vc-badge-neutral`; CSS — ลบ rules `.vc-section-hdr-icon.is-purple`, `.vc-bcard-name.is-purple > .vc-icon-sm`, `.vc-progress.is-purple > span` (เหลือ `.vc-btn-purple`/`.vc-badge-purple` CSS rules ไว้ Phase 8 cleanup). **4b Spacing rhythm:** `.vc-bcard` padding `--vc-space-4`→`--vc-space-5`, ลบ flat `gap`, ใช้ asymmetric child `margin-top` (date space-1, pct-hero space-5, progress space-3, amounts space-2, rem space-4, approver space-2); `.vc-section-hdr` padding-bottom space-3→space-4, margin space-8/space-4→space-10/space-5. **4c Typography:** `.vc-bcard-pct` text-lg→**text-2xl** (32px) + `--vc-tracking-tight` + line-height 1; `.vc-bcard-pct-label` xs/400→**11px/500 uppercase + tracking-wide + padding-bottom 4px** (SMALL CAPS premium tag); `.vc-section-hdr-amount` md→lg + letter-spacing -0.01em; `.vc-section-hdr-amount-label` xs/400→11px/500 uppercase tracking-wide; `.vc-bcard-name` letter-spacing -0.01em; `.vc-bcard-amounts .used` sm→md; `.vc-bcard-amounts .total` xs→sm. **4d Color cleanup:** `.vc-progress > span` baseline `--vc-green`→`--vc-fg` (Linear monochrome — semantic warn/danger ยังทำงาน); new modifier `.vc-kpi-cell--signal { background: var(--vc-bg-subtle); }` apply กับ "คงเหลือ" cell ลด monotony โดยไม่ใช้สี; `.budget-personal-row .value` green→fg (icon ยัง green = warmth signal). **4e Animations:** page-scoped tokens `--bm-ease-out: cubic-bezier(0.16, 1, 0.3, 1)` + `--bm-dur-fast 180ms/-base 240ms/-slow 480ms` (declared under `.vc-scope` แต่ load เฉพาะ budget_manage.css → safe in practice); keyframes `bm-fade-up` (translateY 8px→0 + opacity), `bm-progress-fill` (scaleX 0→1), `bm-fade-in`; `.vc-bcard { animation: bm-fade-up base ease-out both; animation-delay: var(--bm-delay, 0ms); }` + template inline `style="--bm-delay: {{ loop.index0 * 40 }}ms"` ทั้ง central + dept loops (40ms stagger); `.vc-progress > span` animation `bm-progress-fill slow ease-out` delay `calc(--bm-delay + 200ms)` (signature motion — fills หลัง card settle); `.vc-bcard:hover` เพิ่ม `background: var(--vc-bg-subtle)` (restraint = no transform); `.vc-dropdown-menu` transition `var(--vc-transition)`→`var(--bm-dur-fast) var(--bm-ease-out)` + translateY -4px→-6px; `@media (prefers-reduced-motion: reduce)` block disable animation ทั้งหมด. **4f Modal entrance:** class `budget-modal-enter` บน 4 modal divs (setBudget/topUp/adjust/refund); CSS scoped `.budget-modal-enter.show .modal-dialog { animation: bm-fade-up base }` + `.budget-modal-enter ~ .modal-backdrop.show { animation: bm-fade-in fast }` (ไม่แตะ shared `.vc-modal` → ไม่ leak))
- 2026-05-21 (**budget_manage redesign Phase 3** (2026-05-21): card hierarchy — `.vc-bcard-row` (label+pct sibling) → `.vc-bcard-pct-hero` wrapper (flex/baseline/gap 8px) containing hero `.vc-bcard-pct` (mono/lg/600/-0.01em letter-spacing) + secondary `.vc-bcard-pct-label` (xs/400/muted); ลบ `.vc-bcard-row` block, lift `.vc-bcard-pct` to standalone (was nested); demote `.vc-bcard-amounts` typography (`.used` md/600/fg → sm/500/fg-muted, `.total` sm/fg-muted → xs/fg-subtle); ลบ markup `.used.is-purple` ใน dept loop + ลบ CSS rule `.vc-bcard-amounts .used.is-purple` (purple บน data value หลุดจาก scope — accent reserved สำหรับ focus ring + sidebar active). Both central + dept loops migrated)
- 2026-05-21 (**budget_manage redesign Phase 1+2** (2026-05-21): Phase 1 — CTA `vc-btn-purple`→`vc-btn-primary` (sole tpl call site line 348), `.vc-bcard--inactive` stripe gradient→flat `var(--vc-bg-subtle)` + `border-style:dashed` + opacity 0.6 (per design_system §13 no-gradient + WCAG), `.vc-dropdown-item` padding `6px 10px`→`8px 10px`; Phase 2 — KPI 6→4 cells (`vc-kpi-group--6`→`--4`, ลบ cell "งบส่วนกลาง" + "งบงานกอง" ออก), amounts 2 ตัวย้ายขึ้นเป็น `.vc-section-hdr-amount` + `.vc-section-hdr-amount-label` (mono/tnum, `margin-left:auto` push amount+actions ขวา, mobile ≤575px full-width below); `kpi.central_budget`/`kpi.dept_budget` still computed in view — section headers consume. `.vc-btn-purple` CSS class kept until Phase 8 cleanup)
- 2026-05-21 (**notification sort fix + unread tab sticky**: (a) `vehicle_view.py` L999 sort bug — เดิม sort `g['latest']['created_at']` ที่เป็น string `DD/MM/YYYY HH:MM` → lexically wrong (`"31/01/2026" < "01/02/2026"`); fix โดย track `g['_sort_dt']` (raw datetime) — regular group set ตอน `latest is None` ครั้งแรก (`n.created_at`, notifs sorted DESC แล้ว → first = latest), synthetic group compute `max(valid_dts)` จาก event sources (booking.created_at/updated_at + 7 event ts + 3 mileage ts + log.created_at, fallback `booking.created_at`); sort `key=g['_sort_dt'] or EPOCH(1970)` reverse=True; strip `_sort_dt` ก่อน jsonify (Flask ไม่ serialize datetime). (b) `pages/notification.js` `state.justRead = new Set()` — booking_id (number) หรือ `'loose:<id>'` (string) ที่อ่านในรอบ panel นี้; `renderList` unread filter เพิ่ม `|| state.justRead.has(...)` → item ที่เพิ่งอ่านค้างใน unread tab จนกว่า user จะปิด panel (closePanel → `state.justRead.clear()`); `toggleGroup` mark-as-read fetch สำเร็จ → `state.justRead.add(bid)`, `open-single` loose card → `state.justRead.add('loose:'+id)`. Mark-all button คงพฤติกรรมเดิม (explicit user action = immediate disappear))
- 2026-05-21 (**ลบ `notif-timeline-item` fallback — stage timeline ครอบทุก booking**: `vehicle_view.py` `api_notifications` — (a) `_build_user_stages` เพิ่ม fallback Stage 0 (pending/forwarded) เมื่อ booking ยังไม่ approved และยังไม่ถูก rejected (`clock` icon "รอ Admin พิจารณา" หรือ `send` icon "รอหัวหน้าแผนกอนุมัติ" ตามว่ามี `forwarded` event มั้ย) + terminal rejected stage (`x-circle` "ถูกปฏิเสธโดย Admin/หัวหน้าแผนก" + reason) (b) `_build_approver_stages` Stage 1 emit เสมอ (fallback `booking.created_at` + title "รอ Admin ส่งต่อ" เมื่อไม่มี forwarded event) (c) filter L998 drop ทุก group ที่ `stages=[]` (เดิม drop เฉพาะ synthetic). admin builder ไม่แก้ (มี `created` always-emit อยู่แล้ว). `pages/notification.js` `renderGroup` ลบ fallback branch — ใช้ `renderStageTimeline(g.stages)` เท่านั้น (timeline div fixed `notif-timeline--stages`); `displayCount = stages.length` เสมอ; `renderTabs` tAll simplify `sum(stages.length) + loose.length`. CSS `.notif-timeline-item` ยังอยู่เป็น dead block (ไม่ผลิตใน DOM แล้ว — ลบเมื่อมีคน cleanup) — Option B confirmed)
- 2026-05-21 (**role differentiation ใน notification panel**: `pages/notification.js` helpers `roleIcon(role)` → Lucide name (`user`/`user-check`/`shield`), `roleLabel(role)` → tooltip text, `roleChipText(role)` → "อนุมัติ"/"Admin" (user role ไม่มี chip — default). `renderGroup`: cat-icon split จาก preview icon — head ใช้ `headerIcon = roleIcon(g.role) || pickIcon(latest)` (role context), preview row ใช้ `pickIcon(latest)` เดิม (latest event); เพิ่ม `title=` attribute บน cat-icon = role label; เพิ่ม `<span.notif-role-chip>` ใน title-row หลัง count (เฉพาะ approver/admin). `notification.css` `.notif-role-chip` block: monochrome pill 17px height, `var(--vc-bg)` + `var(--vc-border)`, font .62rem 600, `var(--vc-fg-muted)` — match `.notif-group-id`/`.notif-group-count` pattern. Option A confirmed: booking status ที่ยัง `pending`/`waiting_approver` → no stages → fall back raw message timeline (ไม่บังคับให้มี stage))
- 2026-05-21 (**stage timeline — admin/approver roles + synthetic groups**: `vehicle_view.py` `api_notifications` ขยายให้รองรับ 3 roles (priority: user > approver > admin via `_resolve_role(booking)`). **Synthetic groups**: ก่อน build groups เสร็จ → query เพิ่ม (a) approver: `VehicleBooking.trip_department_id in DeptApprover.dept_id` + status in waiting_approver/approved/rejected + 90 วัน, limit 50 (b) admin (`role_vehicle=='admin' or is_superadmin`): non-ad-hoc + status in pending/waiting_approver/approved + 60 วัน, limit 50 → booking ที่ไม่อยู่ใน `groups_map` แล้วเพิ่มเป็น synthetic (items=[], unread_count=0, `is_synthetic=True`). **Event extractor** `_extract_events(notifs)` map icon/category/message → event_key (booking_created/admin_assigned/forwarded/admin_approved/approver_approved/admin_rejected/approver_rejected/merged/mileage_start_notif/mileage_end_notif/budget_deducted/payment_required/payment_confirmed) — approve discriminator ใช้ `saw_forwarded` flag (approver_approved ถ้า forward มาก่อน, admin_approved ถ้าไม่มี); reject ใช้ message contains 'หัวหน้าแผนก'. **Stage builders** 3 ตัว: `_build_user_stages` (4 stages เดิม + ใช้ event ts), `_build_admin_stages` (7 stages: created/assigned/decision(branch forward|approve|reject)/approver_decision/trip_done/budget/payment_received), `_build_approver_stages` (4 stages: forwarded/my_decision/trip_done/budget — dept budget only). Bulk fetch `updater_users` map สำหรับ actor names (`booking.updated_by` + `booking.user_id`). **Synthetic preview/sort**: stage สุดท้ายของ synthetic → populate `g.latest` (message=desc_main\|desc\|title, icon=stage icon, created_at=stage ts, action_url=`/vehicle/detail/{id}`) — sort + preview row ทำงานได้. Drop synthetic groups ที่ build แล้ว stages=[] (ไม่มีอะไรแสดง). `pages/notification.js` `renderGroup` `displayCount` = `stages.length` ถ้า items[] ว่าง else `items.length`; `renderTabs` tAll sum รวม stages.length สำหรับ synthetic groups)
- 2026-05-21 (**stage timeline polish (alignment + text + trip mates)**: `notification.css` `.notif-timeline--stages` padding-left 14→30px + `.notif-stage` grid 32px→28px col-gap 12→10px + icon 32→28px + `::before` connector left 15→13px top 32→28px → align กับ `.notif-group-head` (cat-icon left edge 30px, main text 68px). `vehicle_view.py` `api_notifications` Stage 1 text เปลี่ยน: `desc` → `desc_main`="อนุมัติรถ {snap_vehicle_plate||assigned_vehicle.license_plate}" (fallback "อนุมัติคำขอจองรถ" ถ้าไม่มี plate) + `desc_sub`="เดินทางร่วมกับ {names}" เมื่อมี `trip_group` (bulk fetch `VehicleBooking.trip_group.in_(trip_groups)` + `User.full_name` map, fallback `contact_name`); Stage 4 budget name "{bt} {MM}/{YYYY}" → "งบส่วน{กลาง\|กอง} - {department.name}" (bt 'central'→'งบส่วนกลาง' else 'งบส่วนกอง', ลบ MM/YYYY))
- 2026-05-21 (**notification stage timeline (user bookings)**: `vehicle_view.py` `api_notifications` (L498+) — เพิ่ม bulk fetch `VehicleBooking` filter by `user_id == current_user.id` + `VehicleMileage` + latest `VehicleBudgetLog` (event_type=deduct) ของทุก booking_id ใน groups → compute `g['stages']` (เฉพาะ booking ของ user); import `VehicleBudgetLog`. 4 stages (option 1B: ซ่อน stage ที่ยังไม่เกิด, append เฉพาะที่มี timestamp): (1) `approved` `check-circle-2` ts=`booking.updated_at` เมื่อ `status=='approved'` (2) `trip_start` `play-circle` ts=`mileage.actual_start||created_at` เมื่อมี `odometer_start` desc="เริ่มต้นที่ N กม." (3) `trip_end` `flag` ts=`mileage.actual_end` เมื่อมี `odometer_end` desc="รวมระยะทาง N กม." (4) `budget` `wallet` ts=`log.created_at` 2-line desc `desc_main`="ใช้ ฿N" + `desc_sub`="หักจาก {bt.name MM/YYYY}". `pages/notification.js` `renderStageTimeline()` helper; `renderGroup()` ถ้า `g.stages` มี → ใช้ stages แทน message timeline (class `.notif-timeline--stages`). `notification.css` block ใหม่ `.notif-stage*` — grid 32px/1fr/auto, vertical connector line `var(--vc-border)` 2px ที่ left:15px (กึ่งกลาง icon), icon circle 32px border-only, 3 text levels (title fg/desc fg-muted/sub fg-subtle), time tabular-nums right column. ไม่มี CTA ปุ่มก้นการ์ด (option 4A))
- 2026-05-20 (**notification panel Vercel redesign**: `_notification_panel.html` + `notification.css` + `pages/notification.js` — Font Awesome → Lucide ทั้ง panel (panel head, tabs, sticky payment, group card, timeline, empty/loading/error states, toast); ลบ "ดูรายละเอียด booking" footer ใน group timeline + ดูรายละเอียด link ใน user payment card (consistent removal); tab switching = client-side filter only (zero refetch, instant), เพิ่ม keyboard nav ArrowLeft/Right/Home/End + aria-selected/role=tab; flatten colored `.notif-cat-icon` 7 variants → single neutral chip (`var(--vc-bg-subtle)` + `var(--vc-border)`); `.notif-group.unread` `--vc-accent-light` → `--vc-bg-subtle`; tab active underline `--vc-accent` → `--vc-fg` (Linear/Vercel pattern, accent reserved for focus); group-count unread invert `--vc-accent` → `--vc-fg`; sticky payment HEX literals → `--vc-amber/-border` + `--vc-red`; toast border-left colored stripes → single neutral border; mark-all hover ลบ accent-light; btn-link `--vc-accent` → `--vc-fg-muted`; เพิ่ม `notifMarkAll` button + `notifPanelTitle` ใน template (ของเดิมไม่มี); เพิ่ม loading skeleton (3 shimmer rows) + error state (`wifi-off` + retry button); เพิ่ม focus-visible rings 2px accent ทุก interactive; JS `faToLucide()` mapper (icon ตัวที่ server ส่งมาเป็น FA class → Lucide name); `lucide.createIcons()` หลังทุก innerHTML write; `loader-2` spin via `@keyframes notif-spin`; ARIA dialog/tablist/tabpanel ครบ; ลบ orphan `open-booking` handler)
- 2026-05-20 (**booking detail modal redesign**: `vehicle-modal-detail.html` + `pages/vehicle.js` `openEventDetail()` + `vehicle.css` `.bk-detail-*` block — ทิ้ง 44px header status circle → inline `.bk-detail-status` pill ใต้ title (6 modifiers `--{approved|pending|approver|rejected|group|completed}` map ไป `--vc-{green|amber|blue|red}-bg` + matching fg); modal width 440 → **480px**; title weight 700→600, date drop ไป `--vc-fg-subtle`; dashed divider → **solid full-bleed 1px**; `.bk-detail-text` color `--vc-fg-muted` → `--vc-fg` (primary info ควร primary); `.bk-detail-row` align center → flex-start (multi-line wrap); section label "ผู้จอง" → "**ผู้โดยสาร · N คน**" (count anchor) ใน `.bk-detail-section-header`; member row avatar เปลี่ยนจาก `vc-status-dot--*` → neutral `.bk-detail-member-avatar` (32px gray circle + user icon); fix `"ขึ้นรถที่ :: "` → `"ขึ้นรถที่: "` typo; footer collapse เมื่อ `detailActions` ว่าง (ประหยัด ~50px); footer button order reverse — `[ยกเลิกการจอง]` ซ้าย / `[แก้ไข]` ขวา (Vercel/Linear convention); `STATUS_ICON.completed` `circle-check` → `check-circle-2` (แยก approved/completed visually); ลบ ids `detailHeaderDot/detailHeaderIcon` ออกจาก template + JS. Primary info คงเดิม time/plate/driver — ไม่ promote dest/purpose ขึ้น primary)
- 2026-05-20 (**calendar overflow popover redesign**: `pages/vehicle.js` — ย้าย `+N รายการ` trigger DOM order จาก "เหนือ" chips → "ใต้" (Google/Apple Calendar convention); เพิ่ม class `.vc-cal-more` + Lucide `chevron-down` icon 12px เป็น affordance; popover constructor เพิ่ม `customClass:'vc-cal-pop'`; ลบ `data-bs-title` (popover header redundant กับ trigger label); refactor row markup จาก inline-styled flex → semantic classes `.vc-cal-pop-row/-dot/-time/-dest` (grid `auto 44px 1fr` lock columns); status indicator pill badge → 6px dot (`vc-cal-pop-dot--{approved|pending|approver|rejected|group}` ใช้ `--vc-{green|amber|blue|red}` tokens; group ยังใช้ `--vc-blue` เดิม); เพิ่ม Escape-to-close global handler; `vehicle.css` `.event-more` → `.event-more.vc-cal-more` (Vercel-style ghost ปุ่ม: muted resting / hover `--vc-bg-hover` + `--vc-fg`); CSS block ใหม่ใน `vercel.css` `.popover.vc-cal-pop*` (no shadow, border-only, no header, 240px width, max-height 320px scroll, arrow recolor))
- 2026-05-18 (**approver-inbox Phase 5.11 mobile redesign**: `vehicle/approver_inbox.html` + `approver_inbox.css` ปรับเป็น mobile-first — `.approver-wrap` 560px center; pending card header เปลี่ยนเป็น `BK-{id:04d}` mono + status badge (department text หาย); body restructure ใหม่เป็น `.ac-fields` 2-col CSS grid (6 fields: ผู้จอง/วันที่/เวลา/ปลายทาง=full-width/วัตถุประสงค์/ผู้โดยสาร) ทุก label มี Lucide icon นำหน้า (user/calendar/clock/map-pin/target/users); Font Awesome → Lucide ทุกจุด (รวม circle-x/circle-check/chevron-down/credit-card/message-square-x); tabs underline → pill style (`.inbox-tabs` gray container + `.inbox-tab.active` white); action buttons เป็น `.ac-action-row` 2-col (approve black solid / reject red outline); reject form ใช้ `.ac-reject-cancel` + `.btn-reject-confirm`; approved/rejected tabs ใช้ pattern เดียวกัน. JS handler names เดิม — `pages/approver-inbox.js` ไม่ต้องแก้) · **budget Phase 5.9 toggle is_active**: เพิ่ม column `vehicle_budget.is_active BOOLEAN NOT NULL DEFAULT 1` (migration `2026-05-18_vehicle-budget-is-active.sql`); `budget_service.py` เพิ่ม `set_active(budget, active)` → log event `set_active`/`set_inactive` (ไม่กระทบ used_amount); `vehicle_view.py` เพิ่ม `_lookup_budget_for_booking()` helper + check is_active ใน `approve_booking` admin+approver path + block `top_up`/`manual_adjust` เมื่อ inactive + POST action `toggle_active` + KPI sum filter active; `budget_manage.html` dropdown item "ปิด/เปิดใช้งานงบนี้" (form POST) + badge "ปิดใช้งาน" + `.vc-bcard--inactive` (opacity+stripe); `budget_manage.css` เพิ่ม `.vc-bcard--inactive` + `.vc-dropdown-item.is-danger` + `.vc-dropdown-divider` + `.vc-dropdown-form`. ไม่ block mileage deduct/refund — booking เก่าปิดทริปได้) · **budget Phase 5.8 Vercel spacing redesign**: `budget_manage.html` — rename `fuel-*` page classes → `budget-*`; ลบ inline `style=""` 18 จุด → semantic classes (`.budget-flash-stack/-kpi-strip/-grid/-grid--last/-empty-block/-filter-month/-refund-scroll/-refund-hint/-empty-icon/-modal-mono/-modal-alert*`, `.vc-bcard-approver`); inline `<script>` setBudgetModal handler → `pages/budget-admin.js`; `display:none` → `hidden`; rename `budget_admin.css` → `budget_manage.css` (โหลดเดี่ยว แทน fuel_admin+budget_admin); section-hdr `:first-of-type` looser top; `title=""` ครบทุกปุ่ม; responsive ≤575px stack) · **vehicle-admin Phase 6.1 UX features**: week strip volume dots (1=sm, 2-3=md 15px, 4+=lg 25px) — `.va-week-day-dot--sm/--md/--lg` + `title="N รายการ"`; assignModal — conflict warning under each select (FN#2, helpers `findConflict()` + `ACTIVE_STATUSES`), driver dropdown workload suffix "• N งานวันนี้" (FN#5, helper `driverDayCount()`); budget bar tone `.va-budget--{ok|warn|danger}` + `<p.va-budget-warn>` (FN#6, <20% amber, <10% red); fix legacy `--ds-success/warning/danger` → `--vc-green/amber/red` in `updateModalBudget()`. New template nodes: `#vehConflictWarn` `#drvConflictWarn` `#modalBudgetWarn`) · maintenance redesign: BBCenter V2 redesign `maintenance/maintenance.html` — vc-scope shell, form → modal, 5-cell KPI (admin), vc-table+DataTable, vc-badge statuses, Lucide icons; สร้าง `maintenance.css` + `pages/maintenance.js`) · Phase 0.5 + 1 + 2 + 3 frontend refactor: ลบ dead `vehicle_calendar.html` + route `/vehicle/calendar` · fix typo `--vc-bg-card` → `--vc-bg` · ลบ duplicate `design-system.css` load · **Phase 1**: แยก tokens ออกเป็น `app/static/css/tokens.css`, เพิ่ม `--vc-accent` Indigo · **Phase 2**: สร้าง `app/static/css/components/*.css` 8 ไฟล์ + `app/templates/_components/*.html` 7 macros; ย้าย KPI+filter จาก fuel_admin.css/budget_admin.css ไป components/ · **Phase 3.1 (fuel-admin)**: align macros + component CSS ให้ตรงกับ skill `bbcenter-design` (flat vocab `vc-badge-warning`, `vc-empty-title`, lucide icons), ลบ duplicate badge+empty จาก `fuel_admin.css`, migrate `admin_fuel.html` empty states → macro, update `/bbcenter-design` SKILL.md เพิ่ม §2.0 macro shorthand · **Phase 3.2 (vehicle-admin)**: ลบ `.bl-badge`/`.badge-pending`/`.badge-approver`/`.badge-approved`/`.badge-rejected`/`.badge-cancel`/`.badge-group`/`.bl-empty` จาก `vehicle_admin.css` (6 HEX violations + page-specific empty), update `vehicle_admin.js` STATUS_BADGE map + 4 innerHTML spots → `vc-badge-warning|blue|success|danger|neutral|solid` + `vc-empty` + `vc-empty-icon/-title` + lucide; re-add `vc-badge-solid` ใน `components/badge.css` สำหรับ "งานรวม" emphasis · **Phase 3.3 (mileage-admin)**: stripped `mileage_admin.css` 1095 → ~35 lines (ลบ ~30 dead `.mlg-kpi*/-btn*/-panel*/-breakdown*/-month*/-filter*/-table*/-empty*/-summary` blocks + duplicate `ds-alert*` + modal/state styling ที่ template inlines แล้ว); fix `mileage_admin.html` inline HEX `#DC2626`→`var(--vc-red)` ×3 + `var(--ds-accent)`→`var(--vc-fg)` ×1 · **Phase 3.4 (repair)**: migrate empty state → `empty_state` macro (`{% call %}`); เปลี่ยน `repair.css` `--ds-*` ทั้งหมด → `--vc-*` + HEX icon bg/border → `--vc-amber/blue/green-bg/border` · **Phase 3.5 (driver)**: 2 empty states → macro; ลบ inline `style=""` ทั้งหมด → `.driver-flash-alert/-plate/-unset`; ลบ dead `§1/.driver-page/.driver-container` + `§11/.driver-empty*` + `§13` icon overrides; `#D4D4D4` → `var(--vc-border-hover)` · **Phase 3.6 (vehicle user-facing)**: `STATUS_BADGE` map → `vc-badge-dot`; ลบ `.badge-approved/.badge-pending/.badge-approver` CSS; `vehicle-modal-book.html` info card `badge-pending` → `.bk-info-note`; `--ds-border`/`#111827` → `--vc-*` ใน bk-modal · **Phase 3.7 (ot/vehicle_cost)**: `--ds-text-2xl/sm` → `--vc-text-*`; HEX print section → `var(--vc-fg/fg-muted/fg-subtle/border)`; เพิ่ม `.cost-action-group`; ลบ inline `style=""` 4 จุดใน template · **Phase 3.8 (approver-inbox)**: ลบ inline `<style>` 197 lines + `<script>` → `approver_inbox.css` + `approver_inbox.js`; badge-waiting/approved/rejected → `vc-badge-dot`; 3 empty states → `vc-empty`; ลบ inline `style=""` ทั้งหมด · **Phase 4.0 (core JS)**: สร้าง `app/static/js/core/{icons,format,http}.js` เป็น ES modules — foundation สำหรับ feature module migration (Phase 4.1+). ยังไม่สร้าง `modal/toast/form` (รอ feature ต้องการ) · **Phase 4.1 pilot (approver-inbox)**: `approver_inbox.js` → `pages/approver-inbox.js` ES module; template `<script type="module">`; window expose สำหรับ legacy `onclick=""` · **Phase 4.1.5 (sidebar extract)**: ย้าย `.sidebar/.sb-*` (+ ลบ legacy `.menu-item/.brand-logo/.sidebar-menu`) จาก `vehicle.css` → `components/sidebar.css` + เพิ่มเข้า `design-system.css` @import → approver_inbox sidebar กลับมาทำงาน (และทุก page ได้ฟรี); vehicle.css 618 → 460 lines · **Phase 4.2 (repair)**: `repair.js` → `pages/repair.js` ES module; imports `initIcons`/`bindModalReinit` จาก `core/icons.js`; ลบ `$(document).ready()` wrapper · **Phase 4.3 (vehicle-admin)**: `vehicle_admin.js` (1141 lines) → `pages/vehicle-admin.js` ES module; imports `initIcons` จาก `core/icons.js`; `lucide.createIcons()` → `initIcons()` (3 จุด); expose 20+ funcs ไป `window.*` สำหรับ legacy `onclick` ใน template + JS-rendered HTML strings · **Phase 4.4 (driver-home)**: extract inline `<script>` (67 lines) → `pages/driver-home.js` ES module; ลบ duplicate lucide CDN (มาจาก `_header.html` แล้ว); ไม่มี `onclick=""` → ไม่ต้อง window expose · **Phase 4.5 (mileage-admin)**: `mileage_admin.js` (279 lines) → `pages/mileage-admin.js` ES module; ลบ IIFE wrapper; expose `openMileage/goEditEnd/clearSelection` ผ่าน `Object.assign(window, …)` · **Phase 4.6 (fuel-admin)**: `fuel_admin.js` (424 lines) → `pages/fuel-admin.js` ES module; enhance `core/icons.js` `initIcons(scope?)` + `bindModalReinit` ส่ง `e.target` เป็น scope; ลบ IIFE + local `initIcons` + DOMContentLoaded guard; ไม่มี `onclick=""` → ไม่ต้อง window expose · **Phase 4.7 (vehicle)**: `vehicle.js` (790 lines) → `pages/vehicle.js` ES module; expose `openEventDetail`/`openEditBookingModal`/`openMoreEvents`/`openBookingModal` + modal instances (`eventDetailModal`/`moreEventsModal`) ผ่าน `Object.defineProperty` getter; ย้าย `?pay=<id>` deep-link IIFE จาก `vehicle.html` เข้า module (ลบ retry loop — in-module ไม่ต้อง `typeof` check); ลบ DOMContentLoaded wrapper · **Phase 4.8 (ot-admin)**: `ot_admin.js` (213 lines) → `pages/ot-admin.js` ES module; imports `initIcons` จาก `core/icons.js`; ลบ IIFE + DOMContentLoaded wrapper; pure data-attr delegation → ไม่ต้อง window expose · **Phase 4.9 (notification)**: `notification.js` (453 lines) → `pages/notification.js` ES module; โหลดจาก `_header.html` → ทุกหน้า; ลบ IIFE + `window.__notifInit` guard (module เรียกครั้งเดียว); Font Awesome icons → ไม่ใช้ `core/icons.js`; ไม่มี `onclick=""` → ไม่ต้อง window expose · **Phase 4.10 (budget-admin)**: `budget_admin.js` (98 lines) → `pages/budget-admin.js` ES module; imports `initIcons`/`bindModalReinit` จาก `core/icons.js`; ลบ IIFE + local `initLucide` + duplicate lucide CDN; ไม่มี `onclick=""` → ไม่ต้อง window expose. `main.js` (login เท่านั้น) **kept legacy** — jQuery IIFE, isolated page → Phase 4.x migration complete; `app/static/js/` ตอนนี้แค่ `core/` + `pages/` + `main.js` (legacy) · **Phase 5.5 (lucide self-host + API fix, 2026-05-16)**: (1) download `lucide@1.16.0` UMD → `app/static/vendor/lucide/lucide.min.js` (392KB); `_header.html` script src CDN → local + ลบ `defer`. (2) **lucide v1.x breaking API**: `createIcons()` ต้องส่ง `{ icons }` (ไม่ auto-include เหมือน v0.x) → update `core/icons.js` `initIcons()` ส่ง `{ icons: l.icons \|\| l }` + inline DOMContentLoaded listener ใน `_header.html`. (3) Bug fix `pages/vehicle-admin.js`: `renderBefore()` 9 callers (toggleBeforeExpand/setFilter/groupMode/notifyMode toggles) populate innerHTML แต่ไม่ตามด้วย `initIcons()` → เพิ่ม `initIcons()` end of `renderBefore()` (อาการ: collapsed บน initial load → user คลิก expand → icon ค้าง `<i>`) · **Phase 5.1 (token retirement, 2026-05-16)**: `--ds-*` Part A ลบออกจาก `tokens.css` ครบแล้ว; migrate `var(--ds-*)` → `var(--vc-*)` ใน CSS 3 ไฟล์ (design-system/notification/vehicle) + templates 4 ไฟล์; เพิ่ม `--vc-z-*`/`--vc-sidebar-width`/`--vc-header-height` ใน Part B; lint rule ใน tokens.css header; CLAUDE.md updated · **Phase 5.2**: CSS coupling audit — clean (ไม่มี cross-page dependency); design-system.css @import chain ถูกต้อง · **Phase 5.3** (inline `style=""` cleanup): defer → future_features.md (112 จุดใน mileage_admin.html เป็น dynamic Jinja values) · **Phase 5.4 polish (vehicle-admin, 2026-05-16)**: icon FOUC fix (`[data-lucide]:not(svg)` reserve 14×14), week navigator redesign (กรอบ+padding+responsive ≤575px), booking row 2-line → **spacious card** (ลบ `.bl-row-top/.bl-meta-dest/.bl-dot/.bl-actions` → เพิ่ม `.bl-body/.bl-row-head/.bl-meta-chip/.bl-row-assigned/.bl-row-actions` + แสดง vehicle/driver row เมื่อ approved), `.va-list` flex+gap, HEX cleanup 13 จุด → tokens (`--vc-{red\|green\|blue\|amber}-{bg\|border}`) → **zero HEX** ใน vehicle_admin.css. `renderSingleRow()` rewritten + `bl-actions` → `bl-row-actions` ใน group row · **vehicle user-facing Phase 1 (2026-05-17)**: เริ่ม redesign `vehicle/vehicle.html` ไป BBCenter V2 — viewport `maximum-scale=1.0`; ลบ DataTables CSS + IBM Plex Sans Thai; CSS link เพิ่ม `design-system.css` + `vehicle_admin.css`; `<main>` ใส่ `vc-scope`; flash `alert` → `ds-alert` ใน `vc-stack`; toolbar 3 ปุ่ม → `vc-btn vc-btn-ghost vc-btn-icon` + Lucide chevron-left/right + `vc-btn-secondary` "วันนี้"; calendar wrapper `card h-100` → `vc-card`; mobile "จองรถ" → `vc-btn-primary` + Lucide plus · **vehicle user-facing Phase 2 (2026-05-17)**: redesign 4 modals — `book/edit/detail/more-events` — `bi-*`/`fa-*` icons → Lucide; footer buttons → `vc-btn vc-btn-secondary/primary` + `title=""`; ลบ `shadow` + inline `border-radius:14px` ใน more-events; ลบ inline style 9 จุดใน detail → 8 classes (`bk-detail-header-dot/title/date/divider/info/row/icon/text/section-label`); เพิ่ม `bk-modal-dialog-sm` + `bk-duration-preview` ใน `vehicle.css`; `initFlatpickr()` เพิ่ม `updateDuration()` closure (compute end−start → "ระยะเวลา X ชม. Y นาที", bind change ทั้ง bkStart/bkEnd). **vehicle user-facing Phase 3 (2026-05-17)**: ds-status-dot refactor — `.ds-status-dot*` (5 variants) → `.vc-status-dot*` ใน `design-system.css` §7 + hex literals 5 ชุด → tokens (`--vc-{amber|blue|green|red|purple}-bg` + foreground); เพิ่ม `--vc-purple-bg`/`--vc-purple-border` ใน tokens.css (สำหรับ group dot). `pages/vehicle.js` STATUS_ICON FA strings → Lucide names (clock/send/circle-check/circle-x); 5 จุดที่ใช้ class swap → `vc-status-dot` + `<i data-lucide="..." class="vc-icon-sm">` (group→users, single→${iconName}, detail header→${headerIconName} via `outerHTML` swap, members→user); `import { initIcons, bindModalReinit }` จาก `core/icons.js` + เรียก `bindModalReinit()` (detail modal re-init on show) + `initIcons(content)` ปลาย `updateMobileList`. `vehicle-modal-detail.html` initial header dot → vc-status-dot + Lucide circle-check. **vehicle user-facing Phase 4 (2026-05-17)**: cleanup + UX polish เสร็จ — (a) `var(--ds-text-heading|text-muted|border)` 14 จุดใน `pages/vehicle.js` HTML strings → `var(--vc-fg|fg-muted|border)` (Phase 5.1 violation cleared); HEX `#f3f4f6`/`#2563EB` → tokens; `EVENT_CARD_STYLE` 5 statuses × 3 HEX → tokens (`--vc-{amber|blue|green|red}-{bg|border}` + `--vc-bg-subtle/--vc-border/--vc-fg-muted` สำหรับ completed). (b) FA/BI icons ทั้งฉบับ → Lucide (~17 จุด: empty state calendar-x, group card truck/user/clock/pencil/chevron-down/arrow-up-right, single card user/clock/map-pin/pencil, openMoreEvents user, members list users/arrow-right/map-pin, footer actions pencil/trash-2, popover users); inline-style buttons → `vc-btn vc-btn-ghost vc-btn-icon vc-btn-sm` หรือ `vc-btn-secondary/danger` + `title=""`; "งานรวม" HEX badge → `vc-badge vc-badge-blue`. (c) **UX polish**: เพิ่ม keydown listener (← → เปลี่ยนเดือน, T = วันนี้; skip ถ้า focus input/modal); เพิ่ม `shown.bs.popover` listener → `initIcons` ทุก `.popover` (เพราะ popover async ไม่ใช่ modal); `createCell` เพิ่ม `role="gridcell"` + `aria-label="{day} {th-month} {buddhist-year}"` + `aria-current="date"` ถ้า today. (d) `vehicle.html` calendar grid: `role="grid"` + `aria-label` (อธิบาย shortcut) + header row `role="row"` + cells `role="columnheader" aria-label="{full day name}"` + body `role="rowgroup"`

---

## INDEX_ui.md — Templates § history (ย้ายจาก INDEX_ui.md 2026-07-05 เพื่อลด token budget — ดู tools/doc-stats.sh)

_ต่อ template · ย้อนเก่า→ใหม่ตามที่เขียนไว้เดิม (ก่อน migrate เป็น bb-* 2026-07-05) · state ปัจจุบันดูที่ INDEX_ui.md § Templates_

### vehicle/admin/vehicle_budget.html (จัดการงบ)

**Phase 4.10 (2026-05-16):** script → ES module `<script type="module" src="js/pages/budget-admin.js">`; ลบ duplicate lucide CDN (มาจาก `_header.html` แล้ว). **Phase 5.8 polish (2026-05-18):** Vercel spacing pass — `fuel-*` classes (header/title/subtitle/header-actions) → `budget-*`; ลบ inline `style=""` 18 จุด → semantic classes (`.budget-flash-stack/-kpi-strip/-grid/-grid--last/-empty-block/-filter-month/-refund-scroll/-refund-hint/-empty-icon/-modal-mono/-modal-alert/-modal-alert--flush`, `.vc-bcard-approver`); inline `<script>` setBudgetModal handler → `pages/budget-admin.js`; `display:none` → `hidden` attr; `<i style="width:20px;height:20px;">` → `.budget-empty-icon`; ทุก `<button>` มี `title=""` ครบตามกฎ §0; section header รับ `:first-of-type` spacing; โหลด `budget_manage.css` แทน `fuel_admin.css`+`budget_admin.css`. **Phase 5.9 (2026-05-18):** budget card dropdown เพิ่ม divider + form POST item "ปิดใช้งานงบนี้" / "เปิดใช้งานงบนี้" (action=`toggle_active` + hidden year/month/budget_id/to_active); `<div class="vc-bcard">` รับ `{% if not b.is_active %}vc-bcard--inactive{% endif %}`; เพิ่ม badge "ปิดใช้งาน" (`vc-badge-neutral vc-badge-dot` + `power-off` icon) ใน head เมื่อ inactive. **Phase 1+2 redesign (2026-05-21):** Phase 1 — CTA "ตั้งงบกอง" `vc-btn-purple`→`vc-btn-primary`; `.vc-bcard--inactive` ทิ้ง stripe → dashed border; dropdown padding 6→8px. Phase 2 — KPI 6→4 cells (ลบ "งบส่วนกลาง"+"งบงานกอง" cells, `--6`→`--4`), amount 2 ตัวย้ายขึ้น `vc-section-hdr` เป็น `.vc-section-hdr-amount`. **Phase 3 (2026-05-21):** card hierarchy — central+dept loops `.vc-bcard-row` → `.vc-bcard-pct-hero` (percent เป็น hero, label "ใช้ไปแล้ว" demoted muted); drop `is-purple` markup จาก dept loop amounts (`<span class="used">` ไม่มี `.is-purple` แล้ว). **Phase 4 (2026-05-21):** premium polish — purple retirement complete (dept section icon, name icon, progress fallback, refund modal badge → neutral; markup zero purple, CSS rules `.vc-btn-purple`/`.vc-badge-purple` kept for Phase 8); KPI "คงเหลือ" cell ได้ `vc-kpi-cell--signal` (bg-subtle ลด monotony); per-card inline `style="--bm-delay: {{ loop.index0 * 40 }}ms"` stagger ทั้ง 2 loops; 4 modals ได้ class `budget-modal-enter` (scoped animation, ไม่ leak `.vc-modal`). **Phase 5 (2026-05-22):** `.budget-personal-row` (L153) → `.budget-personal-line` (card → inline lightweight strip). **Phase 6 (2026-05-22):** 3 modal alert boxes → meta lines: setBudget (L559) `ds-alert-info` → `<p.budget-modal-notice>` + info icon; topUp (L593) `ds-alert-info` → `<dl.budget-modal-meta>` 2 `<dt>/<dd>` rows (`data-bind` ย้ายไป `<dd>`); adjust (L641) same pattern. **Phase 7 (2026-05-22):** แทรก 2 `<details class="vc-card vc-pivot-wrap budget-pivot-card">` (Pivot งบส่วนกลาง × เดือน + Pivot งบกอง × เดือน) ระหว่าง personal-line กับ "งบประมาณส่วนกลาง" section header; collapsed default + fiscal year (มี.ค.→ก.พ.) cols + heat-tinted cells + drill-down link `url_for('adminfleet.budget_manage', year=y, month=m)` (fiscal mapping ถูกต้อง); receive new template vars `pivot`/`fiscal_year_start_ad`/`TH_MONTHS`. **Phase 8 (2026-05-22):** dead purple CSS cleanup — `.vc-btn-purple` + `.vc-badge-purple` blocks ใน `budget_manage.css` §7 + `.vc-kpi-value--purple` ใน `components/kpi.css` ลบครบ (markup zero usage; tokens เก็บไว้). **UX writer Phase 1 (2026-05-22):** wording overhaul + confirm guard บนปุ่ม destructive — (1) page-header CTA `คืนงบจาก booking` (rotate-ccw) → `ยกเลิกการจองที่ยังไม่ปิดทริป` (x-circle) (สื่อ flow จริงว่ารายการคือ booking ที่ยังไม่หักงบ); (2) `setBudgetModal` รองรับ create vs edit dynamic — เพิ่ม `id="sbAmountLabel/sbAmountHint/sbNotice/sbNoticeText/sbSubmitText"` ให้ JS swap คำได้ (edit: `แก้เพดาน<group> — <dept>` + submit `อัปเดตเพดาน`; create: `ตั้ง<group>ใหม่` + submit `บันทึกงบ`); แต่งคำ label/hint ของ approver row + start/end ใหม่; (3) `budgetRefundModal` retitle `ยกเลิกการจองที่ยังไม่ปิดทริป` + hint อธิบาย rule "ปิดทริปแล้วยกเลิกไม่ได้" + ปุ่ม `ยกเลิก + คืนงบ` → `ยืนยันยกเลิกการจอง` (icon x-circle); (4) `budgetAdjustModal` retitle `แก้ยอดงบที่หักผิด` + warning notice "ไม่ใช่การคืนเงินจริง" + label/hint อธิบายค่าบวก/ลบชัดเจน + placeholder ตัวอย่างเคสจริง + submit `บันทึกการแก้ไข`; (5) `budgetTopUpModal` retitle `เพิ่มเพดานงบ (top-up)` + hint "ยอดนี้บวกกับเพดานเดิมทันที" + submit `เพิ่มเข้าเพดานงบ`; (6) toggle_active form (central + dept loops) รับ `data-confirm-toggle data-dept-name data-to-active` → JS confirm dialog ก่อน submit. **Phase 2 redesign round 1 (2026-05-22):** scrap 4-cell KPI strip + 2 `vc-section-hdr` + `.budget-personal-line` + 2 pivot details → (1) **Summary card** บนสุดแทน KPI strip — `<section.budget-summary>` 1 hero amount `฿used / ฿cap` + 4px progress (ok/warn/danger tone จาก pct_of_cap) + meta row "% ของเพดาน · เหลือ ฿X" + signals footer (รวม pending + over-budget + personal-unpaid → pill format). (2) **Pivot รวม 3 section** — 2 `<details>` (central + dept) → 1 `<details id="pivotAllDetails">` 3 `<tbody.vc-pivot-section>` (ส่วนกลาง / งานกอง / ส่วนตัว) ทุก section header row ใช้ `.vc-pivot-section-head td[colspan=col_count]`; personal row drill-down ไป `adminfleet.budget_personal`; heat scale per-section independent (`central_max/dept_max/personal_max`). (3) **3-section accordion** แทน vc-section-hdr — `<details.budget-section open id="sectionCentral/sectionDept">` (default open) + `<details.budget-section id="sectionPersonal">` (default closed); summary = label (chevron + icon + title + count + pending badge) + stats (`฿used / ฿cap` + `%pct--{tone}`); body = `.budget-section-actions` (ปุ่ม `[+ ตั้งงบ]` ย้ายเข้าด้านใน) + existing `budget-grid` (รอบ 2 จะแทน → table). Section "ส่วนตัว" รอบ 1 = link out ไป `adminfleet.budget_personal` + placeholder; รอบ 2 จะ embed mileage table + ปุ่ม `[รับเงิน]` inline. (4) Template vars ใหม่: `kpi.personal_unpaid_count/-amount` (mileage ปิดทริปแล้วแต่ admin ยังไม่ confirm รับเงิน, scope = เดือนที่เลือก) + `kpi.over_budget_count` (active rows ที่ used > cap) + `kpi.pct_of_cap` + `pivot.personal/-max`. **Phase 2 redesign round 2 (2026-05-22):** card → table conversion. (1) ภายใน sectionCentral + sectionDept body: `.row.g-3.budget-grid` (vc-bcard grid) → `<table.vc-table.budget-table>` ใน `<div.budget-table-wrap>`. Cols ส่วนกลาง: หมวด · เพดาน · ใช้ไป · % (mono + `.vc-progress--mini`) · คงเหลือ · tags · dropdown kebab. Cols กอง: + `ผู้อนุมัติ` col (data shape: pill ปกติ หรือปุ่ม `.budget-row-approver--missing` ที่เปิด setBudgetModal ตรง field approver). Row inactive ใช้ `.budget-row--inactive` (opacity 0.58 + repeating-linear-gradient stripe + border-left strong). Dropdown markup เก็บเดิม ทั้ง 3 items (แก้เพดาน/top-up/adjust) + form toggle_active (data-confirm-toggle เก่าทำงานปกติ). (2) sectionPersonal expanded by default ถ้ามี unpaid + body embed `<table.budget-table--personal>` cols: วันที่ปิดทริป · BK-id · ผู้จอง · ปลายทาง · ค่าน้ำมัน · สถานะ (badge รับแล้ว/ค้างรับ) · ปุ่ม `[ยืนยันรับเงิน]` (form POST `adminfleet.budget_personal_mark_paid` พร้อม `data-confirm-pay/-user/-amount`). Backend route ใหม่: `personal_rows[]` query mileage paid+unpaid ของเดือนที่เลือก (paid ใช้ `personal_paid_at`, unpaid ใช้ `actual_end`). (3) JS `budget-admin.js` เพิ่ม document-level submit listener `form[data-confirm-pay]` → confirm() + AJAX fetch (route return JSON) → location.reload(); btn loader state + error fallback. (4) CSS §13 `.budget-table-wrap/.budget-table/.budget-row/--inactive/-name/-sub/-pct-cell/-pct[/.is-warn/.is-danger]/-rem-over/-tags/-approver/--missing/.budget-personal-paid-at/.vc-progress--mini`. ลบ §2 `.budget-grid/-grid--last` margin rules (dead). **Phase 3 redesign round 3 (2026-05-22):** flat layout — กำจัด accordion + simplify. (1) **3A** standalone `<form.vc-filter-bar id="filterForm">` ถูกย้ายเข้าไปอยู่ใน `.budget-summary-head` (right side) → `<form.budget-summary-filter>` + 2 `<select.budget-summary-filter-select>` (month + year compact 32px). (2) **3B** `<details.budget-section open>` × 3 (Phase 2C) → `<section.budget-section>` + `<header.budget-section-head>` แบบ flat (ไม่ collapsible, ไม่มี chevron); section title + count + pending badge + stats + actions อยู่บนแถวเดียวกัน; ปุ่ม `[+ ตั้งงบ]` ย้ายเข้า `.budget-section-actions` ใน head (ไม่ใช่ body separate). (3) **3C** ลบ col "% ใช้" + `.vc-progress--mini` → แสดง pct inline หลัง amount ใน col "ใช้ไป": `<span.budget-row-used>฿11,128</span> <span.budget-row-pct-inline>(74.2%)</span>` พร้อม tone class (`.is-warn` ≥70%, `.is-danger` ≥90%) — color signal มี ทั้ง 2 spans (cohesive). (4) **3D** `.vc-dropdown-menu` z-index 5→60 + `box-shadow` gentle + `.budget-table-wrap:has(.vc-dropdown.is-open)` → `overflow: visible` (release horizontal-scroll clip ตอน menu เปิด, CSS `:has()` Level 4). (5) **3E** sectionPersonal restructure — ลบ "ดูทั้งหมด" external link + ลบ col "จัดการ" (รวมเข้า col "สถานะ"). เพิ่ม `<div.budget-personal-tabs>` (segmented pills: ทั้งหมด/ค้างรับ/รับแล้ว + count badges); default active = ค้างรับ ถ้ามี unpaid, else ทั้งหมด. Row markup รับ `data-personal-row="paid|unpaid"` + `hidden` initial state จาก server (ค้างรับ default). BK format `#{:04d}` → `BK-{id}` plain. Badge "ค้างรับ" เปลี่ยนจาก `<span>` → `<button type="submit">` ภายใน `<form.budget-personal-pay-form.d-inline>` (Option A — clickable badge → existing `data-confirm-pay` handler trigger). (6) CSS revise §12 (flat header) + new §12B `.budget-summary-filter/-filter-select` + new §14 `.budget-personal-tabs/.budget-personal-tab/.budget-personal-tab-count/.budget-personal-status[/button.budget-personal-status:hover]`. Dead: `.budget-row-pct-cell/-pct/.vc-progress--mini/.budget-section-summary/-chev/-body` cleanup. **Phase 3 hotfix (2026-05-22 same day):** (a) `.budget-table-wrap` เพิ่ม `overflow-y: hidden` (suppress browser auto-derived vertical scrollbar — fix badge clipped ใน row top). (b) Stacking context fix สำหรับ dropdown — `.budget-row` มี `animation: bm-fade-up` (transform) → implicit stacking context per row, row ด้านล่าง DOM cover dropdown menu ของ row ด้านบน. แก้ด้วย `.budget-table tbody tr { position: relative }` + `tr:has(.vc-dropdown.is-open) { z-index: 70 }` (bump เฉพาะ row ที่กำลังเปิด). (c) `.budget-personal-tabs` revise style → ลอก `.mlg-status-chips/.mlg-chip` exactly (white container + black active bg + ลบ count badge). **2026-06-06 (งบช่วงเวลา):** + Section 4 `#sectionArchived` ("คลังงบ") ใต้ sectionPersonal — `<table.budget-table>` (งบ/ประเภท/เพดาน/ใช้ไป/ช่วงเดิม/สถานะ-badge/ปุ่ม "เพิ่มเวลา") loop `archived_budgets`; ปุ่มเปิด modal `#extendBudgetModal` (action `extend_period`: date start/end required + topup optional) ผ่าน `data-bid/-dept/-start/-end`. **2026-06-08 (cmdk menus):** filter `month` + `year` (`.vc-filter-select.budget-summary-filter-select`, `onchange=this.form.submit()`) ได้ `data-dropdown` → enhance เป็น `.vc-dd` (dd dispatch `change` → inline onchange submit ทำงานต่อ); โหลด `core/js/dropdown.js` (module) ต่อท้าย `vehicle_budget.js`. trigger รับ class `budget-summary-filter-select` มา size แต่ override `padding-right:8px` (chevron จริง ไม่ใช้ bg-chevron) ใน `vehicle_budget.css`. kebab `.vc-dropdown` (`data-dropdown-trigger`) เป็นคนละ component ไม่กระทบ. **2026-06-08 redesign (pivot/KPI/table):** (1) **Pivot** `#pivotAllDetails` `<details>` matrix-always-on → `<section.vc-card>` + 3 `<details.budget-pivot-cat>` (ส่วนกลาง/ส่วนกอง/ส่วนตัว) — default = สรุปแถว (icon+ชื่อ+count + เพดานรวมปีงบ `.budget-pivot-cat-cap` + badge ใช้ไป% `.budget-pivot-cat-badge--{ok\|warn\|danger}`), กดขยาย = matrix ใช้ไป×เดือน (col "รวม" ต่อท้าย) ผ่าน `{% macro pivot_cat_table(...) %}`; ใช้ `pivot.summary` (key ใหม่จาก `_build_budget_pivot`). (2) **KPI** single `.budget-summary-bar` → `.budget-summary-stack` stacked segment (central/dept = indigo เฉด `#4F46E5`/`#818cf8` + track เทา=เหลือ) + `.budget-summary-legend` (dots central/dept/free + personal `--aside` ดันขวา). (3) **Table col align** central+dept: ย้าย `ผู้อนุมัติ` col → sub-line ใต้ชื่อกอง (`.budget-row-sub--approver`/`--approver-missing`), ตัด pending "รอปิด" badge + `.budget-section-stats` ออกจาก dept (col ตรงกับ central). (4) **เพิ่ม col bin** (`.budget-bin-btn` trash-2 แดง) ทั้ง 2 section = **soft delete** → form `toggle_active to_active=0` (reuse `data-confirm-toggle` JS เดิม). (5) ตัด `.budget-section-stats` amount ออกจาก central+dept (ซ้ำกับ KPI bar). CSS §17–19 ใน `vehicle_budget.css`. **2026-06-09 (approver searchable):** `#sbApprover` (`setBudgetModal`) `<select class="vc-select">` → เพิ่ม `data-autocomplete placeholder="ค้นหาชื่อผู้อนุมัติ…"` → enhance เป็น searchable combobox (component 3 `select[data-autocomplete]`, ดู §Key Functions VCMenus) คง submit `approver_id` (id) เดิม; `vehicle_budget.js` setBudgetModal preset เพิ่ม `sel.dispatchEvent(new Event('change'))` หลัง set value เพื่อ sync visible label. **Phase 5 personal pivot (2026-06-12):** personal pivot table aggregate row label "ค่าน้ำมัน" → "รวมทั้งหมด" + class `vc-pivot-row-summary`; เพิ่ม per-user sub-rows iterating `pivot.personal_by_user` พร้อม class `vc-pivot-td-sub`. **Tab redesign (2026-06-15):** จาก stacked sections → tab layout. (1) **Summary card** redesign 2-col — `.budget-summary-grid` (`.budget-summary-main` icon+label+`.budget-summary-amount` ซ้าย · `.budget-summary-divider` · `.budget-summary-breakdown` ขวา = `.budget-summary-bd-head` pct/เหลือ + stack bar + legend); filter (month/year) **ย้ายออก** จาก summary head → toolbar. stack bar เปลี่ยน semantics เป็น composition ค่าใช้จ่าย 3 หมวด (central/dept/personal) + เพิ่ม `.budget-summary-seg--personal`. (2) **Tab bar** `<nav.budget-tabs>` 5 ปุ่ม `.budget-tab[data-budget-tab=pivot|central|dept|personal|archived]` เส้น underline เต็ม container, active = accent blue. (3) **Toolbar** `#budgetToolbar.budget-toolbar` (filter `#filterForm` + `.budget-toolbar-actions` add buttons `[data-tab-add=central|dept]`) — `hidden` ตอน tab pivot, add สลับตาม tab. ปุ่ม add ย้ายออกจาก section header ของ central/dept (`.budget-section-actions` หาย 2 จุด). (4) **Panels** `.budget-panels` > 5 `<section.budget-panel[data-budget-panel]>` (pivot default `.is-active`); pivot card/4 budget-section ห่อใน panel ตามลำดับ. (5) JS `vehicle_budget.js` IIFE `initBudgetTabs` — client-side switch (toggle `.is-active` + `hidden`, sync toolbar/add). CSS `vehicle_budget.css` §20 (`.budget-summary-grid/-main/-icon/-label/-divider/-breakdown/-bd-head`, seg/legend สี green/amber/red, `.budget-tabs/.budget-tab`, `.budget-toolbar/-filter/-actions`, `.budget-panel`). **Tab redesign round 2 (2026-06-16):** ปรับตามภาพ ref. (1) summary section ลบ `vc-card` (ไม่ใช่ card) + grid ชิดซ้ายไม่เต็มจอ (`.budget-summary-breakdown` width 340px, mobile 100%); ลบ signals footer (`.budget-summary-signals`) ออกหมด. (2) `.budget-tabs` underline เต็มจอถึง sidebar (`margin-inline:-1rem` negate container px-3) + หนา 4px; `.budget-tab` เพิ่ม class `text-header fw-bold`. (3) 4 budget-table ได้ `data-sortable-table` + `<th data-sort="text|num">` → คลิก header sort (JS IIFE `initSortableTables` ใน `vehicle_budget.js`: parse ฿/,/% → number, localeCompare ไทย, toggle asc/desc, caret `::after` ⇅/↑/↓ accent). image-style polish: `.budget-table-wrap` box-shadow + radius 10px (DESIGN-OVERRIDE ตามที่ผู้ใช้สั่ง "ไม่ต้องสนใจกฏ"). CSS §21. **Round 3 (2026-06-16):** (1) revert tab underline กลับเป็นเดิม (ลบ §21B 4px/full-bleed → §20C 1px line + active 2px). (2) **ลบ `.budget-toolbar`** (filter เดือน/ปี + ปุ่ม add central/dept) ออกหมดตามคำสั่ง — CSS §20D + JS toolbar/addBtns refs ลบครบ; **ผล: ไม่มี filter เดือน/ปี + สร้างงบใหม่ได้ผ่าน dropdown "แก้เพดานงบ" ของแถวเท่านั้น** (ไม่มีปุ่ม "ตั้งงบใหม่"). (3) ลบ `.fuel-header-actions` (ปุ่มเปิด `#budgetRefundModal`) — **modal `budgetRefundModal` ตอนนี้ไม่มี trigger** (เหลือใน DOM). (4) page header เปลี่ยนเป็นสไตล์ vehicle.html: `<h1.fuel-title>`+`<p.fuel-subtitle>` → `<h2 class="fw-bold text-accent pt-3">` + `<h6 class="text-muted">`. **DNA redesign (2026-06-17):** (1) head โหลด **Manrope** (`&family=Manrope:...`) — ตัวเลขทุกที่ใช้ Manrope ผ่าน `.vc-mono` (`--vc-font-mono`). (2) Summary bar: segment ที่ 3 `personal` → **คงเหลือ** (`.budget-summary-seg--remain`/`-legend-dot--remain`); sizing เทียบ `total_budget` (c_w/d_w = used, p_w = remaining); CSS §20B สี central=accent/dept=amber/remain=green. (3) **tab + toolbar บรรทัดเดียว** `<div.budget-tabbar.d-flex.align-items-end.justify-content-between>` ครอบ `<nav.budget-tabs>` (overflow-x auto) + `<div.budget-toolbar>` ขวา = "เลือกเดือน" (vc-dropdown `.budget-month-menu` link `budget_manage?year=&month=` วน `pivot.fiscal_months`) + "ตั้งงบใหม่" (เปิด `setBudgetModal` type=department เปล่า). underline ย้ายมา `.budget-tabbar` (tabs border-bottom 0); `.budget-tabbar` = `display:flex` ตรง + `flex-wrap:nowrap` (tabs `flex:1 1 auto;min-width:0;overflow-x:auto;overflow-y:hidden` ย่อ+scroll · toolbar `flex:0 0 auto` pin ขวา) + **`position:sticky;top:56px;z-index:4;bg ทึบ`** (ตรึงใต้ `.vrc-topbar` 56px ตอนเลื่อนหน้า). (4) **ตัด tab "คลังงบ"** (archived) — ลบ `#tabPanelArchived`; งบไม่ active แสดงเป็น section "ไม่ได้ใช้งาน" ใน tab central/dept แทน (ดึง `archived_budgets|selectattr/rejectattr('budget_type','equalto','central')` → `central_off`/`dept_off`). (5) **central/dept: table → card grid** ผ่าน 2 macro `budget_card(b, btype, icon)` (full dropdown แก้เพดาน/top-up/adjust/ปิดใช้งาน) + `budget_card_off(b, icon)` (disable tone + ปุ่ม "เปิดการใช้งาน" เปิด `extendBudgetModal`); section head dot `.bcard-section-dot--on`(เขียว ใช้งานอยู่)/`--off`(กลวง ไม่ได้ใช้งาน) + `.bcard-section-line`; การ์ด `.bcard` content = ชื่อหมวด → `฿used/cap (%)` (Manrope, `.bcard-pct.is-ok/-warn/-danger`) → `.bcard-divider` → ตั้งแต่วันที่–วันที่ → (dept) ผู้อนุมัติ/`.bcard-meta-missing`. ส่วนตัว **คง table** (`data-sortable-table` เดิม). (6) icon monochrome `--vc-icon #9999b0` บน tile `--vc-icon-bg`. CSS §22 (`.budget-tabbar/-toolbar/.budget-month-menu/.bcard*/.bcard-section-*`). หมายเหตุ: macro ไม่เห็น `loop` → ตัด `--bm-delay` stagger ออก.

---

### vehicle/admin/vehicle_mileage.html (บันทึกเลขไมล์ admin)

**vc-scope shell** (2026-05-13): `fuel-header` pattern, ~~4-cell KPI~~ (ลบ Phase 9.1), `mlg-toolbar` (toolbar pattern), checkbox summary strip (`vc-card`), `vc-table` + `vc-card-head`, lucide icons, `vc-badge`/`vc-btn` (ลบ breakdownPanel/monthPager แล้ว). Loads `fuel_admin.css` + `mileage_admin.css` (page-only row+modal styles). **Phase 3.3 (2026-05-14):** inline HEX `#DC2626` → `var(--vc-red)` (3 จุด), `var(--ds-accent)` → `var(--vc-fg)` (1 จุด). **Phase 4.5 (2026-05-15):** script → ES module `<script type="module" src="js/pages/mileage-admin.js">`. **Phase 9.1 mileage_admin redesign (2026-05-22):** Arto+ inspired filter toolbar — (1) ลบ KPI block ทั้งหมด (~60 บรรทัด: month_total_cost/total_budget/total_used/total_remaining/pending_personal_count cells) เพราะซ้ำ `budget_manage.html` + page เป็น task-oriented; เก็บแค่ `รอกรอก {N}` badge ใน `fuel-header`; (2) แทนที่ `vc-filter-bar mlg-filter-bar` (4 sections) ด้วย `mlg-toolbar` (single-row): **left** = `#datePreset` (เดือนนี้/7วัน/30วัน/ทั้งหมด/กำหนดเอง) + `#dateRangeGroup` (date_start, date_end, hidden ยกเว้น custom) + hidden `show_all`; **center** = `.mlg-status-chips` (segmented control 4 ปุ่ม: ทั้งหมด/รอกรอก/รอกลับ/ครบ → click = set `#statusFilter` + submit); **right** = `#exportLink` + `#advFilterBtn` (toggle `#advSheet`, `is-active` + dot เมื่อมี filter); (3) `.mlg-adv-sheet` (collapsible, 3-col grid) = รถ/คนขับ/ผู้จอง/งบ/หมวด-กอง/ค่าน้ำมัน-min-max (`mlg-range-pair`) + actions row "ล้างทั้งหมด" + "นำไปใช้". IDs ที่ JS contract ต้องคง: `filterForm`, `filterBudgetType`, `filterBudgetSub`, `filterBudgetSubWrap`, `exportLink` (เดิม) + `datePreset`, `dateRangeGroup`, `showAllInput`, `statusFilter`, `advFilterBtn`, `advSheet` (ใหม่). **Phase 5.9 (2026-05-17):** ~~จัด layout `vc-filter-bar` 4 หมวด~~ (ถูก replace โดย Phase 9.1). **Phase 5.8 (2026-05-17):** เพิ่ม 2 filter ใน `vc-filter-bar` — (1) "ผู้จอง" `<input list="bookersList">` + `<datalist>` (autocomplete native, server filter ilike); (2) "งบ" + "หมวด/กอง" — chained dropdown (type=ส่วนกลาง/ส่วนกอง/ส่วนตัว; sub โผล่เฉพาะ central/department, ซ่อนเมื่อ personal/ว่าง); JS rebuild sub options ด้วย `innerHTML` จาก `window.EXPENSE_CATS` + `window.MLG_FILTER_SUB` (pattern ลอกมาจาก `updateExpSubDropdown` ใน vehicle-admin.js). **Phase 5.7 (2026-05-17):** redesigned `<vc-table>` — 14 columns (Booking/งบ/ผู้จอง/ทะเบียน/คนขับ/เติมน้ำมัน/ปลายทาง/ไมล์ออก/ไมล์กลับ/ระยะ/ค่าน้ำมัน/สถานะ/action); column "งบ" แสดง type (`ส่วนกลาง/ส่วนกอง/ส่วนตัว`) + sub (หมวด/แผนก) — personal ไม่มี sub; column "เติมน้ำมัน" `vc-badge-blue` ถ้า FuelBill mileage อยู่ใน [odo_start, odo_end] ของ trip (link ไป admin_fuel); ทะเบียน=license_plate only, คนขับ=ชื่อแรก only; loop `display_rows` แทน `rows` → trip รวม (`trip_group` เดียวกัน) เหลือ 1 row + badge `+N` หลัง BK-id + ผู้จองแสดง "งานร่วม N รายการ" (representative=ตัวแรก). **Phase 5.6 (2026-05-17):** `#summaryStrip` redesigned for mobile — ลบ Bootstrap `row`/`col-12` + `d-flex` บน card, ลบ inline `style=""` ทั้ง block; ใช้ `.mlg-summary-strip/-mode/-mode--hidden/-item/-label/-icon/-clear` (ใน `mileage_admin.css`); ใน mobile ≤767.98px → items stack 100% width + ซ่อน `.vc-dot-sep` + clear button full width. IDs/JS contract คงเดิม (`#modeAll`/`#modeSelected`/`#sumAllDistance`/`#sumAllCost`/`#selCount`/`#selDistance`/`#selCost`/`clearSelection()`). **2026-06-08 (cmdk menus):** `#datePreset` `<select>` ได้ `data-dropdown` + ห่อ `<optgroup presets>`/`<optgroup custom>` (divider คั่นก่อน "กำหนดเอง") → enhance เป็น `.vc-dd` (native select คงเป็น source of truth, `bindDatePreset` ไม่แตะ); `booker_q` `<input list=bookersList>` ได้ `data-autocomplete` → enhance เป็น `.vc-ac` searchable. โหลด `core/js/dropdown.js` (module) **ต่อท้าย** `vehicle_mileage.js`. Component → Design System > Component library. **2026-06-08 (b):** ขยาย `data-dropdown` ครบทุก `<select.vc-filter-select>` ใน adv-sheet — `vehicle_id`/`driver_id`/`budget_type`/`budget_sub` (native select ยัง submit ได้แม้ `display:none`; ไม่ auto-submit แค่ set value รอ "นำไปใช้"). `budget_sub` chained → dd มี MutationObserver(childList) rebuild panel เมื่อ `bindBudgetFilter` swap innerHTML. `.mlg-range-pair` (cost_min/max `type=number`) ซ่อน spinner (`appearance:textfield` + webkit spin-button none ใน `vehicle_mileage.css`) เหมือน text แต่ numeric-only. หมายเหตุ: date_start/end เป็น custom datepicker (`mlg-datepick`/`va-cal`) ไม่ใช่ dd. **Phase 16.3 mileage datepicker (2026-06-08):** แทน 2 native `<input type="date">` ใน `#dateRangeGroup` ด้วย 2 calendar popover (reuse `.va-cal*` จาก vehicle_admin.css). แต่ละ picker = `.mlg-datepick[data-datepick]` > ปุ่ม `.mlg-date-btn[data-datepick-btn]` (icon + `[data-datepick-label]`) + hidden `input[name=date_start|date_end][data-datepick-input]` + `.va-cal[data-datepick-pop]` (`[data-cal-prev/-title/-next/-dow/-days]`). คลิกวัน → set hidden input + `form.submit()`. JS = IIFE `bindDateRangePickers()` ใน vehicle_mileage.js; hidden input คงชื่อเดิม → `bindDatePreset` (query `input[name=date_start]`) ใช้ได้ปกติ. **Phase 16.4 AJAX filter (2026-06-08):** กรองไม่ reload หน้า — ห่อ summary strip + table card ด้วย `<div id="mlgResults">` (AJAX swap target). ทุก filter (date pick/chips/preset/adv "นำไปใช้") → `runFilter()` fetch URL เดิม (GET) → parse `#mlgResults` จาก response → swap innerHTML + `bindResults()` + re-init lucide + `history.pushState`. Backend ไม่ต้องแก้. **Phase 17 adv-filter popover (2026-06-08):** `#advSheet` จาก inline collapsible Row 2 → **dropdown popover** anchored ใต้ `#advFilterBtn` — ห่อปุ่ม+sheet ใน `<div class="mlg-adv">` (relative), sheet ได้ `role="dialog"` + `aria-label` + `hidden` เริ่มเสมอ (ไม่เปิดค้างแม้มี filter), ปุ่มได้ `aria-haspopup="dialog"`; dot indicator (`.mlg-adv-dot`) เปลี่ยนเป็นสีแดง `--vc-red`; grid จัดใหม่ 3-col→**2-col** + group "ค่าน้ำมัน" (`mlg-range-pair`) ย้ายขึ้นบนสุด span เต็มแถว (`.mlg-adv-col-full`). IDs/contract เดิมคง (`advSheet`/`advFilterBtn`/`filterForm` ฯลฯ). **Phase 17.1 budget/booker layout (2026-06-08):** ลำดับ grid = ค่าน้ำมัน(full) → รถ│คนขับ → **ผู้จอง(full, ย้ายเข้า grid ก่อนงบ — เดิมอยู่นอก grid ใน `.mlg-adv` ที่ใช้ผิด)** → งบ → หมวด/กอง. งบ wrap ได้ `id="filterBudgetTypeWrap"` + dynamic col-span: budget_type ว่าง/personal → งบ `.mlg-adv-col-full` (เต็ม 2col) + `#filterBudgetSubWrap` `display:none`; central/department → งบครึ่ง + sub ข้างๆ. server render default class/display ตาม `f.budget_type`; JS toggle runtime (ดู `bindBudgetFilter`). **DNA redesign (2026-06-17):** (1) ลบ page header `h1/h6` ในเนื้อ (title อยู่ topbar แล้ว); (2) ย้าย inline `style=""` ของ `#mileageModal` ทั้งหมด → `mlg-*` class ใน `vehicle_mileage.css` (`mlg-modal-header/-head-main/-icon/-titles/-bk`, `mlg-info-grid/-item/-body/-label/-value(-‑mono)`, `mlg-state-title`, `mlg-field-err`, `mlg-timestamp-box`, `mlg-preview/-title/-grid/-label/-value`, `mlg-refuel-box/-check-label/-wrap`, `mlg-summary-box(-‑complete)`, `mlg-odo-ref-row`, `mlg-complete-row(-‑manual)`, `mlg-modal-footer(-‑split)`) + reuse `.vc-label`/`.vc-required`/`.vc-label-meta` (vehicle_fuel.css); เหลือ inline แค่ `display:none` บน JS-toggled (`.mlg-state`/`#fePreview`/`#feRefuelWrap`/`#cManualRow`/`#feOdoErr`); IDs ครบ 28 ตัวคงเดิม (JS contract); (3) chip active navy `--vc-fg` → `--vc-accent` (`.mlg-chip.is-active` + `.budget-personal-tab.is-active` sync ทั้ง 2 หน้าให้เป็น family เดียวกัน). **Summary→KPI (2026-06-17):** `vc-card mlg-summary-strip` ย้ายขึ้น**บนสุด** (เหนือ `mlg-toolbar`, นอก `#mlgResults`) + แปลงจาก inline dot-sep strip → **KPI** (reuse `.vc-kpi-label/-value/-unit/-meta` จาก components/kpi.css + icon tile `.mlg-kpi-icon`): `#modeAll` icon `route` = label "ระยะรวมทั้งหมด" / value `<sumAllDistance> km / <sumAllCount> รายการ` / meta "ค่าน้ำมันรวม `<sumAllCost>` ฿"; `#modeSelected` icon `square-check` (toggle เมื่อเลือก row). count `#sumAllCount` เป็น **JS-driven** (`calcAllSummary` set `rows.length`) เพราะอยู่นอก AJAX swap region. IDs/JS contract เดิมคง (`modeAll/modeSelected/sumAllDistance/sumAllCost/selCount/selDistance/selCost/clearSelection`). **🎨 Zendenta redesign (2026-06-19):** Bootstrap-first + class กลาง `main.css` — `vc-card` → `.zen-card`; KPI icon `mlg-kpi-icon` → `.kpi-tile` (46px/12px radius/gray) + label/value → Bootstrap text utils + `.kpi-num`; `vc-table` → `table .data-table` (header uppercase 11px/14px padding rows/hover `#fafbfd`); badge `vc-badge vc-badge-{success|warning|neutral|blue}` → `.badge-pill .b-{full|partial|neutral|accent}` (tint pill ไม่มี border); card head `vc-card-head/vc-card-head-title/vc-card-head-meta` → Bootstrap `d-flex/fw-semibold/text-muted`; empty state `vc-empty*` → Bootstrap `text-center py-5`; เก็บ `.mlg-*`/Jinja/JS/route/modal ครบ — ไม่แตะ backend. Loads `core/css/main.css` (via header.html, มี `.zen-card/.kpi-tile/.kpi-num/.data-table/.badge-pill`). **🎨 Zendenta polish รอบ 2 (2026-06-19):** ตาม user review เทียบ mockup — (1) **KPI ไม่มี border**: ลบ `zen-card` จาก `#summaryStrip` → inline strip (`mlg-kpi` ใช้ `.kpi-tile`+`.kpi-num`, ลบ `.mlg-kpi-icon` CSS); (2) **Filter แบบ A**: status chips (`.mlg-status-chips/.mlg-chip` ลบทิ้ง) → **tabs underline** `.zen-tabs.mlg-tabs` > `.zen-tab(.active)` (row 1); toolbar row 2 = `.zen-search` (ซ้าย) + `#datePreset`/Export/adv (ขวา `.mlg-toolbar-right`); `#dateRangeGroup` (va-cal 2 ช่อง) ย้ายเป็น row 3 โผล่เฉพาะ preset "กำหนดเอง"; ลบ `.mlg-toolbar-left`; (3) **Search** `#mlgSearch` (client-side, นอก `#filterForm`) → `applySearch()` กรอง row + `<mark class="mlg-search-hl">` highlight เหลือง + ซ่อน date-group header ที่ไม่มี row visible; re-apply ใน `bindResults` หลัง AJAX swap; debounce 150ms; (4) **Table mobile = overflow-x** (คง `.table-responsive` + `.data-table td/th white-space:nowrap` กันบีบ col), BK-id mono semibold เข้ม (`.mlg-col-bk`). JS: `bindStatusChips`→`bindStatusTabs` (class `is-active`→`active`), +`bindSearch`/`applySearch`/`highlightRow`/`clearHighlight`. IDs/JS contract เดิมครบ. main.css +section `.zen-tabs/.zen-tab(.active)/.zen-tab-count` · `.zen-search` · `.mlg-search-hl`. **Layout + Sort (2026-06-19):** (1) `#dateRangeGroup` ย้ายจาก Row 3 แยก → อยู่ภายใน `.mlg-toolbar-right` inline (va-cal `right:0` align ฝั่งขวาถูกต้อง + ลบ Row 3); (2) 9 `<th>` ได้ `data-sort` attribute (`booking/user/vehicle/destination/odo-start/odo-end/distance/cost/status`) + `.sort-icon <i data-lucide="chevrons-up-down">` (JS สลับ arrow-up/arrow-down เมื่อ active). **bindDateRangePickers refactor (2026-06-19):** calendar เปลี่ยนจาก popover → **modal-on-mobile** — (1) สร้าง `<div class="va-cal-backdrop">` shared 1 ตัวต่อ 2 pickers; (2) `open()` เช็ก `window.innerWidth<=767` → เพิ่ม `va-cal--modal` + แสดง backdrop เฉพาะ mobile; desktop คง position:absolute เดิม; (3) `render()` คำนวณ `bound` จาก input อีกฝั่ง (`isStart ? endInput.value : startInput.value`) → เพิ่ม `.va-cal-cell--disabled` วันที่ `d>bound` (start picker) หรือ `d<bound` (end picker); (4) daysWrap click: `closest('[data-date]:not(.va-cal-cell--disabled)')` — ข้ามวันที่ disabled; (5) backdrop click → `closeAll(null)`; ESC → close.

---

### vehicle/admin/vehicle_admin.html (admin dashboard)

**Vercel shell** (`.vc-scope`), **Phase 5 (2026-05-24):** booking section header "รายละเอียด" → "**รายการจอง**" + เพิ่ม `<span.vc-card-head-meta#beforeCount>` populate by JS "N รายการ". **Phase 3 redesign (2026-05-24):** week navigator refactor — เพิ่ม `.va-week-meta` row (eyebrow `#weekRangeText` + h2 `.va-week-selected#selDateHeading` + `.va-week-today#weekTodayBtn` + `.va-week-picker` wrapper with `#weekPickerBtn` + popover `#weekPickerPop` containing `<input type="date"#weekPickerInput>`); ลบ `.va-week-heading` block. **Phase 1-2 redesign (2026-05-24):** ลบ 4-cell KPI strip ทิ้ง (`.va-kpi-card` + 4 cells), แทนด้วย inline `.va-page-header-stat` 3-item pill (dot amber/blue/green + count + label = pending/approver/approved) ใน `.va-page-header-actions`; eyebrow text "Admin · Approval Queue" → "Admin / Approval queue"; page header strip Bootstrap utility classes (d-flex/align-items-end/justify-content-between/flex-wrap/gap-3/mb-4) → ใช้ CSS อย่างเดียว + `mb-5` Bootstrap util; remaining sections (2-col split: Bookings+Trips col-8 / Vehicle status grid col-4 sticky, 4 modals: `#assignModal` `#swapModal` `#repairModal` `#revertModal`) ไม่เปลี่ยน. Reuses shared primitives: `.vc-kpi-*` (components/kpi.css), `.vc-badge-*` + `.vc-empty-*` (components/badge.css + empty_state.css — Phase 3.2), `.vc-card/.vc-btn/.vc-modal/.vc-form-*` (design-system.css). Lucide icons. KPI cells kept raw HTML (canonical per `/bbcenter-design`); status badges + empty states rendered by JS now emit `vc-badge-{warning\|blue\|success\|danger\|neutral\|solid} vc-badge-dot` + `vc-empty` + `vc-empty-icon/-title` + lucide icons. **Phase 4.3 (2026-05-15):** script → ES module `<script type="module" src="js/pages/vehicle-admin.js">`. **Phase 5.1 align (2026-05-15):** ลบ `fuel_admin.css` link (ไม่ใช้); `vc-icon` → `vc-icon-sm` ×6; ลบ `pe-1 mb-1` ×2 ใน modal label; เพิ่ม `title=""` ทุก control button (week-nav, merge/notify ×4, ftab ×5, btn-close ×4, adm-exp-tab ×3, modal-footer ×8, expand toggle ×1). **Phase 5.2 inline-style purge (2026-05-16):** ลบ inline `style="color:var(--vc-green)"` ที่ L157 (`<i data-lucide="circle-check">` ใน collapsed-bar) → ใช้ CSS rule; แทน inline `style="margin-top/-bottom"` ที่ L274 → class `.va-exp-sub-group`. **Phase 5.3 JS audit (2026-05-16):** `pages/vehicle-admin.js` — L863 `<i class="fa-brands fa-telegram">` → `<i data-lucide="send" class="vc-icon-sm">` + `initIcons(btn)` (rule 4); L929 plain `<p style="--ds-text-muted">` empty → `<div class="vc-empty"> + vc-empty-icon + vc-empty-title` (rule 5); L933-935 swap-veh-status inline HEX `#DCFCE7/#16A34A/#EDE9FE/#4338CA` → `vc-badge vc-badge-{blue\|neutral\|success} vc-badge-dot` (rule 9). **Phase 6 UX polish (2026-05-16):** L260 "EXPENSE TYPE" → "ประเภทค่าใช้จ่าย" (Thai consistency); ลบ dead comment L130 `beforeCount`; เพิ่ม `<div class="va-page-header-actions"></div>` slot ใน page header (§1 skill template alignment, `:empty { display: none; }` กัน blank space). **Phase 6.1 UX features (2026-05-17):** 3 new modal nodes — `#vehConflictWarn` (after `#modalVehSel`) + `#drvConflictWarn` (after `#modalDrvSel`) ใช้ `.va-conflict-warn` แสดง "ซ้อนเวลากับ #N (HH:MM-HH:MM · ...)" เมื่อ `findConflict()` เจอ; `<p.va-budget-warn>` ใน `.va-budget-bar` แสดงข้อความเตือนงบ. **Phase 11 task-card redesign (2026-06-06):** `#sectionBefore` booking cards (JS-rendered) → task-list layout ตาม reference image — head (title=ปลายทาง + desc=purpose · due-time start–end) + foot (avatar+"จองโดย" author | status badge + actions). header/filter-tabs/merge-notify เดิมคงไว้ครบ. **Phase 11.1 (2026-06-06):** ลบ block `#beforeCollapsed` (`.va-collapsed-bar` + `#beforeCollapsedText` + ปุ่ม "ดูรายละเอียด") ทิ้ง — `#beforeExpanded` (filter-tabs + `#bookingList`) แสดงเสมอ ไม่มี collapsed state อีก. **Phase 11.2 (2026-06-06):** `#sectionBefore` class `vc-card` → `va-flat` (เอากรอบ card section ออกตามคำขอ user — header/filter/list ชิดขอบ flat); `#sectionAfter` ยังเป็น `vc-card`. **Phase 12 week-card ghost (2026-06-07):** va-week-card restyle เบาลง — strip active state กล่องดำ `--vc-fg` → underline (`border-bottom`) + ตัวเลข weight 800; `.va-week-nav` + ปุ่ม today/picker → ghost (border transparent); `.va-week-bar` ลบ border-bottom + padding; `.va-week-card` เพิ่ม `padding: --vc-space-5`. **Phase 12.1 meta slim (2026-06-07):** ลบ eyebrow `#weekRangeText` + wrapper `.va-week-meta-left` ออกจาก `.va-week-meta` (เหลือ `h2.va-week-selected#selDateHeading` + `.va-week-meta-right`); heading format ย่อ "วัน{เต็ม}ที่ D {เดือนเต็ม} {ปี} + tag วันนี้" → "{วันย่อ}. D {เดือนย่อ} {ปี}" (เอา today tag ออก — today มี dot บน strip แล้ว); `.va-week-meta` align-items flex-end→center; JS `renderWeekMeta()` (vehicle-admin.js) ตัด rangeText computation ทิ้ง + เพิ่ม const `TH_MON_S` (เดือนย่อ); ลบ CSS rule `.va-week-meta-left` + `.va-week-eyebrow`. **Phase 14 (2026-06-07):** เพิ่ม field `isAdHoc` ใน `window.BOOKINGS_DATA` injection (จาก `VehicleBooking.is_ad_hoc` — งานนอกระบบที่ driver เพิ่มเอง) สำหรับ badge "งานนอกระบบ" ใน bl-card. **Phase 15 (2026-06-07):** inject `otHours`/`otAmount` ใน `window.BOOKINGS_DATA` (จาก `VehicleBooking.driver_ots[0].total_hours/total_amount` backref → `DriverOT`) สำหรับ OT badge ใน trip card (sectionAfter). **Phase 16 inline calendar (2026-06-08):** week-picker popover เปลี่ยนจาก 2-click (ปุ่ม `#weekPickerBtn` → `<input type="date"#weekPickerInput>`) เป็น **inline month calendar คลิกเดียวเลือกวัน** — ลบ `<label.va-week-picker-label>` + `<input#weekPickerInput>` ออกจาก `#weekPickerPop`; popover เปลี่ยน class `.va-week-picker-pop` → `.va-cal` containing `.va-cal-head` (`#calPrevBtn` chevron-left + `.va-cal-title#calTitle` + `#calNextBtn` chevron-right) + `.va-cal-grid.va-cal-dow#calDow` (หัววัน Su–Sa) + `.va-cal-grid#calDays` (grid วัน, JS-rendered). CSS `.va-cal*` block ใหม่ใน `vehicle_admin.css` (selected day = วงกลมทึบดำ `--vc-primary`, dot accent บอกวันมีงาน, ring วันนี้, fade-in animation). **2026-06-21:** เพิ่ม 2 fields ใน `window.BOOKINGS_DATA` per booking: `editUrl` (= `url_for('vehicle.admin_edit_booking', booking_id=b.id)`) + `pickup` (= `b.pickup_location or ''`) สำหรับ admin edit modal.

---

### vehicle/css/vehicle_mileage.css (RETIRED)

mileage admin page only (renamed จาก `mileage_admin.css` ใน prefix migration 2026-06-07). **Phase 3.3 (2026-05-14):** stripped from 1095 → ~35 lines — removed ~30 dead class blocks (`.mlg-page-header/-kpi-*/-btn-*/-missing-pill/-panel-pill/-badge*/-month-current-pill/-panel*/-breakdown-*/-bd-*/-month-pager/-pager-*/-month-page*/-month-row*/-mr-*/-filter*/-summary except summary-box/-table*/-col-check/-col-id/-num/-edit-btn/-empty*/-modal-eyebrow` + duplicate `ds-alert*` + most `.mlg-modal/-info-grid/-info-item/-state-title/-summary-box/-preview/-timestamp-box/-refuel-box` styling that template inlines via `var(--vc-*)`) → page now inherits `vc-modal/vc-table/vc-card/vc-filter-bar/vc-kpi/vc-badge/vc-empty` from `fuel_admin.css` + `components/`. Page-only: `.mlg-row/-row-check/-col-dest/-complete-row/-refuel-box .form-check`. **Phase 5.6 (2026-05-17):** เพิ่ม summary-strip block — `.mlg-summary-strip/-mode/-mode--hidden/-item/-icon/-clear` + `@media (max-width: 767.98px)` (wrap items 100%, hide `.vc-dot-sep`, dashed bottom border, full-width clear button). **Phase 5.7 (2026-05-17):** เพิ่ม classes รองรับคอลัมน์ใหม่ — `.mlg-col-bk` (mono BK-id), `.mlg-group-badge` (`+N` chip), `.mlg-budget-label/-budget-sub` (2-line budget cell, ellipsis ที่ 140px), `.mlg-row--group` (subtle bg + ไม่ hover-flash), `.mlg-group-label` (icon+text "งานร่วม"), `.mlg-refuel-link` (no underline), `.mlg-date-cell` (ย้าย inline style ของ date separator มา CSS). **Phase 9.1 (2026-05-22):** ลบ block `.mlg-filter-bar/-section/-section-title/-section-body/-filter-date/-num/-booker/-actions-row` (Phase 5.9 retired) → แทนด้วย **toolbar pattern**: `.mlg-toolbar` (column gap), `.mlg-toolbar-row` (flex-wrap), `.mlg-toolbar-left/-right` (left auto, right `margin-left:auto`), `.mlg-date-preset` (min-width 130), `.mlg-date-range/-input/-sep` (hideable date range group); `.mlg-status-chips` = segmented control (1px border, 3px padding, 8px radius, inline-flex) + `.mlg-chip` (5×12 padding, hover bg-subtle) + `.mlg-chip.is-active` (bg=fg, color=bg invert); `.mlg-adv-btn.is-active` (border+color = accent) + `.mlg-adv-dot` (6px absolute top-right indicator); `.mlg-adv-sheet` (border card 8px radius) + `.mlg-adv-grid` (3-col → 2-col ≤991.98px → 1-col ≤767.98px) + `.mlg-adv-actions` (right-aligned, dashed top border); `.mlg-range-pair/-sep` (cost min/max inline pair). **Phase 16.3 (2026-06-08):** ลบ `.mlg-date-input`; เพิ่ม `.mlg-datepick` (relative anchor) + `.mlg-date-btn` (inline-flex icon+label, min-width 150, `background-image:none` ลบ caret ของ vc-filter-select, `[aria-expanded=true]` border=fg) + `.mlg-date-btn-label` (flex truncate, muted ตอนยังไม่เลือก) + `.mlg-date-btn--filled` (label = fg); mobile `.mlg-datepick{flex:1}` + `.mlg-date-btn{width:100%}` แทน rule `.mlg-date-input` เดิม. Calendar popover style reuse `.va-cal*` จาก vehicle_admin.css. **Phase 16.4 (2026-06-08):** เพิ่ม `#mlgResults` (transition opacity) + `#mlgResults.is-loading` (opacity 0.55 + pointer-events:none) — dim ระหว่าง AJAX fetch. **Phase 17 adv-filter popover (2026-06-08):** เพิ่ม `.mlg-adv` (relative inline-flex anchor); `.mlg-adv-sheet` จาก border card (8px radius) → **popover absolute** (top calc(100%+4px), right:0, width 560px + max-width calc(100vw−2×space-4), z-index 1000, box-shadow ผ่าน DESIGN-OVERRIDE — mirror `.vc-dd-panel`); `.mlg-adv-dot` background `accent`→`--vc-red`; `.mlg-adv-grid` 3-col→**2-col** (ลบ media 991 ที่ซ้ำ, เหลือ 1-col ≤767); เพิ่ม `.mlg-adv-col-full` (grid-column 1/-1) + force `width:100%` ให้ `.vc-filter-select`/`.vc-dd`/`.vc-dd-trigger`/`.vc-ac`/`.vc-ac>input` ใน grid. **DNA redesign (2026-06-17):** กลับลำ Phase 3.3 ที่เคย inline modal styling — **re-add modal layout classes** (template de-inline กลับมาใช้ class): `.mlg-modal-header/-head-main/-icon/-titles/-bk`, `.mlg-info-grid/-item/-body/-label/-value(+--mono)`, `.mlg-state-title`, `.mlg-field-err`, `.mlg-timestamp-box`, `.mlg-preview/-title/-grid/-label/-value`, `.mlg-refuel-box/-check-label/-wrap`, `.mlg-summary-box(+--complete)`, `.mlg-odo-ref-row`, `.mlg-complete-row(+--manual, flex justify-between แทน old padding/border-bottom)`, `.mlg-modal-footer(+--split)` — radius ทุกตัว `var(--vc-radius-sm)`, token `--vc-*` ล้วน, ไม่มี display (JS-toggled คุมเอง). form label reuse `.vc-label`/`.vc-required`/`.vc-label-meta` (vehicle_fuel.css). `.mlg-chip.is-active` `--vc-fg`→**`--vc-accent`** (sync กับ `.budget-personal-tab.is-active` ใน vehicle_budget.css ให้ active-pill เป็น family เดียวกัน). **Summary→KPI (2026-06-17):** ลบ `.mlg-summary-mode/-item/-label/-icon/-clear` + responsive เดิม → `.mlg-summary-strip` (padding 0), `.mlg-kpi` (flex icon+body, padding 5/6), `.mlg-kpi-icon` (38px tile, `--vc-icon-bg`/`--vc-icon`), `.mlg-kpi-body`, `.mlg-kpi-clear` (margin-left auto), `.mlg-summary-mode--hidden` (display:none คงไว้); typography reuse `.vc-kpi-label/-value/-unit/-meta`. **Sort + mobile (2026-06-19):** `.sort-icon` (inline-flex 11px gray `#cbd0da`; `th[data-sort][aria-sort]` → accent); `th[data-sort]` cursor:pointer + hover:fg; mobile: `.mlg-toolbar-right` + `flex-wrap:wrap`; `.mlg-date-preset` + `flex:1` (ขยายเต็ม); `.mlg-date-range` คง `flex:1 1 100%`. **va-cal position fix (2026-06-19):** `.mlg-datepick .va-cal { right:auto; left:0 }` — override `vehicle_admin.css` ที่ตั้ง `right:0` (calendar เปิดซ้ายจากปุ่ม → off-screen เมื่อ picker ย้ายมาอยู่ กลาง/ซ้าย toolbar แล้ว). **Mobile UX polish (2026-06-19):** (1) toolbar mobile: `.zen-search{flex:1 1 100%}` (max-width fix ใน main.css), `.mlg-toolbar-right>.vc-dd{flex:1 1 0%}` + `.vc-dd-trigger{width:100%}` (dropdown.js wrap select→`.vc-dd` ทำให้ flex target เปลี่ยน), `.mlg-date-range{order:10}` (row ล่างใต้ preset/export/filter); (2) **calendar modal**: `.va-cal-backdrop` (fixed overlay `rgba(26,29,41,.25)` z-1055), `.va-cal--modal` (fixed centered `translate(-50%,-50%)` z-1060, mobile `width:calc(100vw-48px)/max-width:360px`), `@keyframes vaCalModalIn` (from `translate(-50%,-48%)` → ไม่กระตุก ต่างจาก vaCalIn ที่ใช้ scale+translateY), `.va-cal-cell--disabled` (opacity .25 pointer-events:none สำหรับวันนอกช่วง); (3) `.mlg-kpi` mobile `flex-wrap:nowrap` + `.mlg-kpi-clear{flex:0 0 auto;margin-left:auto}` (ปุ่มล้างอยู่ข้างๆ KPI แทนด้านล่าง); (4) `.mlg-summary-strip{position:sticky;top:66px;z-index:90;background:#fff}` — KPI strip ติดหน้าจอตอนเลื่อนลง. **🎨 P1 ".bb-*" redesign (2026-06-28):** ไฟล์นี้ rewrite ทั้งหมด — token `--vc-*` → `--bb-*` ทุกจุด, ลบ section zen layer ออกหมด. Zone layout: `.mlg-summary-strip` (KPI ghost strip · sticky · border-bottom `--bb-n200`) · `.mlg-toolbar` (sticky toolbar · 2 rows) · `.bb-table tbody .mlg-row--group:hover td` (specificity override กัน hover-flash บน date-group rows) · `.mlg-col-cost` (font-weight:600 — ชนะ `bb-cell-num` weight:400 ด้วย source-order) · `.mlg-checkbox` (accent-color `--bb-str`) · `.mlg-search-hl` (rgba เหลืองสำหรับ `<mark>`). modal sections (`.mlg-modal-*`, `.mlg-info-grid-*`, `.mlg-preview-*` ฯลฯ) ใช้ `--bb-*` ทั้งหมด. ห้ามใช้ `--vc-*`/`--ds-*` อีกต่อไปในไฟล์นี้ — bb-* canonical ตั้งแต่ 2026-06-28.

---

### budget_manage.css (RETIRED)

**`/admin/budget` only** (Phase 5.8, 2026-05-18 — renamed from `budget_admin.css`) — `vc-scope` page. Classes: `.budget-header/-title/-subtitle/-header-actions`, stack helpers `.budget-flash-stack/-kpi-strip/-grid/-grid--last/-empty-block`, `.budget-filter-month` (calendar chip), `.vc-section-hdr(-icon[.is-purple]/-title/-count/-amount/-amount-label/-actions)` (`:first-of-type` looser top; **Phase 2 (2026-05-21):** `.vc-section-hdr-amount` mono/tnum 14px/600 + `.vc-section-hdr-amount-label` xs/400 muted — รับ amount จาก KPI cells ที่ลด), `.vc-bcard(-head/-name/-actions/-date/-pct-hero/-pct[.is-warn/-danger]/-pct-label/-amounts/-rem[.is-success/-warn/-danger]/-approver)` (**Phase 3:** `.vc-bcard-row` retired → `.vc-bcard-pct-hero` + `.vc-bcard-pct-label`; **Phase 4 (2026-05-21):** `.is-purple` modifier ลบครบ (sec-hdr-icon/bcard-name/progress); `.vc-bcard` padding space-5 + asymmetric child margin-top + animation `bm-fade-up` + `animation-delay: var(--bm-delay, 0ms)` + hover bg-subtle; `.vc-bcard-pct` text-2xl/tracking-tight; `.vc-bcard-pct-label` 11px uppercase tracking-wide; `.vc-progress > span` baseline `--vc-fg` (monochrome) + animation `bm-progress-fill`; new `.vc-kpi-cell--signal` (bg-subtle); new `.budget-modal-enter` (scoped modal entrance); page-scoped CSS tokens under `.vc-scope`: `--bm-ease-out` (cubic-bezier 0.16,1,0.3,1) + `--bm-dur-fast/-base/-slow` (180/240/480ms); keyframes `bm-fade-up/-progress-fill/-fade-in`; `@media prefers-reduced-motion` block. **Note:** `--bm-*` tokens defined under `.vc-scope` global selector แต่ load เฉพาะ budget_manage.css → ไม่ leak ไปหน้าอื่น) + **`.vc-bcard--inactive`** (Phase 5.9 — opacity 0.65 + repeating diagonal stripe), `.vc-progress[.is-warn/-danger]` (`.is-purple` CSS rule ลบใน Phase 4a; `.vc-btn-purple` + `.vc-badge-purple` blocks ลบใน Phase 8, 2026-05-22), `.vc-dropdown(-menu[.is-open]/-item[.is-danger]/-divider/-form)` (no shadow; Phase 5.9 — danger tone + form-wrapped POST item), `.budget-refund-scroll/-refund-hint` + `[data-pick-booking].is-picked`, **`.budget-personal-line`** (Phase 5, 2026-05-22 — was `.budget-personal-row` card; rewrite block §10 → inline strip: drop border/bg/radius/padding, ใช้ `padding-block` + `border-bottom` separator; `.meta` flex baseline inline; label sm lowercase + value sm mono fg + meta-end `margin-left:auto`; icon 16px green retained), **`.budget-modal-mono/-meta/-meta-row/-notice`** (Phase 6, 2026-05-22 — `-alert/-alert--flush` retired; `.budget-modal-meta` = `<dl>` flex column gap-2 + `border-block` Stripe-style + child `<dt>` 11px uppercase tracking-wide muted + `<dd>` sm/600/fg; `.budget-modal-notice` = flex top-border line w/ lucide icon + `<strong>` fg), **`.budget-pivot-card/-summary-content/-empty`** (Phase 7, 2026-05-22 §12.5 — page-local tweaks สำหรับ pivot section; base styles `.vc-pivot-*` มาจาก `components/pivot.css` shared component), `.budget-empty-icon`. **Phase 2 redesign (2026-05-22):** rename `.budget-kpi-strip` rule → `.budget-summary` margin helper; §2 `.budget-grid/-grid--last` margin rules retired (card grid → table); new §13 **`.budget-table-wrap/.budget-table/.budget-row[--inactive]/-name/-sub/-pct-cell/-pct/-rem-over/-tags/-approver[--missing]/.budget-personal-paid-at/.vc-progress--mini`** — table view สำหรับ central/dept/personal sections (vc-table base + custom row name w/ secondary date line + inline mini progress + dropdown col + inactive row dashed stripe). new §2B **`.budget-summary*`** block — `.budget-summary` (vc-card ผอม padding-5/6) + `-head/-title` (uppercase 13px muted) + `-amount/-used/-sep/-cap` (hero 2.25rem mono + 1.25rem cap) + `-bar/--ok/--warn/--danger` (4px progress) + `-meta/-pct/-pct--{tone}/-remain/-remain--danger/-meta-sep/-meta-dot` + `-signals/-signal/-signal--warn/--danger/-signal-amount` (footer pills, border-top); §10 `.budget-personal-line` retired (cleared block ← comment placeholder); new §12 **`.budget-section*`** block — `.budget-section[open]` `<details>` accordion (border 8px radius + border-strong on open) + `-summary` (cursor pointer flex justify-between, list-style none, hover bg-subtle) + `-label/-chev[rotate 90deg on open]/-icon/-title/-count` + `-stats/-amount/-amount-dot/-amount-warn/-pct[/-pct--warn/-pct--danger]` + `-body` (padding bottom + border-top + animation bm-fade-up) + `-actions` (flex justify-end); responsive ≤575px stacks header actions + section stats. Zero HEX (one purple `#6420A8` hover only — Vercel purple-700). **2026-06-08 redesign (§17–19):** §17 `.budget-summary-stack/-seg[--central\|--dept]/-legend/-legend-item[--aside]/-legend-dot[--central\|--dept\|--free\|--personal]/-legend-val` (stacked KPI bar แทน single `.budget-summary-bar`; central = `--vc-accent`, dept = indigo tint `#818cf8`, free = `--vc-border-strong`, personal = `--vc-green`); §18 `.budget-pivot-head/-title/-hint/-rows/-cat[open]/-cat-summary/-cat-label/-cat-chevron/-cat-icon/-cat-name/-cat-count/-cat-figures/-cat-cap/-cat-badge[--ok\|--warn\|--danger]` (pivot default = 3 สรุปแถว + กดขยาย matrix รายเดือน); §19 `.budget-bin-form/-bin-btn` (soft-delete trash แดง = `toggle_active`) + `.budget-row-sub--approver/--approver-missing` (approver ย้ายเป็น sub-line ใต้ชื่อกอง). HEX exception: `#818cf8` (indigo-400 tint สำหรับ dept segment/dot — derived จาก accent). **DNA redesign (2026-06-17) — ไฟล์จริงชื่อ `vehicle/css/vehicle_budget.css`** (row label `budget_manage.css` = ชื่อเก่า stale): §20B seg/legend สีใหม่ central=`--vc-accent`/dept=`--vc-amber`/**remain**=`--vc-green` (เลิก `--personal`); **§22** ใหม่ — `.budget-tabbar` (tab+toolbar บรรทัดเดียว, underline ย้ายมาที่นี่) · `.budget-toolbar` · `.budget-month-menu` (max-height 320 scroll) · `.budget-summary-icon` monochrome (`--vc-icon`/`--vc-icon-bg`) · `.bcard-section-head[--off]/-dot[--on green/--off กลวง]/-label/-count/-line` · `.bcard-grid` (auto-fill 280px) · `.bcard[:has(.vc-dropdown.is-open)]/-icon/-id/-name/-amount/-amount-cap/-pct[.is-ok/-warn/-danger]/-menu/-divider/-meta/-meta-row/-meta-faint/-meta-missing` · `.bcard--off` (bg `--vc-sidebar-bg` + dashed + disable tone) · `.bcard-activate`. radius 6px (`--vc-radius-sm`), no shadow, border `#f0f0f0`.

---

## CLAUDE.md — project-rules history

_5 entries · ล่าสุดอยู่บน_

- 2026-06-02 (**vehicle_admin sectionBefore: spacing + edit-any-status + check icon**: **(1) Spacing** (`vehicle_admin.css`) — scope `#sectionBefore .va-list { padding:0 space-5 space-5 }` (rows align กับ filter-tabs gutter + bottom breathing room; `#sectionAfter`/tripList ไม่กระทบ); `.va-filter-tabs` margin bottom `space-4`→`space-3` (มัด tabs เข้ากับ list); `.bl-content` gap `2px`→`space-1`; mobile `@media max-width:575.98px` gutter `space-3` กัน pinch. **(2) buildRowActions** (`pages/vehicle-admin.js`) — pending approve button ลบคำ "อนุมัติ" → **pencil icon** (`vc-btn-primary vc-btn-icon`, `title="อนุมัติ"` คงเดิม); `waiting_approver`/`forwarded` เปลี่ยนจาก `.bl-status-text` "รอ Approver" → **ปุ่ม pencil edit** (`vc-btn-ghost vc-btn-icon` → `openAssignModal(id,'edit')`) = admin แก้ assignment ได้ทุกสถานะ แม้ส่ง approver/อนุมัติแล้ว (backend `admin_assign` [vehicle_view.py:1838] เช็กแค่ `is_vehicle_admin()` — ไม่มี status guard; submit map `edit`→`assign_action='approve'` เดิม). `rejected` ยังว่าง (ไม่ขอ); status ยังโชว์ผ่าน title badge. **(3) STATUS_ICON** approved icon `check-circle` (lucide normalize alias → render `lucide-circle-check-big` ตัวหนา) → `circle-check` (render `lucide-circle-check` ตัวเบา ตรงกับ collapsed bar/modal/js).)
- 2026-05-24 (**vehicle_admin booking row Phase B** (sectionBefore selection mode): เพิ่ม **visible checkbox** บน selectable rows ตอน `groupMode`/`notifyMode` ติด — leftmost slot 22px (`.bl-sel-check` 18×18 + transition + animation `va-slide-in-left 220ms`). **CSS** `.va-list--selecting` parent class (toggle ผ่าน `toggleGroupMode/cancelGroupMode/toggleNotifyMode/cancelNotifyMode`) → grid `22px 40px 1fr auto` ทุก card; placeholder `.bl-sel-slot` (18×18 ว่าง) สำหรับ row ที่ select ไม่ได้ (รักษา alignment); `.bl-sel-check--notify` accent-color = blue; `@keyframes va-slide-in-left` + reduce-motion guard เพิ่ม `.bl-sel-check`. **JS** `renderSingleRow` เพิ่ม `checkboxSlot` variable (pending+groupMode→check; approved+notifyMode→check.notify; mode-but-not-selectable→empty slot; default→no checkbox); `renderGroupRow` แยก `titleBadge` ("N งานรวม" คงเดิม) ออกจาก leftmost `grpCheckboxSlot` (notifyMode + approved members → check); `toggleGroupMode`/`toggleNotifyMode` add class; `cancelGroupMode`/`cancelNotifyMode` remove class. **Functional logic** (existing): `groupSel/notifySel` Set, `toggleGroupSel/toggleNotifySel` add/remove, `updateMergeBtn` show `รวม (N)` + disable ถ้า < 2, `confirmMerge` เปิด assign modal `'group_new'`. ปุ่ม header `#btnMerge`→`#btnMergeCancel/#btnMergeConfirm` swap คงเดิม.)
- 2026-05-24 prev (**vehicle_admin booking row revamp Phase A** (visual, sectionBefore): revert Phase 5 flat-rail layout → **bordered card + 40px circle icon**. Grid `40px 1fr auto` (icon/content/right); `.bl-card` มี `border + radius-md + bg-bg`, hover = `border-color:fg-subtle + bg-subtle`, gap ระหว่าง row ผ่าน `.bl-card + .bl-card { margin-top:space-2 }` (แทน `border-bottom` Phase 5). **`.bl-icon` circle 40px** modifier `--{pending\|approver\|approved\|rejected\|group}` map `--vc-{amber\|blue\|green\|red\|green}-{bg/border}` + Lucide icon 18px (`clock/send/check-circle-2/x-circle/git-merge`) แทน `.bl-rail` 3px. **Title row** = dest + inline `.bl-status` (`vc-badge` ตาม STATUS_BADGE map — `รออนุมัติ/ส่ง Approver/อนุมัติแล้ว/ปฏิเสธ`); ลบ `.bl-dept-tag`. **Meta** single-line `pax · HH:MM[-HH:MM] · booker` (สลับจาก Phase 5 2-line). **Right zone** action-only (ลบ `.bl-time` column); pending = `[อนุมัติ primary][ปฏิเสธ secondary.bl-action-secondary]` pair; approved = `[✎][↻ secondary]`; waiting_approver = "รอ Approver" italic; rejected = ว่าง. **Responsive** `@media (max-width:639.98px)` ซ่อน `.bl-action-secondary` (mobile = ปฏิเสธ/ย้อนสถานะ ใน overflow ภายหลัง). **Group row** `.bl-card--group` = `bg:green-bg + border:green-border` (hover `border:green`); icon = `git-merge` ใน `.bl-icon--group`; tag "N งานรวม" คงเดิม. **Sub-rows** ขยาย = light green bg, border-top `green-border`, **ลบ ungroup ต่อ row** (header เท่านั้น per UX choice) — `.bl-group-sub` block + sub-name 14/600 + meta `pax · time · booker`. **JS**: `STATUS_RAIL` → `STATUS_ICON` map (`icon` + `cls`); `renderSingleRow` swap title/booker (dest = title); buildRowActions เพิ่ม reject button (call existing `openAssignModal(id,'reject')`); `renderGroupRow` sub-items ลบ per-row shuffle button. **Selection** `.bl-selected/.bl-notify-selected` ใช้ `border-color` swap (เลิกพึ่ง `.bl-rail` ที่ลบไป). **Phase B (future)**: checkbox + "รวมงาน" toggle bar + bulk merge (ยังไม่ทำ — แยก phase per user choice).)
- 2026-05-24 prev (**vehicle_admin redesign Phase 1-8 (complete)**: **Phase 1-2** ลบ KPI 4-cell strip ออกทั้งหมด, แทนด้วย inline `.va-page-header-stat` 3-item pill (dot amber/blue/green + count + label = pending/approver/approved) ใน `.va-page-header-actions`; eyebrow "Admin · Approval Queue" → "Admin / Approval queue"; refresh `.va-page-title` typography → `clamp(26px, 3.4vw, 32px)` + letter-spacing -0.022em; rename JS helper `setKpi` → `setStat`, call sites 4 → 3 (ตัด rejected). **Phase 3 (hero)** week navigator redesign — เพิ่ม `.va-week-meta` row บน `.va-week-bar` (left: `.va-week-eyebrow` "DD–DD MMM YYYY" range + h2 `.va-week-selected` "วันXที่ DD MMM YYYY" + auto-append `.va-week-today-tag` pill ถ้าเป็นวันนี้; right: `.va-week-today` ghost "วันนี้" + `.va-week-picker` wrapper (button + popover); ลบ `.va-week-heading` (h2 ย้ายขึ้น meta). **Date picker** popover pattern (native `<input type="date">` ใน `<div.va-week-picker-pop[hidden]>` anchored top:100%+6px right:0, animation `va-pop-in` 220ms ease-out, mobile (≤767px) flip ซ้าย). **JS**: helper `_alignWeekStartTo(date)` (Monday-based snap); `bindWeekControls()` ครั้งเดียวตอน init handle: `#weekTodayBtn` click=reset to today + close pop + renderAll, `#weekPickerBtn` click=toggle aria-expanded + open/close pop, `#weekPickerInput` change=parse YYYY-MM-DD + selDate + align weekStart + close + renderAll, click outside=close, Esc=close + return focus to btn; `renderWeekMeta()` populate range text (smart same-month/year compression) + h2 day line + today tag; `shiftWeek()` เรียก `renderWeekMeta()` + trigger `va-week-strip--switching` animation class (remove → force reflow → add) สำหรับ slide-in-right cells 30ms stagger via `--va-i`. **Animation**: เพิ่ม `@keyframes va-fade-up/-pop-in/-week-cell-in` + apply กับ `.va-week-card` (fade-up 60ms delay backwards) + `.va-week-picker-pop` (pop-in) + cell stagger; `prefers-reduced-motion` block ขยายให้ครอบ Phase 3. **Phase 4** filter tabs — rewrite `.va-filter-tabs` + `.ftab` ตาม `mlg-status-chips` pattern (lifted จาก mileage_admin.css): pill container `display:inline-flex; padding:3px; border:1px solid --vc-border; border-radius:8px; bg:--vc-bg` + chip `.ftab` `padding:5px 12px; border-radius:6px; transparent → hover bg-subtle/fg → active --vc-fg/--vc-bg/600` (dark fill); `.ftab-cnt` tabular-nums + opacity 0.6 (active 0.85). ลบ Phase 2 polish underline block (`::after scaleX` + `.ftab-cnt` box). Template/JS ไม่ต้องแก้ (class names เดิม). **Phase 5** booking rows 3-zone flat layout — `.bl-card` เปลี่ยนจาก card-with-border + 40px circle icon → **flat row** (no border + no radius + border-bottom 1px แยก row); grid `3px 1fr auto` (rail / content / right zone); `.bl-rail` 3px vertical bar status indicator (`--{pending\|approver\|approved\|rejected\|group}` map `--vc-{amber\|blue\|green\|red\|green}`) แทน `.bl-licon` circle; `.bl-content` 2-line (line1 = `.bl-title` name 14/500 + `.bl-dept-tag` pill xs muted bg-subtle; line2 = `.bl-meta` dest+pax+purpose sm muted); `.bl-right` stacked column (top = `.bl-time` mono tabular-nums "HH:MM–HH:MM", bottom = `.bl-actions`); selection states `.bl-selected`/`.bl-notify-selected` simplified (ลบ transform + box-shadow → ใช้ bg + rail color swap แทน); hover bg-subtle (เบากว่า Phase 2 polish `border-color:fg + translateY(-1px)`); stagger reveal via `style="--va-i:${idx}"` + `animation-delay: calc(min(--va-i, 8) * 30ms)`. **JS** rewrite `renderSingleRow(b, idx)`/`renderGroupRow(grpName, members, idx)` ใหม่ — pass idx จาก `renderBefore()` map callback; ลบ `STATUS_LICON` → `STATUS_RAIL` map; group row ใช้ ghost icon-only buttons (ungroup/edit/chevron) แทน secondary; trip card hover ลบ (informational §6). **Template** booking section header "รายละเอียด" → "**รายการจอง**" + เพิ่ม `<span.vc-card-head-meta#beforeCount>` populate by JS เป็น "N รายการ". **CSS** `.va-list` ลบ padding+gap (rows flush + border-bottom เอง). **Phase 6** vehicle col-4 polish — section 7 rewrite: `.vs-row` padding tightened `space-3/space-5`, `.vs-icon` 36→32 + border 1px (subtle frame แทน flat fill — active state `--vc-fg`, maintenance `--vc-amber-bg/--vc-amber/--vc-amber-border` แทน red), `.vs-name` mono `--vc-font-mono` + sm/500/tracking 0.01em (จาก 13.5px/700) — plate scan-friendly, `.vs-detail` xs muted, `.vs-detail-available/-maintenance` uppercase tracking 0.06em + **dot pseudo `::before` 6×6** (`var(--vc-green/--vc-amber)`) + ซ่อน Lucide `[data-lucide]` ภายใน (`display:none`) — dot+text uniform pattern; `.va-vehicle-card` animation `va-fade-up` delay 180ms backwards. **Phase 7** page entrance sequencing — เพิ่ม `va-fade-up` animation บน `.va-page-header` (no delay = first), `#sectionBefore` (delay 120ms), `#sectionAfter` (delay 240ms), `.va-vehicle-card` (delay 180ms); reduce-motion block ขยายครอบทุก entry. **Phase 8** modal polish — เปลี่ยน 4 modals (`#assignModal/#swapModal/#repairModal/#revertModal`) dialog enter duration `--vc-dur-slow → --vc-dur-base` (220ms snappy) + translateY 16px→**8px** + เพิ่ม sibling selector `~ .modal-backdrop.fade` ใส่ transition `opacity --vc-dur-fast --vc-ease-out` (backdrop fade-in 140ms).)
- 2026-05-23 (_sidebar v2.1: collapsible admin group + mystuff-style visual refresh — 6 vehicle-admin items nested under "จัดการรถ"; new `.sb-group/.sb-group-toggle/.sb-item--sub` + grid-row height anim + stagger reveal + indent guide; localStorage `bb-sb-groups`; labels shortened (อนุมัติรถ/เลขไมล์/รถและทีม/งบประมาณ/ค่าล่วงเวลา/ค่าน้ำมัน). **Visual**: section-label ตัด UPPERCASE+letter-spacing, weight 500→400, เพิ่ม `border-top` คั่นระหว่าง section; sub active = neutral `--vc-bg-hover` bg; badge dark indigo pill → soft gray pill. **Fix**: `.sb-group-toggle` `font: inherit` → `font-family: inherit` (เคย override font-size ของ `.sb-item` ทำให้ตัวใหญ่). **Badge backend**: เพิ่ม context_processor `inject_admin_pending_tomorrow` (`pending_count` = pending bookings ที่ start = พรุ่งนี้, BKK time, เฉพาะ admin))
