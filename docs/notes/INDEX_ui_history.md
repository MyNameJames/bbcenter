# INDEX_ui — ประวัติการเปลี่ยนแปลง

> แยกจาก [INDEX_ui.md](INDEX_ui.md) 2026-07-31 เพื่อลด token ตอนเปิด index
> verbatim ไม่ตัดทิ้ง — ค้นด้วยชื่อไฟล์

### `_shared/sidebar.html`

**Collapsible admin group** (2026-05-23): admin/mileage/fleet/budget/cost/fuel nested under "จัดการรถ" parent (`.sb-group` + `.sb-group-toggle` + `.sb-item--sub`); grid-row height anim + stagger reveal; localStorage `bb-sb-groups` persists open state; force-open when `active_menu` ∈ group; dot indicator on parent icon when collapsed + pending/active-child. Inline `<script>` at end of file (sidebar-scoped). **Bullet dot restyle (2026-06-15):** เพิ่ม `<span class="sb-dot">` ใน `.sb-item--sub` ทุก 6 รายการ; ลบ `.ps-1 .pt-1` จาก `.sb-label`. **🎨 Zendenta redesign (2026-06-19):** rewrite → class กลาง `.sidebar-*` ใน `core/css/main.css` (เลิก `.sb-*`); พื้น `#fafcfc` (`--vc-sidebar-bg`); Bootstrap-first (`d-flex`/`gap`/`p-`); active = pill accent; icon lucide เทา mono; collapse = `.sidebar-group`(`.is-open`)/`.sidebar-group-toggle`/`.sidebar-chevron`/`.sidebar-link.sub`/`.sidebar-dot` (logic + localStorage `bb-sb-groups` + role/badge/active_menu เดิมครบ); `components/sidebar.css` (`.sb-*`) legacy → retire ได้ (ไม่มี markup ใช้). playbook → [design_guideline.md](design_guideline.md) (zendenta playbook ลบแล้ว · legacy `.zen-*` cookbook = ดู main.css โดยตรง)

### `_shared/header.html`

**🎨 Zendenta redesign (2026-06-19):** `<header class="header d-flex align-items-center gap-3 px-4">` (60px, Bootstrap-first); hamburger `d-xl-none .zen-iconbtn border-0`; title `<h1 class="header-title m-0">`; notification bell → `.zen-iconbtn` (ใน `notification_panel.html`); **flag badge** `.header-flag d-none d-xl-inline-flex` (green pill, icon `flag` + `weekly_done/weekly_total`, future feature #11); **user desktop** = `d-none d-xl-flex` static (icon+ชื่อ+dept ไม่มี dropdown); **user mobile** = `d-xl-none #hdrUserWrap` button `.zen-iconbtn` → dropdown `.hdr-user-menu` (icon+ชื่อ+dept). CSS กลาง → `main.css §HEADER` (`.header`/`.zen-iconbtn`/`.header-flag`/`.header-user-*`). Jinja vars: `page_title`/`page_section`/`weekly_done`/`weekly_total`.

### `_components/*.html`

** [searchable select · `_components/bb/combo.html` · popover portal→body + mutual-exclusion 2026-07-22 · ดู § Design System]/**badge** [2026-06-29: `bb_badge`/`bb_status`/`bb_status_inline`]/**slider** [`bb_slider(min,max,step,dual,name/name_min/name_max,start,end)` — hidden input(s) ได้ `autocomplete="off"` (2026-07-02, กัน browser restore ค่าทับตอน manual reload; ดู `bb-components.js` initSlider() ที่ต้องอ่าน `getAttribute('value')` คู่กัน)]) · `_components/render/` = render layer ของ Python component: `_table.html`/`_badge.html`/`_status.html` (delegate macro `.bb-*`, ดู § Design System). **`bb_daterange` restructure (2026-07-02):** `bb-daterange-cals` แยกเป็น 2 อิสระ `.bb-dr-cal-col` แต่ละคอลัมน์มี head ตัวเอง (nav `<`/`>` + ชื่อเดือน caret + ปฏิทิน) แทน shared header เดิม (`.bb-daterange-header`/`.bb-dr-titles` = **ลบแล้ว**); caret ชื่อเดือน กดแล้ว replace เฉพาะคอลัมน์นั้น (เดิม replace ทั้งคู่) เป็น month/year grid 4 คอลัมน์ (เดิม 3); preset sidebar มือถือ → `.bb-dr-presets-wrap`/`.bb-dr-presets-trigger` dropdown ลอย (position:absolute ไม่ดัน layout, ปิดเมื่อคลิกนอก) แทน sidebar เดิม; footer ปุ่ม ล้าง→ยกเลิก (`is-ghost`→`is-sec`) ใช้→ยืนยัน; แก้ mobile centering bug (`.bb-daterange{left:0}` เคย override `position:fixed` มือถือ). **Portal + mutual-exclusion + footer rename (2026-07-22):** popover เปลี่ยนจาก `position:absolute` ใน parent เดิม → **portal ไป `document.body` (`position:fixed`)** ตอนเปิด (เหมือน `ue_chip_dd`/dropdown pattern) — escape overflow/transform ancestor; เพิ่ม **cross-widget popover mutual-exclusion** ร่วมกับ `bb_combo` (`initCombo`, bb-components.js) ผ่าน convention `window.__bbActivePopoverClose` (เปิด popover ตัวใหม่ → ปิดตัวที่เปิดค้างอัตโนมัติ ไม่ว่าจะเป็น combo หรือ daterange); footer ปุ่ม **"ยกเลิก" → "ปิด"** (ทุกหน้าที่ใช้ `bb_daterange`) — เดิมกดแล้วแค่ล้าง draft date ไม่ได้ปิด popover จริง (latent bug) เปลี่ยนเป็นปิด popover ตรงๆ. First adopter ที่เจอ regression ต้องรีวิว: ทุกหน้าที่ใช้ `bb_daterange` (mileage/cost/fuel filter bar).

### `auth/login.html`

 (2026-06-16 redesign → Sneat-style card): standalone page (ไม่มี shell), card กลางจอ พื้น `--vc-bg-subtle` + dot decoration มุม. Brand = Lucide `building-2` SVG + คำ `bbcenter` สี `--vc-accent`. subtitle "เข้าสู่ระบบด้วยรหัสหน้าแดง". Form POST `/login` (`username`/`password`), password toggle (eye/eye-off SVG). **ไม่มี create account / remember / ลืมรหัสผ่าน** (remember+forgot row ลบ 2026-06-17 — route hardcode `remember=True`, forgot ชี้ external ไม่มี backend จริง). โหลด `tokens.css` + `auth/css/login.css` + `auth/js/login.js` (เลิกใช้ main.css/main.js/bootstrap/select2/tilt). ทั้งหมด `--vc-*` tokens, no shadow→border

### `dashboard/dashboard.html`

 (2026-06-16 redesign จาก KPI/recent dashboard): ทุก role เห็นหน้าเดียวกัน. โครง: `dash-header` greeting + live clock → **เริ่มงานใหม่** Quick Actions 4 `dash-svc` card (link `<svc>.index?new=1`) → **วันนี้ของฉัน** timeline `dash-today` (ถ้ามี `today_items`) → **คำขอของฉัน** KPI strip + ตาราง + ปุ่ม **ทำซ้ำ** (`repeat_url`=`?copy_from=<id>`) / `empty_state` ถ้าไม่มี → superadmin banner (`dash-superadmin`). View `auth.dashboard()` ส่ง `my_requests` + `today_items` (เลิกส่ง stats/recent_*). Loads `dashboard.css` + `dashboard/js/dashboard.js`. Animation: stagger fade-up `dash-fade.d-{1..5}`. **🎨 Zendenta-clean migrate (2026-06-19):** ลบ `.dash-zen` dribbble-frame (พื้นเทา/เงานุ่ม/donut/service icon สี solid) → flat content `#fff`; service icon + today icon = เทา mono (`--vc-icon`/`--vc-icon-bg`, ไม่มีเงา/translateY); คำขอ donut → **KPI strip inline** (`.kpi-tile`+`.kpi-num`+`border-start`, % สีสถานะ) + Bootstrap **`.table.data-table`** (main.css) + **`.badge-pill`** (map `warning→b-warning`·`blue→b-accent`·`success→b-full`); row icon = `.dash-req-tile` (36px mono), repeat = `.dash-repeat`. ลบ dead CSS `dash-kpi*`(เหลือ `dash-kpi-label`)/`dash-alert`/`dash-mine*`/`dz-*`/`dash-recent-card .vc-table`/`dash-req-title`. main.css เพิ่ม tint `.b-warning`. logic/route/JS เดิมครบ

### `repair/repair.html`

 (2026-05-13 redesign): `repair-header` pattern, 5-cell KPI (admin only), `vc-table` + `vc-card-head`, DataTable (langUrl via data attr), 3 modals (repairFormModal/acceptModal/closeModal), `vc-badge`/`vc-btn`, Lucide icons. Loads `repair.css` + `pages/repair.js` (Phase 4.2 — `type="module"`). **Phase 3.4 (2026-05-15):** empty state migrated → `{% call empty_state(icon='wrench') %}` macro with CTA button caller; ลบ inline `style="width:20px;height:20px;"`. **Phase 4.2 (2026-05-15):** script → ES module `pages/repair.js`. **Redesign (2026-06-16):** header → budget style (`<h1 class="fw-bold text-accent pt-3">` + `<h6 class="text-muted text-header">` แทน `.repair-title/.repair-subtitle`); custom DataTables pagination แบบ pill วงกลม สี accent + go-to-page (สร้างใน repair.js). **DNA migrate (2026-06-17):** (1) ลบ in-body header h1/h6 → เหลือ action bar `justify-content-end` (หัวเรื่องใช้ topbar page-title); (2) KPI `.vc-kpi-group.repair-kpi-5` → `.fuel-kpi` strip (reuse vehicle_fuel.css, `row row-cols-2 row-cols-md-5 g-0`); (3) `.repair-modal-icon` → mono tile (`--vc-icon-bg`/`--vc-icon`, radius-sm) ลบ variant `--warning/--blue/--success`; (4) status "กำลังซ่อม" `vc-badge-blue` → `vc-badge-warning`. repair.css: ลบ `.repair-kpi-5` grid + 3 icon variant.

### `maintenance/maintenance.html`

 (2026-05-17 redesign): `maintenance-header` pattern, 5-cell KPI (admin only), `vc-table` + DataTable (langUrl via `#pageData` data attr), 4 modals (maintenanceFormModal/acceptModal/closeModal/exportModal), ฟอร์มแจ้งซ่อม → modal (create + edit mode, auto-open เมื่อ `edit_ticket`), `vc-badge`/`vc-btn`, Lucide icons. Loads `maintenance.css` + `pages/maintenance.js` (legacy script — jQuery+DataTable ยังใช้ global). **Redesign (2026-06-16):** header → budget style (`<h1 class="fw-bold text-accent pt-3">` + `<h6 class="text-muted text-header">`); custom DataTables pagination pill วงกลม สี accent + go-to-page (mirror repair). **DNA migrate (2026-06-17):** (1) ลบ in-body header h1/h6 → action bar `justify-content-end` (topbar page-title); (2) KPI `.vc-kpi-group` (+`vc-card`/`vc-card-head`) → `.fuel-kpi` strip (reuse vehicle_fuel.css, `row row-cols-2 row-cols-md-5 g-0`); by_category chip ย้ายใต้ strip; (3) 4 modal de-bootstrap: `modal-content` → `vc-modal` (ลบ inline border/radius), `form-control`/`form-select`/`textarea` → `vc-form-input`/`vc-form-select`/`vc-form-textarea` (form_group.css กลาง), `form-label text-muted small` → `vc-form-label`, `text-danger *` → `vc-required`, `form-text` → `vc-form-hint`, ลบ emoji ใน `<option>` ทั้งหมด, input-group เงิน → `vc-form-input vc-mono`; (4) status `vc-badge-blue` → `vc-badge-warning`, technician ช่างใน `vc-badge-blue` → `vc-badge-neutral`. maintenance.css ไม่เปลี่ยน (ไม่มี orphan).

### `room/room.html`

 (2026-05-17 redesign): sidebar + `_header.html` + `room-header` pattern, custom calendar grid (reuse `vehicle.css` calendar styles) + mobile list section (เหมือน `vehicle/vehicle.html`); ลบ FullCalendar + DataTables; แทน room badges สี เล็ก=blue / ใหญ่=amber ผ่าน tokens; data injection (`window.BOOKINGS`/`window.ROOM_CHOICES`); 3 modals: book/edit/detail. Loads `room.css` + `pages/room.js` (ES module). **Zendenta-align redesign (2026-06-15):** structure เหมือน `vehicle/vehicle.html` — `va-page-header` → **`fuel-header`** (desktop title+subtitle ซ้าย, room legend ขวา); toolbar → **`cal-toolbar d-none d-md-grid`** + `cal-toolbar-left` (วันนี้/prev/label/next); เพิ่ม **`vrc-m-hero d-md-none`** block (mobile: title + sub-line + month picker IDs: `currentMonthLabelMobile`/`prevMonthBtnMobile`/`nextMonthBtnMobile`/`todayBtnMobile`); calendar card `vc-card p-3` + `card-header`/`card-body` → **`vc-card overflow-hidden`** + `calendar-grid-head text-header border-bottom`; day headers เพิ่ม `d-none d-md-inline` ชื่อเต็ม + `d-md-none` ย่อ; `.mobile-list-header` → **`vrc-m-daybar`** + `vrc-m-daybar-text` ห่อ `#mobileListDateLabel` + `#mobileDaybarCount`; mobile book button เปลี่ยนจาก `data-bs-toggle` → `onclick="openBookingModal()"`. **DNA migrate (2026-06-17):** ลบ desktop head block (h1 `text-accent` + h6 `text-muted`) + ลบ h2+h6 (`text-danger` subtitle) จาก `.vrc-m-hero`; room legend ย้ายเข้า cal-toolbar col 3 (3-col grid `1fr auto 1fr` ที่มีอยู่แล้ว, desktop) + เพิ่มใต้ month controls (mobile). topbar `page_title` แสดงหัวเรื่องแทน. room.css ไม่แตะ.

### `vehicle/vehicle.html`

 script → ES module `<script type="module" src="js/pages/vehicle.js">`; ลบ inline `<script>` pay-deeplink IIFE (ย้ายเข้า module). **Phase 1 user-facing redesign (2026-05-17):** เริ่ม migrate ไป BBCenter V2 — viewport `maximum-scale=1.0`; ลบ DataTables CSS + IBM Plex Sans Thai; เพิ่ม `design-system.css` + `vehicle_admin.css` ใน link order; `<main>` ใส่ `vc-scope`; flash `alert alert-{cat}` → `ds-alert` ใน `vc-stack`; toolbar 3 ปุ่ม (prev/next/today) → `vc-btn vc-btn-ghost vc-btn-icon` + Lucide chevron-left/right + `vc-btn-secondary` "วันนี้"; calendar wrapper `card h-100` → `vc-card`; mobile "จองรถ" `btn btn-dark` → `vc-btn-primary` + Lucide plus. **Phase 2 (2026-05-17):** modals 4 ไฟล์ + duration preview เสร็จ (ดู modal rows ด้านล่าง). **Phase 3 (2026-05-17):** ds-status-dot/STATUS_ICON refactor เสร็จ — ดู `design-system.css` §7 + `pages/vehicle.js` row ด้านล่าง. **Phase 4 (2026-05-17):** เสร็จครบ — token cleanup + Lucide migration (~17 icons) + UX polish: calendar `role="grid"` + columnheader/gridcell ARIA + `aria-label` Buddhist year + `aria-current="date"`; keyboard nav (← → / T) ใน vehicle.js (skip input/modal); popover `shown.bs.popover` listener + `initIcons`. **Phase 9 (2026-05-22):** BOOKINGS data รับ `cancelUrl` + `canCancel`. **2026-06-12 → 2026-06-20:** `canCancel = (owner AND status == 'pending') OR (admin AND status ∈ {pending, waiting_approver, approved}); AND start_datetime > now` (user เห็นปุ่มยกเลิกเฉพาะ pending — sync กับ `cancel_booking` ที่ user ยกเลิกได้เฉพาะก่อน admin จัดรถ). `index()` ส่ง `now`/`is_vehicle_admin` kwargs เพิ่ม. **Mobile Phase A+B (2026-05-23):** mobile redesign (เป้าหมาย premium look ตาม ux-designer spec) — desktop toolbar 2 blocks (page title + month nav row) เพิ่ม `d-none d-md-flex` ซ่อนบน mobile; เพิ่ม `.vrc-m-hero d-md-none` block (mobile-only) — title 20px/600 + sub-line + month picker row พร้อม IDs ใหม่ `currentMonthLabelMobile`/`prevMonthBtnMobile`/`nextMonthBtnMobile`/`todayBtnMobile`; แทน `.mobile-list-header` ด้วย `.vrc-m-daybar` (sticky bar) — text container ใหม่ `.vrc-m-daybar-text` ห่อ `#mobileListDateLabel` + new `#mobileDaybarCount` (รายการ count); ปุ่ม `#mobileDateCountBtn` "จองรถ" ยังอยู่ในนั้น (จะย้ายเป็น FAB ใน Phase D). ปฏิทินยังเป็น `#calendarBody` ตัวเดียว — restyle ผ่าน CSS เท่านั้น. **Mobile Phase D (2026-05-23):** **ลบปุ่ม `#mobileDateCountBtn` "จองรถ" inline ออกจาก `.vrc-m-daybar`** (daybar เหลือแค่ label + count); เพิ่ม FAB markup ก่อน `</main>` — `<button class="vrc-m-fab d-md-none" id="mobileFabBook" data-bs-toggle="modal" data-bs-target="#bookingModal" aria-label="จองรถ">` + Lucide plus icon (round 56×56 icon-only ตามที่ user เลือก Q2). **OT warning data (2026-06-10):** `index()` ส่ง context `ot_rates` (= `OTRateConfig` active rows → list dict `{label,start,end,rate,dow}`) → inject `window.OT_RATES` (`| tojson`) สำหรับ booking modal คำนวณคำเตือนค่าล่วงเวลาสารถี (ดู modal row + `bk*` JS). **Hero banner (2026-06-13):** desktop title block (`.vrc-page-title/-subtitle` ใน `d-flex justify-between`) → `.vrc-hero d-none d-md-flex` (title `.vrc-hero-title` + sub `.vrc-hero-sub` ซ้าย, `<img.vrc-hero-img>` = `static/vehicle/img/vehicle.png` bleed ขอบขวา) บนพื้น indigo-soft — ลดความเป็นทางการ. CSS `.vrc-hero*` อยู่ใน `core/css/vercel.css` (ใต้ `.vrc-page-subtitle`). mobile ใช้ `.vrc-m-hero` เดิม (ไม่มีรูป) **Zendenta redesign → prod (2026-06-14):** ลบ `.vrc-hero` + custom toolbar → **fuel-header pattern** (`fuel-header`/`fuel-title`/`fuel-subtitle`/`fuel-header-actions` + ปุ่ม "จองรถ" `vc-btn-primary`) + **`.cal-toolbar`** (grid 1fr/auto/1fr: วันนี้ซ้าย · prev/`#currentMonthLabel`/next กลางเสมอ — `.cal-month-label`); weekday header ชื่อเต็มบน desktop (`<span class="d-none d-md-inline">อาทิตย์…` + `d-md-none` ย่อ); `<body class="zd">` + โหลด `vehicle_zendenta.css` (theme: icon ใหญ่ + calendar restyle "ภาพ 2"). IDs คงเดิม (`prevMonthBtn`/`nextMonthBtn`/`currentMonthLabel`/`todayBtn`/`bookingModal`). สีน้ำเงิน `#0046FF` จาก token global. **Mobile booking (2026-06-14):** ปุ่ม `#mobileDateCountBtn` "จองรถ" เปลี่ยนจาก `data-bs-toggle` → `onclick="openBookingModal()"` → modal prefill วันที่ = `selectedDate` (วันที่แตะในปฏิทิน) + เวลา default 08:00–17:00 (logic เดิมใน `openBookingModal()`). **DNA migrate (2026-06-17):** ลบ `fuel-header` block (h1 `text-accent` + h6 `text-muted`/`text-danger`) + ลบ h2+h6 จาก `.vrc-m-hero` (mobile title/subtitle); ย้าย "08:00–17:00 · วันอาทิตย์ไม่ให้บริการ" เป็น `.cal-caption` (`.75rem / --vc-fg-subtle`) ใน cal-toolbar col 3 (desktop) + ใต้ month controls (mobile). `vehicle_zendenta.css` ลบ section 1 icon-size override ทั้งก้อน (sidebar 21px/header 22px) → icon size = global default เหมือนทุกหน้า + เพิ่ม `.cal-caption` rule.

### `vehicle/vehicle_approver.html` *(เดิม approver_inbox.html)*

**Phase 3.8 (2026-05-15):** ลบ inline `<style>` (197 lines) → `vehicle_approver.css`; ลบ inline `<script>` → `vehicle_approver.js`; `.badge-waiting/.badge-approved/.badge-rejected` → `vc-badge vc-badge-warning/success/danger vc-badge-dot`; 3 empty states → `vc-empty/vc-empty-icon/vc-empty-title`; ลบ inline `style=""` ทุกจุด (dynamic width คง). **Phase 4.1 (2026-05-15):** script → ES module. **Phase 5.11 mobile redesign (2026-05-18):** restructure mobile-first — `.approver-wrap` max-width 560px center; pending header → status badge; tabs pill style; approve/reject เป็น 2-col `.ac-action-row` (`.btn-approve-action` primary + `.btn-reject-action` red outline) + reject form `.ac-reject-cancel`/`.btn-reject-confirm`. **Detail-card redesign + fuel badge (2026-06-10):** (1) เพิ่ม Jinja macro `th_date(dt)` → วันไทย "พ. 10 มิ.ย. 2569" (`%w` วัน + เดือนย่อ + ปี พ.ศ. `+543`); (2) เพิ่ม `.ac-fuel-badge` (amber, icon lucide `fuel` + `฿`) ใน `.ac-header-right` ทั้ง 3 tab — ค่าจาก dict `fuel_costs[b.id]` ที่ `approver_inbox()` คำนวณ (override `mileage.fuel_cost` ถ้ามี ไม่งั้น `(dist/fuel_rate)*fuel_price` ผ่าน `FuelPrice.get_for_date`; trip ที่ยังไม่ปิด=0); (3) **ทิ้ง `.ac-fields` 2-col grid** → `.ac-detail-card` layout (เลียน `driver-detail-card`): head=ผู้จอง+pax chip / `.ac-detail__datetime` (วันที่+เวลา, border-bottom) / trip=purpose→destination / pickup; tab ปฏิเสธ ย้ายเหตุผลเป็น `.ac-reject-note` (กล่องแดง). CSS เก่า `.ac-fields/.ac-field*/.ac-reject-reason` ลบทิ้ง. **Header slim (2026-06-12):** accordion header `.ac-ref` โชว์ `th_date(b.start_datetime)` (วันที่ไทย) แทน `trip_department/central_category`; ลบ `.ac-meta` badges row (driver + เวลา + booker) → แทนด้วย `.ac-detail__trip` row (icon clock + เวลา start–end + dot separator + icon map-pin + ปลายทาง) ใน header; ลบ `.ac-detail__datetime` block ออกจาก detail card (ซ้ำกับ header ใหม่); `.ac-detail__trip` ใน detail card ได้ `border-bottom pb-2 mb-2`. **2026-06-21:** (1) fuel badge ทั้ง 3 tab: fc==0 → "ยังไม่เดินทาง" (ก่อนหน้านี้แสดง ฿0); (2) เพิ่ม over-budget warning `.budget-card-warn` หลัง progress bar ทุก budget card (icon `triangle-alert` + "งบเกิน ฿N" สีแดง — แสดงเมื่อ `budget.remaining < 0`)

### `vehicle/admin/vehicle_mileage.html`

**cost_ceiling → view (2026-07-02):** ลบ Jinja loop ที่เคยคำนวณ ceiling ของ cost range-slider จาก `display_rows` ในเทมเพลตเอง (bug: ceiling ยุบเหลือ floor เริ่มต้นเมื่อ date filter ปัจจุบันไม่มี booking) — เปลี่ยนเป็นรับ `cost_ceiling` จาก view ตรงๆ (`_calc_cost_ceiling()` ใน `vehicle_mileage.py`, คำนวณจาก booking ทั้งหมดไม่จำกัด date filter). คู่กับ reload-reset fix ชุดเดียวกัน (ดู `vehicle_mileage.js` + § Component library `bb_slider`/`bb_daterange`). **Container centering fix (2026-07-02):** แยก `<main class="bb-sidebar-main">` ออกจาก `container-xxl` — เดิมรวม class เดียวกัน (`<main class="bb-sidebar-main container-xxl">`) ทำให้เนื้อหาไม่จัดกลางที่จอ ≥1400 เพราะ `.bb-sidebar-main{margin-left:16rem}` ([components.css:618](../../app/static/core/css/components.css#L618)) โหลดหลัง Bootstrap และ specificity เท่ากับ `.container-xxl` → ทับ `margin-left:auto` (margin-right ดูดพื้นที่ว่างไปกองขวาแทนที่จะแบ่งสองข้าง) — เปลี่ยนเป็น `<main class="bb-sidebar-main"><div class="container-xxl">…</div></main>` (sidebar-offset กับ centering คนละ element) แก้ที่ template นี้เท่านั้น ไม่แตะ `components.css` (กันกระทบหน้าอื่นที่ยังไม่ adopt `container-xxl`). พร้อม cleanup: ลบ `<div class="">` wrapper ว่างเปล่าที่ครอบ zone ทั้งหมดโดยไม่มีผล, reindent scaffold ให้ตรงระดับ nesting จริง, inline `style="margin-bottom:1rem"` (flash wrapper) → `class="mb-3"`. **layout.html prototype migrate (2026-07-05):** ทดลอง design ผ่าน prototype page `layout.html` (route `/finance`) ก่อน apply กลับมาที่หน้านี้ + promote ส่วนที่ finalize เข้า `components.css` กลาง — (1) title `h1→h2` + `color:var(--bb-accent)`, container เพิ่ม `px-1 px-md-5`; (2) KPI เหลือ 1 ใบ "ค่าน้ำมันรวม" (`#sumAllCost`, live ตาม tab/filter ผ่าน `calcAllSummary` เดิม); (3) status tabs (`#statusTabs`) restructure เป็น full-bleed border-bottom (แยก `container-xxl` เป็นหลาย block: title+kpi ปิดก่อน → full-bleed wrapper ห่อ tabs → เปิดใหม่สำหรับ toolbar row2+results); (4) desktop table ลดจาก 11 เหลือ 8 คอลัมน์ (checkbox/วันที่/ผู้จอง/คนขับและรถ(รวม)/ปลายทาง/ไมล์ออก→กลับ(รวมค่าน้ำมัน+fuel icon tooltip)/สถานะ/จัดการ) — ตัด Booking(BK-xxx)/ค่าน้ำมันแยก/เติมน้ำมันแยกทิ้ง, ลบ date-group divider row (`colspan=11`) ออก ใช้วันที่ต่อแถวแทน (`TH_MONTHS` สั้น เช่น "12 มิ.ย."); การ์ดห่อ table เพิ่ม `rounded-0`. page-scoped `<style>` เดิมถูกลบทิ้งทั้งหมดหลัง promote → ดู § Design System `components.css` entry สำหรับ token ที่เปลี่ยนจริง (กระทบ global). **🎨 UE-redesign Phase 1 (2026-07-11) — "ถึงอารมณ์" `mockup-orders.html`:** (A1) แก้ page-title ค้าง `คำสั่งซื้อ`→`{{ page_title }}`. (B motion — inline `<style>` `.ml2-*` + `vehicle_mileage.js`) frameIn เข้าหน้า · KPI `#sumAllCost` count-up+bump (`calcAllSummary`) · row stagger+dotPop (`staggerRows()` ใน `bindResults`) · skeleton shimmer ระหว่าง AJAX (`showSkeleton()`+`SKEL_MIN_MS` ใน `runFilter`) · flash→toast (`data-bb-toast-flashes` bridge แทน `.bb-callout`). (C) Inter 400-800 + KPI 26/800. (D1 chrome จริง) `_shared/sidebar2.html` rewrite role-based (port logic จาก `bb_sidebar`: url_for+active-by-endpoint+pending badge, biz-switch=user จริง) + `_shared/header2.html` notification จริง (`notification_panel`). (D2 icon = Material Symbols ทั้งหน้า) header2 **ไม่โหลด Lucide** (stub `window.lucide` no-op) → `core/js/ms-icons.js` (ใหม่) แปลง `[data-lucide]`→`<span material-symbols-outlined>` runtime (MutationObserver จับ dynamic; คง `data-lucide` ให้ combo/sort เดิม query; `updateSortIcons` set ligature ตรง) · sizing คุมด้วย CSS ตาม context (`.bb-btn/.bb-status/.bb-kpi/.bb-filter-btn/...`) เพราะ bb-* เดิม size ผ่าน `svg` · empty icon = `note_stack`. **Contained:** ไม่แตะ shared macro/`bb-components.js` → finance/`layout.html` คง Lucide เดิม. **🔧 Phase 1.5 foundation promote (2026-07-11):** ยก inline → shared แล้วให้ mileage `{% extends '_base_ue.html' %}` — `core/css/ue.css` (tokens+shell+motion+MS sizing, แทน inline `<style>`) · `core/js/ue-motion.js` (`window.ueMotion`: countUp/staggerRows/showSkeleton, แทน local ใน vehicle_mileage.js) · **`templates/_base_ue.html`** (base layout). mileage เหลือเฉพาะ block content/modals/scripts. หน้าอื่นที่จะ redesign → extends base เดียวกัน (Phase 2 ถูกลง). **Zone 1 cleanup + KPI (2026-07-21, ตาม [doc/2026-07-21_mileage-cleanup-plan.md](doc/2026-07-21_mileage-cleanup-plan.md)):** หลัง UE redesign (`803d857`) Zone 1 เหลือ KPI เดียว (`#sumAllCost`) แต่ controller/JS ยังคำนวณ selection-summary (`recalcSummary`/`calcAllSummary`) + KPI 6 ตัวจาก `_calc_mileage_kpi()` ที่ template ไม่ render (gap). แก้: **(1) Zone 1a summary strip** คืนมา — `#summaryStrip` > `#modeAll`(ghost KPI `#sumAllCount`/`#sumAllDistance`/`#sumAllCost`) / `#modeSelected`(`#selCount`/`#selDistance`/`#selCost` + ปุ่ม `clearSelection()`), toggle โหมดด้วย **inline `style.display` ตรงๆ ไม่ใช่ class `d-flex`/`d-none`** (Bootstrap utility `!important` จะทับ JS ที่เซ็ต `.style.display` ไม่ได้). **(2) Zone 1b — 4 KPI action cards ใหม่** (`.bb-kpi.is-card`): ค่าน้ำมันเดือนนี้ / งานยังกรอกไมล์ไม่ครบ (คลิกกรอง) / รอยืนยันจ่ายส่วนตัว (คลิกกรอง) / งบคงเหลือทั้งปี (ตัวเลขแดงเมื่อ `< 0` — ปิด gap "A2 เตือนงบเกิน" in-place แทน banner แยก). การ์ดคลิกได้ = `<div data-kpi-filter="incomplete|pending_personal" role="button" tabindex="0">` ห่อ `bb_kpi`. **(3) JS bug fix** `recalcSummary()` (vehicle_mileage.js) เดิมอ่าน `cb.checked`/`cb.disabled`/`$checkAll.indeterminate` (native input API) ผิด component จริง (`bb-check-box` = `<span>`) → เปลี่ยนอ่าน `.classList.contains('is-on'/'is-disabled')` + ใช้ `setCheck()`; เพิ่มเรียก `recalcSummary()` ท้าย `checkAll`/row-click handler ใน `bindResults()` ที่หายไปเดิม (indeterminate visual ข้าม — ไม่มี CSS `.is-indeterminate` รองรับ). **(4) filter param ใหม่** `status_filter=incomplete` (none+partial) + `pending_personal=1` (parse ใน `_parse_mileage_filters`/กรองใน `_build_mileage_rows`, vehicle_mileage.py) — เกณฑ์ตรงกับที่ `_calc_mileage_kpi` นับ; hidden input `#pendingPersonalFilter` ใน `#filterForm`; status tabs เพิ่ม tab "ยังไม่ครบ" (`value=incomplete`). KPI card คลิก → `bindKpiFilters()` (vehicle_mileage.js, ใหม่) จำลองคลิก tab หรือ set hidden input แล้วเรียก `runFilter()` เดิม (ไม่มี fetch ใหม่); `bb_filter` clear button เพิ่ม reset `pending_personal`. **นอก scope:** C2 breakdown (`_build_vehicle_breakdown`/`breakdown`/`breakdown_totals`) ยังคงในโค้ดเหมือนเดิม — ยังไม่ตัดสินใจตัด/ทำ chart ต่อ (ดู plan §5) → **เคาะแล้ว 2026-07-27: ลบทิ้ง** (ดู INDEX_code.md § `mileage_log()`). **60-30-10 color-signal fix (2026-07-22):** review ตาม `design_guideline.md` §2 พบสี semantic ถูกยืมผิด role 3 จุด — (1) OT-hours `.bb-badge` inline `--bb-wr-bg` ชนสีกับ status "รอกรอกไมล์กลับ" (ส้มเดียวกันคนละความหมาย) → `.bb-badge.is-neutral`; (2) mobile trip-card status badge เดิมมีแค่สี+label ไม่มี icon (ผิดกฎ §2 status ต้องมี icon+label คู่) → เปลี่ยนเป็น `.bb-status.is-ok/.is-wr/.is-neutral` + icon `check-circle`/`clock`/`circle` ให้ตรง desktop table เดิม; (3) `#summaryStrip` text 4 จุด inline `style="color:var(...)"` ล้วน → utility class ใหม่ `.bb-text-str`/`.bb-text-mut` (เพิ่มต่อจาก `.bb-subtext` ใน `components.css`). ไม่แตะ inline style ที่ผสม font-size/weight (mobile card meta/modal preview) — คนละ scope. **ปรับเพิ่มเติมวันเดียวกัน (ตามคำขอ user หลังรีวิว):** (1) OT-hours badge กลับไปใช้ `.bb-badge.is-wr` (เพิ่ม CSS variant ใหม่ใน `components.css`) แทน `is-neutral` — ตั้งใจย้อนกลับทางเลือกสีจาก fix ข้อ (1) ด้านบน ตามที่ user ยืนยันเลือกเอง; (2) desktop table status column ("สถานะ") เอา icon ออกทั้ง 3 สถานะ เหลือ label อย่างเดียว — ขัด §12 "icon+label เสมอ" โดยตั้งใจ บันทึกเป็นข้อยกเว้นเฉพาะหน้านี้ใน `design_guideline.md` §14 (เคาะแล้ว 2026-07-22); mobile card list ยังคง icon+label ตามเดิม (asymmetry desktop/mobile ตั้งใจ). **Layout flatten + filter→ue_chip migrate (2026-07-22, สรุปเต็ม → [CHANGELOG.md](CHANGELOG.md#indexuimd--templates--history)):** Zone 1b (4 KPI action cards, 2026-07-21) **ลบทั้งหมด**; ghost-KPI summary strip → title+desc text block ล้วน (reuse id เดิม `sumAll*`/`sel*`, ไม่แก้ JS logic); desktop table **ไม่มี `.bb-card` wrapper แล้ว** (flush ไม่มีกรอบการ์ด). **Filter chips 4 ตัว** (รถ/คนขับ/งบ/หมวด-กอง) migrate จาก page-local `bb_ml_dd` (custom `.bb-select`/`.bb-menu`) → macro กลาง `ue_chip_dd`/`ue_chip_opt` (radio single-select, `_components/bb/ue_chip.html`) — `bb_ml_dd` ในไฟล์นี้เหลือแค่ thin wrapper รอบ `ue_chip_dd`; ย้ายออกจาก `bb_filter` flyout มาเป็น chip แถว toolbar เอง → **`bb_filter` (cost-range slider + ปุ่ม "ล้างการเลือก") ถูกถอดออกจากหน้านี้ทั้งก้อน** (macro ยังอยู่ใน `_components/bb/filter.html` ไม่ได้ลบไฟล์ — แค่ไม่ถูกเรียกจากหน้านี้แล้ว) — backend ที่ค้างเป็น dead path (`cost_ceiling`/`f.cost_min`/`f.cost_max`) **ลบครบแล้ว 2026-07-27** (ดู INDEX_code.md § `mileage_log()`). Booker filter (`bb_combo`) เพิ่มระหว่าง session แล้วถอดออกทั้งหมดพร้อม JS wiring (สุทธิ = ไม่มี filter ผู้จองในหน้านี้). **JS bug fixes** (`vehicle_mileage.js`): `clearSelection()` เดิม no-op บน custom `<span>` checkbox (`.checked=false` ไม่มีผล) → ใช้ `setCheck()`; row-click handler exclude แค่ `button, a` ไม่รวม `[role="button"]` → เมนู "⋮" (`<span role="button">`) เคยลาก toggle เลือกแถวไปด้วย → แก้ selector; `openMileage()` เคย crash ที่ `querySelector('svg, i').style` (`ms-icons.js` แปลง `<i data-lucide>`→`<span data-lucide>` ไม่เหลือ svg/i ให้ match) → แก้ selector + null guard. Empty-state icon hardcode `<span class="material-symbols-outlined">note_stack</span>` → convention `<i data-lucide="note-stack">` เดิม (+ `components.css` `.bb-empty-icon .material-symbols-outlined{font-size:1.75rem}` เพราะ Material Symbols คุมขนาดด้วย font-size ไม่ใช่ width/height). Shared component behavior เปลี่ยน (portal + popover mutual-exclusion ของ `bb_combo`/`bb_daterange`, ปุ่ม `bb_daterange` เปลี่ยนชื่อ) → กระทบหน้าอื่นด้วย ดู § Design System entry `_components/*.html` + Component library table. **`#mileageModal` — Case 17 merge (2026-07-22, ต่อยอด mockup Case 17 ใน `bootstrap-cases-gallery.html`):** เพิ่มเส้นประคั่น header/`bb-timeline` + `bb-tl-item` เดิม (odometer in/out) เพิ่ม 2 บรรทัดใหม่ (`mmDriverPhone`/`mmBudgetLine` — plate+dest เดิมคงไว้ไม่แตะ) + 2 `bb-tl-item` ใหม่ (`#mmOtStop`/`#mmCostStop`, ซ่อนด้วย inline `display:none` จนกว่า JS จะโชว์เมื่อทริปปิดแล้ว) แสดง breakdown ค่า OT หลาย time-band (`DriverOTSlot`) + เส้นเทียบเวลาปกติ/OT + ค่าใช้จ่ายรวม ปุ่ม submit เปลี่ยนคำ "บันทึก"→"ยืนยัน". row data-attributes เพิ่ม `data-phone`/`data-budget-label`/`data-fuel-price`/`data-ot-total`/`data-ot-slots`(JSON)/`data-actual-start`/`data-actual-end` (ทั้ง desktop `<tr>` + mobile `.bb-ml-trip-card` สองจุด). **ปรับรอบ 2 (2026-07-22 หลังทดสอบจริง):** stop 1 marker `.bb-avatar`(user)→`.bb-tl-dot` ให้ตรงกับ stop 2/3 · `#mmAvatar` icon `bike`→`bus` · `.bb-modal-body` เพิ่ม class `modal-body` คู่กันให้ Bootstrap `.modal-dialog-scrollable` ทำงานจริง (เดิม CSS scroll rule หา `.modal-body` ไม่เจอเพราะ wrapper เป็น custom class) · `data-actual-start`/`data-actual-end` เปลี่ยนจาก HH:MM → datetime-local เต็มเพื่อรองรับข้อ 4. **Bar OT — legend + segment fix (2026-07-27, verify ด้วยตาในเบราว์เซอร์ครั้งแรก):** legend ใต้ bar เดิม hardcode `Regular time`/`Overtime` ซ้าย-ขวาเสมอ → `#mmOtBarLegend` + `renderOtBarLegend()` วาดเฉพาะชนิดที่มีจริงบน bar (swatch = แถบสั้น ไม่ใช่จุด เพราะ `.bb-dot` ถูกลบทั้งระบบ) · `buildOtBarSegments()` เดิมเลื่อน slot ที่เริ่มก่อนทริปไป +24h → slot ที่กว้างกว่าช่วงทริป (เช่น `08:53-19:53` กับทริป `15:59-16:00`) ได้ `width:101400%` → เปลี่ยนเป็น clip เข้าช่วงทริป + ตัด slot ที่ไม่ทับ (รายละเอียด+ผล test → [CHANGELOG.md](CHANGELOG.md#indexmd--symbol--template--css-js-history)). **กติกา OT + ป้ายเวลา (2026-07-27, รอบ 2):** ป้ายบนแถบเหลือ **เวลาทริปเท่านั้น** (`เริ่มทริป HH:MM` ซ้าย / `จบทริป HH:MM` ขวา) — เดิมเอาขอบ slot OT มาเรียงปนจนเวลาไม่เรียงซ้าย→ขวา (`15:59 · 08:53 · 19:53 · 16:00`); **ช่วงเวลา OT ย้ายไปต่อท้าย legend** ใต้แถบแทน · legend label เป็นไทย `ช่วงที่คิด OT` / `ช่วงที่ไม่คิด OT` · เพิ่ม callout `#mmOtMismatch` (`.bb-callout.is-dg`) โผล่เมื่อ `data-ot-mismatch="1"` = ช่วง OT หลุดกรอบเวลาทริป (ค่า OT มาจากเวลาชุดเก่า) + badge `ชม.` ในตารางได้ไอคอน `alert-triangle` + tooltip เตือน. row data attr ใหม่: `data-ot-mismatch` (ทั้ง desktop row + mobile card). **Filter chip multi-select (2026-07-28):** chip ทั้ง 4 (รถ/คนขับ/งบ/หมวด-กอง) เปลี่ยน `ue_chip_opt` จาก `type='radio'` → **checkbox** เลือกหลายค่าได้ · **ตัด option "ทั้งหมด" (`value=''`) ทิ้งทุกตัว** — ไม่ติ๊ก = ไม่กรอง, ล้างใช้ลิงก์ "ล้าง" หัว panel · เทมเพลตเทียบด้วย `in f.<key>s` (list) แทน `== f.<key>` · `#filterBudgetSubSec` ซ่อน/แสดงจาก `sub_opts` ว่างไหม (เดิมเช็ก `f.budget_type in ['central','department']`) และ `sub_opts` รวมหมวดของงบทุกประเภทที่ติ๊ก · `window.BBML_FILTER_SUB` เป็น **list** แล้ว. **filter ใหม่ 2 ตัว:** chip `วัน` = `ue_chip_dd` + checkbox 7 วัน (label ชื่อเต็ม `จันทร์`…`อาทิตย์`, value = เลข `strftime('%w')`; รอบแรกทำเป็น toggle chip 7 ตัวเรียงกันแต่กินความกว้าง toolbar เลยยุบเป็น panel เดียว — `.bb-ml-chiprow` ที่เคยคุม row นั้นถูกลบออกจาก `components.css` แล้ว) + chip `มี OT` = `ue_chip` toggle กดตรงๆ ไม่ต้องเปิด panel

### `vehicle/vehicle_driver.html` *(เดิม driver_home.html)*

 (2026-05-08, rev2): `<body class="vc-scope">` + lucide icons only (no Font Awesome). Header + segmented tabs (วันนี้/พรุ่งนี้, no "ย้อนหลัง"), accordion cards (`[data-card]` open one→close others within active panel), inline mileage form (no modal), upload zone (no separate camera button), CTA black `--vc-primary`. Tomorrow tab read-only with "เริ่มงานได้ในวันที่ …" note. Refuel UI removed. **Phase 3.5 (2026-05-15):** 2 empty states → `{{ empty_state(...) }}` macro; ลบ inline `style=""` ทั้งหมด (flash alert → `driver-flash-alert`, plate span → `driver-plate`, unset span → `driver-unset`). **Phase 4.4 (2026-05-15):** ลบ inline `<script>` (67 lines) + duplicate lucide CDN → ES module `<script type="module" src="js/pages/driver-home.js">`; lucide CDN+init มาจาก `_header.html` แล้ว. **Card redesign req1+2 (2026-06-09):** card head reorder = `ทะเบียน · เวลา · [pill]` (`.driver-card__plate`/`__time` แทน `__id`/`__summary`, ตัด destination); body: (1) emergency เปลี่ยนรถก่อนออก — `.driver-emergency` form (label แดง) + `<select data-dropdown>` (vc-dd) → POST `/driver/change-vehicle`, render เฉพาะ state waiting; (2) `.driver-detail-card` (white `--vc-bg-subtle`, no border) = ผู้จอง+`passenger_count` มุมขวา / `purpose`→`destination` / `pickup_location`; (3) ฟอร์มไมล์ออก reorder = title+`latest_odo` hint → upload (`.driver-upload--wide`) → `odometer_start` input (`min=latest_odo`) → submit. ทั้ง today+tomorrow tab ใช้ detail-card. โหลด `core/css/components/dropdown.css` + `core/js/dropdown.js` (classic, ต่อท้าย) สำหรับ vc-dd. **Ad-hoc collapse + OCR req3 (2026-06-09):** ปุ่ม "งานนอกระบบ" จาก modal → **collapse panel** (`.driver-adhoc-panel[data-adhoc-panel]`, toggle ปุ่ม `[data-adhoc-toggle]`, icon หมุน 45°): ผู้จอง `<select data-autocomplete>` (vc-ac autocompleteselect — strict, เก็บ user id) + รถ `<select data-dropdown>` (vc-dd) + `purpose` + `destination` + บล็อกไมล์ออก (upload wide + `odometer_start`); POST `/driver/ad-hoc-trip` (สร้าง booking + VehicleMileage start ทันที). ลบ modal+combo เดิมหมด. ทุก input เลขไมล์ได้ `data-odo-input` (ใช้โดย ad-hoc vehicle hint set `min`). _auto-OCR ตัดทิ้ง 2026-06-09 (tesseract อ่าน 7-seg เพี้ยน) — เก็บ upload + กรอกเอง._ CSS dead ที่เหลือ: `.driver-modal*`/`.driver-combo*` (เหลือ `.driver-modal__btn--ghost` reused).

### `vehicle/modals/vehicle_book.html` *(เดิม vehicle-modal-book.html, ย้าย→modals/ ขั้น 4 2026-06-07)*

 ลบ `badge-pending` ออกจาก info card → ใช้ `.bk-info-note` แทน. **Phase 2 user-facing redesign (2026-05-17):** 7 `bi-*` + `bi-arrow-right-circle-fill` → Lucide (folder-plus/check-circle-2/arrow-right/info); footer 2 ปุ่ม `btn-secondary/btn-dark rounded-2` → `vc-btn vc-btn-secondary/vc-btn-primary` + `title=""`; เพิ่ม `<div id="bk_duration_preview" class="bk-duration-preview" aria-live="polite">` ใต้ช่วงเวลา (JS compute end−start). **⭐ DateField/TimeRangeField component redesign (2026-07-29) — ตอนนี้เป็น modal มาตรฐาน/reference สำหรับ modal ฟอร์มใหม่ในระบบ:** ลบ inline calendar/time-dropdown เดิม (`.bk-datepick`/`.bk-cal`/`.bk-timepick*`, ~450 บรรทัด HTML/CSS/JS) ทิ้งทั้งหมด แทนด้วย `{{ component(date_field) }}`/`{{ component(time_range_field) }}` (`DateField`/`TimeRangeField` สร้างใน `vehicle_booking.py::index()`, id `bk_date_field`/`bk_timerange_field` — ดู § Design System ด้านล่าง). OT warning (`bkComputeOT`/`bkUpdateWarning`) logic ไม่เปลี่ยน แต่ sync ผ่าน event `bb-datefield:change`/`bb-timerangefield:change` แทนอ่าน native input ตรง; date field disable วันที่ผ่านมาแล้วในตัว (component). purpose/pickup_location/destination เปลี่ยนจาก native input → `.bb-field-box` markup ตรง (icon `description`/`hail`/`directions_run`) — id เดิมคงไว้ (`bk_purpose`/`bk_pickup_location`/`bk_destination`) ต่อสาย `vehicle.js` เดิมได้ไม่ต้องแก้; ทุก required field เพิ่ม `autocomplete="off"` (กัน browser autofill dropdown ทับกล่อง); `.bb-field-box input:invalid`/`.was-validated` ring แดงย้ายไปกรอบนอกแทนไอคอนในตัว input (`components.css`). **"ต้องการสารถีไหม" toggle ถอดออกแล้ว (2026-07-30):** `need_driver` เป็น hidden input ค่าคงที่ `on` เสมอ (view ไม่ต้องแก้ อ่าน `request.form.get('need_driver')=='on'` เหมือนเดิม) — **ผลข้างเคียง:** แก้ไข booking เก่าที่เคย `need_driver=False` ผ่าน modal นี้จะโดน flip เป็น `True` ทุกครั้งที่บันทึก. card-footer เอา `border-top` ออก. **⚠️ ค้าง (ยังไม่ทำ):** "หมายเหตุ" (`id="travelDate"`) ไม่มี `name` — กรอกแล้วไม่ถูกบันทึก (ไม่มี column ใน `VehicleBooking` รองรับ); global unscoped `.material-symbols-rounded{font-variation-settings:'wght' 300}` ใน `<style>` ของไฟล์นี้ยังกระทบไอคอนทั้งหน้า. JS ใน `vehicle.js` (ดู § Frontend JS `bk*`)

### `vehicle/vehicle-modal-edit.html`

 `bi-pencil-square` → Lucide `pencil`; `bi-check-circle-fill` → Lucide `check`; footer 2 ปุ่ม → `vc-btn vc-btn-secondary/vc-btn-primary` + `title=""`. **Mirror book layout (2026-05-23):** restructured ให้เหมือน `vehicle-modal-book.html` ทั้งหมด — header เปลี่ยนจาก pencil-icon + "แก้ไขการจอง" title → avatar circle (`bk-detail-member-avatar` + `--vc-fg` bg) + `{{ current_user.full_name }}` + dept/email sub-text; datetime fields แตกจาก 2 `editStartDatetime`/`editEndDatetime` (flatpickr datetime picker) → **5 fields** = hidden `editStartDatetime` + `editEndDatetime` (name=`start_datetime`/`end_datetime`, JS compose ตอน submit) + visible `edit_date` (`type="date"` → flatpickr Thai BE format) + `edit_start_time`/`edit_end_time` (`type="time"`) + `edit_duration_preview` (live "X ชม. Y นาที" ใต้ช่วงเวลา); ทุก field ห่อ `.bk-field-wrap` + `.bk-valid-icon` (check-circle-2 green เมื่อ `:is-valid`); เพิ่ม `editPickup` (required, ใหม่ — เดิมไม่ required, no wrap); ลบ `bk-modal-dialog` + `bk-modal-content` (book ไม่มี); เพิ่ม `.bk-info-note` "หมายเหตุ: แก้ไขได้เฉพาะรายการที่ยังรออนุมัติ — การแก้ไขจะถูกบันทึกและแจ้ง Admin"; footer ปุ่ม "บันทึก" คงเดิม

### `vehicle/vehicle-modal-detail.html`

**Phase 2 (2026-05-17):** 3 info icons (clock/truck/person) `bi-*` → Lucide; ลบ `card border-0 rounded-2` (เหลือ `bk-modal-content`); ลบ inline style 9 จุด → 8 classes ใหม่ใน vehicle.css (`bk-detail-*`). **Phase 3 (2026-05-17):** initial `ds-status-dot ds-status-dot--approved` → `vc-status-dot vc-status-dot--approved`; FA `fa-regular fa-circle-check` → Lucide `data-lucide="circle-check"`. Header icon ถูก JS swap ผ่าน `outerHTML` ใน `openEventDetail()` (vehicle.js) + `bindModalReinit()` แปลง Lucide เป็น svg ตอน `shown.bs.modal`. **Detail modal redesign (2026-05-20):** ทิ้ง 44px header circle (ids `detailHeaderDot`/`detailHeaderIcon`) → inline `.bk-detail-status` pill ใต้ title (ids `detailStatusPill`/`-Icon`/`-Text`); modal width 480px; dashed → solid full-bleed divider; section label "ผู้จอง" → `.bk-detail-section-header` "ผู้โดยสาร · N คน" + `#detailMemberCount`; member rows ใน JS ใช้ `.bk-detail-member-avatar` แทน `vc-status-dot--*`; footer button order reverse + `card-footer` collapse เมื่อว่าง. **Premium redesign (2026-05-23):** date-first hierarchy — ทิ้ง `.bk-detail-title` → eyebrow "การจองรถ" + hero date (1.0625rem/600/`letter-spacing:-0.01em`); status pill border-radius xs→full + matching `--vc-{green\|amber\|blue\|red}-border` + stroke-width 1.75→2; info rows reorder time/plate/driver → **plate (primary) → driver → time** (plate .9375rem/600 with monospace `.bk-detail-plate-code` + muted `.bk-detail-plate-brand`); `font-variant-numeric: tabular-nums` ทุก row; section header `justify-content:space-between` + `.bk-detail-section-count` (label ซ้าย, count ขวา) + drop UPPERCASE; member rows → **`.bk-detail-member` card tiles** (border + radius-sm + 8px gap + hover) + avatar ring 36×36 + sub-classes `bk-detail-member-body/-head/-name/-trip/-pickup`; close button absolute top-right; footer wrap → `.bk-detail-footer` (border-top + `--vc-bg-subtle`) + **CSS `:has(#detailActions:empty){display:none}`** แทน JS toggle. 4 keyframes (`bk-content-rise/-pill-in/-row-in/-member-in`) + member stagger via `style="--bk-i:${idx}"` + `cubic-bezier(.16,1,.3,1)` easing + `prefers-reduced-motion` guard. **Scoped under `.bk-detail-content`** กัน leak ไป `room/room-modal-detail.html` ที่ใช้ `bk-detail-*` ซ้อนกัน. JS: `_plateLabel()` คืน HTML (escape ด้วย `_esc()`) → `detailPlate.innerHTML`; footer collapse JS L760-761 ลบ. **2026-06-21:** เพิ่ม `#detailEditSection` (d-none, admin-only edit form — 6 fields ใน 2-col grid: `#editStart`/`#editEnd`/`#editDest`/`#editPurpose`/`#editPax`/`#editPickup`; ปุ่ม "บันทึก" → `saveAdminEdit()` + "ยกเลิก" → `cancelAdminEdit()`; วางก่อน `.bk-detail-footer`)

### `vehicle/admin/vehicle_admin.html`

**Title/container align กับ mileage (2026-07-05):** container เพิ่ม `px-1 px-md-5`; title block `d-none d-xl-block px-1 pt-4`→`pt-5`, `<h1 style="...color:var(--bb-str)">อนุมัติรถ</h1>` (hardcode text) → `<h2 class="pt-3" style="color:var(--bb-accent)">{{ page_title }}</h2>` — ตรง pattern เดียวกับ `vehicle_mileage.html` (ดูแถวนั้น). ยังไม่แตะ tabs/card radius (parked — รอ decision). **WeekStrip → Case 15 gallery redesign (2026-07-05):** `#adminWeekStripWrap` (`{{ component(weekstrip) }}`) เปลี่ยนหน้าตาต่อวันตาม [bootstrap-cases-gallery.html § Case 15](../../app/static/core/bootstrap-cases-gallery.html) — count badge มุมขวาบน (`.bb-ws-count`) → indicator bar ด้านล่าง (`.bb-ws-ind` + `.bb-ws-ind-dot`(1)/`.bb-ws-ind-bar.is-wr`(2-5)/`.is-dg`(6+), 0=ไม่แสดง); เลขวัน active เปลี่ยนจากกล่องทึบทั้งเซลล์ (`.bb-ws-day.is-on{background}`) → วงกลม `.bb-ws-dnum-active` ครอบเฉพาะเลข (ผ่าน wrapper `.bb-ws-dnum-wrap` height 2rem จัดกึ่งกลางแนวตั้งทุกวันให้ตรงกัน); เซลล์ไม่มี border/bg อีกต่อไป (flat) — `is-today` (ไม่ active) เปลี่ยนจาก border-color เป็น box-shadow ring รอบเลข. แก้ที่ [bb-components.js](../../app/static/core/js/bb-components.js) `initWeekStrip()`/`render()` (per-day markup, JS-generated ไม่ใช่ template) + [components.css](../../app/static/core/css/components.css) `.bb-ws-*` — ไม่แตะ template/macro (`_components/bb/weekstrip.html`, `app/components/weekstrip.py` signature เดิม) เพราะ prev/next/click/data-attribute contract ไม่เปลี่ยน. ~~Header (title+ปุ่ม "วันนี้") ไม่แตะ~~ (แก้แล้วรอบถัดไป — ดูข้อถัดไป). blast radius แคบ (WeekStrip component ใช้แค่หน้านี้หน้าเดียว, ยืนยันด้วย grep). **Weekday color + DatePicker แทนปุ่ม "วันนี้" (2026-07-05):** (1) วันอาทิตย์/เสาร์ใน week-strip สีแดง/ฟ้า — `bb-components.js render()` เพิ่ม class `.is-sun`/`.is-sat` ต่อวัน (loop index 0/6 ตรงกับ Sun/Sat เสมอ) + CSS `.bb-ws-day.is-sun{color:var(--bb-dg)}`/`.is-sat{color:var(--bb-info)}`; (2) แก้ `.bb-ws-dnum-wrap` เพิ่ม `width:2rem;flex-shrink:0` (เดิมมีแค่ height ทำให้วงแหวน `is-today` เพี้ยนเป็นวงรี ไม่ใช่วงกลม); (3) เอาปุ่ม `#weekTodayBtn` ออก แทนด้วย `{{ component(week_datepicker) }}` — `DatePicker(name='week_jump_date', value=now.date().isoformat(), id='weekJumpDp')` สร้างใน `vehicle_admin.py` (import จาก `components`) ส่งเข้า template, default = วันนี้; `vehicle_admin.js bindDateControls()` เปลี่ยนจากฟัง click `#weekTodayBtn` → ฟัง event `bb-datepicker:change` บน `#weekJumpDp` → เรียก `jumpWeekStrip(e.detail.date)` เดิม (fallback วันนี้ถ้า date ว่างจากปุ่ม "ล้าง"). **DatePicker popover clip fix (2026-07-05):** ปฏิทิน `#weekJumpDp` เดิมโดน `.bb-card{overflow:hidden}` ของ `#adminWeekStripWrap` ตัด (class `.bb-datepicker` default `position:absolute`) — แก้ที่ `initDatePicker()` (bb-components.js) เพิ่ม `place()` คำนวณจาก `btn.getBoundingClientRect()` แล้วตั้ง inline `position:fixed`+top/left(หรือ right ถ้า align-right) ทับ class เดิมด้วย specificity (ไม่แก้ CSS class `.bb-datepicker` ตรงๆ เพราะ class เดียวกัน reuse โดย `vehicle_budget.html` custom datepicker 4 จุด ที่ผูก JS คนละชุด `data-datepick-pop` ไม่ผ่าน `initDatePicker` — แก้ class กลางจะกระทบหน้านั้นด้วย); reposition ตอนเปิดผ่าน `scroll`(capture)/`resize` listener, ถอดตอนปิด. **Remove selDateHeading heading (2026-07-05):** `#adminDateBar` เดิมมี `<h4#selDateHeading>` คู่กับ `{{ component(week_datepicker) }}` (class `justify-content-between`) — ลบ h4 ออก (เหลือ datepicker child เดียว) + class → `justify-content-end`; `#adminWeekStripWrap` เพิ่ม `rounded-1`. `renderWeekMeta()` (vehicle_admin.js) เลิก set `#selDateHeading.textContent` → set `aria-label` ของ `#weekJumpDp [data-bb-dp-btn]` แทน (ทับ static `aria-label="เลือกวันที่"` จาก macro `bb_datepicker`). หมายเหตุ: mention ของ `#selDateHeading` ใน entry Phase 3/12.1 ด้านบน = historical, node ถูกลบแล้ว ณ จุดนี้. **DatePicker align + apply-label (2026-07-05):** `week_datepicker` (vehicle_admin.py) เพิ่ม `align='right'` (popover กางไปทางซ้าย/ชิดขอบขวา ให้พอดีตำแหน่ง `#weekJumpDp`); macro `bb_datepicker` (`_components/bb/datepicker.html`) ปุ่ม `data-bb-dp-apply` เปลี่ยน label "ใช้" → "ยืนยัน" — shared macro แก้ตรงนี้กระทบทุกหน้าที่ใช้ `bb_datepicker` แต่ปัจจุบันใช้จริงแค่หน้านี้ (`vehicle_budget.html` มี custom datepicker คนละชุด ไม่ผ่าน macro นี้). **Re-add static header + shrink DatePicker trigger (2026-07-05):** `<head>` เพิ่ม page-scoped `<style>` block ตัวแรกของไฟล์นี้ — `#weekJumpDp .bb-dp-trigger` ย่อขนาดปุ่ม (min-width:0, bg `--bb-n50`, ไม่มี border, padding `.25rem .625rem`, font `.8125rem`/400, สี `--bb-mut`, icon `.875rem`) mimic mockup Case 15, scope เฉพาะ id ไม่แตะ `.bb-dp-trigger` กลาง. `#adminDateBar` class `justify-content-end`→`justify-content-between` + เพิ่ม `<h4 class="fw-bold mb-0">รายละเอียด</h4>` เป็น child แรกก่อน `{{ component(week_datepicker) }}` — หมายเหตุ: อัปเดต entry "Remove selDateHeading heading" ด้านบน คนละ heading/mechanism (static text ธรรมดา ไม่ใช่ `#selDateHeading` แบบ JS-driven เดิม). **Consolidate vehicleList+tripList → Case 10 chrome (2026-07):** ลบการ์ด "รายละเอียดการเดินทาง" ทั้งก้อน (`#sectionAfter`/`#tripList`/`#afterCount`) ออกจาก col-8 ซ้าย — เหลือแค่ weekstrip + booking list; mileage/cost/OT/payment (เดิมอยู่การ์ดนี้) ย้ายไปรวมกับการ์ด "การใช้รถ" (col-4 ขวา) แทน ดู `renderVehicleRow`/`renderVehicleJobBlock` ใน [INDEX_code.md § Frontend JS](INDEX_code.md). การ์ด col-4 เปลี่ยน container จาก `.bb-card`+`.bb-card-head` เป็นโครง **Case 10** ([bootstrap-cases-gallery.html § Case 10](../../app/static/core/bootstrap-cases-gallery.html)) แทน: `<div class="card border-0 shadow-sm rounded-4"><div class="card-body p-4"><h3 class="h6 fw-bold mb-3">การใช้รถ</h3><hr class="mt-0 mb-3"><div class="bb-buy-list" id="vehicleList"></div></div></div>` — list เดิม (`.bb-avatar`/`.bb-card-head`) เลิกใช้แล้ว, ดู § Design System `components.css` entry สำหรับ `.bb-buy-thumb`/`.bb-buy-item` ที่ promote ตามมา. **ผลข้างเคียง:** ปุ่ม "ส่งซ่อม"/"เสร็จซ่อม" (เดิมอยู่แถวรถใน vehicleList) ถูกตัดออกตาม design ใหม่ (ตัดสินใจโดยผู้ใช้ ไม่ใช่ bug) — `openRepairModal()`/`fixDone()` (vehicle_admin.js) กับ modal `#repairModal` ไม่มีปุ่มเรียกใช้จากที่ไหนในแอปแล้ว (ยืนยันด้วย grep) รอ decision ว่าจะย้ายจุดเรียกไปหน้าอื่นไหม. **Preview table + card list ใหม่ แทน booking card เดิม (2026-07-27→28):** col-8 ซ้ายเปลี่ยนจาก `#bookingList` (การ์ดต่อ booking) เป็น **ตาราง `.bb-table#adminPreviewTable` (desktop `d-none d-md-block`, ไม่มี `<thead>` → page CSS `#adminPreviewTable{border-top:none}` กัน border บนลอย) + card list (`d-md-none`)** render จาก `bookings` จริงด้วย `pt*` family ใน [vehicle_admin.js](../../app/static/vehicle/js/vehicle_admin.js) (ดู [INDEX_code.md § Frontend JS](INDEX_code.md)). tab2 (`_shared/tab2.html`) ผูก `curFilter` จริงแล้วผ่าน `bindTab2Tabs()` (เดิม hardcode). action bar เหนือตาราง = ปุ่ม `ALL` (`.ue-chip`) + "รวมงาน"/"แจ้ง Telegram" ที่ **เริ่มเป็น `disabled`** แล้วเปิดตามจำนวน checkbox ที่เลือก (`ptUpdateActionButtons()`) — เลิกใช้โหมด toggle "เข้าโหมดรวมงาน/แจ้งเตือน" แบบเดิม; bar ห่อด้วย `overflow-x:auto` + ปุ่ม `flex-shrink-0` (scroll แนวนอนแทนบีบ เหมือน tab2). **`#assignModal` redesign ตาม `#mileageModal` (2026-07-28):** header = label เล็ก `#assignModalTitle` (เลขคำขอ `#99`) / ชื่อผู้จอง `#assignModalSub` / บรรทัด `purpose · pax · เวลา` `#assignModalTime` + `.bb-badge#assignModalBadge` / `.bb-avatar#assignModalAvatar` ขวา — **ไม่มีปุ่มปิด X** (ปิดผ่านปุ่ม "ยกเลิก"/backdrop เหมือน mileageModal) และ avatar เปลี่ยนสีตามสถานะโดยใส่ tone class ชุดเดียวกับ badge (`is-wr/is-info/is-ok/is-dg` → ดู `components.css` entry `.bb-avatar.is-*` ด้านล่าง). timeline เพิ่ม item แรก "รายละเอียดคำขอ" = route บรรทัดเดียว (`#assignModalRoute`, `pickup → dest`); select รถ/คนขับใช้ `.bb-input` (**ไม่ใช่ `.bb-select`** — ตัวนั้นเป็น trigger button `display:inline-flex` ใส่กับ `<select>` จริงแล้ว native rendering พังบน WebKit) และ option รถเรียง **ทะเบียนก่อนชื่อรถ**. **ค่าใช้จ่าย = การ์ด radio จริง** (page-scoped CSS `.bb-exp-card`/`.bb-exp-radio`/`.bb-exp-icon`/`.bb-exp-title` ใน `<head>`): `<label>` ห่อ `<input type="radio" name="modalExpType">` → **selected state ขับด้วย `:has(.bb-exp-radio:checked)` ล้วน ไม่มี JS toggle class** (radio group เดียวกันจัดการ mutual exclusion ให้เอง); สี selected = **ink ดำ** (`--bb-ink`) ตาม guideline v2.1 ไม่ใช่ฟ้า; radio วาดเองด้วย `appearance:none` + `radial-gradient(circle, ink 0 35%, #fff 35% 100%)`; icon Material Symbols สีเดียว `font-size:2.5rem` **ไม่มี subtitle**. ⚠️ **bug ที่เสียเวลาหาสองรอบ** — `#modalApproverInfo` เคยมีทั้ง `class="d-flex ..."` และ inline `style="display:none"` ที่ JS toggle: Bootstrap `.d-flex{display:flex!important}` ชนะ inline style ที่ไม่มี `!important` เสมอ → label "ผู้อนุมัติ:" โผล่ทุกตัวเลือกแม้ logic JS ถูกแล้ว. แก้โดย **ถอด `d-flex` ออกจาก class** ให้ inline style คุมคนเดียว (`display:flex` ตอนโชว์ตั้งจาก JS). **Icon → Material Symbols ตรงๆ (2026-07-28):** ทั้งไฟล์ (+ `vehicle_admin.js`) เลิกใช้ `<i data-lucide>` แล้ว 100% (0 จุดเหลือ ยืนยันด้วย grep) เขียน `<span class="material-symbols-outlined">glyph</span>` ตรง (36 จุดรวม 2 ไฟล์ — ส่วนต่างจาก `data-lucide` เดิมที่มีมากกว่านั้นหายไปพร้อม markup เก่าที่ถูกลบทิ้งตอน refactor เป็น `pt*` family ด้านบน ไม่ใช่ทุกจุดถูก "แปลง") — ดู row "MS icon transform" ด้านล่าง

### `vehicle/admin/vehicle_fleet.html` (เดิม `admin_manage_fleet.html`)

**Phase 6.2 redesign (2026-05-17):** Vercel `vc-scope` shell, layout `mf-grid` 2fr/1fr (col-8 Vehicles · col-4 Drivers+Approvers stacked), vc-card/vc-table/vc-badge/vc-form-*, Lucide icons; row icons → `.mf-icon-btn`; modals → `vc-modal`; row stagger via `.mf-stagger`; respects `prefers-reduced-motion`. **Driver profile redesign (2026-06-08):** ฝั่งคนขับ (col-4) เป็น avatar list (`.mf-avatar` รูป/ตัวอักษรย่อ + `.mf-driver-side` badge active/inactive + จำนวนงาน); data-* ทั้งหมดอยู่บน `.mf-driver-row` (JS อ่านผ่าน `.closest()`); ปุ่มต่อ row = view(eye)/edit/delete. modal ใหม่ `#driverDetailModal` (read-only ดู profile + รูปบัตร + ปุ่มแก้ไข→เปิด `#editDriverModal`); add/edit driver modal `modal-lg` + `enctype=multipart/form-data` + ฟิลด์ national_id/addr_*/avatar/idcard. Controller `vehicle_admin.py`: `_save_driver_image()` + add_driver/edit_driver รับ field ใหม่. **Shell+token migrate (2026-07-29):** `{% extends '_base_ue.html' %}` แทน standalone `vc-scope` shell + `vehicle_fleet.css` token เต็มไฟล์เป็น `--bb-*` (ดู [log](log/2026-07-29_vehicle-fleet-shell-token.md)) — ยังพึ่ง `design-system.css`/`vehicle_admin.css`/`vehicle_fuel.css` ชั่วคราว. **Component phase ต่อเนื่อง (2026-07-29):** title `.page-title` override ใน `{% block head %}` กัน `design-system.css` `h1,h2,h3{!important}` ทับ · `.mf-grid` 2 คอลัมน์เดิมแทนที่ด้วย tab `รถ`/`คนขับ` (`tab2_tabs` pattern เดียวกับ `vehicle_admin.html`, `#fleetTabWrap` → toggle `#fleetPanelVehicles`/`#fleetPanelDrivers` ผ่าน `bindFleetTabs()`) — ผู้อนุมัติรวมอยู่ใน tab คนขับ ต่อจาก driver list · ตารางรถจริงเปลี่ยนคอลัมน์เป็น รูปรถ(icon placeholder)/ข้อมูลรถ(ทะเบียน+ยี่ห้อ-รุ่นรวมคอลัมน์)/ที่นั่ง/ไมล์ล่าสุด/สถานะ/แก้ไข (ตัด action ประวัติ+ลบออกจากแถว — modal เดิมยังอยู่แต่ unreachable รอตัดสินใจที่อยู่ใหม่) · ตารางรถ/คนขับ promoted จาก mockup → ใช้จริงทั้งคู่ ผูก `vehicles`/`drivers` จริงจาก `_load_fleet_data()` (2026-07-30) — ลบตาราง `vc-table`/`vc-list` เดิมทิ้ง เหลือ `.bb-table` เดียว/tab; driver action คืนครบ 3 ปุ่ม (view/edit/delete, ผูก class `.fleet-driver-row` บน `<tr>` แทน `.mf-driver-row` เดิมที่เป็น `display:flex` กันพัง table layout); "งานในสัปดาห์" ยังไม่มี data source จริง (`driver_jobs` มีแค่ count/เดือน) โชว์ `—` รอ scope แยก. **`#addVehicleModal` redesign เสร็จ ผูกใช้จริง (2026-07-31):** โครง `.card.modal-content.border-0` + `modal-dialog-centered` (ไม่มี `-lg`) ตาม pattern `#bookingModal` (`vehicle/modals/vehicle_book.html`) ตรงๆ — ไม่มีปุ่มปิด X, `card-footer bg-white border-top-0`; field ผูกชื่อจริง (`license_plate`/`brand`[รวม ยี่ห้อ+รุ่น, `model` ส่ง hidden `''` เพราะ DB บังคับ NOT NULL]/`capacity`[stepper sync hidden input กัน `int(None)` พัง]/`fuel_rate`/`status`/`vehicle_type` ใหม่); `vehicle_type` เพิ่มเป็น column จริงใน `Vehicle` model (`db.String(20)` nullable — migration `2026-07-31_vehicle-add-vehicle-type.sql`); scoped CSS override `#addVehicleModal .card.modal-content{border/-radius/box-shadow}` กัน `design-system.css`'s global `.card{!important}` (บรรทัด 380) บังลุค `.bb-modal` ที่ตั้งใจไว้ (`#bookingModal` ไม่โดนเพราะหน้านั้นไม่โหลด `design-system.css`); widget preview เดิมลบทิ้งแล้ว (เป็น modal จริงแทนที่ ไม่ใช่ preview คู่ขนานอีกต่อไป). **`#editVehicleModal` merge เข้า `#addVehicleModal` (2026-07-31 ต่อเนื่อง):** modal เดียวสลับ add/edit ผ่าน `show.bs.modal` + `e.relatedTarget.dataset.id` (มี = edit, ไม่มี = add) — header/eyebrow/subtitle/ปุ่ม submit title สลับข้อความ, ฟิลด์ทุกตัว pre-fill จาก `data-*` เดิมที่ปุ่มแก้ไขมีอยู่แล้ว (เพิ่ม `data-vehicle-type` ที่ขาดไป), ที่นั่งใช้ `window.avSetCapacity()` ใหม่ (sync stepper+hidden input+text), chip ประเภทรถ/สถานะใช้ `setChecked()`+dispatch `change` event เอง (ตั้ง `.checked` เฉยๆ ไม่ทำให้ `ue-chip-dd` badge/label ใน `bb-components.js` sync — ต้อง native `change` event) `#avServiceSection` (นัดซ่อม/ต่อภาษี — 3 ฟิลด์เดิมจาก editVehicleModal) โผล่เฉพาะ edit mode ผ่าน `.d-none` toggle. `_fleet_edit_vehicle()` เพิ่มอ่าน `vehicle_type` ให้ตรงกับฟิลด์ที่ merge เข้ามา (เดิม add เท่านั้นที่อ่าน). `#editVehicleModal` เดิมลบทิ้งทั้งก้อน.

### `vehicle/admin/vehicle_cost.html`

**Phase 5 (2026-06-12):** เพิ่ม `#otPivotCard` — OT pivot driver × month สำหรับปี `from_year`: ตาราง driver × 12 เดือน (ยอด ฿ + ชม./cell), footer row รวม, grand total; ใช้ `.vc-pivot-row-summary` + `.vc-pivot-td-sticky`. Loads `vehicle_cost.css` + `pages/ot-admin.js`. **Phase 3.7 (2026-05-15):** ลบ inline `style="display:inline;"` ×3 → `.d-inline`; `style="display:inline-flex;gap:4px;"` → `.cost-action-group`; ลบ `style="width:20px;height:20px;"` จาก empty-state lucide icon. **Phase 4.8 (2026-05-16):** script → ES module `<script type="module" src="js/pages/ot-admin.js">`. **Phase 5.10 (2026-05-18):** ลบ `<link>` `fuel_admin.css` (ไม่ใช้แล้ว); rename `fuel-header/-title/-subtitle/-header-actions/-range-chip` → `cost-*` (CSS มีอยู่แล้ว); fix bug: JS template literal (`${opts}/${st}/${en}`) หลุดมาเป็น HTML ใน `#rateConfigModal` body → ลบทิ้ง; redesign `#rateConfigModal` body ใหม่ — static `vc-form-row` × N → `#rateConfigContainer` + `.cost-rate-row` × N (6-col grid label/day/start/end/rate/×) + `#addRateBtn` (เพิ่มแถวใหม่ส่ง `cfg_id[]=""`) + `.js-rate-remove` (existing → confirm + append `cfg_delete[]`; new → drop DOM); inputs `cfg_start[]`/`cfg_end[]` text → `<input type="time">`. **เพิ่ม per-weekday override:** column `cfg_day[]` (`<select>` ทุกวัน/จันทร์–อาทิตย์, value `""`=NULL หรือ `0..6`) + rate banner pill ใส่ `.cost-rate-pill--day` (amber) + day-label prefix (`TH_DAYS`) เมื่อ day-specific; backend `auto_generate_ot()` override logic: ถ้ามี row ที่ `day_of_week == booking.weekday()` → ignore weekday-agnostic rows. **2026-06-08 (cmdk menus):** filter bar 5 `<select.vc-filter-select>` (from_month/from_year/to_month/to_year/driver_id) ได้ `data-dropdown` → `.vc-dd`; โหลด `core/js/dropdown.js` (module) ต่อท้าย `vehicle_ot.js`; dd dispatch `change` → auto-submit เดิม (`vehicle_ot.js:282` ฟัง change ทุก select ใน filterForm) ทำงานต่อ. panel portal→body กัน overflow clip. (modal selects `.vc-select` ไม่แตะ) **2026-06-09 (manual OT):** ปุ่ม `#addOtBtn` (primary) ใน `cost-header-actions` → modal `#addOtModal` (layout เหมือน `#editOtModal`, action `ot_create`); redesign 2 modal — date `<input type="date">` → va-cal datepicker (`budget-datepick`/`data-datepick*`, JS port มาใน `vehicle_ot.js`) + slot rows ใส่ `cost-slot-head` (header เหมือน `rateConfigForm`, input flat). JSON blob เพิ่ม `createUrl` **2026-06-14 (filter งบ):** เพิ่มใน `#costFilterSheet` — `#filterBudgetType` (ทั้งหมด/ส่วนกลาง/ส่วนกอง/จ่ายเอง) + `#filterBudgetSub` cascade ใน `#filterBudgetSubWrap` (IDs เดียวกับ mileage); data injection `window.EXPENSE_CATS` + `window.COST_FILTER_SUB`; `filter_active` ย้ายไปคิดใน view (รวม budget); export link เพิ่ม `budget_type`/`budget_sub` args **2026-06-20 (OT attribution):** เพิ่ม section "OT แยกตามประเภทงาน" (ใต้ `#otPivotCard`) — `vc-card` + `table data-table` จาก `ot_by_expense` (col: ประเภทงาน/หมวด-กอง/ชม./ยอด/จำนวน เรียงยอดมากสุด) + alert-warning "OT งานส่วนตัวที่ยังไม่เรียกเก็บ" (`uncollected_count`/`uncollected_sum` + ลิงก์ filter `budget_type=personal&status=unpaid`); lucide `pie-chart`/`alert-triangle` **Zendenta redesign (2026-06-26):** page header → Bootstrap flex utilities (`.d-flex.justify-content-between`); `.cost-header/.cost-summary` markup ลบออก → ใช้ `.cost-kpi-strip` (inline KPI strip ยังไม่จ่าย/จ่ายแล้ว พร้อม `.vr` divider + `kpi-tile`/`kpi-num` scoped 26px); underline tab redesign (`.cost-chip` margin-bottom -1px ย้ายขอบมา `.cost-toolbar-row` border-bottom); `vc-badge-*` → `badge-pill b-*` (`b-full`/`b-unpaid`/`b-neutral`); `vc-card` → `zen-card`; `vc-btn-*` → `btn-zen`/`btn-zen-outline`; driver col เพิ่ม `.vc-avatar` (30px initials circle, accent-light bg); 4 modal headers ได้ `kpi-tile` 32×32px icon wrapper; `vc-table mb-0` → `table data-table mb-0`; slot tint → `--vc-amber-bg`/purple/indigo vars.

### `vehicle/admin/admin_fuel.html`

**Phase 4.6 (2026-05-15):** script → ES module `<script type="module" src="js/pages/fuel-admin.js">`. **2026-06-08 (cmdk menus):** filter bar 4 `<select.vc-filter-select>` (year/month/vehicle_id/driver_id) ได้ `data-dropdown` → `.vc-dd`; โหลด `core/js/dropdown.js` (module) ต่อท้าย `vehicle_fuel.js`; dd dispatch `change` → auto-submit เดิม (`vehicle_fuel.js:358` ฟัง change `.vc-filter-select` ใน filterForm) ทำงานต่อ. panel portal→body กัน overflow clip. **DNA redesign 2026-06-17:** head +Manrope; ลบ body `<h1 text-accent>` (หัวเรื่องย้ายขึ้น topbar ผ่าน `page_title`); รื้อ **KPI 2 card (group A 4-cell + B 3-cell) → 1 card 3-KPI strip** (`.fuel-kpi` icon tile + Manrope + ใช้/ทั้งหมด: งบทั้งปี / เงินสำรอง / รอเบิก) — ตัด self_paid + อนุมัติ breakdown; subtext "% ของงบ · คงเหลือ X"; คง `data-fuel-kpi="reserve-amount"` ให้ `vehicle_fuel.js`; **Table ไม่แตะ** (`.vc-table` flat อยู่แล้ว, ไม่ sortable); ไม่ใช่ canonical reference อีกต่อไป (DNA reference ย้ายไป `vehicle_budget.html`). **Toolbar iteration 2 (2026-06-17):** ลบ page-action bar บนสุด → รวมปุ่มใน `vc-card-head-actions` ของ billsCard (ตัวกรอง/รวมบิล/Excel/ตั้งค่าราคา/บิลใหม่); **filter bar → ปุ่ม "ตัวกรอง" popover** (`#filterToggle` + `form.fuel-filter-panel#filterForm` `position:fixed` กัน clip — JS `wireFilterPopover()`); KPI wrapper `vc-card` → `.fuel-kpi-strip` borderless (เส้นกั้น cell + เส้นใต้); `.vc-card-head` nowrap + `.vc-card-head-actions` overflow-x scroll

### Design System v2.1 (Batch 0-3)

 `:root` ย้ายมารวมที่นี่ทั้งหมด (block token ใน `ue.css` ถูกลบ — เหลือแค่ shell/motion/icon-sizing) · ฐานสี ink `#000000` + เขียว 2 จุด (tint bg `#EAFBF2` · link/ghost `#0B7A3E`) · **component ที่ redesign แล้ว:** Button (+`is-danger-sec`/`is-block`) · Search · Tabs · Dropdown (+`.bb-menu-item.is-locked`/`.bb-menu-note`/`.bb-menu-foot`) · Table (ตารางเปลือย + `.bb-row-group`/`.bb-row-locked`/`.bb-cell-sub`/`.bb-cell-link`/`tr.is-sel`) · Status (badge + icon · **`.bb-dot` ลบแล้ว**) · Pagination (+`.bb-pag-size`) · Callout · **★ ใหม่: `.bb-drawer` · `.bb-section` · `.bb-desc`** (macro `_components/bb/drawer.html` · object `components/drawer.py` = `Drawer`/`Section`/`DescList`) · mockup เทียบก่อน/หลัง: `static/core/mockup-bb-v2*.html` 4 ไฟล์. ที่ยังไม่แตะ + drift ที่เหลือ → [guideline §14](design_guideline.md)

### `design-system.css` (component entry)

**Phase 3 vehicle user-facing (2026-05-17)**: §7 STATUS DOT — rename `.ds-status-dot` + 5 variants (pending/approver/approved/rejected/group) → `.vc-status-dot*`; HEX literals 10 ค่า → `var(--vc-{amber|blue|green|red|purple}-bg)` + foreground. Markup pattern เปลี่ยนเป็น `<i data-lucide="..." class="vc-icon-sm">` แทน Font Awesome. **Modal × scrollbar-gutter fix** ย้ายไป `tokens.css` แล้ว (2026-07-21 consolidate — เดิมอยู่ที่นี่ตั้งแต่ 2026-06-08, รายละเอียด → § Token source ด้านบน). **Button blue (2026-06-13):** `.ds-btn-primary` fill `--vc-fg` (navy)→`--vc-blue` + hover `--vc-blue-hover` (คู่กับ `.vc-btn-primary` ใน `vehicle_fuel.css` ที่เปลี่ยน `--vc-primary`→`--vc-blue` เช่นกัน) — ปุ่ม primary ทุกหน้าเป็นน้ำเงินตามคำขอ user. **Heading recolor (2026-06-13):** §2 TYPOGRAPHY SCALE — เพิ่ม base rule `h1,h2,h3 { font-family: var(--vc-font-heading); color: var(--vc-blue) } !important` คลุมทุกหัวข้อ (title class รายหน้าทับ color เอง → ต้อง !important); `.ds-h1/.ds-h2/.ds-h3` ก็ชี้ token เดียวกัน (ไม่ถูกใช้ใน template ตอนนี้) ตามคำขอ user.

### Pivot table

**Extracted Phase 7 (2026-05-22)** จาก `fuel_admin.css` §22 → shared component สำหรับ admin_fuel (รถ × เดือน) + budget_manage (แผนก × เดือน fiscal year Mar→Feb). **Phase 2 redesign (2026-05-22):** เพิ่ม `.vc-pivot-section` + `.vc-pivot-section-head` — รองรับ 1 `<table>` ที่มีหลาย `<tbody>` ภายใน (เช่น budget_manage รวม central/dept/personal ใน 1 ตาราง); section head row = icon + label + optional count, span ทุก columns ด้วย `colspan`; bg = `--vc-bg-subtle`, font uppercase tracking-wide. **Phase 5 (2026-06-12):** `.vc-pivot-row-summary .vc-pivot-td-sticky` (bold aggregate row) + `.vc-pivot-td-sub` (indented fg-muted sub-row, font .8125rem — per-user personal pivot).

### Dropdown + Autocomplete

**2026-06-08** — cmdk-style menus, progressive enhancement. (1) **dropdown** (`.vc-dd(-trigger/-value/-chevron/-panel/-option[.is-active][aria-selected]/-divider)`) enhances `<select data-dropdown>`: keeps native `<select>` as source of truth (writes `.value` + dispatches `change` → existing select-bound JS untouched), `<optgroup>` boundary → divider line (labels ignored, no text headers). MutationObserver(childList,subtree) บน native select → rebuild panel อัตโนมัติเมื่อ option ถูก swap ภายนอก (รองรับ chained dependent dropdown เช่น budget_type→budget_sub). (2) **autocompleteinput** (`.vc-ac(-panel/-option[.is-active]/-divider/-empty)`) enhances `<input data-autocomplete list="…">`: reads `<datalist>` options, filters on type, fills input on pick, free text still allowed. (3) **autocompleteselect** (`.vc-ac` markup เดียวกับ (2)) enhances `<select data-autocomplete>`: searchable เหมือน (2) **แต่ native `<select>` เป็น source of truth** — pick → write `select.value` + dispatch `change` → submit `<option>` value (id) ไม่ใช่ typed label; **strict combobox** (free text ไม่ได้ — blur/Esc revert label กลับค่าที่เลือก); input clone class ของ select (chevron/sizing เดิม), select ซ่อนด้วย `.vc-dd-native`; preset value แบบ programmatic ต้อง `dispatchEvent('change')` เพื่อ sync label (ดู `vehicle_budget.js` setBudgetModal). ใช้กับ `#sbApprover` (vehicle_budget.html). **2026-06-09:** panel z-index 1000→**1060** (เหนือ Bootstrap modal stack 1050/1055) → portalled panel ใน modal ไม่ถูกบัง. Keyboard ↑/↓ + Enter select + Esc/Tab close, no icons in rows. Subtle shadow (DESIGN-OVERRIDE — floating menus, follows budget `.vc-dropdown-menu` precedent). `window.VCMenus.init(scope)` to enhance dynamically-injected controls. First used in `vehicle_mileage.html` (`#datePreset` + `booker_q` — `booker_q` ถอดออกแล้ว 2026-07-22 · backend ลบตาม 2026-07-27 เหลือ `#datePreset` ที่ใช้จริง). **2026-06-08 (portal fix):** panel ของทั้ง 2 component **portal ไป `<body>` + `position:fixed`** (helper `makeFloating`) ตอนเปิด → escape `overflow:hidden`/scroll/transform ancestor ทุกตัว (card/filter-bar/table-wrap) ไม่ถูก clip อีก; auto flip ขึ้นบนถ้าด้านล่างไม่พอ + reposition ตาม scroll/resize; outside-click เช็คทั้ง `root` + `panel` (panel อยู่นอก root แล้ว). แก้เคส dropdown ใน `.vc-card.budget-summary` ที่เคยโดน overflow clip. ใช้กับ filter เพิ่ม: `vehicle_cost.html` (from/to month+year+driver = 5), `admin_fuel.html` (year/month/vehicle/driver = 4) — โหลด `core/js/dropdown.js` (module) ต่อท้าย page JS, dd dispatch `change` → auto-submit เดิม (`vehicle_ot.js`/`vehicle_fuel.js`) ทำงานต่อ.

### UE foundation CSS

**Phase 1.5 (2026-07-11)** — foundation ของ UE redesign (promote จาก inline ของ mileage): **TOKENS** (green `--bb-accent:#06C167` + `--bb-n*/--bb-mut/--bb-str` — เปลี่ยนสีทั้งระบบที่นี่) · **SHELL** (`.ml2-frame/.ml2-body-row/.ml2-content/.ml2-content-inner` + `h1.page-title` 38/800) · **MOTION** (keyframes `ml2FrameIn/RowIn/DotPop/Bump/Shimmer` + `.ml2-rowin/.ml2-dotpop/.ml2-skel-*` + button micro + reduced-motion) · **ICON-SIZING** (`.bb-* .material-symbols-outlined` ตาม context). prefix `.ml2-*` = legacy (rename `.ue-*` ได้ทีหลัง).

### UE Chip (Uber-style filter chip)

**UE component (2026-07-11)** — namespace `.ue-chip*` (กัน collision `.bb-chip` เดิม). `ue_chip(label,icon,name,value,active)`=toggle · `ue_chip_dd(label,icon,id,align,clear_label)`=dropdown shell (`{% call %}` + `ue_chip_opt(label,name,value,type,checked)`). JS: `bb-components.js` `initUeChipToggle`/`initUeChipDd` (auto-init `[data-ue-chip-toggle]`/`[data-ue-chip-dd]`; panel **portal→body position:fixed** กัน overflow/transform clip; **live no-apply**; radio→label ที่เลือกเข้า chip (ยกเว้น default), checkbox→badge count; clear ขวา header; event `ue-chip:change`/`ue-chip-toggle:change`). active=เขียว `--bb-accent-dk` + MS icon fill (`FILL 1`). demo: `/dev/components#uechip`. **Base restyle (2026-07-22):** `.ue-chip` พื้นฐาน (ไม่ active) เปลี่ยนจาก `border:2px solid n300` + `bg:n0` → **flat** `bg:n50` + `border:transparent` + `color:n700` (เดิม `str`); hover `bg:n100` (เดิม border→mut); **ตัด behavior เปลี่ยนสีตอน selected/open ทิ้ง** (`.ue-chip.is-on`/`.ue-chip-dd.is-open/.is-active` เดิม border+color→`accent-dk` — ตอนนี้ chip ที่เลือกค่าแล้วยังคงสี n50/n700 เดิม ไม่ไฮไลต์ด้วยสี ใช้แค่ label เปลี่ยน/badge count แทน) — กระทบทุกหน้าที่ใช้ `ue_chip`/`ue_chip_dd` (ตอนนี้คือ `vehicle_mileage.html`). **on-state กลับมา (2026-07-28)** — เจ้าของโปรเจกต์เคาะ "ink outline แต่ใช้สีเขียวเข้ม": `.ue-chip.is-on` + `.ue-chip-dd.is-active > .ue-chip` = `bg:n0` (ขาว) + `border-color`/`color` = **`--bb-accent-dk #0B7A3E`** (5.43:1 — ห้ามใช้ `#06C167` 2.38:1 กับเส้น/ตัวอักษร ตาม guideline) + icon fill, hover on → `bg:--bb-accent-bg #EAFBF2`; off คงเดิม n50/n700. ไม่ทำ layout ขยับเพราะ off มี `border:2px transparent` อยู่แล้ว · icon เป็น font จึงรับสีจาก `currentColor` เอง. `.is-open` เฉยๆ (เปิด panel แต่ยังไม่เลือก) **ไม่**เปลี่ยนสี — กันสับสนกับ "เลือกแล้ว". **2 fix ใน `bb-components.js` วันเดียวกัน:** (1) **form owner** — panel portal ออก `document.body` ตอนเปิด ทำให้ input หลุดจาก `<form>` → `new FormData(form)` เก็บค่าไม่ได้ = filter ที่เลือกตอน panel เปิดอยู่**หายเงียบๆ** (เคสจริง: chip รถ/คนขับ หน้า mileage กดแล้วข้อมูลไม่ถูกกรอง) · แก้ด้วย `bindFormOwner()` ยัด attribute `form="<id>"` (HTML spec ถือเป็น form owner ไม่ขึ้นกับตำแหน่ง DOM) เรียกทั้งตอน init และตอน `open()` (เผื่อ option ที่ JS หน้าอื่น rebuild ทีหลัง); (2) **is-active หลอก** — ฝั่ง checkbox เดิมตัดสิน active จาก diff กับ snapshot ตอน init → พอ cascade rebuild option (เช่น `budget_sub`) snapshot เปลี่ยนทั้งที่ยังไม่ได้ติ๊ก chip ก็ติดเขียว · เปลี่ยนไปนับจำนวนที่ติ๊กจริง (`selectedCount() > 0`) ฝั่ง radio ยังใช้ diff เหมือนเดิม

### Combo + Daterange popover (shared behavior)

**2026-07-22:** ทั้งคู่ popover **portal ไป `document.body` (`position:fixed`)** ตอนเปิด (เดิม `position:absolute` ใน parent element ของแต่ละ component) — escape overflow/transform ancestor เหมือน pattern เดิมของ dropdown/autocomplete (`core/js/dropdown.js`) + `ue_chip_dd`. เพิ่ม **cross-widget popover mutual-exclusion** — เปิด popover ตัวใหม่ (combo หรือ daterange) ปิดตัวที่เปิดค้างอัตโนมัติ ผ่าน shared convention `window.__bbActivePopoverClose` ใน `bb-components.js`(`initCombo`)/`bb-daterange.js`. `bb_daterange` footer ปุ่ม "ยกเลิก"→"ปิด" (แก้ latent bug: เดิมล้าง draft date แต่ไม่ปิด popover จริง). **Behavior change กระทบทุกหน้าที่ใช้ 2 macro นี้** ไม่ใช่แค่ `vehicle_mileage.html` (เช่น `vehicle_budget.html` combo-like filter).

### MS icon transform (Lucide→Material Symbols)

**UE-redesign Phase 1 (2026-07-11)** — runtime แปลง `[data-lucide]`→`<span class="material-symbols-outlined">` ผ่าน MutationObserver (จับ dynamic: toast/combo/sort/notification); คง attr `data-lucide` ให้ JS เดิม (combo remove-check, `updateSortIcons`) query ได้; sizing คุมด้วย CSS ตาม context ในหน้า (bb-* เดิม size ผ่าน `svg`). โหลดเฉพาะหน้าที่ opt-in ผ่าน `header2.html` (คู่กับ stub `window.lucide` no-op — หน้านั้น **ไม่โหลด Lucide จริง**). **Contained** — ไม่แตะ shared macro/`bb-components.js` → หน้าอื่นคง Lucide. First use: `vehicle_mileage.html`. **เพิ่ม mapping (2026-07-22):** `'bus': 'directions_bus'` (ตามรอย `'car': 'directions_car'`) สำหรับไอคอนรถใน `#mmAvatar` ของ `vehicle_mileage.html`. **เพิ่ม mapping + fix attribute หาย (2026-07-28):** (1) `'landmark': 'account_balance'` — `vehicle_budget.html` ใช้ `data-lucide="landmark"` อยู่ 2 จุด แต่ `MAP` ไม่มี → fallback ได้ `landmark` ซึ่ง **ไม่ใช่ glyph จริงของ MS** เลย render เป็นตัวหนังสือดิบมาตลอด; (2) **bug จริง** — `toMS()` เดิม copy แค่ `class`/`style`/`data-lucide` ไป span ใหม่ ทำให้ **`id` หาย**: `<i id="detailStatusIcon" data-lucide="circle-check">` ใน `vehicle/modals/vehicle_detail.html` (partial ที่ `vehicle.html` + `vehicle_admin.html` include ทั้งคู่) พอถูกแปลงแล้ว `document.getElementById('detailStatusIcon')` คืน `null` → `.outerHTML = ...` ใน `vehicle.js`/`vehicle_admin.js` throw ตอนเปิด detail modal. แก้เป็น loop copy attribute **ทุกตัวยกเว้น `class`/`style`** (สองตัวนี้จัดการแยกอยู่แล้ว). **หมายเหตุ target ใหม่ (2026-07-28):** โค้ดใหม่ให้เขียน `<span class="material-symbols-outlined">` ตรงๆ ไม่ผ่าน `data-lucide` แล้ว (ดู [design_guideline.md §7](design_guideline.md)) — shim นี้ยังต้องอยู่เพราะ shared macro + หน้าเก่ายังใช้ `data-lucide`.

### Sidebar

**Moved Phase 4.1.5 (2026-05-15)** from `vehicle.css` so every page (incl. approver_inbox ที่ไม่โหลด vehicle.css) ได้ sidebar styles ฟรีผ่าน `design-system.css` @import. **Vercel refresh Phase 6.0 (2026-05-20)**: width 250→256, bg `#fff`→`var(--vc-bg-subtle)`, brand row 62→56 height, `.sb-logo-icon` 40→28 + radius 6, `.sb-section-label` UPPERCASE + letter-spacing .04em + `.68rem`, `.sb-item` height 32 + font `.82rem` + padding `0 8px`, `.sb-icon` 20→18 (+ explicit Lucide SVG `width/height 16px stroke 1.5`), logout hover `var(--vc-red-bg)`. `.sb-badge` rules left untouched per user instruction (keep dark pill with active inverted). **Collapsible group + visual refresh (2026-05-23):** เพิ่ม `.sb-group` + modifiers (`.has-active-child` `.is-open` `.has-pending`) + `.sb-group-toggle` (parent button reuse `.sb-item`) + `.sb-chevron` (14px, rotate 180° on open) + `.sb-group-body` (grid-row 0fr→1fr height anim, 280ms) + `.sb-group-inner` (overflow hidden) + `.sb-sub` + `.sb-sub::before` (1px vertical guide line at left:23px, scaleY(0→1) on `.is-open`, 320ms `--sb-ease`) + `.sb-item--sub` (height 32, no icon, padding-left 36, color `--vc-fg-subtle`, opacity+translateY stagger 30ms/item via `:nth-child(1..6)` transition-delay). Active sub = neutral `--vc-bg-hover` bg (no accent bar). Easing token `--sb-ease: cubic-bezier(.32,.72,0,1)`. **Visual refresh same date**: `.sb-section-label` ตัด UPPERCASE+letter-spacing, weight 500→400, font-size .68→.72rem, เพิ่ม `border-top 1px var(--vc-border)` คั่นระหว่าง section (first-child no border); `.sb-badge` dark indigo pill → soft gray pill (`--vc-bg-hover` bg + `--vc-fg-muted` text, weight 600→500); active badge bg `--vc-fg-inverted`→`rgba(0,0,0,.06)`. `@media (prefers-reduced-motion)` ตัด stagger + translateY. **Fix (same day)**: `.sb-group-toggle` `font: inherit` → `font-family: inherit` (เคย override `.sb-item` font-size .82rem ทำให้ "จัดการรถ" ตัวใหญ่กว่า sibling). **Blue recolor (2026-06-13 rev1):** bg `--vc-accent-light`→**`--vc-bg-subtle`** (#FAFAFA light gray); non-active `.sb-item`/`.sb-group-toggle` text→`--vc-fg` (#1C2E4A); `.sb-icon` color→`--vc-fg-muted`; hover bg keep `--vc-blue-bg` + text `--vc-blue`; active (`.sb-item.active` + `.sb-item--sub.active`) bg `--vc-blue-mid`→**`--vc-accent-light`** (#EEF2FF soft-indigo fill) + text `--vc-blue` + weight 600; `.sb-item--sub` non-active→`--vc-fg-muted`; active-child dot ring `--vc-accent-light`→`--vc-bg-subtle` (match new sidebar bg). **Zendenta restyle (2026-06-13 rev2):** `.sb-section-label` ลบ `border-top` divider (flat sections + whitespace) + กลับมา `text-transform:uppercase` + `letter-spacing:--vc-tracking-wide` + .72→.68rem; hover (`.sb-item`/`.sb-item--sub` + icon) blue tint → neutral `--vc-bg-hover`/`--vc-fg`; active text `--vc-blue`→`--vc-accent` (+ icon) + **new `::before` left-bar** 3px indigo (`left:-8px` ชนขอบ sidebar, share rule `.sb-item.active` + `.sb-item--sub.active`); `.sb-group.has-active-child .sb-group-toggle` + collapsed dot `::after` `--vc-blue`→`--vc-accent`. **Bullet dot + rail restyle (2026-06-15):** sub-item เป็น bullet-dot + left rail แบบ Gmail. `.sb-dot` 4px circle (`--vc-fg-muted` resting / `--vc-fg` hover / `--vc-accent` active). `.sb-item--sub` มี `margin-left:21px` (ดัน pill ไปขวาของ rail → bg ไม่คลุมเส้น) + own `border-radius` pill + height 35. แยก `.sb-item.active::before` (sidebar-edge bar, top-level only) จาก sub. **Rail = per-item `.sb-item--sub::before`** (left:-8px, width 2px, `top/bottom:-2px` ให้ segment ชนกันต่อเนื่อง; `:first-child` top:50% + `:last-child` bottom:50% → rail เริ่ม-จบที่ dot center): `--vc-border` resting / `--vc-fg-muted` hover / `--vc-accent` active. **Active = neutral gray pill** (`--vc-bg-hover` — ไม่ใช่ accent-light) + bold + accent rail/dot/text (single-color signal). `@media reduced-motion` selector `.sb-item--sub::before`. **🎨 Zendenta (2026-06-19):** `sidebar.html` rewrite เลิกใช้ `.sb-*` → class กลาง `.sidebar-*` ใน `main.css`; component นี้ (legacy `.sb-*`) **ไม่มี markup ใช้แล้ว → retire ได้** (ยังโหลดผ่าน design-system @import จนกว่าจะลบ)

### Font stack (Sarabun / Inter)

**ตัวเลข = Manrope** (`--vc-font-mono` = `'Manrope','Sarabun'`, 2026-06-17) ผ่าน `.vc-mono` + `tabular-nums` — โหลด Manrope ใน head เฉพาะหน้าที่ใช้ (vehicle_budget); หน้าอื่น fallback Sarabun. **Heading h1-h3 (2026-06-13):** `--vc-font-heading` = `'Sukhumvit Set', 'Sarabun', sans-serif` (Apple system font, fallback Sarabun บน Win/Android). ใช้กับ **element `h1,h2,h3`** ตรงๆ + color `--vc-blue` พร้อม `!important` (title class รายหน้า เช่น `vrc-*-title`/`fuel-title`/`dash-title` set color เอง specificity ชนะ element → ต้องบังคับ). `.ds-h1/.ds-h2/.ds-h3` (typography scale, ไม่ถูกใช้ใน template ปัจจุบัน) ก็ชี้ token เดียวกันไว้เพื่อ consistency. **Util (2026-06-14):** `.text-header` (Sukhumvit Set, weight 300/light) + `.text-header-bold` (weight 700) — opt-in class ใช้กับ element ไหนก็ได้ (เช่น `#currentMonthLabel`)

### `main.css`

 [core/css/main.css](../../app/static/core/css/main.css) rewrite จาก legacy login CSS → design layer ใหม่. โหลดผ่าน `_shared/header.html` หลัง `design-system.css` → **override** ("อันไหนทับใช้อันใหม่"). **sections ที่มีแล้ว:** app layout (`.main-content`) · sidebar (`.sidebar-*`) · content card (`.zen-card`) · KPI tile (`.kpi-tile` `.kpi-num`) · data table (`.data-table` — `td/th white-space:nowrap` → mobile overflow-x) · badge pill (`.badge-pill .b-full/.b-partial/.b-neutral/.b-accent`) · **topbar** (`.header` `.header-title` `.zen-iconbtn` `.header-flag` `.header-user-*`) · **tabs** (`.zen-tabs` `.zen-tab(.active)` `.zen-tab-count` — underline accent) · **search** (`.zen-search` input bg เทา + `.mlg-search-hl` highlight เหลือง) — tabs/search เพิ่มตอน polish mileage 2026-06-19. **sections เพิ่มเมื่อ migrate cost (2026-06-26):** `.btn-zen` (filled indigo CTA) · `.btn-zen-outline` (white bg hairline border secondary) · `.vc-avatar` (30px initials circle, `--vc-accent-light` bg + `--vc-accent` text, Manrope 700 12px). **กฎ:** Bootstrap-first · custom เฉพาะ สี/badge/radius/active/collapse · icon lucide · sidebar `#fafcfc`/content `#fff`/ไม่ fluid. playbook + cookbook + prompt → [design_guideline.md](design_guideline.md) (zendenta playbook ลบแล้ว · legacy `.zen-*` cookbook = ดู main.css โดยตรง). migrate ทีละหน้า (sidebar ✅ · mileage ✅ → header → dashboard → ...). **Mobile + sticky (2026-06-19):** `.zen-search { max-width:100% }` ใน `@media ≤767.98px` (override `max-width:320px` ที่ main.css โหลดหลัง page CSS); `.header { position:sticky; top:0; z-index:100 }` — sticky ทุกหน้าที่ใช้ `.header`.

### `components.css`

 [core/css/components.css](../../app/static/core/css/components.css) — prefix `.bb-*` + token `--bb-*` (accent น้ำเงิน `#4081EC`/hover `#1766E8` — migrate จาก blurple 2026-07-05 → เฉดสว่างขึ้น 2026-07 (ดู entry ท้ายบรรทัดนี้), ทดลองผ่าน `layout.html` ก่อน promote · cool neutral · Inter ตัวเลข · soft cool-shadow ตาม guideline). **Sidebar width (2026-07-05):** `.bb-sidebar`/`.bb-sidebar-main` 16rem→**17.25rem** (+20px) + `.bb-sidebar-link` padding/margin-bottom ขยาย + `.bb-sidebar-logo margin-top:40px`. **13+3 component:** button · input · search · filter (seg·chip·token·daterange·select·menu) · tabs (underline=status default) · dropdown · card · kpi (card/ghost, เลข Inter **600** — เปลี่ยนจาก 300 2026-07-05) · table (v2 canonical: checkbox·`.sortable` th·inline status) · badge · status · pagination · modal · timeline · empty · loading. spec → [design_guideline.md §12](design_guideline.md). **Flatten radius + mileage promote (2026-07-05):** ทดลองผ่าน `layout.html` prototype ก่อน promote เข้าที่นี่ (blast radius แคบ — component เหล่านี้ตอนนี้มีแค่ `vehicle_mileage.html` เรียกใช้จริง ยกเว้น `.bb-select` ที่ใช้กว้างกว่า) — `body` bg `var(--bb-n50)`→`#fff`; `.bb-kpi.is-card` border-radius `var(--bb-r-lg)`(12px)→**hardcode 4px**; `.bb-kpi-value` font-weight `300`→`600`; `.bb-tabs` border-bottom เดิมลบทิ้ง (ให้ page ที่ใช้ห่อ full-bleed wrapper เองแทน), `.bb-tab` font-size `.875rem`→`1rem` + padding ซ้ายขวา `.25rem`→`.5rem`, `.is-on` font-weight `600`→`700`; `.bb-select` border-radius `var(--bb-r-md)`(8px)→**hardcode 4px** (กระทบ dropdown/combo/datepicker/timepicker/timerange/daterange ทุกหน้ารวม `vehicle_budget.html`); `.bb-table thead th` color `var(--bb-mut)`→`var(--bb-accent-i)` + `:first-child`/`:last-child` border-radius `var(--bb-r-md)`→`0`. ⚠️ radius เหล่านี้ hardcode ไม่ใช้ `--bb-r-*` token — ยังไม่ sync กับ [design_guideline.md §12](design_guideline.md) token system, ถือเป็น exception เฉพาะจุดที่ต้องแก้ถ้าจะ generalize ทีหลัง. **ยังไม่ wire เข้า `header.html`** (namespaced `.bb-*` ปลอดภัย ไม่ชน legacy) — `<link>` เมื่อ adopt หน้าแรก. `.bb-*` = target ที่ `--vc-*`/`.zen-*`/`.data-table` จะ migrate เข้าหา · **macro:** [`_components/bb/table.html`](../../app/templates/_components/bb/table.html) → `bb_table(columns, wrap_cls)` = shell (.bb-table) thead จาก column spec (`label`/`sort`/`align`/`cls`/`raw`) + tbody ผ่าน `{% call %}` (ใช้แล้วที่ mileage 2026-06-28 · เคสตาราง row ซับซ้อน) · **`bb_table_v2(cfg)`** = config-driven เต็มตัว (เทียบ PHP getTableV2) รับ dict → thead+data(loop)+info+pagination+empty · column `{key,label,sort,align,fmt,render,cls}` (`fmt`=money/num/custom format string เช่น `'{:,.1f}'`/`'฿{:,.0f}'` · `render`=macro รับ row สำหรับ pill/avatar · `cls`=ลงทั้ง th+td) · helper `.bb-cell-muted` · **ใช้แล้วที่ cost** (ตาราง `ot_by_expense` 2026-06-28, config-driven adopter แรก) · เหมาะตารางข้อมูลธรรมดา (cost/fuel/users) — เพิ่ม macro `.bb-*` ตัวอื่นใน `_components/bb/` เมื่อต้องการ · **🐍 Python wrapper (2026-06-29):** `components.Table`/`Column` ([app/components/](../../app/components/)) = thin object ครอบ `bb_table_v2` — controller สร้าง `Table(data=…, columns=[Column(...)])` ส่งเข้า template → `{{ component(table) }}` (jinja global จาก `register_components` ใน app.py) → render ผ่าน `_components/render/_table.html` → macro เดิม. ปรัชญา Controller→Component→Jinja (HTML อยู่ Jinja เท่านั้น). custom cell (badge/status/ปุ่ม): **Cell Component (2026-06-29)** — `Column(cell=lambda row: Component)` → map เป็น cfg `render` callable, `bb_table_v2` เรียก `{{ col.render(row) }}` ได้ (Jinja เรียก Python callable ได้) · เคส row ซับซ้อนมาก (group row/`data-*` บน `<tr>`) ยังใช้ shell `bb_table`. adopter แรก = cost `ot_expense_table` · **`components.Badge`/`Status` (2026-06-29):** ครอบ macro `bb_badge`/`bb_status`/`bb_status_inline` — `Badge(text, variant='neutral'|'accent', icon=)` · `Status(text, tone='ok'|'wr'|'dg'|'info'|'neutral', inline=False, icon=)`. (Cell Component = Status ในตาราง = step ถัดไป) · **🖼️ Living Gallery `/dev/components`** ([templates/dev/components.html](../../app/templates/dev/components.html)) = render Python component จริงผ่าน `{{ component(obj) }}` (drift ไม่ได้) — โตทีละตัวจน absorb static `components-gallery.html` ครบแล้ว → **retired (2026-07-19)**, ไฟล์ static gallery ถูกลบออกจาก repo · **DateRange mobile fix + redesign (2026-07-02):** แก้ cascade bug `.bb-daterange{left:0}` (ไม่ scope media query) เคย override `position:fixed` centering บนมือถือ ทำให้ popover ไหลออกขอบจอ — ย้าย align rule เข้า `@media (min-width:768px)`; เพิ่ม `.bb-dr-cal-col`/`.bb-dr-cal-head`/`.bb-dr-cal-title`/`.bb-dr-mpick` (month-picker 4 คอลัมน์) + `.bb-dr-presets-wrap`/`.bb-dr-presets-trigger` (mobile dropdown ลอย); [bb-daterange.js](../../app/static/core/js/bb-daterange.js) เพิ่ม `clampToViewport()` กัน popover desktop ล้นขอบจอ (translateX) · **reload-reset fix (2026-07-02):** `enhance()` อ่าน `appliedStart`/`appliedEnd` จาก `startIn.getAttribute('value')`/`endIn.getAttribute('value')` แทน `.value` — browser restore ค่า hidden input ทับที่ **property** `.value` ตอน manual reload (F5) แต่ไม่แตะ **attribute** ที่ server render สดทุกครั้ง; ใช้ pattern เดียวกับ [bb-components.js](../../app/static/core/js/bb-components.js) `initSlider()` (อ่าน `input.getAttribute('value')` แทน `input.value`) — ทั้งคู่แก้บั๊ก filter ค้างค่าเดิมข้าม reload ที่หน้า `vehicle_mileage.html`. **Sidebar/search/btn/filter tweak + new `.bb-buy-*` component (2026-07):** `.bb-sidebar-link.sub` เพิ่ม `padding-top/-bottom:.375rem`; `.bb-search input` bg `var(--bb-n100)`→`#fff` + border-radius `var(--bb-r-md)`→`var(--bb-r-xs)` + เพิ่ม `border:1px solid var(--bb-n200)`; `.bb-btn`/`.bb-filter-btn` border-radius → hardcode `0`; `.bb-filter-btn` เพิ่ม `line-height:1` (แก้สูงไม่เท่า `.bb-btn` เพราะไม่มี line-height set มาก่อน). **`.bb-buy-thumb`/`.bb-buy-item` (ใหม่)** — promote จาก demo-only CSS ใน `bootstrap-cases-gallery.html` § Case 10 เข้ามาเป็น shared component เพราะเอาไปใช้จริงใน `vehicle_admin.html` "การใช้รถ" list (ดู `vehicle_admin.html` row ด้านล่าง) — thumb 3rem (`is-ok`=เขียว มีงานวันนี้, `is-wr`=ส้ม กำลังซ่อม, ไม่มี modifier=เทา ว่าง) + `.bb-buy-item + .bb-buy-item` dashed divider แทน manual border-bottom. **Accent hue lighten (2026-07):** `--bb-accent`/`--bb-accent-i` `#1E40AF`→`#4081EC`, `--bb-accent-h` `#1E3A8A`→`#1766E8` (คำนวณจากเฉดใหม่ ไม่ใช่ derive จากเดิม) + `--bb-ring` และ rgba glow hardcode 4 จุด (`.bb-btn.is-pri`/`.bb-search input:focus`/`.bb-token-x:hover`/`.bb-select.is-active`) แปลงจาก `rgba(30,64,175,*)`→`rgba(64,129,236,*)` (ค่า CSS var เก็บเป็น hex string, rgba แยกต้องแปลงเอง ไม่ auto). hardcode แยกอีกจุดที่ [_components/_modal.html](../../app/templates/_components/_modal.html) `info-soft` variant ก็แก้ตามแล้ว. ⚠️ **`accent` เฉดใหม่ contrast บนขาว = 3.8:1 ไม่ผ่าน AA text ปกติ** (ผ่านแค่ large-text/UI ≥3:1) — ใช้ `accent-hover` (`#1766E8`, 5.1:1 ✅) แทนถ้าเป็น body text/link เล็ก รายละเอียด → [design_guideline.md §2](design_guideline.md). **scrollbar-hide + modal-gap fix consolidate (2026-07-21):** ลบ local scrollbar-hide block (เคยซ้ำกับ tokens.css/design-system.css) → เพิ่ม `@import url('./tokens.css')` แทน (บรรทัดแรกหลัง header comment) — ได้ `--vc-*` tokens มาด้วยเฉยๆ (ไม่ใช้ ไม่ชน `--bb-*`) แต่จุดสำคัญคือได้ modal-open/backdrop fix ที่ขาดมาตลอด → แก้ gap ที่เห็นตอนเปิด modal บนหน้า bb-* (`vehicle_mileage.html` ผ่าน `_base_ue.html`, `vehicle_admin.html`, `vehicle_cost.html`, `vehicle_budget.html`). **`.bb-badge` fix (2026-07-28):** radius `--bb-r-xs` (4px) → **`--bb-r-pill`** + หน่วย `rem` → **px** (ผิด guideline §5 ทั้งคู่ — badge = คลิกได้จึงต้อง pill, และ radius 4 อยู่ในรายการห้าม) · เพิ่ม `line-height:1` + **`.bb-badge .material-symbols-outlined { font-size:14px }`** — ต้นเหตุจริงที่ badge สูงเกินและไอคอนล้น: `ms-icons.js` แปลง `<i data-lucide>` เป็น **font span** ทำให้กฎ `width/height` เดิมไม่มีผลเลย (`.bb-status` มี `font-size` อยู่แล้ว แต่ `.bb-badge` ไม่มี) · ลบ `.bb-ml-chiprow` (ตายหลังยุบ chip วันเป็น dropdown). **`.bb-avatar` tone modifier (2026-07-28):** เพิ่ม `.bb-avatar.is-wr/.is-info/.is-ok/.is-dg` (bg/color = `--bb-{wr,info,ok,dg}-{bg,tx}`) ต่อท้าย base `.bb-avatar` — ใช้ cls **ชุดเดียวกับ `.bb-badge`/`.bb-status`** เพื่อให้ avatar กับ badge ในหัว `#assignModal` เปลี่ยนสีพร้อมกันจาก `STATUS_BADGE[b.status].cls` ตัวเดียว. วางไว้ที่ `components.css` (ไม่ใช่ page CSS) เพราะทั้ง `.bb-avatar` และ tone class เป็นของ shared component library ทั้งคู่; ไม่ได้เพิ่ม token ใหม่ ใช้ของเดิมล้วน. **ไม่ใช่** `PT_TONE` (map คนละตัวใน `vehicle_admin.js` ที่ `pending` = เทา — จงใจไม่ reuse เพราะจะขัดกับ badge ที่ `pending` = ส้ม). **DateField/TimeRangeField + `.bb-field-box` (2026-07-29):** component ใหม่ 2 ตัวแยกจาก DatePicker/TimeRange เดิม (ไม่แทนที่ — ดู [CHEATSHEET.md](../../app/components/CHEATSHEET.md) § Date/Time สำหรับ interaction diff เต็ม). CSS: `.bb-field-box*` (§2b — กล่อง icon+input flex เดียว ใช้เป็น trigger ของ picker ได้ด้วย ต่างจาก `.bb-input-wrap` ที่ icon overlay ทับ input; รวม fix `input:invalid`/`.was-validated` ring แดงที่กรอบนอกแทน native icon ในตัว input + `min-width:0` กัน flex overflow เวลา 2 กล่อง start/end เบียดกันในพื้นที่แคบ), `.bb-df-*` (§2c, panel ปฏิทินใต้ trigger, disable วันที่ผ่านมาแล้ว), `.bb-trf-*` (§2d, dropdown list เวลาแบน step เดียว). JS: [bb-components.js](../../app/static/core/js/bb-components.js) เพิ่ม `initDateField`/`initTimeRangeField` (auto-init `[data-bb-datefield]`/`[data-bb-timerangefield]`, expose `__bbSetValue`/`__bbGetValue`/`__bbClear`/`__bbSetRange`/`__bbGetRange` ให้ page JS เรียก prefill ตอน edit — pattern เดียวกับ `initSidebar`'s `__bbOpenDrawer`). Python: `components.DateField`/`TimeRangeField` ([datefield.py](../../app/components/datefield.py)/[timerangefield.py](../../app/components/timerangefield.py)) → macro [`_components/bb/datefield.html`](../../app/templates/_components/bb/datefield.html)/[`timerangefield.html`](../../app/templates/_components/bb/timerangefield.html). First/only adopter: `vehicle/modals/vehicle_book.html` (`#bookingModal` — modal มาตรฐาน/reference สำหรับ modal ฟอร์มใหม่ในระบบ ดู § Templates ด้านบน).

### `vehicle.css`

**Phase 3.6 (2026-05-15):** ลบ `.badge-approved/.badge-pending/.badge-approver` (3 HEX blocks); `--ds-border`→`--vc-border` + `#111827`→`var(--vc-fg)` ใน bk-modal; เพิ่ม `.bk-info-note` (amber token). **Phase 4.1.5 (2026-05-15):** ลบ sidebar section + legacy `.menu-item`/`.brand-logo`/`.sidebar-menu`/`.menu-section` (158 lines, 618 → 460); ย้ายไป `components/sidebar.css`. **Calendar overflow trigger (2026-05-20):** `.event-more` rewrite → `.event-more.vc-cal-more` (inline-flex + chevron icon 12px, ghost ปุ่มสไตล์ Vercel: muted color resting → `--vc-fg` + `--vc-bg-hover` bg เมื่อ hover, radius `--vc-radius-xs`, ลบ `text-decoration: underline`); เก็บ class `.event-more` ไว้สำหรับ mobile-hide rule. Popover content styles อยู่ใน `vercel.css`. **Detail modal redesign (2026-05-20):** `.bk-modal-dialog` 440→**480px**; ลบ `.bk-detail-header-dot`; เพิ่ม `.bk-detail-status` + 6 modifiers (`--approved/--pending/--approver/--rejected/--group/--completed` → `--vc-{green|amber|blue|red}-bg` + matching fg); `.bk-detail-divider` dashed inset → **solid full-bleed 1px**; `.bk-detail-text` color muted → `--vc-fg`; `.bk-detail-row` align center → flex-start; เพิ่ม `.bk-detail-section-header` (flex container + count); `.bk-detail-section-label` letter-spacing .04em; เพิ่ม `.bk-detail-member-avatar` (32px neutral gray circle + 14px user icon — แทน `vc-status-dot--*` ใน member rows). **Detail modal premium redesign (2026-05-23):** new scoped block under `.bk-detail-content` (กัน leak ไป `room/room-modal-detail.html` ที่ใช้ `bk-detail-*` base classes ซ้อนกัน) — `.bk-detail-dialog` 480→**440px**; `.bk-detail-content` border-radius 10px + overflow-hidden + `bk-content-rise` anim; new `.bk-detail-head` (replaces `card-header` padding) + `.bk-detail-eyebrow` (.7rem muted) + `.bk-detail-date` upgrade เป็น hero (1.0625rem/600/`letter-spacing:-0.01em`) + `.bk-close` absolute top-right; pill: height 22→24px, padding 8→10px, border-radius xs→**full** + matching `--vc-{green\|amber\|blue\|red}-border` + stroke-width 1.75→2 + `bk-pill-in` anim; info rows: padding 16→18-20px gap 12→14, `font-variant-numeric: tabular-nums` ทุก `.bk-detail-text`, new `.bk-detail-row--primary` modifier (plate weight 600 size .9375rem, icon `--vc-fg`) + `.bk-detail-plate-code` (monospace + 0.02em tracking) + `.bk-detail-plate-brand` (muted), row stagger via `nth-child(1\|2\|3)` + `bk-row-in`; section header: `justify-content:space-between` + new `.bk-detail-section-count` (right) + drop UPPERCASE letter-spacing on label; new `.bk-detail-members` wrap (flex col, gap 8px) + `.bk-detail-member` card tiles (border + radius-sm + 12px padding + hover bg-subtle/border-hover + `bk-member-in` w/ `--bk-i` stagger) + `.bk-detail-member-avatar` ring upgrade (36×36 bg:#fff + border) + sub-classes `bk-detail-member-body/-head/-name/-trip/-pickup`; new `.bk-detail-footer` (border-top + `--vc-bg-subtle` action bar, padding 14×20, btn-sm height 32px) + **CSS `:has(#detailActions:empty){display:none}`** แทน JS toggle; 4 keyframes `bk-content-rise/-pill-in/-row-in/-member-in` + `cubic-bezier(.16,1,.3,1)` easing + `prefers-reduced-motion` guard. Base classes `.bk-detail-{title/date/status/divider/info/row/icon/text/section-header/section-label/member-avatar}` คงไว้สำหรับ `room-modal-detail.html` (ไม่ break). **Mobile Phase A+B (2026-05-23):** rewrite `@media (max-width: 767px)` calendar block — `#calendarView .vc-card` drop card chrome (border/bg/radius); `.calendar-grid` gap 0→2px + padding 0 4px; `.calendar-header-cell` 11px UPPER tiny + zero border; `.calendar-cell` `aspect-ratio:1` (square) + `border-radius:8px` + zero borders + press scale `:active{transform:scale(.94)}` + `transition:background var(--vc-dur-fast) var(--vc-ease-snap)`; `.calendar-cell.today` filled black (`background:--vc-fg; color:--vc-on-primary` — inverted text, premium); `.calendar-cell.selected` inset ring `box-shadow:inset 0 0 0 1.5px --vc-fg` (no layout shift); selected+today combo = white inner ring on black fill; `.mobile-indicator` single 4px dot for Phase A (Phase F = 1=dot/2-3=short bar/4+=long bar). เพิ่มท้ายไฟล์ `.vrc-m-hero/.vrc-m-hero-title/-sub/.vrc-m-month/.vrc-m-month-label/.vrc-m-month-controls/.vrc-m-today-btn/.vrc-m-daybar/.vrc-m-daybar-text/.vrc-m-daybar-label/.vrc-m-daybar-count` (mobile-only, desktop hides via `d-md-none`); daybar = `position:sticky; top:0; z-index:--vc-z-sticky` (pins under header เมื่อ list scroll); 2 keyframes `vrc-m-rise/vrc-m-fade` + `@media (prefers-reduced-motion: no-preference)` guard. **Mobile Phase C (2026-05-23):** event-card block ใหม่ — `.vrc-m-list` (flex col gap 8px) + `.vrc-m-evt` (border + radius-md + press scale `:active{transform:scale(.985)}` + hover border `--vc-fg-subtle`) + `.vrc-m-evt-head` (flex/gap 10/padding 12×14) + `.vrc-m-evt-dot` (8×8 circle, `margin-top:7px` optical-align กับ first text line) × 6 variants `--{approved\|pending\|approver\|rejected\|group\|completed}` (map ไป `--vc-{green\|amber\|blue\|red\|accent\|fg-subtle}`) + `.vrc-m-evt-body/-title/-meta` (14px/500/tabular-nums title, 12.5px muted meta with `.sep` color `--vc-fg-disabled`) + `.vrc-m-evt-status` (22px pill, `border-radius:full`, bg + matching `--vc-*-border` like `bk-detail-status` premium pattern) × 6 variants + `.vrc-m-evt-toggle` (28px chevron button, rotate 180° เมื่อ `:not(.collapsed)` transition `--vc-dur-base --vc-ease-out`) + `.vrc-m-evt-sub` (border-top + `--vc-bg-subtle` indented container) + `.vrc-m-evt-sub-row` (button + bg-white nested card, hover bg-hover) + `.vrc-m-evt-sub-time/-body/-title/-meta` + `.vrc-m-empty` (56px circle icon + 2-line title/sub, 56px vertical padding). **Mobile Phase D (2026-05-23):** FAB block — `.vrc-m-fab` (fixed bottom-right 56×56 round, `--vc-fg` bg + `--vc-on-primary` icon, plus icon 22px, `z-index:--vc-z-raised`, `bottom: max(20px, env(safe-area-inset-bottom))` รองรับ iOS safe area, `:active{transform:scale(.92)}`); `:hover` ใช้ `--vc-primary-hover`; `.is-disabled/:disabled` opacity .35 + pointer-events:none (past-date silent disable, Q4). **Shadow exception** — `box-shadow: 0 6px 16px rgba(0,0,0,.18), 0 2px 6px rgba(0,0,0,.10)` ละเมิด no-shadow rule, justified เพราะ FAB ลอยเหนือ scroll plane ต้อง depth cue (Material/Apple pattern). Spring entry animation `vrc-m-fab-in` keyframe (`scale .7→1 + opacity`) duration slow easing spring delay .2s, wrap ใน `prefers-reduced-motion: no-preference` guard. **Mobile Phase E (2026-05-23):** stagger reveal block — `.vrc-m-evt` ใช้ keyframe ใหม่ `vrc-m-item-in` (opacity 0 + translateY(8px) → 1+none) duration `--vc-dur-base` (220ms) easing `--vc-ease-out`, `animation-delay: calc(var(--i, 0) * 30ms)` — JS เซ็ต `--i` inline ผ่าน `style="--i:${idx}"` (running counter ข้าม group + single loops); `.vrc-m-empty` ใช้ keyframe เดียวกัน duration slow ไม่มี delay; ทุก animation wrap ใน `@media (prefers-reduced-motion: no-preference)`. เพิ่ม calendar fade-in ใน mobile media block — `#calendarBody` ใช้ keyframe เดิม `vrc-m-fade` duration slow delay .05s (fires ทุก renderCalendar = month nav + day pick). **Mobile Phase F (2026-05-23):** multi-dot indicator volume hint — `.mobile-indicator` (default 4×4 round dot สำหรับ 1 booking) + 2 modifiers ใหม่ `.mobile-indicator--bar-md` (14×3 pill, 2-3 bookings) และ `.mobile-indicator--bar-lg` (22×3 pill, 4+ bookings); ทั้ง 2 bars ใช้ `border-radius:999px` + `height:3px` override; `.calendar-cell.today .mobile-indicator` rule (white-on-black opacity .7) apply กับทุก variant อัตโนมัติ. **Fix bundle (2026-05-23):** (a) `.calendar-cell.selected.today` เพิ่ม `background: var(--vc-fg)` (re-assert today fill เพราะ `.selected` set transparent → today auto-selected on load จะกลายเป็น white-on-white invisible — bug ที่ user รายงาน); (b) `.vrc-m-daybar` ลบ `position: sticky` + `z-index: var(--vc-z-sticky)` (leak ขึ้นใต้ notification panel + UX value น้อย — daybar scroll ตาม content); (c) ลบ `.vrc-m-fab` block + `@keyframes vrc-m-fab-in` (FAB ถูก revert กลับเป็น inline button ใน daybar ตามที่ user เลือก) **Zendenta redesign → prod (2026-06-14):** **ลบ block page-head ที่เคยเพิ่ม** (`.vrc-phead-*`/`.vrc-nav-*`/`.vrc-today-btn`/`.vrc-cta-btn`) — แทนด้วย fuel-header + `vc-btn` มาตรฐาน (ดู `vehicle.html` row). `.event-card` base → pill: `background:--vc-bg-subtle` + `border:1px solid transparent` + radius-xs + padding 2px 6px (inline status `border-color` จาก JS `EVENT_CARD_STYLE` ยังทำงาน). Calendar restyle "ภาพ 2" + icon sizing ย้ายไปไฟล์ใหม่ `vehicle_zendenta.css` (scope `body.zd`) — ดู row ด้านล่าง.

### `vehicle_zendenta.css` *(ใหม่ 2026-06-14)*

**`.cal-caption`** (ใหม่ 2026-06-17): `.75rem / --vc-fg-subtle` สำหรับ service hours caption ใน col 3 toolbar + mobile. หมายเหตุ: token indigo `#4059e6` อยู่ใน `tokens.css` global (ไฟล์นี้ไม่ override token); ชื่อ "zendenta" = direction reference. **DNA migrate (2026-06-17):** ลบ section 1 icon-size override ออกทั้งหมด (`.sb-icon svg` 21px · `.vrc-topbar svg` 22px + chevron/brand) → vehicle.html ใช้ icon size global เหมือนทุกหน้า. **Mobile (≤767):** `@media` block — calendar-grid-head/header-cell โปร่ง; `.mobile-indicator` → `--vc-accent` + `position:absolute` ก้นช่อง; today = เลขปกติ / selected = วงกลมน้ำเงิน.

### `vehicle_driver.css` *(เดิม driver.css)*

 (2026-05-08, **rev2 → Vercel namespace**) — uses `--vc-*` tokens (matches `fuel_admin.css` standard), body wrapped in `.vc-scope`. Classes: `.driver-flash-alert/-plate/-unset/-header/-title/-subtitle/-header-meta/-tabs(__btn,__count)/-card(__head,__id,__summary,__chevron,__body, .is-open, --readonly)/-meta(__item,__icon,__label,__value)/-summary(__row,__label,__value)/-form(__title)/-upload(.has-file,__icon,__label,__hint)/-cta/-pill(--waiting,--ontrip,--done,--upcoming)/-done-summary/-readonly-note/-panel(.is-active)`. Mono font (Geist Mono) for BK-id, KPI numbers, inputs. Black primary CTA via `--vc-primary`. **Phase 3.5 (2026-05-15):** ลบ dead `.driver-page/-container` (§1), `.driver-empty*` (§11 — ใช้ macro แทน), icon overrides scoped to `.driver-page` (§13); แทน `#D4D4D4` → `var(--vc-border-hover)`; เพิ่ม `.driver-flash-alert/-plate/-unset`. **Card redesign req1+2 (2026-06-09):** เพิ่ม §16-19 — §16 `.driver-card__plate` (mono/600) + `__time` (muted/flex:1 — head reorder ทะเบียน·เวลา·pill); §17 `.driver-detail-card` (white `--vc-bg-subtle`, no border) + `.driver-detail__label/__head/__requester/__pax/__trip/__purpose/__dest/__arrow/__pickup`; §18 `.driver-emergency` (red-tinted `--vc-red-bg/-border`) + `__label` (สีแดง) + `__row` (flex, ห่อ `.vc-dd`) + `__btn` (red); §19 `.driver-form__hint` (เลขไมล์ล่าสุด inline mono) + `.driver-upload--wide` (taller + 44px round icon) + `.driver-upload__browse` (accent) + `.driver-odo-input`. responsive: `.driver-card__time` ลด font ≤575px. **Ad-hoc collapse req3 (2026-06-09):** เพิ่ม §20 — `.driver-adhoc-panel` (collapse card + `@keyframes driver-adhoc-in` fade/slide) + `.driver-adhoc-form` (vc-dd/vc-ac full-width override) + `.driver-adhoc-mileage` (top divider) + `.driver-adhoc-foot` (flex, submit flex:1) + `.driver-adhoc-btn.is-open`/`__icon` (rotate 45°). _dead เดิม `.driver-card__id/__summary/.driver-meta*` + `.driver-modal*`/`.driver-combo*` ยังไม่ลบ (เลิกใช้แล้ว เหลือ `.driver-modal__btn--ghost` reused)._ ใช้คู่ `core/css/components/dropdown.css` (vc-dd/vc-ac).

### `vehicle_admin.css`

 ลบ animation ทั้งหมดใน week navigator — `.va-week-card` (`va-fade-up`), `.va-week-picker-pop` (`va-pop-in` + `transform-origin`), `.va-week-day`/`.va-week-nav` press `:active{transform:scale}` + spring `transform` transition; ลบ rule `.va-week-strip--switching .va-week-day` + `@keyframes va-pop-in`/`va-week-cell-in` (เหลือเฉพาะ `va-fade-up` ที่ page sections ยังใช้); ล้าง selector ที่ตายแล้วออกจาก reduce-motion block. Mobile `@media (≤767.98px)` rework ให้ `.va-week-meta` อยู่บรรทัดเดียว — `flex-wrap:nowrap`, `.va-week-meta-left` ยืด+ตัด ellipsis (`min-width:0`), `.va-week-meta-right` คงขนาด, `.va-week-picker-btn span` ซ่อน (picker เหลือ icon), popover flip กลับขวา. **Phase 6-8 (2026-05-24):** P6 vehicle list section 7 rewrite — `.vs-row` padding `space-3/space-5`, `.vs-icon` 32px + border + active `--vc-fg` / maintenance `--vc-amber-bg/--vc-amber-border`, `.vs-name` mono sm/500/tracking 0.01em, `.vs-detail-available/-maintenance` uppercase 10.5px tracking 0.06em + dot pseudo `::before` 6×6 + hide inner Lucide; `.va-vehicle-card` `va-fade-up` 180ms delay. P7 page entrance — `.va-page-header` (0ms) + `#sectionBefore` (120ms) + `#sectionAfter` (240ms) ทั้งหมด `va-fade-up base ease-out backwards`; reduce-motion ขยายครอบทุก entry + `.bl-card`. P8 modal polish — `translateY 16→8px`, duration slow→base (220ms snappy); เพิ่ม `~ .modal-backdrop.fade { transition: opacity dur-fast ease-out }` (backdrop fade 140ms). **Phase 5 redesign (2026-05-24):** section 10 booking row rewrite — `.bl-card` card-with-border + 40px circle icon → **flat row** (border:0 + border-radius:0 + border-bottom `--vc-border` + last-child no border); grid `3px minmax(0,1fr) auto` col-gap space-4 padding space-4/space-5; `.bl-rail` 3px vertical bar (`align-self:stretch; border-radius:999px`) แทน `.bl-licon` circle — 5 modifiers `--{pending\|approver\|approved\|rejected\|group}` → `--vc-{amber\|blue\|green\|red\|green}`; `.bl-content` (`.bl-title` text-base/500/letter-spacing -0.005em + `.bl-dept-tag` pill xs muted bg-subtle radius 999px + `.bl-meta` sm/muted flex-wrap + `.bl-meta-text` truncate + `.bl-meta-dot` opacity 0.6); **new** `.bl-right` flex-column align-end gap-2 + `.bl-time` mono/sm/500/tabular-nums; selection simplified (no transform/box-shadow → bg + rail color override); hover `bg-subtle` (เบากว่า); stagger via `style="--va-i"` + `animation: va-fade-up base ease-out backwards + delay calc(min(--va-i, 8) * 30ms)`. Section 6 `.va-list` ลบ padding+gap. `.va-trip:hover` border-color change ลบ (informational). **Phase 4 redesign (2026-05-24):** section 4 filter tabs rewrite — `.va-filter-tabs` strip+border-bottom → **pill container** (inline-flex gap 4px padding 3px margin space-4/space-5 border `--vc-border` radius 8px bg `--vc-bg` + max-width compute + overflow-x auto + scrollbar hidden); `.ftab` chip (padding 5px 12px radius 6px sm/500 muted → hover bg-subtle/fg → **active `--vc-fg`/`--vc-bg`/600** dark fill) + focus-visible 2px accent ring; `.ftab-cnt` tabular-nums opacity 0.6 (active 0.85, inherits color); ลบ Phase 2 polish underline block ทั้งหมด (`.ftab::after scaleX` + `.ftab-cnt` box pill) — comment header แก้ให้ระบุว่า Phase 4 ลบ underline. Pattern lifted จาก `mileage_admin.css .mlg-status-chips/.mlg-chip`. **Phase 3 redesign (2026-05-24):** section 2 rewrite — `.va-week-card` overflow visible (ให้ popover escape) + animation `va-fade-up 60ms backwards`; เพิ่ม `.va-week-meta/-left/-right` (flex space-between row, padding `--vc-space-5 --vc-space-6`), `.va-week-eyebrow` (xs/500/uppercase tabular-nums), `.va-week-selected` (h2 lg/600/letter-spacing -0.014em), `.va-week-today-tag` (xs uppercase pill bg-subtle inline ใน h2), `.va-week-today` ghost btn (sm/500 muted → hover fg), `.va-week-picker` (relative anchor) + `.va-week-picker-btn` (secondary + caret rotate via `[aria-expanded="true"]`) + `.va-week-picker-pop` (absolute top:100%+6px right:0, min-width 240px, animation `va-pop-in`, `transform-origin: top right`); `.va-week-bar` ลบ border-bottom (meta แทน) + padding `--vc-space-3 --vc-space-4`; ลบ `.va-week-heading` (deprecated). เพิ่ม `@keyframes va-fade-up/-pop-in/-week-cell-in` + class `.va-week-strip--switching .va-week-day` apply cell-in 30ms stagger via `--va-i`; mobile (≤767px) wrap meta full-width + popover flip ซ้าย. **Phase 1-2 redesign (2026-05-24):** ลบ section "KPI STRIP — 4 cells override" + Phase 2 polish KPI rules + KPI entry ใน `prefers-reduced-motion`; ปรับ `.va-page-header` gap/padding tokens; `.va-page-title` font-size `clamp(26px, 3.4vw, 32px)` + letter-spacing -0.022em; eyebrow weight 600→500; เพิ่ม `.va-page-header-stat` (pill-shape inline strip — border + radius 999px + tabular-nums) + `.va-stat-item/-num/-lbl/-sep` + dot color modifiers `.va-stat-dot--{amber\|blue\|green}` (แทน KPI 4 cells); reduce-motion block ขยายครอบ Phase 3 (`.va-week-card`/`.va-week-picker-pop`/strip-switching/caret). **Rewritten 2026-05-07** for Vercel shell. **Phase 3.2 (2026-05-14):** ลบ `.bl-badge`/`.badge-pending`-`.badge-group`/`.bl-empty` (รวม 6 HEX colors) → page ใช้ `components/badge.css` + `components/empty_state.css` แทน. **Phase 5.0 (2026-05-16):** redesign booking row 2-line layout. **Phase 5.4 polish (2026-05-16):** (1) icon FOUC fix — `.vc-scope [data-lucide]:not(svg)` reserve 14×14 ก่อน lucide swap; (2) week navigator: padding ใน bar, borders แต่ละ day cell, hover/active border, today marker top-right, mobile breakpoint ≤575px; (3) booking row redesign 2-line → **spacious card** — `.bl-body/.bl-row-head/.bl-name/.bl-head-spacer/.bl-dept-pill/.bl-row-meta(+.bl-meta-chip/.bl-meta-text)/.bl-row-assigned(+.bl-assigned-chip/.bl-assigned-label, only when approved/forwarded & has vehicle/driver)/.bl-row-actions/.bl-group-sub` ลบ `.bl-row-top/.bl-meta-dest/.bl-dot/.bl-actions`; (4) `.va-list` ใช้ flex-column + gap แทน `.card mb-2`; (5) cleanup HEX literals 13 จุด → tokens (`.vs-icon-maintenance/.pts-exp-*/.pts-btn-paid/.pts-btn-telegram/.va-approver-info/.va-budget-track/.va-modal-warning/.bl-ico-approve|reject:hover` → `--vc-{red\|green\|blue\|amber}-{bg\|border}`); (6) trip detail box bg subtle + radius; (7) action icon buttons 36×36 default with `var(--vc-red\|green-bg)` hover. Depends on `components/kpi.css` + `components/filter_bar.css` + `components/badge.css` + `components/empty_state.css` (Phase 2+3 primitives) + `design-system.css`. **Zero HEX** ทั้งไฟล์ตอนนี้. Mobile breakpoints ≤767px (list actions 36px tap target, modal compact) and ≤575px (vehicle row + filter tab + week navigator compact). **Phase 6.1 (2026-05-17):** เพิ่ม week-day-dot variants `.va-week-day-dot--sm` (4×4 dot) / `--md` (15×4 bar) / `--lg` (25×4 bar) + active state inverts to `var(--vc-bg)`; budget bar tone modifiers `.va-budget--warn` (amber fill) / `--danger` (red fill); `.va-budget-warn` ข้อความเตือนใต้ bar (amber/red bg+border); `.va-conflict-warn` warning box ใต้ select ใน modal (amber). **Backdrop coverage moved global (2026-06-06):** ลบ block `#assignModal/#swapModal/#repairModal/#revertModal ~ .modal-backdrop { inset:0; width:100% }` (เคยทำแถบขาวขอบขวาเพราะชน `scrollbar-gutter:stable`) → ย้ายไป global `.modal-backdrop` (2026-07-21: ย้ายอีกครั้งไป `tokens.css` เป็น single source — ดู § Token source); คง backdrop fade-transition (scoped animation) ไว้. **Phase 11 task-card redesign (2026-06-06):** section 10 เขียนใหม่ทั้ง block (left-icon layout เดิม = dead เพราะ JS เปลี่ยน markup ก่อนแต่ CSS ไม่ตามมา) → **task-list card** ตาม reference image ที่ user ส่ง. `.bl-card` = `display:flex; flex-wrap:wrap` (ไม่ใช่ grid; ไม่มี icon column — เลิกใช้ `.bl-icon/.bl-licon`), **DESIGN-OVERRIDE `border-radius:12px`** + fill `bg` ขาว (hover → `bg-subtle`) ตาม reference (**Phase 13, 2026-06-07:** เดิม soft-fill bg-subtle → กลับด้านเป็นขาว default ให้ pending card เด่นสะอาด), padding space-4/space-5; `.bl-card--done` (approved = ขาว + title หรี่), `.bl-card--group` ขอบเขียว. `.bl-body` (flex:1 flex-col gap space-3) = `.bl-head`(`.bl-head-text`[`.bl-title` 15/700 1-line + `.bl-desc` 13/muted 1-line] space-between `.bl-due`[`.bl-due-label` subtle + `.bl-due-value` 700]) + `.bl-foot`(border-top divider; `.bl-author`[`.bl-avatar` 26px initial circle + `.bl-author-text`] space-between `.bl-foot-right`[`.bl-foot-pax` + `.bl-status` + `.bl-actions`]). Checkbox: `.bl-sel-check` align-self center / `.bl-sel-slot` 18px. Selection = inset 3px rail (`.bl-selected` fg / `.bl-notify-selected` blue) ไม่ดัน layout. `.bl-grp-collapse` = `flex-basis:100%` (sibling ของ `.bl-body` — ดันลงเต็มแถวล่าง แทน `grid-column` เดิม). Entrance stagger `@keyframes blCardIn` delay `calc(--va-i * 40ms)` (gated `prefers-reduced-motion:no-preference`). ลบ dead `.bl-licon/.bl-content/.bl-title-row/.bl-meta/.bl-status-text` (คง `.bl-meta-ico/.bl-meta-dot` ใช้ใน group sub + pax). ลบ Phase 2 duplicate `.bl-card` hover/selection block (1078-1103). Mobile ≤767px: `.bl-head` เป็น column (due ลงล่าง) + ซ่อน `.bl-action-secondary`. **Phase 11.1 (2026-06-06):** ลบ section 5 `.va-collapsed-bar` (+`:hover` +`circle-check`) ทิ้ง — booking list แสดงเสมอ. **Phase 14 bl-card 3-line (2026-06-07):** single booking row redesign — ลบ dead `.bl-author/.bl-avatar/.bl-author-text/.bl-foot-pax` (group ไม่ใช้); เพิ่ม `.bl-trip` row (`.bl-trip-purpose` muted max-45% truncate + `.bl-trip-arrow` subtle + `.bl-trip-dest` fg/600 truncate = purpose→dest), `.bl-badges` (flex:1 overflow-x:auto scrollbar hidden — มือถือ badge scroll บรรทัดเดียว) + `.bl-badge` (flex-shrink:0) + `.bl-badge-adhoc` (purple subtle `--vc-purple-bg/-border`); scoped `.bl-card:not(.bl-card--group) .bl-head{align-items:center}` + `.bl-title{flex:1;min-width:0}`; mobile `.bl-head{flex-direction:column}` → rescope `.bl-card--group .bl-head` (single คง row ชื่อ|status). `.bl-head/.bl-foot/.bl-due/.bl-foot-right/.bl-meta-ico` คงไว้ (group ใช้ร่วม). **Phase 14.1 group align (2026-06-07):** group row ใช้โครง 3-line เดียวกับ single → `.bl-head` align-items flex-start→center (เลิก scope `:not(.bl-card--group)` — รวมเข้า base) + `.bl-title` flex:1 1 auto/min-width:0 เข้า base; ลบ dead `.bl-head-text/.bl-desc/.bl-due/.bl-due-label/.bl-due-value/.bl-foot-right` (single+group เลิกใช้ทั้งคู่); mobile ลบ `.bl-card--group .bl-head{flex-direction:column}` + `.bl-due` rules (group head เป็น row แล้ว). `.bl-meta-ico/.bl-meta-dot` คงไว้ (group sub). **Phase 15 (2026-06-07):** trip card cost badges — ลบ dead `.va-trip-amount`; เพิ่ม `.va-trip-badges` (flex wrap gap) + `.va-trip-badge` (nowrap) + `.va-trip-badge--ot` (blue subtle `--vc-blue-bg/-border`). **Phase 16 inline calendar (2026-06-08):** เพิ่ม `.va-cal*` block (popover ปฏิทินเดือน คลิกเดียวเลือกวัน) — `.va-cal` (absolute top:100%+6px right:0, 280px, border + box-shadow + fade-in `vaCalIn`), `.va-cal-head/-title/-nav`, `.va-cal-grid` (7-col), `.va-cal-dow-cell` (หัววัน, --sun แดง/--sat ฟ้า), `.va-cal-cell` (aspect 1:1 radius-full; `--active` วงกลมทึบ `--vc-primary`; `--today` ring; `--sun/--sat` สี), `.va-cal-dot` (accent dot บอกวันมีงาน). **Phase 16.1 clip-fix (2026-06-08):** `.va-week-card` base = `overflow:hidden` (clip มุม); เพิ่ม `.va-week-card:has(.va-cal:not([hidden])){overflow:visible}` ปลด clip เฉพาะตอนปฏิทินเปิด (history Phase 3 "overflow visible" ถูก override โดย Phase 12 ที่ตั้ง hidden — :has() คือ fix จริงตอนนี้). **Phase 16.2 jitter-fix (2026-06-08):** เพิ่ม `display:flow-root` ใน `.va-week-card` (BFC ถาวร) — กัน margin-bottom ของ `.va-week-bar` (`mb-2`) collapse ออกนอก card ตอน `:has()` สลับ overflow hidden→visible (BFC จาก overflow:hidden หายตอน visible → card หด ~8px = กระตุก). **Phase 5 micro-interactions (2026-06-12):** `.bl-card` hover lift (`translateY(-1px)` + transform transition); `@keyframes pendingDotPulse` → `.vc-badge-warning.vc-badge-dot::before` pulse 2.4s (pending dot พัลส์เบาๆ); `.bl-actions .vc-btn:active` scale(0.93) press; `prefers-reduced-motion` guard ครอบทั้ง 3.

### `vehicle_cost.css`

**Phase 5.10 (2026-05-18):** เพิ่ม `.cost-rate-row` (6-col grid label/day/start/end/rate/×, vc-bg-subtle bg) / `.cost-rate-row-field` / `.cost-rate-row-day` / `.cost-rate-row-remove` (32×32 icon button) / `.cost-rate-row.is-removed` (opacity .4 + pointer-events none for soft-deleted existing rows) + mobile ≤575px breakpoint; rate banner: `.cost-rate-pill--day` (amber bg/border, `--vc-amber-bg/-border/--vc-amber`) + `.cost-rate-pill-day` (label chip "อาทิตย์" prefix). **2026-06-09 (manual OT):** redesign `.cost-slot-row` → mirror `.cost-rate-row` (flat transparent input, hover/focus border) + เพิ่ม `.cost-slot-head`/`.cost-slot-head-spacer` (column header เหมือน thead, label sr-only desktop, stacked ≤575px); เพิ่ม datepicker `.budget-datepick`/`.budget-date-btn`/`.budget-date-label`(+`--filled`)/`.va-cal--start`/`.va-cal--end` (va-cal core มาจาก `vehicle_admin.css`). **Zendenta redesign (2026-06-26):** ลบ `.cost-header/-title/-subtitle/-header-actions` + `.cost-summary/-sep` ออก; เพิ่ม `.cost-kpi-strip` (flex strip border-bottom, scoped `.kpi-num` 26px); `.cost-toolbar-row` (border-bottom เต็มแถวรวม filter button); `.cost-tabs` (flex:1 ไม่มี border-bottom เอง); `.cost-chip` underline tab (margin-bottom -1px, border-bottom 2px transparent → accent เมื่อ `.is-active`); `.cost-filter` (padding-bottom 8px align filter button); `.cost-chip-cnt` badge pill; slot tint remap → `--vc-amber-bg`/`#ede9fe`/`#e6f0ff`; ลบ `.cost-summary*` mobile rules.

### `repair.css`

 (2026-05-14) — `vc-scope` page. Classes: `.repair-header/-header-actions`, `.repair-modal-icon` (mono tile, no variant), `.repair-modal-title/-subtitle`, `.repair-form-section-label`, `.repair-info-box/-info-avatar/.repair-dashed`, `.repair-upload-zone`, `.resolved-note-cell`, `.repair-flash-wrap`, `.repair-kpi-card`, `.repair-time-sub`, `.repair-category-sub`, `.repair-reporter-name`, `.repair-select`, `.repair-input-lg`, `.repair-textarea`, `.repair-required`, `.repair-note-hint`. **Phase 3.4 (2026-05-15):** เปลี่ยน `--ds-*` ทั้งหมด → `--vc-*`. ไม่มี `--ds-*` เหลือ. **Redesign (2026-06-16):** ลบ `.repair-title/.repair-subtitle`; เพิ่ม custom DataTables pagination scoped `#repairTable_wrapper` (pill วงกลม สี accent, prev/next chevron ผ่าน `::before`) + `.repair-goto/-label/-select/-btn` (go-to-page). **DNA migrate (2026-06-17):** retired `.repair-kpi-5` (KPI ใช้ `.fuel-kpi` strip จาก vehicle_fuel.css แทน) + `.repair-modal-icon--warning/--blue/--success` (icon เป็น mono tile `--vc-icon-bg`/`--vc-icon`, radius-sm); ลบ `.repair-title/.repair-subtitle` ออกจาก inventory แล้ว (ลบไป 2026-06-16).

### `vehicle_approver.css` *(เดิม approver_inbox.css)*

 (Phase 3.8, 2026-05-15; mobile redesign Phase 5.11, 2026-05-18) — ใช้ `--vc-*` ทั้งหมด. Classes: `.approver-wrap` (max-width 560px center), `.approver-flash`, **budget block** `.budget-card/-card-row/-card-left/-card-icon/-card-name/-card-amount/-card-total/-card-used/-card-sep/-progress/-bar`, **tabs (pill)** `.inbox-tabs/.inbox-tab(.active)`, **card** `.approver-card`, **header** `.ac-header/-header-row/-header-right/-ref/-meta/-meta-sep/-chevron(.rotated)/-body`, **actions** `.ac-action-row/-action-form/.btn-approve-action/.btn-reject-action`, **reject form** `.ac-reject-wrap/-reject-input/-reject-cancel/.btn-reject-confirm`. **Detail-card redesign (2026-06-10):** ลบ field-grid block (`.ac-fields/-field/-field-full/-field-label/-field-value/-reject-reason`); เพิ่ม **fuel badge** `.ac-fuel-badge` (amber tone, mono tnum), **detail card** `.ac-detail-card`(`:not(:last-child)` margin-bottom)`/-detail__head/__requester/__pax/__datetime/__date/__time/__trip/__purpose/__dest/__arrow/__pickup` (เลียน `driver-detail-card`, `--vc-bg-subtle` bg), **reject note** `.ac-reject-note` (red box แทน field). Mobile breakpoint ≤380px (datetime gap + compact buttons). **2026-06-21:** เพิ่ม `.budget-card-warn` (over-budget warning row ใต้ progress bar — flex icon `triangle-alert` + text "งบเกิน ฿N", `--vc-danger` สีแดง, font-weight 600)

### `room.css`

 (2026-05-17) — `vc-scope` page. Classes: `.room-legend`, `.room-badge(--small/--large)` + `.room-dot`, `.event-card.room-small/.event-card.room-large` (override calendar event color per room), `.room-list-dot(--small/--large)`, `.room-detail-dot(--small/--large)`, `.room-mobile-empty`. Reuse `vehicle.css` calendar grid. **Toolbar/grid-head (2026-06-16):** เพิ่ม `.cal-toolbar` (grid `1fr auto 1fr`), `.cal-toolbar-left` (flex), `.cal-month-label` (`<h6>` reset margin + tabular-nums จัดกลาง), `.calendar-grid-head/-header-cell` (พื้น `--vc-bg-subtle`) — copy/unscope จาก `vehicle_zendenta.css` เพราะ room.html ไม่โหลดไฟล์นั้น (ไม่มี `body.zd`) ทำให้ toolbar style หาย. Zero HEX (tokens only).

### `manage_fleet.css` (ไฟล์จริง `vehicle_fleet.css`)

 (Phase 6.2, 2026-05-17; **shell+token migrate 2026-07-29** → [log](log/2026-07-29_vehicle-fleet-shell-token.md)) — page-exclusive, token `--bb-*` เต็มไฟล์ (เดิม `--vc-*` จาก `design-system.css`). Classes ที่เหลือจริง (2026-07-30 cleanup): `.mf-username-chip/-dept-chip` (ใช้ใน approver list + `driverDetailModal`), `.mf-avatar(-lg)/-initials` (ใช้ใน `driverDetailModal` เท่านั้น — vehicle/driver table จริงย้ายไป `.bb-avatar` แล้ว), `.mf-stagger` (row enter 40ms cascade, `cubic-bezier(0.23,1,0.32,1)` inline literal — ใช้กับ `.bb-table` จริงด้วย), `.mf-approver-row(-name/-meta)`, `.mf-readonly-note`, `.mf-switch-row`, `.mf-hist-total(-label/-value/-unit)`, `.mf-spin/-hist-loading/-distance-pos`, `.mf-form-section(-title)/-current-file` (add/edit driver modal), `.vc-modal` motion (section 8, ใช้กับทุก modal ยกเว้น `#addVehicleModal` ที่เลิกใช้ `vc-modal` class แล้ว 2026-07-31). **ลบแล้ว (dead code, หลายรอบ):** `.mf-header/-title/-subtitle`/`.mf-grid`/`.mf-col-right`/`.mf-name-primary` (2026-07-29) · `.mf-plate/-name-line(-sub)/-cap/-odo-unit`/`.mf-actions`/`.mf-icon-btn(--danger)`/`.mf-driver-row(-main/-name/-meta/-jobs/-side)` (2026-07-30 — table จริงย้ายไป `.bb-table`/`.bb-btn.is-sec.is-icon` แทนหมด). Ease vars `--mf-ease-out/-in-out` เดิม scope บน `.vc-scope` (ลบไปพร้อม wrapper ตอน migrate) → inline literal cubic-bezier ทุกจุดแล้ว. Modal dialog enter `translateY(8px) scale(0.97)` → `0/1` 200ms ease-out (Emil: never `scale(0)`). Respects `prefers-reduced-motion` (animation off, opacity-only). Zero HEX.

### `dashboard.css`

 (2026-05-17) — `vc-scope` landing page. Classes: `.dash-header/-title/-subtitle`, `.dash-section/-section-label`, `.dash-grid-{2,3,4}` (responsive: 4→2→1, 3→1, 2→1), `.dash-svc(-head/-icon/-title/-desc/-foot/-cta)` (service card — icon เทา mono, border-hover ไม่มีเงา), `.dash-kpi-label` (KPI strip label), `.dash-superadmin(-info/-icon/-title/-desc)` (dark inverted CTA + inverted vc-btn), **`.dash-req-tile`** (36px mono icon tile ใน row คำขอ) + **`.dash-repeat`** (ปุ่มทำซ้ำ soft secondary), **`.dash-today(-row/-time/-icon/-body/-title)`** (วันนี้ของฉัน timeline — icon เทา mono) + `.dash-recent-sub`, `.dash-fade.d-{1..5}` (stagger entrance — 60ms delay, `cubic-bezier(0.23,1,0.32,1)`, 280ms, scale 0.995 + translateY 6px; `prefers-reduced-motion: opacity-only`). No shadow, flat. **🎨 Zendenta-clean (2026-06-19):** purge `.dash-zen`/`dz-*` dribbble-frame + dead `.dash-mine*`/`.dash-kpi(-value/-meta)`/`.dash-alert`/`.dash-recent-card .vc-table/-time`/`.dash-req-title`. คำขอ table/badge → class กลาง `.data-table`/`.badge-pill` (main.css); KPI strip → `.kpi-tile`/`.kpi-num` (main.css).

### `notification.css`

 icon สีตามสถานะ ntype — `.notif-cat-icon.notif-icon-{success\|info\|warning\|danger}` = color `--vc-{green\|primary\|amber\|red}` + tint bg/border `--vc-{green\|blue\|amber\|red}-bg/-border` (info→blue); โครงสร้าง/text ใช้ `--vc-fg`, เน้น unread ด้วย `--vc-accent` (dot/text). icon uniform ทุกระดับ: container 28px + glyph 14px + `stroke-width 1.75` (inner เลิก inline `22px` → ใช้ base 28px). `.notif-inner` = timeline 2 บรรทัด (title เด่น `.notif-inner-title` + subtitle clamp 1 บรรทัด `.notif-inner-msg` + `.notif-inner-time` ชิดขวา); connector แนวตั้ง (`::before` left:14px = กลาง icon-node, **เฉพาะช่องว่าง top:41/bottom:-5 ไม่ชน icon, สี `--vc-fg`** ให้เห็นชัด — 2026-06-15) + `.notif-cat-icon` z-index:1; `.notif-timeline--notifs` expanded padding-left **30px** → inner icon-node/text align ใต้ parent icon column (x=44/68)

### `vercel.css`

 (2026-05-20) — global via `_header.html` link. Class prefix `vrc-` (กัน Bootstrap `.btn-primary` + namespace `vc-*`). Uses existing `--vc-*` tokens. **Phase 6.0 (_header)**: `.vrc-topbar` (56px sticky bar, bg-subtle), `.vrc-topbar-title` (**DNA 2026-06-17:** หัวเรื่องใหญ่ 19px/700 ใน topbar แทน breadcrumb เดิม `.vrc-topbar-crumb*` ที่ลบแล้ว — รับ `page_title or page_section`), `.vrc-topbar-btn` 32×32 icon button + descendant rule `.vrc-topbar .ds-btn-icon` ให้ existing notif/user buttons กลมกลืนโดยไม่ต้องแก้ markup, `.vrc-topbar-dot` red dot indicator. **Calendar popover (2026-05-20)**: เพิ่มบล็อก `.popover.vc-cal-pop*` — no shadow, border-only, no header, 240px × max-height 320px scroll, arrow recolor → `--vc-border/--vc-bg`; row grid `.vc-cal-pop-row` (`auto 44px 1fr` columns, hover `--vc-bg-subtle`, last-child no border), `.vc-cal-pop-dot` 6px + variants `--approved/--pending/--approver/--rejected/--group` mapped ไป `--vc-{green|amber|blue|red}` tokens, `.vc-cal-pop-time` mono 12px tabular-nums, `.vc-cal-pop-dest` 13px muted ellipsis + `.vc-cal-pop-dest-icon` 11px (สำหรับ group users icon). **Roadmap**: หลัง _header เสถียร → port sidebar/button variants/calendar จาก `docs/design/vehicle-warm-mockup.html`. **Hero banner (2026-06-13)**: เพิ่มบล็อก `.vrc-hero` (ใต้ `.vrc-page-subtitle`) — `position:relative` flex บนพื้น `--vc-accent-light` + border `--vc-accent-border` + radius-lg, `min-height:136px`; `.vrc-hero-text` (z-2, max-width 60%), `.vrc-hero-title` (text-xl/700 `--vc-accent-dark`), `.vrc-hero-sub` (text-base `--vc-accent`), `.vrc-hero-img` (absolute right/bottom, height 156px, bleed); `@media(max-width:1100px)` ย่อรูป 122px. ใช้กับ `vehicle/vehicle.html` (desktop only).

### `pages/vehicle.js`

 `STATUS_BADGE` map → `vc-badge vc-badge-{warning|blue|success|danger|neutral} vc-badge-dot`; ลบ `badge rounded-pill` wrapper + inline `style="font-size:..."`. **Phase 4.7 (2026-05-15):** legacy `vehicle.js` (790 lines) → `pages/vehicle.js` ES module (`type="module"`); ลบ DOMContentLoaded wrapper (module deferred); expose `openEventDetail`/`openEditBookingModal`/`openMoreEvents`/`openBookingModal` ผ่าน `Object.assign(window, …)` + `eventDetailModal`/`moreEventsModal` ผ่าน `Object.defineProperty` getter (late binding); ย้าย `?pay=<id>` deep-link IIFE จาก `vehicle.html` เข้า module. **Phase 2 user-facing (2026-05-17):** `initFlatpickr()` เพิ่ม `updateDuration()` closure (compute end−start, bind `change` ทั้ง bkStart/bkEnd, output "ระยะเวลา X ชม. Y นาที" ใน `#bk_duration_preview`). **Phase 3 user-facing (2026-05-17):** `import { initIcons, bindModalReinit } from '../core/icons.js'` + `bindModalReinit()` module-top; `STATUS_ICON` map: FA strings → Lucide names (clock/send/circle-check/circle-x); 4 จุดที่ `ds-status-dot` → `vc-status-dot` (group avatar / single dot / detail header / members); FA `<i class="fa-...">` → `<i data-lucide="..." class="vc-icon-sm">` (users/${iconName}/circle-check/user); `updateMobileList` ปลาย: `initIcons(content)`; `openEventDetail` header icon: `outerHTML` swap (รักษา `id="detailHeaderIcon"` + bindModalReinit แปลง Lucide → svg ตอน shown.bs.modal); ลบ `headerDot.style.cssText='width:44px;...'` → ใช้ class `.bk-detail-header-dot`. **Phase 4 user-facing (2026-05-17):** cleanup ครบทุกอย่าง — `var(--ds-text-heading|text-muted|border)` 14 จุด → `var(--vc-fg|fg-muted|border)`; HEX 8 จุด (popover `#f3f4f6`/`#2563EB`, EVENT_CARD_STYLE 5 statuses) → tokens; ~17 FA/BI icons → Lucide (empty state calendar-x, group card truck/user/clock/pencil/chevron-down/arrow-up-right, single card user/clock/map-pin/pencil, openMoreEvents user, members list users/arrow-right/map-pin, footer actions pencil/trash-2, popover users); inline-HEX buttons → `vc-btn` variants; "งานรวม" badge → `vc-badge vc-badge-blue`; `createCell` ARIA (role=gridcell + aria-label + aria-current). เพิ่ม module-top `shown.bs.popover` listener + keydown handler (← →/T). **Phase 9 (2026-05-22):** `detailActions` block ใช้ `e.canCancel` แทน `(e.isOwner && e.isPending)`; ปุ่ม "ยกเลิก" POST ไป `e.cancelUrl` (เปลี่ยนจาก `deleteUrl`); confirm dialog ขยายเป็น "งบจะถูกคืน + แจ้ง Admin/Approver/Driver/ผู้ร่วมเดินทาง"; ปุ่ม "แก้ไข" ยังจำกัด `e.isOwner && e.isPending` (approved booking แก้เองไม่ได้). **Detail modal premium redesign (2026-05-23):** new `_esc()` helper (HTML-escape for innerHTML safety); `_plateLabel()` rewritten — return HTML (`<span class="bk-detail-plate-code">{plate}</span><span class="bk-detail-plate-brand">{brand}</span>`) แทน plain text → `detailPlate.innerHTML` (เปลี่ยนจาก `.textContent`); `detailMembersList` template restructured จาก flat rows w/ `border-bottom` → `.bk-detail-member` card tiles + `style="--bk-i:${idx}"` (CSS var stagger animation); members container ใช้ `.bk-detail-members` (was `px-4 pb-2`); ลบ L760-761 JS footer collapse (`actDiv.closest('.card-footer').style.display`) — CSS `.bk-detail-footer:has(#detailActions:empty){display:none}` ทำแทน. **Book modal date format (2026-05-23):** `initFlatpickr()` แปลง `#bk_date` native date input → flatpickr instance + `altInput:true` (visible Thai display "วันจันทร์ที่ 4 พฤษภาคม 2569" — Buddhist year +543 + Thai day/month full names) + `dateFormat:'Y-m-d'` (hidden value คงเดิมสำหรับ form submit + L155-158 `.value` reads); custom `formatDate` callback branch ตาม marker `'thai_be'` (altFormat) — fallback `flatpickr.formatDate()` static method สำหรับ Y-m-d; `disableMobile:true` (force flatpickr ทุก device); `locale: flatpickr.l10ns.th` (Thai locale โหลดอยู่แล้วใน vehicle.html L178); `openBookingModal()` L802 `bkDate.value = ds` → `bkDate._flatpickr.setDate(ds, true)` (sync altInput display เมื่อเปิด modal จาก calendar cell) + guard `bkDate._flatpickr` กัน init ซ้ำ. **Edit modal mirror book (2026-05-23):** extract 2 helpers `_initThaiDatePicker(input)` + `_bindTimeDuration(startEl, endEl, previewEl)` — shared by `initFlatpickr()` (book) + `initFlatpickrInModal()` (edit); ลบ legacy datetime flatpickr ใน `initFlatpickrInModal()` ทั้งหมด — แทนด้วย `_initThaiDatePicker('#edit_date')` + `_bindTimeDuration('#edit_start_time','#edit_end_time','#edit_duration_preview')`; `openEditBookingModal()` rewritten — populate `edit_start_time.value=e.time` + `edit_end_time.value=e.timeEnd` (แยกจาก e.date) + `editDate._flatpickr.setDate(e.date, true)` ใน setTimeout, ลบ legacy `editStartDatetime._flatpickr.setDate(\`${e.date}T${e.time}\`)`; pre-set `.is-valid` บนทุก `[required]` field ที่มีค่า (mirror book modal `shown.bs.modal` valid-icon refresh); dispatch `change` event บน `edit_end_time` เพื่อ trigger duration preview. `editBookingForm` submit handler L182-185 rewritten — `e.preventDefault()` + check validity → compose `editStartDatetime.value = edit_date + 'T' + edit_start_time` + same สำหรับ end (mirror bookingForm L155-159 pattern); เพิ่ม valid-icon listeners (`input`/`change` toggle `is-valid` ตาม `checkValidity()`) สำหรับทุก `#editBookingForm [required]`; `hidden.bs.modal` cleanup เพิ่ม `.is-valid` removal (mirror book pattern). **Mobile Phase A+B (2026-05-23):** month-nav listeners refactor — extract `_prevMonth/_nextMonth/_gotoToday` arrow funcs + dual-bind ผ่าน `['prevMonthBtn','prevMonthBtnMobile'].forEach(id => document.getElementById(id)?.addEventListener('click', _prevMonth))` (และคู่ next/today ทำนองเดียวกัน) — desktop + mobile toolbar share handler ตัวเดียวกัน; `renderCalendar()` เพิ่ม `_monthLabel` const → write ทั้ง `currentMonthLabel` + `currentMonthLabelMobile` (เช็ก existence ก่อน write); `updateMobileList()` เพิ่ม `_dayCount` (= `mockEvents.filter(e=>e.date===ds).length`) + populate `#mobileDaybarCount` ด้วย `{N} รายการ` (empty string เมื่อ 0 → CSS `:empty` ซ่อน). **Mobile Phase C (2026-05-23):** `updateMobileList()` markup refactor ครบ — เปลี่ยน Bootstrap `card`/`card-body` markup → `.vrc-m-evt*` premium event-cards; empty state จาก inline `<div class="text-center py-5">` → `.vrc-m-empty` 3-element circle-icon + title + sub copy; wrap list ทั้งหมดใน `<div class="vrc-m-list">`. Helpers ใหม่ใน scope `dotKey(s)/statusKey(s)` (handle `'completed' → 'completed'` variant, else fall back `STATUS_DOT[s]\|\|'approved'`). **Group card**: `vrc-m-evt vrc-m-evt--group` ใช้ purple `--vc-accent` dot + status pill "{N} งานรวม" (`--group` variant) + `.vrc-m-evt-toggle` ฝังใน `.vrc-m-evt-head` (เดิมเป็น ghost btn คู่ขนานกับปุ่ม pencil) — ลบปุ่ม pencil "ดูรายละเอียดกลุ่ม" ออก (group head ใช้ data-bs-toggle collapse; รายการ sub-row คลิกเปิด detail). Sub-rows = `.vrc-m-evt-sub-row` button (time prefix + booker + meta), แทน nested card-in-card pattern. **Single card**: flat `.vrc-m-evt` — title = "{time}–{timeEnd} · {dest}" (fallback ถ้าไม่มี dest), meta = booker · {pax} ท่าน · car short, status pill ขวา (`vrc-m-evt-status--{statusKey}` + `STATUS_LABEL[status]`); **ลบปุ่ม edit pencil inline บน single card** — tap ทั้งใบ → openEventDetail() (edit อยู่ใน detail modal). ใช้ plain text + `.sep` separator แทน Lucide icons (user/clock/map-pin) ใน meta — ลด visual noise, premium feel

### `pages/vehicle-admin.js`

**Phase 5 redesign (2026-05-24):** `STATUS_LICON` → `STATUS_RAIL` map (status → rail color class); rewrite `renderSingleRow(b, idx=0)` + `renderGroupRow(grpName, members, idx=0)` ใหม่ — 3-zone markup `<span.bl-rail>` + `<div.bl-content>` (title + `.bl-dept-tag` from `b.dept`) + `<div.bl-right>` (`.bl-time` mono "HH:MM–HH:MM" + `.bl-actions`); group row icon-only ghost buttons (ungroup/edit/chevron); `style="--va-i:${idx}"` per row stagger; `renderBefore()` map callback pass `(item, i)` idx; populate `#beforeCount` "${cnt.all} รายการ". **Phase 3 redesign (2026-05-24):** เพิ่ม `renderWeekMeta()` (smart range text DD–DD MMM YYYY — compress same-month/year vs cross-month vs cross-year; h2 day line "วันXที่ DD MMM YYYY" + auto-append `.va-week-today-tag` ถ้าวันนี้); `_alignWeekStartTo(date)` helper (Monday-based snap); popover funcs `openWeekPicker()`/`closeWeekPicker()` (toggle hidden + aria-expanded + auto-focus input); `bindWeekControls()` bind ครั้งเดียว: `#weekTodayBtn`/`#weekPickerBtn` toggle/`#weekPickerInput` change → parse YYYY-MM-DD + sync weekStart + close + renderAll, document click outside, Esc + return focus; `renderWeekNav()` cells get inline `style.setProperty('--va-i', i)` สำหรับ stagger; `shiftWeek(dir)` เรียก `renderWeekMeta()` + force-reflow + add `.va-week-strip--switching` (skip reduce-motion); `renderAll()` แทน inline `#selDateHeading` set ด้วย `renderWeekMeta()` call; เรียก `bindWeekControls()` ก่อน first `renderAll()`. **Phase 1-2 redesign (2026-05-24):** rename helper `setKpi`/`_kpiRaf` → `setStat`/`_statRaf` (count-up animation logic ยังเดิม 600ms ease-out cubic); call sites ใน `renderBefore()` 4 → 3 (`setStat('statPending'/'statApprover'/'statApproved', cnt.*)` — ตัด rejected). booking + trip list rendering, vehicle status grid, 4 modal handlers (assign/swap/repair/revert), week navigator + date picker, header stat counters, bulk merge + notify selectors. Exposes to `window.*` (20+ funcs: shiftWeek, openAssignModal, openSwapModal, fixDone, submitAssign, etc.) สำหรับ legacy `onclick=""` ใน template + JS-rendered HTML. **Phase 3.2:** emits `vc-badge-{warning\|blue\|success\|danger\|neutral\|solid} vc-badge-dot` + `vc-empty`. **Phase 5.4 polish (2026-05-16):** `renderSingleRow()` rewritten → spacious card layout (`.bl-body` flex-col, `.bl-row-head` w/ booker+dept+badge, `.bl-row-meta` w/ time+dest+pax chips, `.bl-row-assigned` shown when approved/waiting_approver/forwarded & has vehicle/driver, `.bl-row-actions` right-aligned with `stopPropagation`); `renderGroupRow` action class rename `.bl-actions` → `.bl-row-actions`; remove redundant `mb-2` (uses `.va-list` flex+gap). **Phase 6.1 (2026-05-17):** new helpers `ACTIVE_STATUSES` Set + `driverDayCount(driverId, dateStr, excludeBookingId)` + `findConflict(b, 'vehicleId'\|'driverId', resourceId)` + `updateConflictWarnings()`; `renderWeekNav()` count-based dot class (`--sm/--md/--lg`) + title tooltip; `openAssignModal()` driver dropdown suffix "• N งานวันนี้" + Thai placeholder "— เลือกคนขับ —"; `checkAssignReady()` now also calls `updateConflictWarnings()` (vehicle/driver select onchange both trigger); `updateModalBudget()` rewritten — tone classes `va-budget--{ok\|warn\|danger}` (remPct based: ≥20% ok, ≥10% warn, <10%/rem≤0 danger), shows `#modalBudgetWarn` text "งบเหลือน้อย (N% · X บ.)" or "งบหมดแล้ว". **Token fix Phase 5.1 followup**: `--ds-success/warning/danger` literals in `updateModalBudget()` → CSS-driven via tone classes (no inline color). **Phase 11 task-card redesign (2026-06-06):** rewrite `renderSingleRow(b, idx=0)` + `renderGroupRow(grpName, members, idx=0)` → task-list markup `<div.bl-body>` (**ไม่มี status-icon column — เลิกใช้ `.bl-icon` ตามคำขอ user**; `const ico` + `<span.bl-icon>` ลบออกจากทั้ง 2 ฟังก์ชัน แต่ map `STATUS_ICON` คงไว้ — ยังใช้ที่ vehicle status grid). `.bl-head` = `.bl-head-text`[`.bl-title`=`b.dest` + `.bl-desc`=`b.purpose`] + `.bl-due`[label "เวลาเดินทาง" + value `start–end`]; `.bl-foot` = `.bl-author`[`.bl-avatar` initial จาก `b.booker` + `.bl-author-text` "จองโดย <strong>booker</strong> · dept"] + `.bl-foot-right`[`.bl-foot-pax` + `.bl-status` badge + `.bl-actions`]; approved row +`.bl-card--done`; group row = checkbox-slot + `.bl-body` + `.bl-grp-collapse` เป็น 3 sibling ใน `.bl-card` (flex-wrap ดัน collapse ลงเต็มแถวล่าง). ลบ markup เดิม `.bl-content/.bl-title-row/.bl-meta/.bl-meta-text/.bl-right`. `buildRowActions()` ไม่เปลี่ยน. **Phase 11.1 (2026-06-06):** ลบ collapsed-state logic ใน `renderBefore()` (block `shouldCollapse && !beforeExpanded`) + ลบ `let beforeExpanded`/`function toggleBeforeExpand()`/`function isPastOrToday()` (dead) + ลบ `toggleBeforeExpand` จาก window export + ลบ `beforeExpanded = ...` ใน `renderAll()` — booking list render เสมอ. **Phase 14 (2026-06-07):** เพิ่ม helper `budgetLabel(b)` → "ส่วนกอง-{expSub}"/"ส่วนกลาง-{expSub}"/"ส่วนตัว"/null; rewrite `renderSingleRow()` เป็น 3-line — `.bl-head`(`.bl-title`=ชื่อผู้จอง + status badge) + `.bl-trip`(purpose→dest + `arrow-right` icon) + `.bl-foot`(`.bl-badges`[pax neutral / งบ neutral, ยังไม่จัดสรร→warning / `.bl-badge-adhoc` ถ้า `b.isAdHoc`] | `.bl-actions`); เลิกใช้ `.bl-head-text/.bl-desc/.bl-due/.bl-author/.bl-avatar/.bl-foot-pax` ใน single (group ยังใช้). `buildRowActions()` คงเดิม. **Phase 14.1 (2026-06-07):** single เพิ่ม `timeBadge` (lucide clock, ช่วงเวลา start–end) เป็น badge แรกใน `.bl-badges` (ก่อน pax); group `renderGroupRow()` rewrite เป็น 3-line คล้าย single — `.bl-head`(`.bl-title`=vLabel รถ + `titleBadge` "N งานรวม") + `.bl-trip`(`.bl-trip-dest`="N งานในเที่ยวเดียว") + `.bl-foot`(`.bl-badges`[`grpTimeBadge` clock + `grpPaxBadge` users] | `.bl-actions` ungroup/edit/chevron) + `.bl-grp-collapse`; group เลิกใช้ `.bl-head-text/.bl-desc/.bl-due/.bl-foot-right`. **Phase 15 (2026-06-07):** เพิ่ม helper `tripBudgetTag(expType)` → "หักงบส่วนกลาง"/"หักงบส่วนกอง"/"เรียกเก็บ"(personal); `renderTripRow()` เอา `.va-trip-amount` ออกจาก head → เพิ่ม `.va-trip-badges` row = fuelBadge (icon fuel + `fmtBaht(total)` + tag; personal→`vc-badge-warning` else neutral) + otBadge (icon clock + "OT +N ชม. · ฿X (tag)"; แสดงเฉพาะ `otAmount>0`; personal→warning else `.va-trip-badge--ot`). **Phase 16 inline calendar (2026-06-08):** เพิ่ม state `calCursor` (เดือนที่ปฏิทินแสดง) + helper `renderCalendar()` (วาด month grid Sunday-first; highlight selDate/today; dot นับ bookings ต่อวัน) + `shiftCalMonth(dir)`; rewrite `openWeekPicker()` (set calCursor=เดือนของ selDate → renderCalendar → show); rewrite `bindWeekControls()` — ลบ `#weekPickerInput` change handler → bind `#calPrevBtn`/`#calNextBtn` (shiftCalMonth) + event delegation บน `#calDays` (คลิกวัน → set selDate + `_alignWeekStartTo` + close + renderAll คลิกเดียว). **2026-06-12:** `updateExpSubDropdown()` — approver row แสดงเสมอเมื่อ department เลือก: ถ้าไม่มี approver → โชว์ "ยังไม่ได้ตั้งผู้อนุมัติ" สี `var(--vc-warning)` + `display:flex` แทนซ่อน row (`display:none`). **2026-06-21:** (1) `openAdminBookingDetail` rewrite — แก้ bug IDs `single*` (ไม่มีใน DOM) → `detail*`; status pill `bk-detail-status--${cssKey}`; `detailMembersList` เติม booker/purpose/dest/pickup rows; `detailActions` ปุ่ม "แก้ไข" → `openAdminEdit(id)` ถ้า canEdit; เพิ่ม const `STATUS_CSS_KEY` + `_adminEditId`; (2) ฟังก์ชันใหม่ `openAdminEdit(id)` / `cancelAdminEdit()` / `saveAdminEdit()` (AJAX → `b.editUrl` → `patchBooking`+`renderAll`+reopen); expose ทั้ง 3 ใน `Object.assign(window, ...)`

### `vehicle/js/vehicle_mileage.js`

**Phase 9.1 (2026-05-22):** เพิ่ม 3 IIFE สำหรับ toolbar pattern — (1) `bindStatusChips()`: click `.mlg-chip` → set `#statusFilter` value + `form.submit()` (auto-submit pattern); (2) `bindAdvToggle()`: `#advFilterBtn` toggle `[hidden]` บน `#advSheet` + sync `aria-expanded`; (3) `bindDatePreset()`: `#datePreset` change → set `date_start/date_end/show_all` ตาม preset (month=clear all, 7d/30d=ISO date today − N, all=show_all=1, custom=reveal `#dateRangeGroup`) + auto-submit ยกเว้น custom (รอ user กด "นำไปใช้"). Initial preset detect จาก current values (`show_all=1`→all, both dates empty→month, อื่น→custom). **Phase 16.3 (2026-06-08):** เพิ่ม IIFE `bindDateRangePickers()` — 2 calendar popover (start/end) แทน native date input; loop `#dateRangeGroup [data-datepick]`, render month grid (Sunday-first, TH เดือน/พ.ศ.), คลิกวัน → set hidden input + clear `showAllInput` + `form.submit()`; prev/next เลื่อนเดือน, click-outside + Esc ปิด (เปิดได้ทีละตัว). ใช้ `.va-cal*` class จาก vehicle_admin.css (CSS reuse, JS แยก scope). **Phase 16.4 AJAX filter (2026-06-08):** กรองไม่ reload — `runFilter(push=true)` fetch `buildFilterURL()` (GET, FormData→querystring ตัด empty) → `DOMParser` parse `#mlgResults` → swap innerHTML + `mlgInitIcons(results)` + `bindResults()` + `pushState` + `syncExportLink`; race guard ด้วย `_mlgReqToken` (ทิ้งผล stale); error → fallback `location.href`. Selection/summary refactor: `$checkAll/$modeAll/$modeSel/$strip` เป็น `let` + `bindResults()` (re-grab + rebind row-check/checkAll/row-click หลังทุก swap). chips/preset/datepicker เปลี่ยน `form.submit()` → `runFilter()`; chip set `is-active` เอง (ไม่ reload). `form` submit intercept (preventDefault→runFilter) สำหรับปุ่ม "นำไปใช้". `popstate` → `location.reload()` (server render ตาม URL). "ล้างทั้งหมด" ยังเป็น full nav (`<a href>`). **Phase 17 adv-filter popover (2026-06-08):** `bindAdvToggle()` rewrite — popover open/close ผ่าน `setOpen()` helper; `btn` click `stopPropagation` + toggle; เพิ่ม document click handler ปิดเมื่อคลิกนอก (`sheet.contains`/`btn.contains` = ไม่ปิด — รวม `.vc-dd` ข้างใน) + Esc close + return focus. `form` submit handler เพิ่มปิด `#advSheet` (set `hidden` + reset `aria-expanded`) ก่อน `runFilter()` — แก้ปัญหา popover ค้างหลังกด "นำไปใช้" (AJAX swap แค่ `#mlgResults` ซึ่งอยู่นอก popover). **Phase 17.1 (2026-06-08):** `bindBudgetFilter()` เพิ่ม ref `filterBudgetTypeWrap` + toggle class `mlg-adv-col-full` บนงบ wrap ใน `updateBudgetSubDropdown()` — ไม่มี sub (ว่าง/personal) → add (งบเต็ม 2col); central/department → remove (งบครึ่ง + sub ข้าง). **Sort (2026-06-19):** module-level `sortState {col, dir}` คงหลัง AJAX swap; `getSortValue(row, col)` numeric: booking/distance/cost/odo-start/odo-end/status(0/1/2) text: user/vehicle/destination; `applySortToTbody()` ซ่อน `.mlg-date-group` เมื่อ sort + re-order `.mlg-row` + re-apply `applySearch`; `updateSortIcons()` สลับ lucide chevrons-up-down/arrow-up/arrow-down + `aria-sort` + `mlgInitIcons(th)`; `bindSortHeaders()` 3-click cycle asc→desc→none; เรียกท้าย `bindResults()`. **reload-reset fix (2026-07-02):** `runFilter()` ตัด `history.pushState` ออก (เดิม `runFilter(push=true)` push query string ของ filter เข้า address bar ทุกครั้ง → manual reload (F5) โหลด URL เดิมที่มี query ค้าง → server reconstruct filter เดิมกลับมา แทนที่จะ reset); ลบ `popstate → location.reload()` listener ตามไปด้วย (dead code เพราะไม่มี pushState entry ให้ back/forward อีกแล้ว). คู่กับ `bb_slider` `autocomplete="off"` + `getAttribute('value')` pattern (ดู § Component library) — ทั้งหมดคือชุด fix เดียวกันสำหรับบั๊ก "filter ไม่ reset ตอน reload" ที่หน้านี้. ⚠️ **Phase ข้างบน (4.5–17.1) เป็น history ของไฟล์ก่อน UE redesign (`803d857`)** — ตอนนั้นใช้ `.mlg-*`/`#mlgResults`/`goEditEnd()`/`bindStatusChips()` ซึ่ง**ไม่มีในไฟล์ปัจจุบันแล้ว** (ปัจจุบันคือ `.bb-ml-*`/`#bbMlResults`/`bindStatusTabs()`/`tab2-tab`, ดู entry `vehicle_mileage.html` phase "UE-redesign Phase 1 (2026-07-11)" ด้านบนสำหรับจุดเปลี่ยน) — entry นี้ไม่ได้ sync ตามตอนนั้น ใช้อ้างอิง current state จากโค้ดจริงแทนถ้าจะแก้ต่อ. **Selection/KPI filter (2026-07-21):** เพิ่ม `bindKpiFilters()` (คลิก KPI card `[data-kpi-filter]` → `runFilter()`, reuse `bindStatusTabs()` pattern); แก้ `recalcSummary()` เดิมอ่าน native `cb.checked`/`cb.disabled`/`$checkAll.indeterminate` ผิด component (`bb-check-box` = span) → เปลี่ยนอ่าน `.classList.contains('is-on'/'is-disabled')` + `setCheck()`; เพิ่มเรียก `recalcSummary()` ท้าย checkAll/row-click handler ใน `bindResults()`; clearBtn reset `pending_personal` เพิ่ม — รายละเอียดเต็ม ดู entry `vehicle_mileage.html` ด้านบน. **Case 17 merge (2026-07-22):** `openMileage()` state `'none'` เปิด `mmOdoEnd` ให้กรอกพร้อม `mmOdoStart` ได้เลย (เดิม disabled จนกว่าจะมี odo_start อยู่แล้ว); เพิ่ม `renderOtStop()`/`renderCostStop()` (+ helper `hmToMin()`/`buildOtBarSegments()`) สร้าง OT rate lines ต่อ slot + เส้น bar แบบ segment ตาม `data-ot-slots`/`data-actual-start`/`data-actual-end` เรียกท้าย `openMileage()` ทุกครั้ง (ซ่อนเองถ้า state ยังไม่ complete หรือไม่มี OT slot เลย). submit handler เพิ่ม logic ยกระดับ `mmEntryType` จาก `'start'`→`'both'` ตอน submit ถ้า `mmOdoEnd` มีค่า (แล้ว validate end>start เหมือนเดิม). **ปรับรอบ 2 (2026-07-22):** `openMileage()` เลิกผูก `disabled`/`required` ของ `mmOdoStart`/`mmOdoEnd` กับ state — ทั้งสองช่องแก้ได้เสมอ (แก้ record ที่ปิดแล้วได้ด้วย ไม่ใช่แค่ตอนสร้างใหม่); submit handler เปลี่ยนมาตัดสินใจ `entry_type` จาก `hasStart`/`hasEnd` ตอน submit จริง (ไม่ใช่ state ตอนเปิด modal) + guard กันกรอกแค่เลขไมล์กลับทั้งที่ไม่มีเลขไมล์ออกเลย; เพิ่ม `timeOnly()` แยก HH:MM จาก `data-actual-start`/`-end` ที่เปลี่ยนเป็น datetime-local เต็มแล้ว (กันทับเวลาจริงด้วย `now()` ตอนแก้ไข) **กติกา OT + bar/legend (2026-07-27):** `SEG_STYLE` label เป็นไทย (`ช่วงที่คิด OT`/`ช่วงที่ไม่คิด OT`) · `renderOtBarLegend(segments, slots)` รับ `slots` เพิ่มเพื่อต่อท้ายช่วงเวลา OT จริงหลัง label (เวลาย้ายลงมาจากแถบด้านบน) · ป้ายบนแถบเหลือ `เริ่มทริป`/`จบทริป` 2 ตัวเท่านั้น · ใหม่ `renderOtMismatch(ds, slots, actualStart, actualEnd)` — toggle callout `#mmOtMismatch` เมื่อ `data-ot-mismatch="1"` (ช่วง OT หลุดกรอบเวลาทริป = ค่า OT คำนวณจากเวลาชุดเก่า). **Filter multi-select (2026-07-28):** `optRow()` (ตัว build option ของ cascade `budget_sub`) emit `type="checkbox"` แทน `radio` + ไม่มีแถว "ทั้งหมด" · `rebuildBudgetSub(types)` รับ **list** ของ budget_type ที่ติ๊ก (จาก `checkedBudgetTypes()`) แล้วรวมหมวดของ central+department ไว้ panel เดียว · `initialSubs` (จาก `window.BBML_FILTER_SUB`) เป็น list · listener `ue-chip-toggle:change` → `runFilter()` เหลือไว้ให้ chip "มี OT" ตัวเดียว

### `pages/fuel-admin.js`

**Toolbar iteration 2 (2026-06-17, ไฟล์จริง = `vehicle/js/vehicle_fuel.js`):** +`wireFilterPopover()` — ปุ่ม `#filterToggle` toggle `form.fuel-filter-panel#filterForm` (`position:fixed` คำนวณตำแหน่งจาก `getBoundingClientRect`; close on outside-click; reposition on scroll/resize); `wireFilterBar` เดิมคง (selects ใน popover auto-submit).

### `pages/notification.js`

**hybrid feed, 2026-06-15:** booking → group-by-booking (collapsed, click expand เป็น timeline `.notif-inner`) + non-booking → solo `data.items[]`, merge-sort by `ts_ms` ผ่าน `buildFeed`. `renderGroup`/`renderInnerNotif`/`toggleGroup`/`state.expanded` **ใช้งานจริง** — doc เก่าเขียนว่าลบเป็น flat feed = ผิด, แก้ 2026-06-15. `renderInnerNotif` = 2 บรรทัด: title (`n.title` จาก backend) + subtitle (`innerSubtitle()` ตัด prefix "คำขอ/ทริป #N" ออก) + time ขวา), 3 tabs all/unread/payment + sticky payment, toast desktop only (event สำคัญ + ภายใน 45s). Admin/approver เห็นเฉพาะ notification ของตัวเอง (ไม่มี synthetic group injection อีกแล้ว). **Title freeze (2026-06-16):** `n.title` มาจาก `Notification.title` (backend freeze ตอนสร้าง, fallback `_notif_title`); sticky payment card (`renderPaymentCard`) + toast ใช้ `n.title` เป็นหัวด้วย (fallback `booking_title`/`ntypeLabel`).

### `pages/ot-admin.js`

**Phase 5.10 (2026-05-18):** เพิ่ม `buildRateRow()` builder สำหรับ rate config modal (`#rateConfigContainer` + `#addRateBtn`) ที่ include `cfg_day[]` `<select>` (ทุกวัน + TH_DAYS 7 ตัว, default ""=NULL) + delegated click handler `.js-rate-remove` (existing row [`data-cfg-id`] → confirm + append hidden `<input name="cfg_delete[]" value="...">` to form + grey out via `.is-removed`; new row → drop DOM); call `initIcons()` after append. **2026-06-09 (manual OT):** slot logic เป็น scope-based (`SLOT_SCOPES` edit/add: `recomputeScope(container)` + `addSlotRow(container, slot)`) ใช้ร่วม 2 modal; `openAddModal()` (reset + default วันนี้ + 1 ช่วงว่าง) ผูกปุ่ม `#addOtBtn`; `#addOtForm` submit AJAX → `CREATE_URL` (JSON, alert ถ้า fail) → `runFilter`; refreshData อ่าน `createUrl`. Port **va-cal datepicker IIFE** จาก `vehicle_budget.js` (`[data-datepick]` → `.va-cal` popover, hidden ISO input, sync label ตอน `shown.bs.modal`, `data-datepick-required` ตรวจตอน submit ใน capture phase → form handler เช็ก `e.defaultPrevented`). **2026-06-14 (filter งบ):** IIFE `bindBudgetFilter` — cascade `#filterBudgetType`→`#filterBudgetSub` (อ่าน `window.EXPENSE_CATS` + `window.COST_FILTER_SUB`, show/hide `#filterBudgetSubWrap`); change select ใน filterForm → `runFilter` เดิม → กรอง OT ตามงบ (derive จาก booking ผ่าน `_apply_budget_filter` ฝั่ง view). ไฟล์จริง = `vehicle/js/vehicle_ot.js`

### `pages/driver-home.js` *(ไฟล์จริง `vehicle/js/vehicle_driver.js`)*

**2026-06-09 (card redesign req1+2):** หน้านี้โหลด `core/js/dropdown.js` (classic, ต่อท้าย module) เพิ่ม → enhance `<select data-dropdown>` ใน `.driver-emergency` (เปลี่ยนรถ) เป็น vc-dd. **2026-06-09 (ad-hoc collapse req3):** modal open/close + combo logic เดิม → **collapse** — `openAdhoc/closeAdhoc` toggle `[data-adhoc-panel]` + reset form/upload/hint; `[data-adhoc-cancel]` หุบ; ad-hoc vehicle change (`[data-adhoc-vehicle]`) อัปเดต hint เลขไมล์ล่าสุด + `min` จาก `option[data-last-odo]`. _หมายเหตุ: เคยลอง auto-OCR (tesseract.js) แต่ตัดทิ้ง 2026-06-09 — tesseract default อ่านหน้าปัด 7-segment เพี้ยน; เก็บ upload รูป + กรอกเลขเอง. `[data-odo-input]` ยังใช้โดย ad-hoc vehicle hint (set `min`)._

### `pages/budget-admin.js`

**UX writer Phase 1 (2026-05-22):** `setBudgetModal` handler รับ edit-mode detection (`isEdit = !!(deptName && amount)`) → swap title (`แก้เพดาน<group> — <dept>` vs `ตั้ง<group>ใหม่`), notice text (`#sbNoticeText`), submit text (`#sbSubmitText` = `อัปเดตเพดาน` vs `บันทึกงบ`). เพิ่ม document-level `submit` listener สำหรับ `form[data-confirm-toggle]` → `confirm()` dialog ก่อน toggle_active POST (message ใช้ `data-dept-name`/`data-to-active`, ปิดงบเตือน "block booking ใหม่"). **Phase 2E round 2 (2026-05-22):** เพิ่ม submit listener `form[data-confirm-pay]` (personal mileage รับเงิน) → confirm() แสดงชื่อผู้จอง+amount → AJAX fetch (route return JSON, ใช้ form POST ตรงๆ จะได้ raw JSON page) → location.reload(); btn loader state (`กำลังบันทึก...`) + restore on error. **Phase 3E round 3 (2026-05-22):** เพิ่ม click listener `[data-personal-filter]` (segmented pill tabs ใน sectionPersonal) → toggle `.is-active` + set `hidden` ตาม `data-personal-row` (paid/unpaid/all). Client-side filter (ไม่ reload) เพราะ data set เล็ก. **2026-06-06 (งบช่วงเวลา):** + `wireModal('extendBudgetModal', {budget_id:'bid', dept_label:'dept', start_date:'start', end_date:'end'})` — pre-fill modal "นำงบจากคลังกลับมาใช้".

### `pages/room.js`

**2026-06-15:** เพิ่ม mobile month nav listeners (`prevMonthBtnMobile`/`nextMonthBtnMobile`/`todayBtnMobile`); `renderCalendar()` sync `#currentMonthLabelMobile`; `updateMobileList()` update `#mobileDaybarCount` (N รายการ หรือว่างถ้า 0).

### `pages/manage-fleet.js` (ไฟล์จริง `vehicle_fleet.js`)

**เพิ่ม (2026-07-29/30/31):** `bindFleetTabs()` (tab รถ/คนขับ) · `bindFleetCapacityStepper()` (`#addVehicleModal`'s ที่นั่ง stepper — sync `#fleetCapacityInput` hidden field ด้วย ไม่ใช่แค่ text, กัน `capacity` เป็น `None` ตอน submit) · `driverRowOf()` query `.fleet-driver-row` (ไม่ใช่ `.mf-driver-row` เดิม — เปลี่ยนหลังพบว่า `.mf-driver-row` เป็น `display:flex` ใน CSS จะพัง table layout ถ้าใส่บน `<tr>` จริง).
