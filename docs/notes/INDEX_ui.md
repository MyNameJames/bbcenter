# INDEX — Templates + Design System

> Part ของ INDEX.md แยก เพื่อ token budget — [กลับ hub](INDEX.md)
> **อัปเดตล่าสุด:** 2026-06-17

---

## 🎨 Templates

> **2026-06-07 (ขั้น 4):** partials กลางย้าย → `templates/_shared/` (sidebar/header/navbar/notification_panel/notification_toast). modal instances → `<domain>/modals/` + prefix ชื่อ: `vehicle/modals/vehicle_*.html` · `room/modals/room_*.html` · `vehicle/admin/modals/fuel_*.html`. ชื่อ/path `*-modal-*.html` ในตารางด้านล่าง = **ชื่อเก่า** → map: `vehicle-modal-detail` = `vehicle/modals/vehicle_detail.html` (ตัด `vehicle-modal-` ออก, prefix `vehicle_`); เช่นเดียวกับ room/fuel. `_components/` (macro) **ไม่ย้าย**

**Shared partials** (include ทุกหน้า):
| Partial | File |
|---------|------|
| `_shared/sidebar.html` | [app/templates/_shared/sidebar.html](../../app/templates/_shared/sidebar.html) — `active_menu` keys: `dashboard` `vehicle` `repair` `room` `admin` `mileage` `fleet` `cost` `budget` `fuel` `approver` (`history` ลบ 2026-06-07). **Icons: Lucide** (`data-lucide="..."`) — load via `_header.html`. **Collapsible admin group** (2026-05-23): admin/mileage/fleet/budget/cost/fuel nested under "จัดการรถ" parent (`.sb-group` + `.sb-group-toggle` + `.sb-item--sub`); grid-row height anim + stagger reveal; localStorage `bb-sb-groups` persists open state; force-open when `active_menu` ∈ group; dot indicator on parent icon when collapsed + pending/active-child. Inline `<script>` at end of file (sidebar-scoped). **Bullet dot restyle (2026-06-15):** เพิ่ม `<span class="sb-dot">` ใน `.sb-item--sub` ทุก 6 รายการ; ลบ `.ps-1 .pt-1` จาก `.sb-label`. |
| `_shared/header.html` | [app/templates/_shared/header.html](../../app/templates/_shared/header.html) — **Vercel refresh Phase 6.0** (2026-05-20): `top-header container-xxl` → `.vrc-topbar` (56px sticky); optional Jinja vars `page_section` + `page_title` render breadcrumb (`<section> / <title>`); hamburger/notif/user buttons เหลือ `.ds-btn-icon` (no `bg-white`/`rounded-3` — restyled ผ่าน descendant rule ใน `vercel.css`); load `vercel.css` ก่อน `notification.css`. **Icons → Lucide**: hamburger `bi-list` → `menu`, user button + dropdown avatar `fa-user` → `user`, email row `bi-envelope` → `mail`, bell `fa-bell` → `bell` (ใน `_notification_panel.html`). FA icons ใน dropdown panel ภายในยังคง (ไม่ใช่ส่วน header bar visible) **DNA 2026-06-17:** breadcrumb (`<section> / <title>`) → `<h1 class="vrc-topbar-title">{{ page_title or page_section }}</h1>` หัวเรื่องใหญ่ใน topbar แบบ Zendenta (กระทบทุกหน้า — ทุกหน้าส่ง `page_title` ชัดเจนอยู่แล้ว); + โหลด **Manrope** global ต่อท้าย Sarabun (`--vc-font-mono` / `.vc-mono` ได้ Manrope จริงทุกหน้า) |
| `_shared/notification_panel.html` | [app/templates/_shared/notification_panel.html](../../app/templates/_shared/notification_panel.html) |
| `_shared/notification_toast.html` | [app/templates/_shared/notification_toast.html](../../app/templates/_shared/notification_toast.html) |
| `_shared/navbar.html` | [app/templates/_shared/navbar.html](../../app/templates/_shared/navbar.html) — legacy navbar (4 includes) |
| `_components/*.html` | Reusable Jinja macros (Phase 2 + 3) — see § Design System > Component library. Files: `kpi.html` (`kpi_cell` accepts `icon_kind='lucide'\|'fa'`, default lucide), `filter_bar.html`, `badge.html` (`badge(text, tone, dot=False, icon='', size='')`, lucide), `pill.html`, `empty_state.html` (`empty_state(title, desc, icon, compact)`, lucide, `{% call %}` for CTA), `form_group.html`, `table_shell.html`, `_modal.html`. Phase 3 aligned to flat vocab matching `/bbcenter-design` skill. |

**Dashboard templates:**
| File | ใช้สำหรับ |
|------|----------|
| `auth/login.html` | **หน้า login** (2026-06-16 redesign → Sneat-style card): standalone page (ไม่มี shell), card กลางจอ พื้น `--vc-bg-subtle` + dot decoration มุม. Brand = Lucide `building-2` SVG + คำ `bbcenter` สี `--vc-accent`. subtitle "เข้าสู่ระบบด้วยรหัสหน้าแดง". Form POST `/login` (`username`/`password` + `remember` checkbox), password toggle (eye/eye-off SVG). **ไม่มี create account**. โหลด `tokens.css` + `auth/css/login.css` + `auth/js/login.js` (เลิกใช้ main.css/main.js/bootstrap/select2/tilt). ทั้งหมด `--vc-*` tokens, no shadow→border |
| `core/line_link.html` | **ผูกบัญชี LINE** (2026-06-12) — หน้า `/line/link` (`core_bp.line_link`): vc-scope shell (sidebar + header) แสดงโค้ด 6 หลัก (`User.line_link_code`) + 3 ขั้นตอนแอด Official Account; state `is_linked` → การ์ดยืนยัน. Bootstrap util + vc-* tokens (ไม่มี CSS แยก) |
| `dashboard/dashboard.html` | landing page หลัง login — **Action Hub** (2026-06-16 redesign จาก KPI/recent dashboard): ทุก role เห็นหน้าเดียวกัน. โครง: `dash-header` greeting + live clock → **เริ่มงานใหม่** Quick Actions 4 `dash-svc` card (link `<svc>.index?new=1`) → **วันนี้ของฉัน** timeline `dash-today` (ถ้ามี `today_items`) → **คำขอของฉัน** `vc-table` รวมทุก service + สถานะ `vc-badge` + ปุ่ม **ทำซ้ำ** (`repeat_url`=`?copy_from=<id>`) / `empty_state` ถ้าไม่มี → superadmin banner (`dash-superadmin`). View `auth.dashboard()` ส่ง `my_requests` + `today_items` (เลิกส่ง stats/recent_*; **ลบ admin KPI strip / alert / dash-mine ออก**). Loads `dashboard.css` + `dashboard/js/dashboard.js`. Animation: stagger fade-up `dash-fade.d-{1..5}`. **dead CSS:** `dash-kpi*`/`dash-alert`/`dash-mine*` ใน dashboard.css ยังไม่ลบ (orphan) |

**Repair templates:**
| File | ใช้สำหรับ |
|------|----------|
| `repair/repair.html` | ระบบแจ้งซ่อมไอที — **vc-scope shell** (2026-05-13 redesign): `repair-header` pattern, 5-cell KPI (admin only), `vc-table` + `vc-card-head`, DataTable (langUrl via data attr), 3 modals (repairFormModal/acceptModal/closeModal), `vc-badge`/`vc-btn`, Lucide icons. Loads `repair.css` + `pages/repair.js` (Phase 4.2 — `type="module"`). **Phase 3.4 (2026-05-15):** empty state migrated → `{% call empty_state(icon='wrench') %}` macro with CTA button caller; ลบ inline `style="width:20px;height:20px;"`. **Phase 4.2 (2026-05-15):** script → ES module `pages/repair.js`. **Redesign (2026-06-16):** header → budget style (`<h1 class="fw-bold text-accent pt-3">` + `<h6 class="text-muted text-header">` แทน `.repair-title/.repair-subtitle`); custom DataTables pagination แบบ pill วงกลม สี accent + go-to-page (สร้างใน repair.js). |
| `maintenance/maintenance.html` | แจ้งซ่อมอาคารสถานที่ — **vc-scope shell** (2026-05-17 redesign): `maintenance-header` pattern, 5-cell KPI (admin only), `vc-table` + DataTable (langUrl via `#pageData` data attr), 4 modals (maintenanceFormModal/acceptModal/closeModal/exportModal), ฟอร์มแจ้งซ่อม → modal (create + edit mode, auto-open เมื่อ `edit_ticket`), `vc-badge`/`vc-btn`, Lucide icons. Loads `maintenance.css` + `pages/maintenance.js` (legacy script — jQuery+DataTable ยังใช้ global). **Redesign (2026-06-16):** header → budget style (`<h1 class="fw-bold text-accent pt-3">` + `<h6 class="text-muted text-header">`); custom DataTables pagination pill วงกลม สี accent + go-to-page (mirror repair). |

**Room templates:**
| File | ใช้สำหรับ |
|------|----------|
| `room/room.html` | ปฏิทินจองห้องประชุม — **vc-scope shell** (2026-05-17 redesign): sidebar + `_header.html` + `room-header` pattern, custom calendar grid (reuse `vehicle.css` calendar styles) + mobile list section (เหมือน `vehicle/vehicle.html`); ลบ FullCalendar + DataTables; แทน room badges สี เล็ก=blue / ใหญ่=amber ผ่าน tokens; data injection (`window.BOOKINGS`/`window.ROOM_CHOICES`); 3 modals: book/edit/detail. Loads `room.css` + `pages/room.js` (ES module). **Zendenta-align redesign (2026-06-15):** structure เหมือน `vehicle/vehicle.html` — `va-page-header` → **`fuel-header`** (desktop title+subtitle ซ้าย, room legend ขวา); toolbar → **`cal-toolbar d-none d-md-grid`** + `cal-toolbar-left` (วันนี้/prev/label/next); เพิ่ม **`vrc-m-hero d-md-none`** block (mobile: title + sub-line + month picker IDs: `currentMonthLabelMobile`/`prevMonthBtnMobile`/`nextMonthBtnMobile`/`todayBtnMobile`); calendar card `vc-card p-3` + `card-header`/`card-body` → **`vc-card overflow-hidden`** + `calendar-grid-head text-header border-bottom`; day headers เพิ่ม `d-none d-md-inline` ชื่อเต็ม + `d-md-none` ย่อ; `.mobile-list-header` → **`vrc-m-daybar`** + `vrc-m-daybar-text` ห่อ `#mobileListDateLabel` + `#mobileDaybarCount`; mobile book button เปลี่ยนจาก `data-bs-toggle` → `onclick="openBookingModal()"`. |
| `room/room-modal-book.html` | `#bookingModal` — เลือกห้อง/หัวข้อ/วันที่/ช่วงเวลา + duration preview |
| `room/room-modal-edit.html` | `#editBookingModal` — แก้ไขห้อง/หัวข้อ/วันเวลา (flatpickr) |
| `room/room-modal-detail.html` | `#eventDetailModal` — ดูรายละเอียด + แก้ไข/ยกเลิก (เฉพาะ owner) |

**Vehicle templates:**
| File | ใช้สำหรับ |
|------|----------|
| `vehicle/vehicle.html` | หน้าจองหลัก. **Phase 4.7 (2026-05-15):** script → ES module `<script type="module" src="js/pages/vehicle.js">`; ลบ inline `<script>` pay-deeplink IIFE (ย้ายเข้า module). **Phase 1 user-facing redesign (2026-05-17):** เริ่ม migrate ไป BBCenter V2 — viewport `maximum-scale=1.0`; ลบ DataTables CSS + IBM Plex Sans Thai; เพิ่ม `design-system.css` + `vehicle_admin.css` ใน link order; `<main>` ใส่ `vc-scope`; flash `alert alert-{cat}` → `ds-alert` ใน `vc-stack`; toolbar 3 ปุ่ม (prev/next/today) → `vc-btn vc-btn-ghost vc-btn-icon` + Lucide chevron-left/right + `vc-btn-secondary` "วันนี้"; calendar wrapper `card h-100` → `vc-card`; mobile "จองรถ" `btn btn-dark` → `vc-btn-primary` + Lucide plus. **Phase 2 (2026-05-17):** modals 4 ไฟล์ + duration preview เสร็จ (ดู modal rows ด้านล่าง). **Phase 3 (2026-05-17):** ds-status-dot/STATUS_ICON refactor เสร็จ — ดู `design-system.css` §7 + `pages/vehicle.js` row ด้านล่าง. **Phase 4 (2026-05-17):** เสร็จครบ — token cleanup + Lucide migration (~17 icons) + UX polish: calendar `role="grid"` + columnheader/gridcell ARIA + `aria-label` Buddhist year + `aria-current="date"`; keyboard nav (← → / T) ใน vehicle.js (skip input/modal); popover `shown.bs.popover` listener + `initIcons`. **Phase 9 (2026-05-22):** BOOKINGS data รับ `cancelUrl` + `canCancel`. **2026-06-12:** `canCancel = (owner AND status ∈ {pending, waiting_approver}) OR (admin AND status ∈ {pending, waiting_approver, approved}); AND start_datetime > now`. `index()` ส่ง `now`/`is_vehicle_admin` kwargs เพิ่ม. **Mobile Phase A+B (2026-05-23):** mobile redesign (เป้าหมาย premium look ตาม ux-designer spec) — desktop toolbar 2 blocks (page title + month nav row) เพิ่ม `d-none d-md-flex` ซ่อนบน mobile; เพิ่ม `.vrc-m-hero d-md-none` block (mobile-only) — title 20px/600 + sub-line + month picker row พร้อม IDs ใหม่ `currentMonthLabelMobile`/`prevMonthBtnMobile`/`nextMonthBtnMobile`/`todayBtnMobile`; แทน `.mobile-list-header` ด้วย `.vrc-m-daybar` (sticky bar) — text container ใหม่ `.vrc-m-daybar-text` ห่อ `#mobileListDateLabel` + new `#mobileDaybarCount` (รายการ count); ปุ่ม `#mobileDateCountBtn` "จองรถ" ยังอยู่ในนั้น (จะย้ายเป็น FAB ใน Phase D). ปฏิทินยังเป็น `#calendarBody` ตัวเดียว — restyle ผ่าน CSS เท่านั้น. **Mobile Phase D (2026-05-23):** **ลบปุ่ม `#mobileDateCountBtn` "จองรถ" inline ออกจาก `.vrc-m-daybar`** (daybar เหลือแค่ label + count); เพิ่ม FAB markup ก่อน `</main>` — `<button class="vrc-m-fab d-md-none" id="mobileFabBook" data-bs-toggle="modal" data-bs-target="#bookingModal" aria-label="จองรถ">` + Lucide plus icon (round 56×56 icon-only ตามที่ user เลือก Q2). **OT warning data (2026-06-10):** `index()` ส่ง context `ot_rates` (= `OTRateConfig` active rows → list dict `{label,start,end,rate,dow}`) → inject `window.OT_RATES` (`| tojson`) สำหรับ booking modal คำนวณคำเตือนค่าล่วงเวลาสารถี (ดู modal row + `bk*` JS). **Hero banner (2026-06-13):** desktop title block (`.vrc-page-title/-subtitle` ใน `d-flex justify-between`) → `.vrc-hero d-none d-md-flex` (title `.vrc-hero-title` + sub `.vrc-hero-sub` ซ้าย, `<img.vrc-hero-img>` = `static/vehicle/img/vehicle.png` bleed ขอบขวา) บนพื้น indigo-soft — ลดความเป็นทางการ. CSS `.vrc-hero*` อยู่ใน `core/css/vercel.css` (ใต้ `.vrc-page-subtitle`). mobile ใช้ `.vrc-m-hero` เดิม (ไม่มีรูป) **Zendenta redesign → prod (2026-06-14):** ลบ `.vrc-hero` + custom toolbar → **fuel-header pattern** (`fuel-header`/`fuel-title`/`fuel-subtitle`/`fuel-header-actions` + ปุ่ม "จองรถ" `vc-btn-primary`) + **`.cal-toolbar`** (grid 1fr/auto/1fr: วันนี้ซ้าย · prev/`#currentMonthLabel`/next กลางเสมอ — `.cal-month-label`); weekday header ชื่อเต็มบน desktop (`<span class="d-none d-md-inline">อาทิตย์…` + `d-md-none` ย่อ); `<body class="zd">` + โหลด `vehicle_zendenta.css` (theme: icon ใหญ่ + calendar restyle "ภาพ 2"). IDs คงเดิม (`prevMonthBtn`/`nextMonthBtn`/`currentMonthLabel`/`todayBtn`/`bookingModal`). สีน้ำเงิน `#0046FF` จาก token global. **Mobile booking (2026-06-14):** ปุ่ม `#mobileDateCountBtn` "จองรถ" เปลี่ยนจาก `data-bs-toggle` → `onclick="openBookingModal()"` → modal prefill วันที่ = `selectedDate` (วันที่แตะในปฏิทิน) + เวลา default 08:00–17:00 (logic เดิมใน `openBookingModal()`). |
| `vehicle/vehicle_edit.html` | แก้ไข booking |
| `vehicle/vehicle_approver.html` *(เดิม approver_inbox.html)* | Approver inbox — budget card + 3 tabs (รออนุมัติ/อนุมัติแล้ว/ปฏิเสธ) + accordion cards + inline reject form. **Phase 3.8 (2026-05-15):** ลบ inline `<style>` (197 lines) → `vehicle_approver.css`; ลบ inline `<script>` → `vehicle_approver.js`; `.badge-waiting/.badge-approved/.badge-rejected` → `vc-badge vc-badge-warning/success/danger vc-badge-dot`; 3 empty states → `vc-empty/vc-empty-icon/vc-empty-title`; ลบ inline `style=""` ทุกจุด (dynamic width คง). **Phase 4.1 (2026-05-15):** script → ES module. **Phase 5.11 mobile redesign (2026-05-18):** restructure mobile-first — `.approver-wrap` max-width 560px center; pending header → status badge; tabs pill style; approve/reject เป็น 2-col `.ac-action-row` (`.btn-approve-action` primary + `.btn-reject-action` red outline) + reject form `.ac-reject-cancel`/`.btn-reject-confirm`. **Detail-card redesign + fuel badge (2026-06-10):** (1) เพิ่ม Jinja macro `th_date(dt)` → วันไทย "พ. 10 มิ.ย. 2569" (`%w` วัน + เดือนย่อ + ปี พ.ศ. `+543`); (2) เพิ่ม `.ac-fuel-badge` (amber, icon lucide `fuel` + `฿`) ใน `.ac-header-right` ทั้ง 3 tab — ค่าจาก dict `fuel_costs[b.id]` ที่ `approver_inbox()` คำนวณ (override `mileage.fuel_cost` ถ้ามี ไม่งั้น `(dist/fuel_rate)*fuel_price` ผ่าน `FuelPrice.get_for_date`; trip ที่ยังไม่ปิด=0); (3) **ทิ้ง `.ac-fields` 2-col grid** → `.ac-detail-card` layout (เลียน `driver-detail-card`): head=ผู้จอง+pax chip / `.ac-detail__datetime` (วันที่+เวลา, border-bottom) / trip=purpose→destination / pickup; tab ปฏิเสธ ย้ายเหตุผลเป็น `.ac-reject-note` (กล่องแดง). CSS เก่า `.ac-fields/.ac-field*/.ac-reject-reason` ลบทิ้ง. **Header slim (2026-06-12):** accordion header `.ac-ref` โชว์ `th_date(b.start_datetime)` (วันที่ไทย) แทน `trip_department/central_category`; ลบ `.ac-meta` badges row (driver + เวลา + booker) → แทนด้วย `.ac-detail__trip` row (icon clock + เวลา start–end + dot separator + icon map-pin + ปลายทาง) ใน header; ลบ `.ac-detail__datetime` block ออกจาก detail card (ซ้ำกับ header ใหม่); `.ac-detail__trip` ใน detail card ได้ `border-bottom pb-2 mb-2`. |
| `vehicle/admin/vehicle_mileage.html` | บันทึกเลขไมล์ (admin) — **vc-scope shell** (2026-05-13): `fuel-header` pattern, ~~4-cell KPI~~ (ลบ Phase 9.1), `mlg-toolbar` (toolbar pattern), checkbox summary strip (`vc-card`), `vc-table` + `vc-card-head`, lucide icons, `vc-badge`/`vc-btn` (ลบ breakdownPanel/monthPager แล้ว). Loads `fuel_admin.css` + `mileage_admin.css` (page-only row+modal styles). **Phase 3.3 (2026-05-14):** inline HEX `#DC2626` → `var(--vc-red)` (3 จุด), `var(--ds-accent)` → `var(--vc-fg)` (1 จุด). **Phase 4.5 (2026-05-15):** script → ES module `<script type="module" src="js/pages/mileage-admin.js">`. **Phase 9.1 mileage_admin redesign (2026-05-22):** Arto+ inspired filter toolbar — (1) ลบ KPI block ทั้งหมด (~60 บรรทัด: month_total_cost/total_budget/total_used/total_remaining/pending_personal_count cells) เพราะซ้ำ `budget_manage.html` + page เป็น task-oriented; เก็บแค่ `รอกรอก {N}` badge ใน `fuel-header`; (2) แทนที่ `vc-filter-bar mlg-filter-bar` (4 sections) ด้วย `mlg-toolbar` (single-row): **left** = `#datePreset` (เดือนนี้/7วัน/30วัน/ทั้งหมด/กำหนดเอง) + `#dateRangeGroup` (date_start, date_end, hidden ยกเว้น custom) + hidden `show_all`; **center** = `.mlg-status-chips` (segmented control 4 ปุ่ม: ทั้งหมด/รอกรอก/รอกลับ/ครบ → click = set `#statusFilter` + submit); **right** = `#exportLink` + `#advFilterBtn` (toggle `#advSheet`, `is-active` + dot เมื่อมี filter); (3) `.mlg-adv-sheet` (collapsible, 3-col grid) = รถ/คนขับ/ผู้จอง/งบ/หมวด-กอง/ค่าน้ำมัน-min-max (`mlg-range-pair`) + actions row "ล้างทั้งหมด" + "นำไปใช้". IDs ที่ JS contract ต้องคง: `filterForm`, `filterBudgetType`, `filterBudgetSub`, `filterBudgetSubWrap`, `exportLink` (เดิม) + `datePreset`, `dateRangeGroup`, `showAllInput`, `statusFilter`, `advFilterBtn`, `advSheet` (ใหม่). **Phase 5.9 (2026-05-17):** ~~จัด layout `vc-filter-bar` 4 หมวด~~ (ถูก replace โดย Phase 9.1). **Phase 5.8 (2026-05-17):** เพิ่ม 2 filter ใน `vc-filter-bar` — (1) "ผู้จอง" `<input list="bookersList">` + `<datalist>` (autocomplete native, server filter ilike); (2) "งบ" + "หมวด/กอง" — chained dropdown (type=ส่วนกลาง/ส่วนกอง/ส่วนตัว; sub โผล่เฉพาะ central/department, ซ่อนเมื่อ personal/ว่าง); JS rebuild sub options ด้วย `innerHTML` จาก `window.EXPENSE_CATS` + `window.MLG_FILTER_SUB` (pattern ลอกมาจาก `updateExpSubDropdown` ใน vehicle-admin.js). **Phase 5.7 (2026-05-17):** redesigned `<vc-table>` — 14 columns (Booking/งบ/ผู้จอง/ทะเบียน/คนขับ/เติมน้ำมัน/ปลายทาง/ไมล์ออก/ไมล์กลับ/ระยะ/ค่าน้ำมัน/สถานะ/action); column "งบ" แสดง type (`ส่วนกลาง/ส่วนกอง/ส่วนตัว`) + sub (หมวด/แผนก) — personal ไม่มี sub; column "เติมน้ำมัน" `vc-badge-blue` ถ้า FuelBill mileage อยู่ใน [odo_start, odo_end] ของ trip (link ไป admin_fuel); ทะเบียน=license_plate only, คนขับ=ชื่อแรก only; loop `display_rows` แทน `rows` → trip รวม (`trip_group` เดียวกัน) เหลือ 1 row + badge `+N` หลัง BK-id + ผู้จองแสดง "งานร่วม N รายการ" (representative=ตัวแรก). **Phase 5.6 (2026-05-17):** `#summaryStrip` redesigned for mobile — ลบ Bootstrap `row`/`col-12` + `d-flex` บน card, ลบ inline `style=""` ทั้ง block; ใช้ `.mlg-summary-strip/-mode/-mode--hidden/-item/-label/-icon/-clear` (ใน `mileage_admin.css`); ใน mobile ≤767.98px → items stack 100% width + ซ่อน `.vc-dot-sep` + clear button full width. IDs/JS contract คงเดิม (`#modeAll`/`#modeSelected`/`#sumAllDistance`/`#sumAllCost`/`#selCount`/`#selDistance`/`#selCost`/`clearSelection()`). **2026-06-08 (cmdk menus):** `#datePreset` `<select>` ได้ `data-dropdown` + ห่อ `<optgroup presets>`/`<optgroup custom>` (divider คั่นก่อน "กำหนดเอง") → enhance เป็น `.vc-dd` (native select คงเป็น source of truth, `bindDatePreset` ไม่แตะ); `booker_q` `<input list=bookersList>` ได้ `data-autocomplete` → enhance เป็น `.vc-ac` searchable. โหลด `core/js/dropdown.js` (module) **ต่อท้าย** `vehicle_mileage.js`. Component → Design System > Component library. **2026-06-08 (b):** ขยาย `data-dropdown` ครบทุก `<select.vc-filter-select>` ใน adv-sheet — `vehicle_id`/`driver_id`/`budget_type`/`budget_sub` (native select ยัง submit ได้แม้ `display:none`; ไม่ auto-submit แค่ set value รอ "นำไปใช้"). `budget_sub` chained → dd มี MutationObserver(childList) rebuild panel เมื่อ `bindBudgetFilter` swap innerHTML. `.mlg-range-pair` (cost_min/max `type=number`) ซ่อน spinner (`appearance:textfield` + webkit spin-button none ใน `vehicle_mileage.css`) เหมือน text แต่ numeric-only. หมายเหตุ: date_start/end เป็น custom datepicker (`mlg-datepick`/`va-cal`) ไม่ใช่ dd. **Phase 16.3 mileage datepicker (2026-06-08):** แทน 2 native `<input type="date">` ใน `#dateRangeGroup` ด้วย 2 calendar popover (reuse `.va-cal*` จาก vehicle_admin.css). แต่ละ picker = `.mlg-datepick[data-datepick]` > ปุ่ม `.mlg-date-btn[data-datepick-btn]` (icon + `[data-datepick-label]`) + hidden `input[name=date_start|date_end][data-datepick-input]` + `.va-cal[data-datepick-pop]` (`[data-cal-prev/-title/-next/-dow/-days]`). คลิกวัน → set hidden input + `form.submit()`. JS = IIFE `bindDateRangePickers()` ใน vehicle_mileage.js; hidden input คงชื่อเดิม → `bindDatePreset` (query `input[name=date_start]`) ใช้ได้ปกติ. **Phase 16.4 AJAX filter (2026-06-08):** กรองไม่ reload หน้า — ห่อ summary strip + table card ด้วย `<div id="mlgResults">` (AJAX swap target). ทุก filter (date pick/chips/preset/adv "นำไปใช้") → `runFilter()` fetch URL เดิม (GET) → parse `#mlgResults` จาก response → swap innerHTML + `bindResults()` + re-init lucide + `history.pushState`. Backend ไม่ต้องแก้. **Phase 17 adv-filter popover (2026-06-08):** `#advSheet` จาก inline collapsible Row 2 → **dropdown popover** anchored ใต้ `#advFilterBtn` — ห่อปุ่ม+sheet ใน `<div class="mlg-adv">` (relative), sheet ได้ `role="dialog"` + `aria-label` + `hidden` เริ่มเสมอ (ไม่เปิดค้างแม้มี filter), ปุ่มได้ `aria-haspopup="dialog"`; dot indicator (`.mlg-adv-dot`) เปลี่ยนเป็นสีแดง `--vc-red`; grid จัดใหม่ 3-col→**2-col** + group "ค่าน้ำมัน" (`mlg-range-pair`) ย้ายขึ้นบนสุด span เต็มแถว (`.mlg-adv-col-full`). IDs/contract เดิมคง (`advSheet`/`advFilterBtn`/`filterForm` ฯลฯ). **Phase 17.1 budget/booker layout (2026-06-08):** ลำดับ grid = ค่าน้ำมัน(full) → รถ│คนขับ → **ผู้จอง(full, ย้ายเข้า grid ก่อนงบ — เดิมอยู่นอก grid ใน `.mlg-adv` ที่ใช้ผิด)** → งบ → หมวด/กอง. งบ wrap ได้ `id="filterBudgetTypeWrap"` + dynamic col-span: budget_type ว่าง/personal → งบ `.mlg-adv-col-full` (เต็ม 2col) + `#filterBudgetSubWrap` `display:none`; central/department → งบครึ่ง + sub ข้างๆ. server render default class/display ตาม `f.budget_type`; JS toggle runtime (ดู `bindBudgetFilter`). |
| `vehicle/vehicle_driver.html` *(เดิม driver_home.html)* | หน้าคนขับ — **Vercel namespace** (2026-05-08, rev2): `<body class="vc-scope">` + lucide icons only (no Font Awesome). Header + segmented tabs (วันนี้/พรุ่งนี้, no "ย้อนหลัง"), accordion cards (`[data-card]` open one→close others within active panel), inline mileage form (no modal), upload zone (no separate camera button), CTA black `--vc-primary`. Tomorrow tab read-only with "เริ่มงานได้ในวันที่ …" note. Refuel UI removed. **Phase 3.5 (2026-05-15):** 2 empty states → `{{ empty_state(...) }}` macro; ลบ inline `style=""` ทั้งหมด (flash alert → `driver-flash-alert`, plate span → `driver-plate`, unset span → `driver-unset`). **Phase 4.4 (2026-05-15):** ลบ inline `<script>` (67 lines) + duplicate lucide CDN → ES module `<script type="module" src="js/pages/driver-home.js">`; lucide CDN+init มาจาก `_header.html` แล้ว. **Card redesign req1+2 (2026-06-09):** card head reorder = `ทะเบียน · เวลา · [pill]` (`.driver-card__plate`/`__time` แทน `__id`/`__summary`, ตัด destination); body: (1) emergency เปลี่ยนรถก่อนออก — `.driver-emergency` form (label แดง) + `<select data-dropdown>` (vc-dd) → POST `/driver/change-vehicle`, render เฉพาะ state waiting; (2) `.driver-detail-card` (white `--vc-bg-subtle`, no border) = ผู้จอง+`passenger_count` มุมขวา / `purpose`→`destination` / `pickup_location`; (3) ฟอร์มไมล์ออก reorder = title+`latest_odo` hint → upload (`.driver-upload--wide`) → `odometer_start` input (`min=latest_odo`) → submit. ทั้ง today+tomorrow tab ใช้ detail-card. โหลด `core/css/components/dropdown.css` + `core/js/dropdown.js` (classic, ต่อท้าย) สำหรับ vc-dd. **Ad-hoc collapse + OCR req3 (2026-06-09):** ปุ่ม "งานนอกระบบ" จาก modal → **collapse panel** (`.driver-adhoc-panel[data-adhoc-panel]`, toggle ปุ่ม `[data-adhoc-toggle]`, icon หมุน 45°): ผู้จอง `<select data-autocomplete>` (vc-ac autocompleteselect — strict, เก็บ user id) + รถ `<select data-dropdown>` (vc-dd) + `purpose` + `destination` + บล็อกไมล์ออก (upload wide + `odometer_start`); POST `/driver/ad-hoc-trip` (สร้าง booking + VehicleMileage start ทันที). ลบ modal+combo เดิมหมด. ทุก input เลขไมล์ได้ `data-odo-input` (ใช้โดย ad-hoc vehicle hint set `min`). _auto-OCR ตัดทิ้ง 2026-06-09 (tesseract อ่าน 7-seg เพี้ยน) — เก็บ upload + กรอกเอง._ CSS dead ที่เหลือ: `.driver-modal*`/`.driver-combo*` (เหลือ `.driver-modal__btn--ghost` reused). |
| `vehicle/modals/vehicle_book.html` *(เดิม vehicle-modal-book.html, ย้าย→modals/ ขั้น 4 2026-06-07)* | `#bookingModal`. **Phase 3.6 (2026-05-15):** ลบ `badge-pending` ออกจาก info card → ใช้ `.bk-info-note` แทน. **Phase 2 user-facing redesign (2026-05-17):** 7 `bi-*` + `bi-arrow-right-circle-fill` → Lucide (folder-plus/check-circle-2/arrow-right/info); footer 2 ปุ่ม `btn-secondary/btn-dark rounded-2` → `vc-btn vc-btn-secondary/vc-btn-primary` + `title=""`; เพิ่ม `<div id="bk_duration_preview" class="bk-duration-preview" aria-live="polite">` ใต้ช่วงเวลา (JS compute end−start). **Date/time/OT redesign (2026-06-10):** (1) `<input type=date#bk_date>` → ปฏิทิน va-cal คลิกเดียว — ปุ่ม `#bk_datepick_btn` (โชว์ "อา. 7 มิ.ย. 2569") + popover `#bk_cal_pop` (reuse `.va-cal*` + class override `.bk-cal` span เต็ม field) `[#bk_cal_prev/-title/-next/-dow/-days]` + hidden `#bk_date` (validate เองใน submit, ไม่ใช้ `required`); (2) `<input type=time>` 2 ช่อง → custom time picker `.bk-timepick[data-target][data-default]` (ปุ่ม + popover list `00:00–23:30` step 30 นาที, ไม่มี AM/PM) + hidden `#bk_start_time`/`#bk_end_time`; default 08:00–17:00 reset ทุกครั้งที่เปิด modal; (3) warning `#bk_ot_warn` (`.bk-ot-warn` amber) โชว์เมื่อเลือกวันอาทิตย์ (300/วัน) หรือเวลานอก 08:00–17:00 (rate จาก `window.OT_RATES` ← `index()` context `ot_rates`); โชว์เฉพาะเมื่อ `#needDriver` เปิด; (4) `#bk_passenger_count` += `.bk-noarrow` (ซ่อน spinner) + `inputmode=numeric` + JS strip ตัวอักษร. CSS ใหม่ใน `vehicle.css` (`.bk-datepick*/.bk-cal/.bk-timepick*/.bk-ot-warn*/.bk-inline-err/.bk-noarrow`); JS ใน `vehicle.js` (ดู § Frontend JS `bk*`) |
| `vehicle/vehicle-modal-edit.html` | `#editBookingModal`. **Phase 2 (2026-05-17):** `bi-pencil-square` → Lucide `pencil`; `bi-check-circle-fill` → Lucide `check`; footer 2 ปุ่ม → `vc-btn vc-btn-secondary/vc-btn-primary` + `title=""`. **Mirror book layout (2026-05-23):** restructured ให้เหมือน `vehicle-modal-book.html` ทั้งหมด — header เปลี่ยนจาก pencil-icon + "แก้ไขการจอง" title → avatar circle (`bk-detail-member-avatar` + `--vc-fg` bg) + `{{ current_user.full_name }}` + dept/email sub-text; datetime fields แตกจาก 2 `editStartDatetime`/`editEndDatetime` (flatpickr datetime picker) → **5 fields** = hidden `editStartDatetime` + `editEndDatetime` (name=`start_datetime`/`end_datetime`, JS compose ตอน submit) + visible `edit_date` (`type="date"` → flatpickr Thai BE format) + `edit_start_time`/`edit_end_time` (`type="time"`) + `edit_duration_preview` (live "X ชม. Y นาที" ใต้ช่วงเวลา); ทุก field ห่อ `.bk-field-wrap` + `.bk-valid-icon` (check-circle-2 green เมื่อ `:is-valid`); เพิ่ม `editPickup` (required, ใหม่ — เดิมไม่ required, no wrap); ลบ `bk-modal-dialog` + `bk-modal-content` (book ไม่มี); เพิ่ม `.bk-info-note` "หมายเหตุ: แก้ไขได้เฉพาะรายการที่ยังรออนุมัติ — การแก้ไขจะถูกบันทึกและแจ้ง Admin"; footer ปุ่ม "บันทึก" คงเดิม |
| `vehicle/vehicle-modal-detail.html` | `#eventDetailModal` (single + group รวมใน modal เดียว). **Phase 2 (2026-05-17):** 3 info icons (clock/truck/person) `bi-*` → Lucide; ลบ `card border-0 rounded-2` (เหลือ `bk-modal-content`); ลบ inline style 9 จุด → 8 classes ใหม่ใน vehicle.css (`bk-detail-*`). **Phase 3 (2026-05-17):** initial `ds-status-dot ds-status-dot--approved` → `vc-status-dot vc-status-dot--approved`; FA `fa-regular fa-circle-check` → Lucide `data-lucide="circle-check"`. Header icon ถูก JS swap ผ่าน `outerHTML` ใน `openEventDetail()` (vehicle.js) + `bindModalReinit()` แปลง Lucide เป็น svg ตอน `shown.bs.modal`. **Detail modal redesign (2026-05-20):** ทิ้ง 44px header circle (ids `detailHeaderDot`/`detailHeaderIcon`) → inline `.bk-detail-status` pill ใต้ title (ids `detailStatusPill`/`-Icon`/`-Text`); modal width 480px; dashed → solid full-bleed divider; section label "ผู้จอง" → `.bk-detail-section-header` "ผู้โดยสาร · N คน" + `#detailMemberCount`; member rows ใน JS ใช้ `.bk-detail-member-avatar` แทน `vc-status-dot--*`; footer button order reverse + `card-footer` collapse เมื่อว่าง. **Premium redesign (2026-05-23):** date-first hierarchy — ทิ้ง `.bk-detail-title` → eyebrow "การจองรถ" + hero date (1.0625rem/600/`letter-spacing:-0.01em`); status pill border-radius xs→full + matching `--vc-{green\|amber\|blue\|red}-border` + stroke-width 1.75→2; info rows reorder time/plate/driver → **plate (primary) → driver → time** (plate .9375rem/600 with monospace `.bk-detail-plate-code` + muted `.bk-detail-plate-brand`); `font-variant-numeric: tabular-nums` ทุก row; section header `justify-content:space-between` + `.bk-detail-section-count` (label ซ้าย, count ขวา) + drop UPPERCASE; member rows → **`.bk-detail-member` card tiles** (border + radius-sm + 8px gap + hover) + avatar ring 36×36 + sub-classes `bk-detail-member-body/-head/-name/-trip/-pickup`; close button absolute top-right; footer wrap → `.bk-detail-footer` (border-top + `--vc-bg-subtle`) + **CSS `:has(#detailActions:empty){display:none}`** แทน JS toggle. 4 keyframes (`bk-content-rise/-pill-in/-row-in/-member-in`) + member stagger via `style="--bk-i:${idx}"` + `cubic-bezier(.16,1,.3,1)` easing + `prefers-reduced-motion` guard. **Scoped under `.bk-detail-content`** กัน leak ไป `room/room-modal-detail.html` ที่ใช้ `bk-detail-*` ซ้อนกัน. JS: `_plateLabel()` คืน HTML (escape ด้วย `_esc()`) → `detailPlate.innerHTML`; footer collapse JS L760-761 ลบ. |
| `vehicle/vehicle-modal-group.html` | *(merged into vehicle-modal-detail.html)* |
| `vehicle/vehicle-modal-more-events.html` | `#moreEventsModal`. **Phase 2 (2026-05-17):** ลบ class `shadow` + inline `border-radius:14px`; outer wrapper → `bk-modal-dialog-sm` + `bk-modal-content` (reuse จาก vehicle.css) |
| `vehicle/admin/vehicle_admin.html` | admin dashboard — **Vercel shell** (`.vc-scope`), **Phase 5 (2026-05-24):** booking section header "รายละเอียด" → "**รายการจอง**" + เพิ่ม `<span.vc-card-head-meta#beforeCount>` populate by JS "N รายการ". **Phase 3 redesign (2026-05-24):** week navigator refactor — เพิ่ม `.va-week-meta` row (eyebrow `#weekRangeText` + h2 `.va-week-selected#selDateHeading` + `.va-week-today#weekTodayBtn` + `.va-week-picker` wrapper with `#weekPickerBtn` + popover `#weekPickerPop` containing `<input type="date"#weekPickerInput>`); ลบ `.va-week-heading` block. **Phase 1-2 redesign (2026-05-24):** ลบ 4-cell KPI strip ทิ้ง (`.va-kpi-card` + 4 cells), แทนด้วย inline `.va-page-header-stat` 3-item pill (dot amber/blue/green + count + label = pending/approver/approved) ใน `.va-page-header-actions`; eyebrow text "Admin · Approval Queue" → "Admin / Approval queue"; page header strip Bootstrap utility classes (d-flex/align-items-end/justify-content-between/flex-wrap/gap-3/mb-4) → ใช้ CSS อย่างเดียว + `mb-5` Bootstrap util; remaining sections (2-col split: Bookings+Trips col-8 / Vehicle status grid col-4 sticky, 4 modals: `#assignModal` `#swapModal` `#repairModal` `#revertModal`) ไม่เปลี่ยน. Reuses shared primitives: `.vc-kpi-*` (components/kpi.css), `.vc-badge-*` + `.vc-empty-*` (components/badge.css + empty_state.css — Phase 3.2), `.vc-card/.vc-btn/.vc-modal/.vc-form-*` (design-system.css). Lucide icons. KPI cells kept raw HTML (canonical per `/bbcenter-design`); status badges + empty states rendered by JS now emit `vc-badge-{warning\|blue\|success\|danger\|neutral\|solid} vc-badge-dot` + `vc-empty` + `vc-empty-icon/-title` + lucide icons. **Phase 4.3 (2026-05-15):** script → ES module `<script type="module" src="js/pages/vehicle-admin.js">`. **Phase 5.1 align (2026-05-15):** ลบ `fuel_admin.css` link (ไม่ใช้); `vc-icon` → `vc-icon-sm` ×6; ลบ `pe-1 mb-1` ×2 ใน modal label; เพิ่ม `title=""` ทุก control button (week-nav, merge/notify ×4, ftab ×5, btn-close ×4, adm-exp-tab ×3, modal-footer ×8, expand toggle ×1). **Phase 5.2 inline-style purge (2026-05-16):** ลบ inline `style="color:var(--vc-green)"` ที่ L157 (`<i data-lucide="circle-check">` ใน collapsed-bar) → ใช้ CSS rule; แทน inline `style="margin-top/-bottom"` ที่ L274 → class `.va-exp-sub-group`. **Phase 5.3 JS audit (2026-05-16):** `pages/vehicle-admin.js` — L863 `<i class="fa-brands fa-telegram">` → `<i data-lucide="send" class="vc-icon-sm">` + `initIcons(btn)` (rule 4); L929 plain `<p style="--ds-text-muted">` empty → `<div class="vc-empty"> + vc-empty-icon + vc-empty-title` (rule 5); L933-935 swap-veh-status inline HEX `#DCFCE7/#16A34A/#EDE9FE/#4338CA` → `vc-badge vc-badge-{blue\|neutral\|success} vc-badge-dot` (rule 9). **Phase 6 UX polish (2026-05-16):** L260 "EXPENSE TYPE" → "ประเภทค่าใช้จ่าย" (Thai consistency); ลบ dead comment L130 `beforeCount`; เพิ่ม `<div class="va-page-header-actions"></div>` slot ใน page header (§1 skill template alignment, `:empty { display: none; }` กัน blank space). **Phase 6.1 UX features (2026-05-17):** 3 new modal nodes — `#vehConflictWarn` (after `#modalVehSel`) + `#drvConflictWarn` (after `#modalDrvSel`) ใช้ `.va-conflict-warn` แสดง "ซ้อนเวลากับ #N (HH:MM-HH:MM · ...)" เมื่อ `findConflict()` เจอ; `<p.va-budget-warn>` ใน `.va-budget-bar` แสดงข้อความเตือนงบ. **Phase 11 task-card redesign (2026-06-06):** `#sectionBefore` booking cards (JS-rendered) → task-list layout ตาม reference image — head (title=ปลายทาง + desc=purpose · due-time start–end) + foot (avatar+"จองโดย" author | status badge + actions). header/filter-tabs/merge-notify เดิมคงไว้ครบ. **Phase 11.1 (2026-06-06):** ลบ block `#beforeCollapsed` (`.va-collapsed-bar` + `#beforeCollapsedText` + ปุ่ม "ดูรายละเอียด") ทิ้ง — `#beforeExpanded` (filter-tabs + `#bookingList`) แสดงเสมอ ไม่มี collapsed state อีก. **Phase 11.2 (2026-06-06):** `#sectionBefore` class `vc-card` → `va-flat` (เอากรอบ card section ออกตามคำขอ user — header/filter/list ชิดขอบ flat); `#sectionAfter` ยังเป็น `vc-card`. **Phase 12 week-card ghost (2026-06-07):** va-week-card restyle เบาลง — strip active state กล่องดำ `--vc-fg` → underline (`border-bottom`) + ตัวเลข weight 800; `.va-week-nav` + ปุ่ม today/picker → ghost (border transparent); `.va-week-bar` ลบ border-bottom + padding; `.va-week-card` เพิ่ม `padding: --vc-space-5`. **Phase 12.1 meta slim (2026-06-07):** ลบ eyebrow `#weekRangeText` + wrapper `.va-week-meta-left` ออกจาก `.va-week-meta` (เหลือ `h2.va-week-selected#selDateHeading` + `.va-week-meta-right`); heading format ย่อ "วัน{เต็ม}ที่ D {เดือนเต็ม} {ปี} + tag วันนี้" → "{วันย่อ}. D {เดือนย่อ} {ปี}" (เอา today tag ออก — today มี dot บน strip แล้ว); `.va-week-meta` align-items flex-end→center; JS `renderWeekMeta()` (vehicle-admin.js) ตัด rangeText computation ทิ้ง + เพิ่ม const `TH_MON_S` (เดือนย่อ); ลบ CSS rule `.va-week-meta-left` + `.va-week-eyebrow`. **Phase 14 (2026-06-07):** เพิ่ม field `isAdHoc` ใน `window.BOOKINGS_DATA` injection (จาก `VehicleBooking.is_ad_hoc` — งานนอกระบบที่ driver เพิ่มเอง) สำหรับ badge "งานนอกระบบ" ใน bl-card. **Phase 15 (2026-06-07):** inject `otHours`/`otAmount` ใน `window.BOOKINGS_DATA` (จาก `VehicleBooking.driver_ots[0].total_hours/total_amount` backref → `DriverOT`) สำหรับ OT badge ใน trip card (sectionAfter). **Phase 16 inline calendar (2026-06-08):** week-picker popover เปลี่ยนจาก 2-click (ปุ่ม `#weekPickerBtn` → `<input type="date"#weekPickerInput>`) เป็น **inline month calendar คลิกเดียวเลือกวัน** — ลบ `<label.va-week-picker-label>` + `<input#weekPickerInput>` ออกจาก `#weekPickerPop`; popover เปลี่ยน class `.va-week-picker-pop` → `.va-cal` containing `.va-cal-head` (`#calPrevBtn` chevron-left + `.va-cal-title#calTitle` + `#calNextBtn` chevron-right) + `.va-cal-grid.va-cal-dow#calDow` (หัววัน Su–Sa) + `.va-cal-grid#calDays` (grid วัน, JS-rendered). CSS `.va-cal*` block ใหม่ใน `vehicle_admin.css` (selected day = วงกลมทึบดำ `--vc-primary`, dot accent บอกวันมีงาน, ring วันนี้, fade-in animation). |
| `vehicle/admin/vehicle_fleet.html` (เดิม `admin_manage_fleet.html`) | จัดการรถ + คนขับ + ตารางผู้อนุมัติประจำกอง (view-only); service/tax date อยู่ใน edit modal. CSS `vehicle/css/vehicle_fleet.css` · JS `vehicle/js/vehicle_fleet.js`. **Phase 6.2 redesign (2026-05-17):** Vercel `vc-scope` shell, layout `mf-grid` 2fr/1fr (col-8 Vehicles · col-4 Drivers+Approvers stacked), vc-card/vc-table/vc-badge/vc-form-*, Lucide icons; row icons → `.mf-icon-btn`; modals → `vc-modal`; row stagger via `.mf-stagger`; respects `prefers-reduced-motion`. **Driver profile redesign (2026-06-08):** ฝั่งคนขับ (col-4) เป็น avatar list (`.mf-avatar` รูป/ตัวอักษรย่อ + `.mf-driver-side` badge active/inactive + จำนวนงาน); data-* ทั้งหมดอยู่บน `.mf-driver-row` (JS อ่านผ่าน `.closest()`); ปุ่มต่อ row = view(eye)/edit/delete. modal ใหม่ `#driverDetailModal` (read-only ดู profile + รูปบัตร + ปุ่มแก้ไข→เปิด `#editDriverModal`); add/edit driver modal `modal-lg` + `enctype=multipart/form-data` + ฟิลด์ national_id/addr_*/avatar/idcard. Controller `vehicle_admin.py`: `_save_driver_image()` + add_driver/edit_driver รับ field ใหม่. |
| `vehicle/admin/vehicle_cost.html` | จัดการค่าล่วงเวลา (OT) คนขับ — KPI (3 cell raw), filter bar (date range/driver GET), table + status tabs (vc-tab/vc-tab-count), edit modal (slot rows dynamic), rate config modal (rate rows dynamic), print receipt (`@media print`). **Phase 5 (2026-06-12):** เพิ่ม `#otPivotCard` — OT pivot driver × month สำหรับปี `from_year`: ตาราง driver × 12 เดือน (ยอด ฿ + ชม./cell), footer row รวม, grand total; ใช้ `.vc-pivot-row-summary` + `.vc-pivot-td-sticky`. Loads `vehicle_cost.css` + `pages/ot-admin.js`. **Phase 3.7 (2026-05-15):** ลบ inline `style="display:inline;"` ×3 → `.d-inline`; `style="display:inline-flex;gap:4px;"` → `.cost-action-group`; ลบ `style="width:20px;height:20px;"` จาก empty-state lucide icon. **Phase 4.8 (2026-05-16):** script → ES module `<script type="module" src="js/pages/ot-admin.js">`. **Phase 5.10 (2026-05-18):** ลบ `<link>` `fuel_admin.css` (ไม่ใช้แล้ว); rename `fuel-header/-title/-subtitle/-header-actions/-range-chip` → `cost-*` (CSS มีอยู่แล้ว); fix bug: JS template literal (`${opts}/${st}/${en}`) หลุดมาเป็น HTML ใน `#rateConfigModal` body → ลบทิ้ง; redesign `#rateConfigModal` body ใหม่ — static `vc-form-row` × N → `#rateConfigContainer` + `.cost-rate-row` × N (6-col grid label/day/start/end/rate/×) + `#addRateBtn` (เพิ่มแถวใหม่ส่ง `cfg_id[]=""`) + `.js-rate-remove` (existing → confirm + append `cfg_delete[]`; new → drop DOM); inputs `cfg_start[]`/`cfg_end[]` text → `<input type="time">`. **เพิ่ม per-weekday override:** column `cfg_day[]` (`<select>` ทุกวัน/จันทร์–อาทิตย์, value `""`=NULL หรือ `0..6`) + rate banner pill ใส่ `.cost-rate-pill--day` (amber) + day-label prefix (`TH_DAYS`) เมื่อ day-specific; backend `auto_generate_ot()` override logic: ถ้ามี row ที่ `day_of_week == booking.weekday()` → ignore weekday-agnostic rows. **2026-06-08 (cmdk menus):** filter bar 5 `<select.vc-filter-select>` (from_month/from_year/to_month/to_year/driver_id) ได้ `data-dropdown` → `.vc-dd`; โหลด `core/js/dropdown.js` (module) ต่อท้าย `vehicle_ot.js`; dd dispatch `change` → auto-submit เดิม (`vehicle_ot.js:282` ฟัง change ทุก select ใน filterForm) ทำงานต่อ. panel portal→body กัน overflow clip. (modal selects `.vc-select` ไม่แตะ) **2026-06-09 (manual OT):** ปุ่ม `#addOtBtn` (primary) ใน `cost-header-actions` → modal `#addOtModal` (layout เหมือน `#editOtModal`, action `ot_create`); redesign 2 modal — date `<input type="date">` → va-cal datepicker (`budget-datepick`/`data-datepick*`, JS port มาใน `vehicle_ot.js`) + slot rows ใส่ `cost-slot-head` (header เหมือน `rateConfigForm`, input flat). JSON blob เพิ่ม `createUrl` **2026-06-14 (filter งบ):** เพิ่มใน `#costFilterSheet` — `#filterBudgetType` (ทั้งหมด/ส่วนกลาง/ส่วนกอง/จ่ายเอง) + `#filterBudgetSub` cascade ใน `#filterBudgetSubWrap` (IDs เดียวกับ mileage); data injection `window.EXPENSE_CATS` + `window.COST_FILTER_SUB`; `filter_active` ย้ายไปคิดใน view (รวม budget); export link เพิ่ม `budget_type`/`budget_sub` args |
| `vehicle/admin/vehicle_budget.html` | จัดการงบ. **Phase 4.10 (2026-05-16):** script → ES module `<script type="module" src="js/pages/budget-admin.js">`; ลบ duplicate lucide CDN (มาจาก `_header.html` แล้ว). **Phase 5.8 polish (2026-05-18):** Vercel spacing pass — `fuel-*` classes (header/title/subtitle/header-actions) → `budget-*`; ลบ inline `style=""` 18 จุด → semantic classes (`.budget-flash-stack/-kpi-strip/-grid/-grid--last/-empty-block/-filter-month/-refund-scroll/-refund-hint/-empty-icon/-modal-mono/-modal-alert/-modal-alert--flush`, `.vc-bcard-approver`); inline `<script>` setBudgetModal handler → `pages/budget-admin.js`; `display:none` → `hidden` attr; `<i style="width:20px;height:20px;">` → `.budget-empty-icon`; ทุก `<button>` มี `title=""` ครบตามกฎ §0; section header รับ `:first-of-type` spacing; โหลด `budget_manage.css` แทน `fuel_admin.css`+`budget_admin.css`. **Phase 5.9 (2026-05-18):** budget card dropdown เพิ่ม divider + form POST item "ปิดใช้งานงบนี้" / "เปิดใช้งานงบนี้" (action=`toggle_active` + hidden year/month/budget_id/to_active); `<div class="vc-bcard">` รับ `{% if not b.is_active %}vc-bcard--inactive{% endif %}`; เพิ่ม badge "ปิดใช้งาน" (`vc-badge-neutral vc-badge-dot` + `power-off` icon) ใน head เมื่อ inactive. **Phase 1+2 redesign (2026-05-21):** Phase 1 — CTA "ตั้งงบกอง" `vc-btn-purple`→`vc-btn-primary`; `.vc-bcard--inactive` ทิ้ง stripe → dashed border; dropdown padding 6→8px. Phase 2 — KPI 6→4 cells (ลบ "งบส่วนกลาง"+"งบงานกอง" cells, `--6`→`--4`), amount 2 ตัวย้ายขึ้น `vc-section-hdr` เป็น `.vc-section-hdr-amount`. **Phase 3 (2026-05-21):** card hierarchy — central+dept loops `.vc-bcard-row` → `.vc-bcard-pct-hero` (percent เป็น hero, label "ใช้ไปแล้ว" demoted muted); drop `is-purple` markup จาก dept loop amounts (`<span class="used">` ไม่มี `.is-purple` แล้ว). **Phase 4 (2026-05-21):** premium polish — purple retirement complete (dept section icon, name icon, progress fallback, refund modal badge → neutral; markup zero purple, CSS rules `.vc-btn-purple`/`.vc-badge-purple` kept for Phase 8); KPI "คงเหลือ" cell ได้ `vc-kpi-cell--signal` (bg-subtle ลด monotony); per-card inline `style="--bm-delay: {{ loop.index0 * 40 }}ms"` stagger ทั้ง 2 loops; 4 modals ได้ class `budget-modal-enter` (scoped animation, ไม่ leak `.vc-modal`). **Phase 5 (2026-05-22):** `.budget-personal-row` (L153) → `.budget-personal-line` (card → inline lightweight strip). **Phase 6 (2026-05-22):** 3 modal alert boxes → meta lines: setBudget (L559) `ds-alert-info` → `<p.budget-modal-notice>` + info icon; topUp (L593) `ds-alert-info` → `<dl.budget-modal-meta>` 2 `<dt>/<dd>` rows (`data-bind` ย้ายไป `<dd>`); adjust (L641) same pattern. **Phase 7 (2026-05-22):** แทรก 2 `<details class="vc-card vc-pivot-wrap budget-pivot-card">` (Pivot งบส่วนกลาง × เดือน + Pivot งบกอง × เดือน) ระหว่าง personal-line กับ "งบประมาณส่วนกลาง" section header; collapsed default + fiscal year (มี.ค.→ก.พ.) cols + heat-tinted cells + drill-down link `url_for('adminfleet.budget_manage', year=y, month=m)` (fiscal mapping ถูกต้อง); receive new template vars `pivot`/`fiscal_year_start_ad`/`TH_MONTHS`. **Phase 8 (2026-05-22):** dead purple CSS cleanup — `.vc-btn-purple` + `.vc-badge-purple` blocks ใน `budget_manage.css` §7 + `.vc-kpi-value--purple` ใน `components/kpi.css` ลบครบ (markup zero usage; tokens เก็บไว้). **UX writer Phase 1 (2026-05-22):** wording overhaul + confirm guard บนปุ่ม destructive — (1) page-header CTA `คืนงบจาก booking` (rotate-ccw) → `ยกเลิกการจองที่ยังไม่ปิดทริป` (x-circle) (สื่อ flow จริงว่ารายการคือ booking ที่ยังไม่หักงบ); (2) `setBudgetModal` รองรับ create vs edit dynamic — เพิ่ม `id="sbAmountLabel/sbAmountHint/sbNotice/sbNoticeText/sbSubmitText"` ให้ JS swap คำได้ (edit: `แก้เพดาน<group> — <dept>` + submit `อัปเดตเพดาน`; create: `ตั้ง<group>ใหม่` + submit `บันทึกงบ`); แต่งคำ label/hint ของ approver row + start/end ใหม่; (3) `budgetRefundModal` retitle `ยกเลิกการจองที่ยังไม่ปิดทริป` + hint อธิบาย rule "ปิดทริปแล้วยกเลิกไม่ได้" + ปุ่ม `ยกเลิก + คืนงบ` → `ยืนยันยกเลิกการจอง` (icon x-circle); (4) `budgetAdjustModal` retitle `แก้ยอดงบที่หักผิด` + warning notice "ไม่ใช่การคืนเงินจริง" + label/hint อธิบายค่าบวก/ลบชัดเจน + placeholder ตัวอย่างเคสจริง + submit `บันทึกการแก้ไข`; (5) `budgetTopUpModal` retitle `เพิ่มเพดานงบ (top-up)` + hint "ยอดนี้บวกกับเพดานเดิมทันที" + submit `เพิ่มเข้าเพดานงบ`; (6) toggle_active form (central + dept loops) รับ `data-confirm-toggle data-dept-name data-to-active` → JS confirm dialog ก่อน submit. **Phase 2 redesign round 1 (2026-05-22):** scrap 4-cell KPI strip + 2 `vc-section-hdr` + `.budget-personal-line` + 2 pivot details → (1) **Summary card** บนสุดแทน KPI strip — `<section.budget-summary>` 1 hero amount `฿used / ฿cap` + 4px progress (ok/warn/danger tone จาก pct_of_cap) + meta row "% ของเพดาน · เหลือ ฿X" + signals footer (รวม pending + over-budget + personal-unpaid → pill format). (2) **Pivot รวม 3 section** — 2 `<details>` (central + dept) → 1 `<details id="pivotAllDetails">` 3 `<tbody.vc-pivot-section>` (ส่วนกลาง / งานกอง / ส่วนตัว) ทุก section header row ใช้ `.vc-pivot-section-head td[colspan=col_count]`; personal row drill-down ไป `adminfleet.budget_personal`; heat scale per-section independent (`central_max/dept_max/personal_max`). (3) **3-section accordion** แทน vc-section-hdr — `<details.budget-section open id="sectionCentral/sectionDept">` (default open) + `<details.budget-section id="sectionPersonal">` (default closed); summary = label (chevron + icon + title + count + pending badge) + stats (`฿used / ฿cap` + `%pct--{tone}`); body = `.budget-section-actions` (ปุ่ม `[+ ตั้งงบ]` ย้ายเข้าด้านใน) + existing `budget-grid` (รอบ 2 จะแทน → table). Section "ส่วนตัว" รอบ 1 = link out ไป `adminfleet.budget_personal` + placeholder; รอบ 2 จะ embed mileage table + ปุ่ม `[รับเงิน]` inline. (4) Template vars ใหม่: `kpi.personal_unpaid_count/-amount` (mileage ปิดทริปแล้วแต่ admin ยังไม่ confirm รับเงิน, scope = เดือนที่เลือก) + `kpi.over_budget_count` (active rows ที่ used > cap) + `kpi.pct_of_cap` + `pivot.personal/-max`. **Phase 2 redesign round 2 (2026-05-22):** card → table conversion. (1) ภายใน sectionCentral + sectionDept body: `.row.g-3.budget-grid` (vc-bcard grid) → `<table.vc-table.budget-table>` ใน `<div.budget-table-wrap>`. Cols ส่วนกลาง: หมวด · เพดาน · ใช้ไป · % (mono + `.vc-progress--mini`) · คงเหลือ · tags · dropdown kebab. Cols กอง: + `ผู้อนุมัติ` col (data shape: pill ปกติ หรือปุ่ม `.budget-row-approver--missing` ที่เปิด setBudgetModal ตรง field approver). Row inactive ใช้ `.budget-row--inactive` (opacity 0.58 + repeating-linear-gradient stripe + border-left strong). Dropdown markup เก็บเดิม ทั้ง 3 items (แก้เพดาน/top-up/adjust) + form toggle_active (data-confirm-toggle เก่าทำงานปกติ). (2) sectionPersonal expanded by default ถ้ามี unpaid + body embed `<table.budget-table--personal>` cols: วันที่ปิดทริป · BK-id · ผู้จอง · ปลายทาง · ค่าน้ำมัน · สถานะ (badge รับแล้ว/ค้างรับ) · ปุ่ม `[ยืนยันรับเงิน]` (form POST `adminfleet.budget_personal_mark_paid` พร้อม `data-confirm-pay/-user/-amount`). Backend route ใหม่: `personal_rows[]` query mileage paid+unpaid ของเดือนที่เลือก (paid ใช้ `personal_paid_at`, unpaid ใช้ `actual_end`). (3) JS `budget-admin.js` เพิ่ม document-level submit listener `form[data-confirm-pay]` → confirm() + AJAX fetch (route return JSON) → location.reload(); btn loader state + error fallback. (4) CSS §13 `.budget-table-wrap/.budget-table/.budget-row/--inactive/-name/-sub/-pct-cell/-pct[/.is-warn/.is-danger]/-rem-over/-tags/-approver/--missing/.budget-personal-paid-at/.vc-progress--mini`. ลบ §2 `.budget-grid/-grid--last` margin rules (dead). **Phase 3 redesign round 3 (2026-05-22):** flat layout — กำจัด accordion + simplify. (1) **3A** standalone `<form.vc-filter-bar id="filterForm">` ถูกย้ายเข้าไปอยู่ใน `.budget-summary-head` (right side) → `<form.budget-summary-filter>` + 2 `<select.budget-summary-filter-select>` (month + year compact 32px). (2) **3B** `<details.budget-section open>` × 3 (Phase 2C) → `<section.budget-section>` + `<header.budget-section-head>` แบบ flat (ไม่ collapsible, ไม่มี chevron); section title + count + pending badge + stats + actions อยู่บนแถวเดียวกัน; ปุ่ม `[+ ตั้งงบ]` ย้ายเข้า `.budget-section-actions` ใน head (ไม่ใช่ body separate). (3) **3C** ลบ col "% ใช้" + `.vc-progress--mini` → แสดง pct inline หลัง amount ใน col "ใช้ไป": `<span.budget-row-used>฿11,128</span> <span.budget-row-pct-inline>(74.2%)</span>` พร้อม tone class (`.is-warn` ≥70%, `.is-danger` ≥90%) — color signal มี ทั้ง 2 spans (cohesive). (4) **3D** `.vc-dropdown-menu` z-index 5→60 + `box-shadow` gentle + `.budget-table-wrap:has(.vc-dropdown.is-open)` → `overflow: visible` (release horizontal-scroll clip ตอน menu เปิด, CSS `:has()` Level 4). (5) **3E** sectionPersonal restructure — ลบ "ดูทั้งหมด" external link + ลบ col "จัดการ" (รวมเข้า col "สถานะ"). เพิ่ม `<div.budget-personal-tabs>` (segmented pills: ทั้งหมด/ค้างรับ/รับแล้ว + count badges); default active = ค้างรับ ถ้ามี unpaid, else ทั้งหมด. Row markup รับ `data-personal-row="paid|unpaid"` + `hidden` initial state จาก server (ค้างรับ default). BK format `#{:04d}` → `BK-{id}` plain. Badge "ค้างรับ" เปลี่ยนจาก `<span>` → `<button type="submit">` ภายใน `<form.budget-personal-pay-form.d-inline>` (Option A — clickable badge → existing `data-confirm-pay` handler trigger). (6) CSS revise §12 (flat header) + new §12B `.budget-summary-filter/-filter-select` + new §14 `.budget-personal-tabs/.budget-personal-tab/.budget-personal-tab-count/.budget-personal-status[/button.budget-personal-status:hover]`. Dead: `.budget-row-pct-cell/-pct/.vc-progress--mini/.budget-section-summary/-chev/-body` cleanup. **Phase 3 hotfix (2026-05-22 same day):** (a) `.budget-table-wrap` เพิ่ม `overflow-y: hidden` (suppress browser auto-derived vertical scrollbar — fix badge clipped ใน row top). (b) Stacking context fix สำหรับ dropdown — `.budget-row` มี `animation: bm-fade-up` (transform) → implicit stacking context per row, row ด้านล่าง DOM cover dropdown menu ของ row ด้านบน. แก้ด้วย `.budget-table tbody tr { position: relative }` + `tr:has(.vc-dropdown.is-open) { z-index: 70 }` (bump เฉพาะ row ที่กำลังเปิด). (c) `.budget-personal-tabs` revise style → ลอก `.mlg-status-chips/.mlg-chip` exactly (white container + black active bg + ลบ count badge). **2026-06-06 (งบช่วงเวลา):** + Section 4 `#sectionArchived` ("คลังงบ") ใต้ sectionPersonal — `<table.budget-table>` (งบ/ประเภท/เพดาน/ใช้ไป/ช่วงเดิม/สถานะ-badge/ปุ่ม "เพิ่มเวลา") loop `archived_budgets`; ปุ่มเปิด modal `#extendBudgetModal` (action `extend_period`: date start/end required + topup optional) ผ่าน `data-bid/-dept/-start/-end`. **2026-06-08 (cmdk menus):** filter `month` + `year` (`.vc-filter-select.budget-summary-filter-select`, `onchange=this.form.submit()`) ได้ `data-dropdown` → enhance เป็น `.vc-dd` (dd dispatch `change` → inline onchange submit ทำงานต่อ); โหลด `core/js/dropdown.js` (module) ต่อท้าย `vehicle_budget.js`. trigger รับ class `budget-summary-filter-select` มา size แต่ override `padding-right:8px` (chevron จริง ไม่ใช้ bg-chevron) ใน `vehicle_budget.css`. kebab `.vc-dropdown` (`data-dropdown-trigger`) เป็นคนละ component ไม่กระทบ. **2026-06-08 redesign (pivot/KPI/table):** (1) **Pivot** `#pivotAllDetails` `<details>` matrix-always-on → `<section.vc-card>` + 3 `<details.budget-pivot-cat>` (ส่วนกลาง/ส่วนกอง/ส่วนตัว) — default = สรุปแถว (icon+ชื่อ+count + เพดานรวมปีงบ `.budget-pivot-cat-cap` + badge ใช้ไป% `.budget-pivot-cat-badge--{ok\|warn\|danger}`), กดขยาย = matrix ใช้ไป×เดือน (col "รวม" ต่อท้าย) ผ่าน `{% macro pivot_cat_table(...) %}`; ใช้ `pivot.summary` (key ใหม่จาก `_build_budget_pivot`). (2) **KPI** single `.budget-summary-bar` → `.budget-summary-stack` stacked segment (central/dept = indigo เฉด `#4F46E5`/`#818cf8` + track เทา=เหลือ) + `.budget-summary-legend` (dots central/dept/free + personal `--aside` ดันขวา). (3) **Table col align** central+dept: ย้าย `ผู้อนุมัติ` col → sub-line ใต้ชื่อกอง (`.budget-row-sub--approver`/`--approver-missing`), ตัด pending "รอปิด" badge + `.budget-section-stats` ออกจาก dept (col ตรงกับ central). (4) **เพิ่ม col bin** (`.budget-bin-btn` trash-2 แดง) ทั้ง 2 section = **soft delete** → form `toggle_active to_active=0` (reuse `data-confirm-toggle` JS เดิม). (5) ตัด `.budget-section-stats` amount ออกจาก central+dept (ซ้ำกับ KPI bar). CSS §17–19 ใน `vehicle_budget.css`. **2026-06-09 (approver searchable):** `#sbApprover` (`setBudgetModal`) `<select class="vc-select">` → เพิ่ม `data-autocomplete placeholder="ค้นหาชื่อผู้อนุมัติ…"` → enhance เป็น searchable combobox (component 3 `select[data-autocomplete]`, ดู §Key Functions VCMenus) คง submit `approver_id` (id) เดิม; `vehicle_budget.js` setBudgetModal preset เพิ่ม `sel.dispatchEvent(new Event('change'))` หลัง set value เพื่อ sync visible label. **Phase 5 personal pivot (2026-06-12):** personal pivot table aggregate row label "ค่าน้ำมัน" → "รวมทั้งหมด" + class `vc-pivot-row-summary`; เพิ่ม per-user sub-rows iterating `pivot.personal_by_user` พร้อม class `vc-pivot-td-sub`. **Tab redesign (2026-06-15):** จาก stacked sections → tab layout. (1) **Summary card** redesign 2-col — `.budget-summary-grid` (`.budget-summary-main` icon+label+`.budget-summary-amount` ซ้าย · `.budget-summary-divider` · `.budget-summary-breakdown` ขวา = `.budget-summary-bd-head` pct/เหลือ + stack bar + legend); filter (month/year) **ย้ายออก** จาก summary head → toolbar. stack bar เปลี่ยน semantics เป็น composition ค่าใช้จ่าย 3 หมวด (central/dept/personal) + เพิ่ม `.budget-summary-seg--personal`. (2) **Tab bar** `<nav.budget-tabs>` 5 ปุ่ม `.budget-tab[data-budget-tab=pivot|central|dept|personal|archived]` เส้น underline เต็ม container, active = accent blue. (3) **Toolbar** `#budgetToolbar.budget-toolbar` (filter `#filterForm` + `.budget-toolbar-actions` add buttons `[data-tab-add=central|dept]`) — `hidden` ตอน tab pivot, add สลับตาม tab. ปุ่ม add ย้ายออกจาก section header ของ central/dept (`.budget-section-actions` หาย 2 จุด). (4) **Panels** `.budget-panels` > 5 `<section.budget-panel[data-budget-panel]>` (pivot default `.is-active`); pivot card/4 budget-section ห่อใน panel ตามลำดับ. (5) JS `vehicle_budget.js` IIFE `initBudgetTabs` — client-side switch (toggle `.is-active` + `hidden`, sync toolbar/add). CSS `vehicle_budget.css` §20 (`.budget-summary-grid/-main/-icon/-label/-divider/-breakdown/-bd-head`, seg/legend สี green/amber/red, `.budget-tabs/.budget-tab`, `.budget-toolbar/-filter/-actions`, `.budget-panel`). **Tab redesign round 2 (2026-06-16):** ปรับตามภาพ ref. (1) summary section ลบ `vc-card` (ไม่ใช่ card) + grid ชิดซ้ายไม่เต็มจอ (`.budget-summary-breakdown` width 340px, mobile 100%); ลบ signals footer (`.budget-summary-signals`) ออกหมด. (2) `.budget-tabs` underline เต็มจอถึง sidebar (`margin-inline:-1rem` negate container px-3) + หนา 4px; `.budget-tab` เพิ่ม class `text-header fw-bold`. (3) 4 budget-table ได้ `data-sortable-table` + `<th data-sort="text|num">` → คลิก header sort (JS IIFE `initSortableTables` ใน `vehicle_budget.js`: parse ฿/,/% → number, localeCompare ไทย, toggle asc/desc, caret `::after` ⇅/↑/↓ accent). image-style polish: `.budget-table-wrap` box-shadow + radius 10px (DESIGN-OVERRIDE ตามที่ผู้ใช้สั่ง "ไม่ต้องสนใจกฏ"). CSS §21. **Round 3 (2026-06-16):** (1) revert tab underline กลับเป็นเดิม (ลบ §21B 4px/full-bleed → §20C 1px line + active 2px). (2) **ลบ `.budget-toolbar`** (filter เดือน/ปี + ปุ่ม add central/dept) ออกหมดตามคำสั่ง — CSS §20D + JS toolbar/addBtns refs ลบครบ; **ผล: ไม่มี filter เดือน/ปี + สร้างงบใหม่ได้ผ่าน dropdown "แก้เพดานงบ" ของแถวเท่านั้น** (ไม่มีปุ่ม "ตั้งงบใหม่"). (3) ลบ `.fuel-header-actions` (ปุ่มเปิด `#budgetRefundModal`) — **modal `budgetRefundModal` ตอนนี้ไม่มี trigger** (เหลือใน DOM). (4) page header เปลี่ยนเป็นสไตล์ vehicle.html: `<h1.fuel-title>`+`<p.fuel-subtitle>` → `<h2 class="fw-bold text-accent pt-3">` + `<h6 class="text-muted">`. **DNA redesign (2026-06-17):** (1) head โหลด **Manrope** (`&family=Manrope:...`) — ตัวเลขทุกที่ใช้ Manrope ผ่าน `.vc-mono` (`--vc-font-mono`). (2) Summary bar: segment ที่ 3 `personal` → **คงเหลือ** (`.budget-summary-seg--remain`/`-legend-dot--remain`); sizing เทียบ `total_budget` (c_w/d_w = used, p_w = remaining); CSS §20B สี central=accent/dept=amber/remain=green. (3) **tab + toolbar บรรทัดเดียว** `<div.budget-tabbar.d-flex.align-items-end.justify-content-between>` ครอบ `<nav.budget-tabs>` (overflow-x auto) + `<div.budget-toolbar>` ขวา = "เลือกเดือน" (vc-dropdown `.budget-month-menu` link `budget_manage?year=&month=` วน `pivot.fiscal_months`) + "ตั้งงบใหม่" (เปิด `setBudgetModal` type=department เปล่า). underline ย้ายมา `.budget-tabbar` (tabs border-bottom 0); `.budget-tabbar` = `display:flex` ตรง + `flex-wrap:nowrap` (tabs `flex:1 1 auto;min-width:0;overflow-x:auto;overflow-y:hidden` ย่อ+scroll · toolbar `flex:0 0 auto` pin ขวา) + **`position:sticky;top:56px;z-index:4;bg ทึบ`** (ตรึงใต้ `.vrc-topbar` 56px ตอนเลื่อนหน้า). (4) **ตัด tab "คลังงบ"** (archived) — ลบ `#tabPanelArchived`; งบไม่ active แสดงเป็น section "ไม่ได้ใช้งาน" ใน tab central/dept แทน (ดึง `archived_budgets|selectattr/rejectattr('budget_type','equalto','central')` → `central_off`/`dept_off`). (5) **central/dept: table → card grid** ผ่าน 2 macro `budget_card(b, btype, icon)` (full dropdown แก้เพดาน/top-up/adjust/ปิดใช้งาน) + `budget_card_off(b, icon)` (disable tone + ปุ่ม "เปิดการใช้งาน" เปิด `extendBudgetModal`); section head dot `.bcard-section-dot--on`(เขียว ใช้งานอยู่)/`--off`(กลวง ไม่ได้ใช้งาน) + `.bcard-section-line`; การ์ด `.bcard` content = ชื่อหมวด → `฿used/cap (%)` (Manrope, `.bcard-pct.is-ok/-warn/-danger`) → `.bcard-divider` → ตั้งแต่วันที่–วันที่ → (dept) ผู้อนุมัติ/`.bcard-meta-missing`. ส่วนตัว **คง table** (`data-sortable-table` เดิม). (6) icon monochrome `--vc-icon #9999b0` บน tile `--vc-icon-bg`. CSS §22 (`.budget-tabbar/-toolbar/.budget-month-menu/.bcard*/.bcard-section-*`). หมายเหตุ: macro ไม่เห็น `loop` → ตัด `--bm-delay` stagger ออก. |
| `vehicle/admin/budget_personal.html` | personal reimbursement |
| `vehicle/admin/admin_fuel.html` | **Phase 2.3–2.7 + 3 + 4.1/4.3** — Vercel shell, 6 KPI cells (raw), filter bar (year/month/vehicle/driver GET — raw), Bills data table (Excel export link, anchor `#billsCard`), Reimbursements accordion, **Pivot รถ×เดือน** (heatmap, sticky col, footer sum, **drill-down → Bills filter year+vehicle+month**), **5 modals (bill/reimb/reserve/price/budget)** + JS controller. **Phase 3:** empty states use `empty_state` macro; KPI/filter/table kept raw as canonical reference for `/bbcenter-design`. **Phase 4.6 (2026-05-15):** script → ES module `<script type="module" src="js/pages/fuel-admin.js">`. **2026-06-08 (cmdk menus):** filter bar 4 `<select.vc-filter-select>` (year/month/vehicle_id/driver_id) ได้ `data-dropdown` → `.vc-dd`; โหลด `core/js/dropdown.js` (module) ต่อท้าย `vehicle_fuel.js`; dd dispatch `change` → auto-submit เดิม (`vehicle_fuel.js:358` ฟัง change `.vc-filter-select` ใน filterForm) ทำงานต่อ. panel portal→body กัน overflow clip. **DNA redesign 2026-06-17:** head +Manrope; ลบ body `<h1 text-accent>` (หัวเรื่องย้ายขึ้น topbar ผ่าน `page_title`); รื้อ **KPI 2 card (group A 4-cell + B 3-cell) → 1 card 3-KPI strip** (`.fuel-kpi` icon tile + Manrope + ใช้/ทั้งหมด: งบทั้งปี / เงินสำรอง / รอเบิก) — ตัด self_paid + อนุมัติ breakdown; subtext "% ของงบ · คงเหลือ X"; คง `data-fuel-kpi="reserve-amount"` ให้ `vehicle_fuel.js`; **Table ไม่แตะ** (`.vc-table` flat อยู่แล้ว, ไม่ sortable); ไม่ใช่ canonical reference อีกต่อไป (DNA reference ย้ายไป `vehicle_budget.html`). **Toolbar iteration 2 (2026-06-17):** ลบ page-action bar บนสุด → รวมปุ่มใน `vc-card-head-actions` ของ billsCard (ตัวกรอง/รวมบิล/Excel/ตั้งค่าราคา/บิลใหม่); **filter bar → ปุ่ม "ตัวกรอง" popover** (`#filterToggle` + `form.fuel-filter-panel#filterForm` `position:fixed` กัน clip — JS `wireFilterPopover()`); KPI wrapper `vc-card` → `.fuel-kpi-strip` borderless (เส้นกั้น cell + เส้นใต้); `.vc-card-head` nowrap + `.vc-card-head-actions` overflow-x scroll |
| `vehicle/admin/fuel-modal-bill.html` | Bill create/edit/delete modal — date/vehicle/driver/amount/payment radio segmented/mileage/note. `#fuelBillModal` |
| `vehicle/admin/fuel-modal-reimbursement.html` | Reimbursement create/edit modal — bill list summary + เลขใบเบิก/แหล่ง/วันส่ง/note. `#fuelReimbModal` |
| `vehicle/admin/fuel-modal-reserve.html` | Reserve adjust modal — current summary + signed change + note (required) + history 20. `#fuelReserveModal` |
| `vehicle/admin/fuel-modal-price.html` | Fuel price modal — add new + history with delete. `#fuelPriceModal` |
| `vehicle/admin/fuel-modal-budget.html` | Annual budget modal — single number input + summary. `#fuelBudgetModal` |

**กฎสำคัญ:** modal ห้ามมี inline `<script>` — JS อยู่ใน `pages/vehicle.js` ทั้งหมด

---

## 🎨 Design System

> **2026-06-07 (ขั้น 5):** asset ย้าย → `static/<domain>/{css,js}/` + `static/core/` (shared). ชื่อ `css/X.css` / `js/pages/X.js` ใน 2 ตารางด้านล่าง = **เก่า** → mapping:
> - **core/** (shared): `design-system` `tokens` `main` `util` `vercel` `notification` `components/*` (css) · `icons` `format` `http` `main` `notification` (js)
> - **vehicle/** (prefix `vehicle_`): `vehicle.css`(base) · `fuel_admin`→`vehicle_fuel` · `budget_manage`→`vehicle_budget` · `mileage_admin`→`vehicle_mileage` · `manage_fleet`→`vehicle_fleet` · `driver`→`vehicle_driver` · `approver_inbox`→`vehicle_approver` · `vehicle_admin`/`vehicle_cost` คงชื่อ (js: `-`→`_`, `ot-admin`→`vehicle_ot`). หมายเหตุ: `activity_timeline`/`vehicle_history` css+js ลบ 2026-06-07 (dead)
> - **repair/room/maintenance/dashboard/**: `<domain>.css` + `<domain>.js` · images = shared คง `static/images/`

**Token source (single):** [app/static/core/css/tokens.css](../../app/static/core/css/tokens.css) — 2026-05-14 split out (Phase 1)
- `--vc-*` **canonical** (Vercel-Black + Indigo accent) — ใช้ใน code ทุกที่ เพิ่ม `--vc-z-*`/`--vc-sidebar-width`/`--vc-header-height` ใน Phase 5.1; **2026-06-10:** font หลัก + ปุ่ม primary เปลี่ยนจากดำ `#111827` → กรมท่า/navy `#1C2E4A` (`--vc-fg` + `--vc-primary`, hover `#26405F`) — กระทบ text หลัก, `ds-btn-primary`, `vc-btn-primary`, sidebar logo box ทั้งระบบ; **Phase 3 vehicle user-facing (2026-05-17)** เพิ่ม `--vc-purple-bg` (`rgba(121,40,202,.10)`) + `--vc-purple-border` (`rgba(121,40,202,.25)`) ใต้ `--vc-purple` สำหรับ `vc-status-dot--group`
- `--ds-*` **RETIRED** (Phase 5.1, 2026-05-16) — ลบ Part A ออกครบแล้ว; `var(--ds-*)` ต้องไม่มีใน codebase; ถ้าเจอ = bug (vehicle user-facing Phase 4 2026-05-17 — `pages/vehicle.js` cleared 14 จุด; เหลือ `pages/vehicle-admin.js` L344/L369 ใน admin page)
- `@import`-ed by `design-system.css` (everyone loads it transitively)

**Component entry:** [app/static/core/css/design-system.css](../../app/static/core/css/design-system.css) — typography + components + `.vc-mono/-caption/-icon*/-scope` utilities (no token defs anymore). `@import`s `tokens.css` + every file in `components/`. **Phase 3 vehicle user-facing (2026-05-17)**: §7 STATUS DOT — rename `.ds-status-dot` + 5 variants (pending/approver/approved/rejected/group) → `.vc-status-dot*`; HEX literals 10 ค่า → `var(--vc-{amber|blue|green|red|purple}-bg)` + foreground. Markup pattern เปลี่ยนเป็น `<i data-lucide="..." class="vc-icon-sm">` แทน Font Awesome. **Modal × scrollbar-gutter fix (2026-06-08):** `html{scrollbar-gutter:stable}` จอง gutter ~15px ไว้ตลอด. เดิมตอน modal เปิดสลับ `html:has(body.modal-open){ scrollbar-gutter:auto }` (เลิกจอง gutter) → ทำให้ layout **กระตุก** ขยับขวาตอนเปิด modal. **แก้ใหม่:** เลิก toggle gutter — gutter ค้าง `stable` ตลอด (ความกว้างไม่เปลี่ยน) แล้วล้าง padding ที่ BS ใส่ชดเชยแทน → `body.modal-open{ padding-right:0 !important }`. แถบขอบขวาให้ global `.modal-backdrop{ width:100vw; height:100dvh }` คลุม. global = ทุก modal (ปัจจุบัน + อนาคต) ไม่ต้อง list ID. **Button blue (2026-06-13):** `.ds-btn-primary` fill `--vc-fg` (navy)→`--vc-blue` + hover `--vc-blue-hover` (คู่กับ `.vc-btn-primary` ใน `vehicle_fuel.css` ที่เปลี่ยน `--vc-primary`→`--vc-blue` เช่นกัน) — ปุ่ม primary ทุกหน้าเป็นน้ำเงินตามคำขอ user. **Heading recolor (2026-06-13):** §2 TYPOGRAPHY SCALE — เพิ่ม base rule `h1,h2,h3 { font-family: var(--vc-font-heading); color: var(--vc-blue) } !important` คลุมทุกหัวข้อ (title class รายหน้าทับ color เอง → ต้อง !important); `.ds-h1/.ds-h2/.ds-h3` ก็ชี้ token เดียวกัน (ไม่ถูกใช้ใน template ตอนนี้) ตามคำขอ user.

**Component library (Phase 2, 2026-05-14):**
| Component | CSS | Macro | Notes |
|-----------|-----|-------|-------|
| KPI strip | [components/kpi.css](../../app/static/core/css/components/kpi.css) | [_components/kpi.html](../../app/templates/_components/kpi.html) | `kpi_group(cols=3\|4\|6)` + `kpi_cell(label,value,unit,icon,meta,tone)`. Tones: muted/success/danger/blue/warn. Extracted from `fuel_admin.css`+`budget_admin.css`. (4-col added 2026-05-18 for admin_fuel KPI group A. `--purple` retired Phase 8, 2026-05-22.) |
| Filter bar | [components/filter_bar.css](../../app/static/core/css/components/filter_bar.css) | [_components/filter_bar.html](../../app/templates/_components/filter_bar.html) | `filter_bar()` wraps a `<form>`; `filter_select`/`filter_date` for fields. Extracted from `fuel_admin.css §21`. |
| Badge | [components/badge.css](../../app/static/core/css/components/badge.css) | [_components/badge.html](../../app/templates/_components/badge.html) | `.vc-badge` + tones `-neutral/-warning/-blue/-success/-danger/-solid`, `.vc-badge-dot` left dot, `.vc-badge-xs` small. `-solid` = inverted black-fg/white-bg for strong emphasis (re-added Phase 3.2 for "X งานรวม" pill). (Page-local `.vc-badge-purple` ลบ Phase 8, 2026-05-22 — markup zero usage.) Phase 3 aligned to flat vocab matching `/bbcenter-design` skill. |
| Pill | [components/pill.css](../../app/static/core/css/components/pill.css) | [_components/pill.html](../../app/templates/_components/pill.html) | Rounded chip for filter tabs/segmented. `.is-active`, tones `accent/success/danger`. |
| Empty state | [components/empty_state.css](../../app/static/core/css/components/empty_state.css) | [_components/empty_state.html](../../app/templates/_components/empty_state.html) | `empty_state(title, desc, icon, compact)` — lucide icon. Use `{% call %}…{% endcall %}` for CTA button. Phase 3 aligned to flat vocab (`vc-empty-title/-desc/-icon`). |
| Form group | [components/form_group.css](../../app/static/core/css/components/form_group.css) | [_components/form_group.html](../../app/templates/_components/form_group.html) | `form_input/form_select/form_group(call)`. States `.has-error/.is-disabled`. |
| Table shell | [components/table_shell.css](../../app/static/core/css/components/table_shell.css) | [_components/table_shell.html](../../app/templates/_components/table_shell.html) | `.vc-table-shell` wrapper + `.vc-table` skin. |
| Modal | [components/modal_shell.css](../../app/static/core/css/components/modal_shell.css) | [_components/_modal.html](../../app/templates/_components/_modal.html) | Bootstrap-based macro (predates Phase 2) + new tokenized helpers (`.vc-modal-section/-divider`). |
| Pivot table | [components/pivot.css](../../app/static/core/css/components/pivot.css) | (no macro — used inline as `<details class="vc-card vc-pivot-wrap">`) | `.vc-pivot-wrap/-summary/-scroll/-table/-th-sticky/-td-sticky/-cell/-cell-empty/-total-col/-link` + `--cell-heat` (0-100 heat scale, accent blue at low opacity via `rgba(var(--vc-accent-rgb), …)`, 2026-06-16; เดิม indigo). Collapsed via `<details>` + `vc-collapse-chevron` rotation. **Extracted Phase 7 (2026-05-22)** จาก `fuel_admin.css` §22 → shared component สำหรับ admin_fuel (รถ × เดือน) + budget_manage (แผนก × เดือน fiscal year Mar→Feb). **Phase 2 redesign (2026-05-22):** เพิ่ม `.vc-pivot-section` + `.vc-pivot-section-head` — รองรับ 1 `<table>` ที่มีหลาย `<tbody>` ภายใน (เช่น budget_manage รวม central/dept/personal ใน 1 ตาราง); section head row = icon + label + optional count, span ทุก columns ด้วย `colspan`; bg = `--vc-bg-subtle`, font uppercase tracking-wide. **Phase 5 (2026-06-12):** `.vc-pivot-row-summary .vc-pivot-td-sticky` (bold aggregate row) + `.vc-pivot-td-sub` (indented fg-muted sub-row, font .8125rem — per-user personal pivot). |
| Dropdown + Autocomplete | [components/dropdown.css](../../app/static/core/css/components/dropdown.css) | (no macro — JS hook `data-dropdown` / `data-autocomplete`; JS: [core/js/dropdown.js](../../app/static/core/js/dropdown.js)) | **2026-06-08** — cmdk-style menus, progressive enhancement. (1) **dropdown** (`.vc-dd(-trigger/-value/-chevron/-panel/-option[.is-active][aria-selected]/-divider)`) enhances `<select data-dropdown>`: keeps native `<select>` as source of truth (writes `.value` + dispatches `change` → existing select-bound JS untouched), `<optgroup>` boundary → divider line (labels ignored, no text headers). MutationObserver(childList,subtree) บน native select → rebuild panel อัตโนมัติเมื่อ option ถูก swap ภายนอก (รองรับ chained dependent dropdown เช่น budget_type→budget_sub). (2) **autocompleteinput** (`.vc-ac(-panel/-option[.is-active]/-divider/-empty)`) enhances `<input data-autocomplete list="…">`: reads `<datalist>` options, filters on type, fills input on pick, free text still allowed. (3) **autocompleteselect** (`.vc-ac` markup เดียวกับ (2)) enhances `<select data-autocomplete>`: searchable เหมือน (2) **แต่ native `<select>` เป็น source of truth** — pick → write `select.value` + dispatch `change` → submit `<option>` value (id) ไม่ใช่ typed label; **strict combobox** (free text ไม่ได้ — blur/Esc revert label กลับค่าที่เลือก); input clone class ของ select (chevron/sizing เดิม), select ซ่อนด้วย `.vc-dd-native`; preset value แบบ programmatic ต้อง `dispatchEvent('change')` เพื่อ sync label (ดู `vehicle_budget.js` setBudgetModal). ใช้กับ `#sbApprover` (vehicle_budget.html). **2026-06-09:** panel z-index 1000→**1060** (เหนือ Bootstrap modal stack 1050/1055) → portalled panel ใน modal ไม่ถูกบัง. Keyboard ↑/↓ + Enter select + Esc/Tab close, no icons in rows. Subtle shadow (DESIGN-OVERRIDE — floating menus, follows budget `.vc-dropdown-menu` precedent). `window.VCMenus.init(scope)` to enhance dynamically-injected controls. First used in `vehicle_mileage.html` (`#datePreset` + `booker_q`). **2026-06-08 (portal fix):** panel ของทั้ง 2 component **portal ไป `<body>` + `position:fixed`** (helper `makeFloating`) ตอนเปิด → escape `overflow:hidden`/scroll/transform ancestor ทุกตัว (card/filter-bar/table-wrap) ไม่ถูก clip อีก; auto flip ขึ้นบนถ้าด้านล่างไม่พอ + reposition ตาม scroll/resize; outside-click เช็คทั้ง `root` + `panel` (panel อยู่นอก root แล้ว). แก้เคส dropdown ใน `.vc-card.budget-summary` ที่เคยโดน overflow clip. ใช้กับ filter เพิ่ม: `vehicle_cost.html` (from/to month+year+driver = 5), `admin_fuel.html` (year/month/vehicle/driver = 4) — โหลด `core/js/dropdown.js` (module) ต่อท้าย page JS, dd dispatch `change` → auto-submit เดิม (`vehicle_ot.js`/`vehicle_fuel.js`) ทำงานต่อ. |
| Sidebar | [components/sidebar.css](../../app/static/core/css/components/sidebar.css) | (template: `_sidebar.html`) | `.sidebar` + `.sb-brand/-logo(-icon,-text)/-close/-nav/-section-label/-item(.active)/-icon/-label/-badge/-footer/-logout`. Defines `--sidebar-width`. **Moved Phase 4.1.5 (2026-05-15)** from `vehicle.css` so every page (incl. approver_inbox ที่ไม่โหลด vehicle.css) ได้ sidebar styles ฟรีผ่าน `design-system.css` @import. **Vercel refresh Phase 6.0 (2026-05-20)**: width 250→256, bg `#fff`→`var(--vc-bg-subtle)`, brand row 62→56 height, `.sb-logo-icon` 40→28 + radius 6, `.sb-section-label` UPPERCASE + letter-spacing .04em + `.68rem`, `.sb-item` height 32 + font `.82rem` + padding `0 8px`, `.sb-icon` 20→18 (+ explicit Lucide SVG `width/height 16px stroke 1.5`), logout hover `var(--vc-red-bg)`. `.sb-badge` rules left untouched per user instruction (keep dark pill with active inverted). **Collapsible group + visual refresh (2026-05-23):** เพิ่ม `.sb-group` + modifiers (`.has-active-child` `.is-open` `.has-pending`) + `.sb-group-toggle` (parent button reuse `.sb-item`) + `.sb-chevron` (14px, rotate 180° on open) + `.sb-group-body` (grid-row 0fr→1fr height anim, 280ms) + `.sb-group-inner` (overflow hidden) + `.sb-sub` + `.sb-sub::before` (1px vertical guide line at left:23px, scaleY(0→1) on `.is-open`, 320ms `--sb-ease`) + `.sb-item--sub` (height 32, no icon, padding-left 36, color `--vc-fg-subtle`, opacity+translateY stagger 30ms/item via `:nth-child(1..6)` transition-delay). Active sub = neutral `--vc-bg-hover` bg (no accent bar). Easing token `--sb-ease: cubic-bezier(.32,.72,0,1)`. **Visual refresh same date**: `.sb-section-label` ตัด UPPERCASE+letter-spacing, weight 500→400, font-size .68→.72rem, เพิ่ม `border-top 1px var(--vc-border)` คั่นระหว่าง section (first-child no border); `.sb-badge` dark indigo pill → soft gray pill (`--vc-bg-hover` bg + `--vc-fg-muted` text, weight 600→500); active badge bg `--vc-fg-inverted`→`rgba(0,0,0,.06)`. `@media (prefers-reduced-motion)` ตัด stagger + translateY. **Fix (same day)**: `.sb-group-toggle` `font: inherit` → `font-family: inherit` (เคย override `.sb-item` font-size .82rem ทำให้ "จัดการรถ" ตัวใหญ่กว่า sibling). **Blue recolor (2026-06-13 rev1):** bg `--vc-accent-light`→**`--vc-bg-subtle`** (#FAFAFA light gray); non-active `.sb-item`/`.sb-group-toggle` text→`--vc-fg` (#1C2E4A); `.sb-icon` color→`--vc-fg-muted`; hover bg keep `--vc-blue-bg` + text `--vc-blue`; active (`.sb-item.active` + `.sb-item--sub.active`) bg `--vc-blue-mid`→**`--vc-accent-light`** (#EEF2FF soft-indigo fill) + text `--vc-blue` + weight 600; `.sb-item--sub` non-active→`--vc-fg-muted`; active-child dot ring `--vc-accent-light`→`--vc-bg-subtle` (match new sidebar bg). **Zendenta restyle (2026-06-13 rev2):** `.sb-section-label` ลบ `border-top` divider (flat sections + whitespace) + กลับมา `text-transform:uppercase` + `letter-spacing:--vc-tracking-wide` + .72→.68rem; hover (`.sb-item`/`.sb-item--sub` + icon) blue tint → neutral `--vc-bg-hover`/`--vc-fg`; active text `--vc-blue`→`--vc-accent` (+ icon) + **new `::before` left-bar** 3px indigo (`left:-8px` ชนขอบ sidebar, share rule `.sb-item.active` + `.sb-item--sub.active`); `.sb-group.has-active-child .sb-group-toggle` + collapsed dot `::after` `--vc-blue`→`--vc-accent`. **Bullet dot + rail restyle (2026-06-15):** sub-item เป็น bullet-dot + left rail แบบ Gmail. `.sb-dot` 4px circle (`--vc-fg-muted` resting / `--vc-fg` hover / `--vc-accent` active). `.sb-item--sub` มี `margin-left:21px` (ดัน pill ไปขวาของ rail → bg ไม่คลุมเส้น) + own `border-radius` pill + height 35. แยก `.sb-item.active::before` (sidebar-edge bar, top-level only) จาก sub. **Rail = per-item `.sb-item--sub::before`** (left:-8px, width 2px, `top/bottom:-2px` ให้ segment ชนกันต่อเนื่อง; `:first-child` top:50% + `:last-child` bottom:50% → rail เริ่ม-จบที่ dot center): `--vc-border` resting / `--vc-fg-muted` hover / `--vc-accent` active. **Active = neutral gray pill** (`--vc-bg-hover` — ไม่ใช่ accent-light) + bold + accent rail/dot/text (single-color signal). `@media reduced-motion` selector `.sb-item--sub::before`. |

Total: 9 component CSS files + 8 macros (7 new in Phase 2 + 1 pre-existing `_modal.html`).

**Highlights:**
- `--vc-accent` `#4059e6` (indigo, 2026-06-17 redesign; เดิม `#014198` sila5) = `--vc-primary` — active states + focus ring + selected/today (legacy alias `--ds-accent` ยังมีใน tokens.css Part A — ห้ามอ้างใน code ใหม่)
- `--vc-accent-rgb` `64, 89, 230` (2026-06-17) — channel triplet สำหรับ `rgba(var(--vc-accent-rgb), …)` ที่ต้องคุม opacity (pivot heat-tint, link hover bg). **Indigo drift cleanup (2026-06-16):** rebrand 2026-06-14 แก้แค่ token แต่ component CSS ยัง hardcode indigo เก่า — ไล่แก้ครบ: `pivot.css` (heat-tint rgba `79,70,229`→`var(--vc-accent-rgb)` + drop fallback `#4F46E5`), `vehicle_budget.css` (central→`--vc-accent`, dept `#818cf8`→`--vc-blue-mid`), `vehicle_mileage.css` + `vehicle.css` (drop indigo fallback, focus ring→`--vc-accent-ring`). grep zero-drift ผ่าน
- `--vc-primary` `#4059e6` (indigo, 2026-06-17 redesign; เดิม `#014198` sila5) — primary CTA · `--vc-blue` unify เข้า brand เฉดเดียว · text = navy `#162334` (`--vc-fg`) · `--vc-border` `#f0f0f0` · `--vc-radius-md` 8px (card) · redesign ใช้ 6px (`--vc-radius-sm` = rounded-2) · **+token** `--vc-icon` `#9999b0` / `--vc-icon-bg` `#f0f0f0` / `--vc-sidebar-bg` `#fafbfc` / `--vc-sidebar-active` `#e6ecfa`
- Shadow: ไม่มี (border only) — ยกเว้น modal
- Font: ไทย = Sarabun (`--vc-font-sans`, โหลด global ใน `_header.html`); **ตัวเลข = Manrope** (`--vc-font-mono` = `'Manrope','Sarabun'`, 2026-06-17) ผ่าน `.vc-mono` + `tabular-nums` — โหลด Manrope ใน head เฉพาะหน้าที่ใช้ (vehicle_budget); หน้าอื่น fallback Sarabun. **Heading h1-h3 (2026-06-13):** `--vc-font-heading` = `'Sukhumvit Set', 'Sarabun', sans-serif` (Apple system font, fallback Sarabun บน Win/Android). ใช้กับ **element `h1,h2,h3`** ตรงๆ + color `--vc-blue` พร้อม `!important` (title class รายหน้า เช่น `vrc-*-title`/`fuel-title`/`dash-title` set color เอง specificity ชนะ element → ต้องบังคับ). `.ds-h1/.ds-h2/.ds-h3` (typography scale, ไม่ถูกใช้ใน template ปัจจุบัน) ก็ชี้ token เดียวกันไว้เพื่อ consistency. **Util (2026-06-14):** `.text-header` (Sukhumvit Set, weight 300/light) + `.text-header-bold` (weight 700) — opt-in class ใช้กับ element ไหนก็ได้ (เช่น `#currentMonthLabel`)
- `.vc-scope` = opt-in body class for pages using `--vc-*` foundation

**Per-page CSS:**
| File | ใช้กับ |
|------|--------|
| `vehicle.css` | หน้า user vehicle + history (calendar ถูกลบใน Phase 0.5). **Phase 3.6 (2026-05-15):** ลบ `.badge-approved/.badge-pending/.badge-approver` (3 HEX blocks); `--ds-border`→`--vc-border` + `#111827`→`var(--vc-fg)` ใน bk-modal; เพิ่ม `.bk-info-note` (amber token). **Phase 4.1.5 (2026-05-15):** ลบ sidebar section + legacy `.menu-item`/`.brand-logo`/`.sidebar-menu`/`.menu-section` (158 lines, 618 → 460); ย้ายไป `components/sidebar.css`. **Calendar overflow trigger (2026-05-20):** `.event-more` rewrite → `.event-more.vc-cal-more` (inline-flex + chevron icon 12px, ghost ปุ่มสไตล์ Vercel: muted color resting → `--vc-fg` + `--vc-bg-hover` bg เมื่อ hover, radius `--vc-radius-xs`, ลบ `text-decoration: underline`); เก็บ class `.event-more` ไว้สำหรับ mobile-hide rule. Popover content styles อยู่ใน `vercel.css`. **Detail modal redesign (2026-05-20):** `.bk-modal-dialog` 440→**480px**; ลบ `.bk-detail-header-dot`; เพิ่ม `.bk-detail-status` + 6 modifiers (`--approved/--pending/--approver/--rejected/--group/--completed` → `--vc-{green|amber|blue|red}-bg` + matching fg); `.bk-detail-divider` dashed inset → **solid full-bleed 1px**; `.bk-detail-text` color muted → `--vc-fg`; `.bk-detail-row` align center → flex-start; เพิ่ม `.bk-detail-section-header` (flex container + count); `.bk-detail-section-label` letter-spacing .04em; เพิ่ม `.bk-detail-member-avatar` (32px neutral gray circle + 14px user icon — แทน `vc-status-dot--*` ใน member rows). **Detail modal premium redesign (2026-05-23):** new scoped block under `.bk-detail-content` (กัน leak ไป `room/room-modal-detail.html` ที่ใช้ `bk-detail-*` base classes ซ้อนกัน) — `.bk-detail-dialog` 480→**440px**; `.bk-detail-content` border-radius 10px + overflow-hidden + `bk-content-rise` anim; new `.bk-detail-head` (replaces `card-header` padding) + `.bk-detail-eyebrow` (.7rem muted) + `.bk-detail-date` upgrade เป็น hero (1.0625rem/600/`letter-spacing:-0.01em`) + `.bk-close` absolute top-right; pill: height 22→24px, padding 8→10px, border-radius xs→**full** + matching `--vc-{green\|amber\|blue\|red}-border` + stroke-width 1.75→2 + `bk-pill-in` anim; info rows: padding 16→18-20px gap 12→14, `font-variant-numeric: tabular-nums` ทุก `.bk-detail-text`, new `.bk-detail-row--primary` modifier (plate weight 600 size .9375rem, icon `--vc-fg`) + `.bk-detail-plate-code` (monospace + 0.02em tracking) + `.bk-detail-plate-brand` (muted), row stagger via `nth-child(1\|2\|3)` + `bk-row-in`; section header: `justify-content:space-between` + new `.bk-detail-section-count` (right) + drop UPPERCASE letter-spacing on label; new `.bk-detail-members` wrap (flex col, gap 8px) + `.bk-detail-member` card tiles (border + radius-sm + 12px padding + hover bg-subtle/border-hover + `bk-member-in` w/ `--bk-i` stagger) + `.bk-detail-member-avatar` ring upgrade (36×36 bg:#fff + border) + sub-classes `bk-detail-member-body/-head/-name/-trip/-pickup`; new `.bk-detail-footer` (border-top + `--vc-bg-subtle` action bar, padding 14×20, btn-sm height 32px) + **CSS `:has(#detailActions:empty){display:none}`** แทน JS toggle; 4 keyframes `bk-content-rise/-pill-in/-row-in/-member-in` + `cubic-bezier(.16,1,.3,1)` easing + `prefers-reduced-motion` guard. Base classes `.bk-detail-{title/date/status/divider/info/row/icon/text/section-header/section-label/member-avatar}` คงไว้สำหรับ `room-modal-detail.html` (ไม่ break). **Mobile Phase A+B (2026-05-23):** rewrite `@media (max-width: 767px)` calendar block — `#calendarView .vc-card` drop card chrome (border/bg/radius); `.calendar-grid` gap 0→2px + padding 0 4px; `.calendar-header-cell` 11px UPPER tiny + zero border; `.calendar-cell` `aspect-ratio:1` (square) + `border-radius:8px` + zero borders + press scale `:active{transform:scale(.94)}` + `transition:background var(--vc-dur-fast) var(--vc-ease-snap)`; `.calendar-cell.today` filled black (`background:--vc-fg; color:--vc-on-primary` — inverted text, premium); `.calendar-cell.selected` inset ring `box-shadow:inset 0 0 0 1.5px --vc-fg` (no layout shift); selected+today combo = white inner ring on black fill; `.mobile-indicator` single 4px dot for Phase A (Phase F = 1=dot/2-3=short bar/4+=long bar). เพิ่มท้ายไฟล์ `.vrc-m-hero/.vrc-m-hero-title/-sub/.vrc-m-month/.vrc-m-month-label/.vrc-m-month-controls/.vrc-m-today-btn/.vrc-m-daybar/.vrc-m-daybar-text/.vrc-m-daybar-label/.vrc-m-daybar-count` (mobile-only, desktop hides via `d-md-none`); daybar = `position:sticky; top:0; z-index:--vc-z-sticky` (pins under header เมื่อ list scroll); 2 keyframes `vrc-m-rise/vrc-m-fade` + `@media (prefers-reduced-motion: no-preference)` guard. **Mobile Phase C (2026-05-23):** event-card block ใหม่ — `.vrc-m-list` (flex col gap 8px) + `.vrc-m-evt` (border + radius-md + press scale `:active{transform:scale(.985)}` + hover border `--vc-fg-subtle`) + `.vrc-m-evt-head` (flex/gap 10/padding 12×14) + `.vrc-m-evt-dot` (8×8 circle, `margin-top:7px` optical-align กับ first text line) × 6 variants `--{approved\|pending\|approver\|rejected\|group\|completed}` (map ไป `--vc-{green\|amber\|blue\|red\|accent\|fg-subtle}`) + `.vrc-m-evt-body/-title/-meta` (14px/500/tabular-nums title, 12.5px muted meta with `.sep` color `--vc-fg-disabled`) + `.vrc-m-evt-status` (22px pill, `border-radius:full`, bg + matching `--vc-*-border` like `bk-detail-status` premium pattern) × 6 variants + `.vrc-m-evt-toggle` (28px chevron button, rotate 180° เมื่อ `:not(.collapsed)` transition `--vc-dur-base --vc-ease-out`) + `.vrc-m-evt-sub` (border-top + `--vc-bg-subtle` indented container) + `.vrc-m-evt-sub-row` (button + bg-white nested card, hover bg-hover) + `.vrc-m-evt-sub-time/-body/-title/-meta` + `.vrc-m-empty` (56px circle icon + 2-line title/sub, 56px vertical padding). **Mobile Phase D (2026-05-23):** FAB block — `.vrc-m-fab` (fixed bottom-right 56×56 round, `--vc-fg` bg + `--vc-on-primary` icon, plus icon 22px, `z-index:--vc-z-raised`, `bottom: max(20px, env(safe-area-inset-bottom))` รองรับ iOS safe area, `:active{transform:scale(.92)}`); `:hover` ใช้ `--vc-primary-hover`; `.is-disabled/:disabled` opacity .35 + pointer-events:none (past-date silent disable, Q4). **Shadow exception** — `box-shadow: 0 6px 16px rgba(0,0,0,.18), 0 2px 6px rgba(0,0,0,.10)` ละเมิด no-shadow rule, justified เพราะ FAB ลอยเหนือ scroll plane ต้อง depth cue (Material/Apple pattern). Spring entry animation `vrc-m-fab-in` keyframe (`scale .7→1 + opacity`) duration slow easing spring delay .2s, wrap ใน `prefers-reduced-motion: no-preference` guard. **Mobile Phase E (2026-05-23):** stagger reveal block — `.vrc-m-evt` ใช้ keyframe ใหม่ `vrc-m-item-in` (opacity 0 + translateY(8px) → 1+none) duration `--vc-dur-base` (220ms) easing `--vc-ease-out`, `animation-delay: calc(var(--i, 0) * 30ms)` — JS เซ็ต `--i` inline ผ่าน `style="--i:${idx}"` (running counter ข้าม group + single loops); `.vrc-m-empty` ใช้ keyframe เดียวกัน duration slow ไม่มี delay; ทุก animation wrap ใน `@media (prefers-reduced-motion: no-preference)`. เพิ่ม calendar fade-in ใน mobile media block — `#calendarBody` ใช้ keyframe เดิม `vrc-m-fade` duration slow delay .05s (fires ทุก renderCalendar = month nav + day pick). **Mobile Phase F (2026-05-23):** multi-dot indicator volume hint — `.mobile-indicator` (default 4×4 round dot สำหรับ 1 booking) + 2 modifiers ใหม่ `.mobile-indicator--bar-md` (14×3 pill, 2-3 bookings) และ `.mobile-indicator--bar-lg` (22×3 pill, 4+ bookings); ทั้ง 2 bars ใช้ `border-radius:999px` + `height:3px` override; `.calendar-cell.today .mobile-indicator` rule (white-on-black opacity .7) apply กับทุก variant อัตโนมัติ. **Fix bundle (2026-05-23):** (a) `.calendar-cell.selected.today` เพิ่ม `background: var(--vc-fg)` (re-assert today fill เพราะ `.selected` set transparent → today auto-selected on load จะกลายเป็น white-on-white invisible — bug ที่ user รายงาน); (b) `.vrc-m-daybar` ลบ `position: sticky` + `z-index: var(--vc-z-sticky)` (leak ขึ้นใต้ notification panel + UX value น้อย — daybar scroll ตาม content); (c) ลบ `.vrc-m-fab` block + `@keyframes vrc-m-fab-in` (FAB ถูก revert กลับเป็น inline button ใน daybar ตามที่ user เลือก) **Zendenta redesign → prod (2026-06-14):** **ลบ block page-head ที่เคยเพิ่ม** (`.vrc-phead-*`/`.vrc-nav-*`/`.vrc-today-btn`/`.vrc-cta-btn`) — แทนด้วย fuel-header + `vc-btn` มาตรฐาน (ดู `vehicle.html` row). `.event-card` base → pill: `background:--vc-bg-subtle` + `border:1px solid transparent` + radius-xs + padding 2px 6px (inline status `border-color` จาก JS `EVENT_CARD_STYLE` ยังทำงาน). Calendar restyle "ภาพ 2" + icon sizing ย้ายไปไฟล์ใหม่ `vehicle_zendenta.css` (scope `body.zd`) — ดู row ด้านล่าง. |
| `vehicle_zendenta.css` *(ใหม่ 2026-06-14)* | **`vehicle.html` only** — vehicle theme (scope `body.zd`, link ต่อท้าย). (1) **icon ใหญ่เฉพาะหน้านี้**: `.sb-icon svg` 21px · `.vrc-topbar svg` 22px (+ chevron/brand) — ★ จุดแก้ขนาด icon; (2) **calendar restyle "ภาพ 2"**: `.cal-toolbar` grid `1fr auto 1fr` → `#currentMonthLabel` กลางเสมอ (+ `.cal-month-label`); `.calendar-grid-head` พื้นใส + `.calendar-header-cell`; `.date-number` มุมขวาบน (`align-self:flex-end`); **today = เลขปกติ** (ลบวงกลม base), **selected = เลขวงกลมน้ำเงินทึบ** (วงกลมเดียว); `.calendar-cell.other-month` opacity .5; `.event-card` pill. หมายเหตุ: token indigo `#4059e6` (2026-06-17 redesign; เดิม `#014198`) อยู่ใน `tokens.css` global แล้ว (ไฟล์นี้ไม่ override token); ชื่อ "zendenta" = direction reference. **Mobile (≤767):** `@media` block — calendar-grid-head/header-cell โปร่ง; `.mobile-indicator` → `--vc-accent` + `position:absolute` ก้นช่อง (เลขไม่ถูกดันขึ้น, อยู่กลางช่องด้วย `align-self:center`); today = เลขปกติ / selected = วงกลมน้ำเงิน (เหมือน desktop, วงกลมเดียว). |
| `vehicle_driver.css` *(เดิม driver.css)* | **`/driver` only** (2026-05-08, **rev2 → Vercel namespace**) — uses `--vc-*` tokens (matches `fuel_admin.css` standard), body wrapped in `.vc-scope`. Classes: `.driver-flash-alert/-plate/-unset/-header/-title/-subtitle/-header-meta/-tabs(__btn,__count)/-card(__head,__id,__summary,__chevron,__body, .is-open, --readonly)/-meta(__item,__icon,__label,__value)/-summary(__row,__label,__value)/-form(__title)/-upload(.has-file,__icon,__label,__hint)/-cta/-pill(--waiting,--ontrip,--done,--upcoming)/-done-summary/-readonly-note/-panel(.is-active)`. Mono font (Geist Mono) for BK-id, KPI numbers, inputs. Black primary CTA via `--vc-primary`. **Phase 3.5 (2026-05-15):** ลบ dead `.driver-page/-container` (§1), `.driver-empty*` (§11 — ใช้ macro แทน), icon overrides scoped to `.driver-page` (§13); แทน `#D4D4D4` → `var(--vc-border-hover)`; เพิ่ม `.driver-flash-alert/-plate/-unset`. **Card redesign req1+2 (2026-06-09):** เพิ่ม §16-19 — §16 `.driver-card__plate` (mono/600) + `__time` (muted/flex:1 — head reorder ทะเบียน·เวลา·pill); §17 `.driver-detail-card` (white `--vc-bg-subtle`, no border) + `.driver-detail__label/__head/__requester/__pax/__trip/__purpose/__dest/__arrow/__pickup`; §18 `.driver-emergency` (red-tinted `--vc-red-bg/-border`) + `__label` (สีแดง) + `__row` (flex, ห่อ `.vc-dd`) + `__btn` (red); §19 `.driver-form__hint` (เลขไมล์ล่าสุด inline mono) + `.driver-upload--wide` (taller + 44px round icon) + `.driver-upload__browse` (accent) + `.driver-odo-input`. responsive: `.driver-card__time` ลด font ≤575px. **Ad-hoc collapse req3 (2026-06-09):** เพิ่ม §20 — `.driver-adhoc-panel` (collapse card + `@keyframes driver-adhoc-in` fade/slide) + `.driver-adhoc-form` (vc-dd/vc-ac full-width override) + `.driver-adhoc-mileage` (top divider) + `.driver-adhoc-foot` (flex, submit flex:1) + `.driver-adhoc-btn.is-open`/`__icon` (rotate 45°). _dead เดิม `.driver-card__id/__summary/.driver-meta*` + `.driver-modal*`/`.driver-combo*` ยังไม่ลบ (เลิกใช้แล้ว เหลือ `.driver-modal__btn--ghost` reused)._ ใช้คู่ `core/css/components/dropdown.css` (vc-dd/vc-ac). |
| `vehicle_admin.css` | admin dashboard + budget pages. **Week-card de-animate + mobile one-line meta (2026-06-06):** ลบ animation ทั้งหมดใน week navigator — `.va-week-card` (`va-fade-up`), `.va-week-picker-pop` (`va-pop-in` + `transform-origin`), `.va-week-day`/`.va-week-nav` press `:active{transform:scale}` + spring `transform` transition; ลบ rule `.va-week-strip--switching .va-week-day` + `@keyframes va-pop-in`/`va-week-cell-in` (เหลือเฉพาะ `va-fade-up` ที่ page sections ยังใช้); ล้าง selector ที่ตายแล้วออกจาก reduce-motion block. Mobile `@media (≤767.98px)` rework ให้ `.va-week-meta` อยู่บรรทัดเดียว — `flex-wrap:nowrap`, `.va-week-meta-left` ยืด+ตัด ellipsis (`min-width:0`), `.va-week-meta-right` คงขนาด, `.va-week-picker-btn span` ซ่อน (picker เหลือ icon), popover flip กลับขวา. **Phase 6-8 (2026-05-24):** P6 vehicle list section 7 rewrite — `.vs-row` padding `space-3/space-5`, `.vs-icon` 32px + border + active `--vc-fg` / maintenance `--vc-amber-bg/--vc-amber-border`, `.vs-name` mono sm/500/tracking 0.01em, `.vs-detail-available/-maintenance` uppercase 10.5px tracking 0.06em + dot pseudo `::before` 6×6 + hide inner Lucide; `.va-vehicle-card` `va-fade-up` 180ms delay. P7 page entrance — `.va-page-header` (0ms) + `#sectionBefore` (120ms) + `#sectionAfter` (240ms) ทั้งหมด `va-fade-up base ease-out backwards`; reduce-motion ขยายครอบทุก entry + `.bl-card`. P8 modal polish — `translateY 16→8px`, duration slow→base (220ms snappy); เพิ่ม `~ .modal-backdrop.fade { transition: opacity dur-fast ease-out }` (backdrop fade 140ms). **Phase 5 redesign (2026-05-24):** section 10 booking row rewrite — `.bl-card` card-with-border + 40px circle icon → **flat row** (border:0 + border-radius:0 + border-bottom `--vc-border` + last-child no border); grid `3px minmax(0,1fr) auto` col-gap space-4 padding space-4/space-5; `.bl-rail` 3px vertical bar (`align-self:stretch; border-radius:999px`) แทน `.bl-licon` circle — 5 modifiers `--{pending\|approver\|approved\|rejected\|group}` → `--vc-{amber\|blue\|green\|red\|green}`; `.bl-content` (`.bl-title` text-base/500/letter-spacing -0.005em + `.bl-dept-tag` pill xs muted bg-subtle radius 999px + `.bl-meta` sm/muted flex-wrap + `.bl-meta-text` truncate + `.bl-meta-dot` opacity 0.6); **new** `.bl-right` flex-column align-end gap-2 + `.bl-time` mono/sm/500/tabular-nums; selection simplified (no transform/box-shadow → bg + rail color override); hover `bg-subtle` (เบากว่า); stagger via `style="--va-i"` + `animation: va-fade-up base ease-out backwards + delay calc(min(--va-i, 8) * 30ms)`. Section 6 `.va-list` ลบ padding+gap. `.va-trip:hover` border-color change ลบ (informational). **Phase 4 redesign (2026-05-24):** section 4 filter tabs rewrite — `.va-filter-tabs` strip+border-bottom → **pill container** (inline-flex gap 4px padding 3px margin space-4/space-5 border `--vc-border` radius 8px bg `--vc-bg` + max-width compute + overflow-x auto + scrollbar hidden); `.ftab` chip (padding 5px 12px radius 6px sm/500 muted → hover bg-subtle/fg → **active `--vc-fg`/`--vc-bg`/600** dark fill) + focus-visible 2px accent ring; `.ftab-cnt` tabular-nums opacity 0.6 (active 0.85, inherits color); ลบ Phase 2 polish underline block ทั้งหมด (`.ftab::after scaleX` + `.ftab-cnt` box pill) — comment header แก้ให้ระบุว่า Phase 4 ลบ underline. Pattern lifted จาก `mileage_admin.css .mlg-status-chips/.mlg-chip`. **Phase 3 redesign (2026-05-24):** section 2 rewrite — `.va-week-card` overflow visible (ให้ popover escape) + animation `va-fade-up 60ms backwards`; เพิ่ม `.va-week-meta/-left/-right` (flex space-between row, padding `--vc-space-5 --vc-space-6`), `.va-week-eyebrow` (xs/500/uppercase tabular-nums), `.va-week-selected` (h2 lg/600/letter-spacing -0.014em), `.va-week-today-tag` (xs uppercase pill bg-subtle inline ใน h2), `.va-week-today` ghost btn (sm/500 muted → hover fg), `.va-week-picker` (relative anchor) + `.va-week-picker-btn` (secondary + caret rotate via `[aria-expanded="true"]`) + `.va-week-picker-pop` (absolute top:100%+6px right:0, min-width 240px, animation `va-pop-in`, `transform-origin: top right`); `.va-week-bar` ลบ border-bottom (meta แทน) + padding `--vc-space-3 --vc-space-4`; ลบ `.va-week-heading` (deprecated). เพิ่ม `@keyframes va-fade-up/-pop-in/-week-cell-in` + class `.va-week-strip--switching .va-week-day` apply cell-in 30ms stagger via `--va-i`; mobile (≤767px) wrap meta full-width + popover flip ซ้าย. **Phase 1-2 redesign (2026-05-24):** ลบ section "KPI STRIP — 4 cells override" + Phase 2 polish KPI rules + KPI entry ใน `prefers-reduced-motion`; ปรับ `.va-page-header` gap/padding tokens; `.va-page-title` font-size `clamp(26px, 3.4vw, 32px)` + letter-spacing -0.022em; eyebrow weight 600→500; เพิ่ม `.va-page-header-stat` (pill-shape inline strip — border + radius 999px + tabular-nums) + `.va-stat-item/-num/-lbl/-sep` + dot color modifiers `.va-stat-dot--{amber\|blue\|green}` (แทน KPI 4 cells); reduce-motion block ขยายครอบ Phase 3 (`.va-week-card`/`.va-week-picker-pop`/strip-switching/caret). **Rewritten 2026-05-07** for Vercel shell. **Phase 3.2 (2026-05-14):** ลบ `.bl-badge`/`.badge-pending`-`.badge-group`/`.bl-empty` (รวม 6 HEX colors) → page ใช้ `components/badge.css` + `components/empty_state.css` แทน. **Phase 5.0 (2026-05-16):** redesign booking row 2-line layout. **Phase 5.4 polish (2026-05-16):** (1) icon FOUC fix — `.vc-scope [data-lucide]:not(svg)` reserve 14×14 ก่อน lucide swap; (2) week navigator: padding ใน bar, borders แต่ละ day cell, hover/active border, today marker top-right, mobile breakpoint ≤575px; (3) booking row redesign 2-line → **spacious card** — `.bl-body/.bl-row-head/.bl-name/.bl-head-spacer/.bl-dept-pill/.bl-row-meta(+.bl-meta-chip/.bl-meta-text)/.bl-row-assigned(+.bl-assigned-chip/.bl-assigned-label, only when approved/forwarded & has vehicle/driver)/.bl-row-actions/.bl-group-sub` ลบ `.bl-row-top/.bl-meta-dest/.bl-dot/.bl-actions`; (4) `.va-list` ใช้ flex-column + gap แทน `.card mb-2`; (5) cleanup HEX literals 13 จุด → tokens (`.vs-icon-maintenance/.pts-exp-*/.pts-btn-paid/.pts-btn-telegram/.va-approver-info/.va-budget-track/.va-modal-warning/.bl-ico-approve|reject:hover` → `--vc-{red\|green\|blue\|amber}-{bg\|border}`); (6) trip detail box bg subtle + radius; (7) action icon buttons 36×36 default with `var(--vc-red\|green-bg)` hover. Depends on `components/kpi.css` + `components/filter_bar.css` + `components/badge.css` + `components/empty_state.css` (Phase 2+3 primitives) + `design-system.css`. **Zero HEX** ทั้งไฟล์ตอนนี้. Mobile breakpoints ≤767px (list actions 36px tap target, modal compact) and ≤575px (vehicle row + filter tab + week navigator compact). **Phase 6.1 (2026-05-17):** เพิ่ม week-day-dot variants `.va-week-day-dot--sm` (4×4 dot) / `--md` (15×4 bar) / `--lg` (25×4 bar) + active state inverts to `var(--vc-bg)`; budget bar tone modifiers `.va-budget--warn` (amber fill) / `--danger` (red fill); `.va-budget-warn` ข้อความเตือนใต้ bar (amber/red bg+border); `.va-conflict-warn` warning box ใต้ select ใน modal (amber). **Backdrop coverage moved global (2026-06-06):** ลบ block `#assignModal/#swapModal/#repairModal/#revertModal ~ .modal-backdrop { inset:0; width:100% }` (เคยทำแถบขาวขอบขวาเพราะชน `scrollbar-gutter:stable`) → ย้ายไป global `.modal-backdrop` ใน design-system.css; คง backdrop fade-transition (scoped animation) ไว้. **Phase 11 task-card redesign (2026-06-06):** section 10 เขียนใหม่ทั้ง block (left-icon layout เดิม = dead เพราะ JS เปลี่ยน markup ก่อนแต่ CSS ไม่ตามมา) → **task-list card** ตาม reference image ที่ user ส่ง. `.bl-card` = `display:flex; flex-wrap:wrap` (ไม่ใช่ grid; ไม่มี icon column — เลิกใช้ `.bl-icon/.bl-licon`), **DESIGN-OVERRIDE `border-radius:12px`** + fill `bg` ขาว (hover → `bg-subtle`) ตาม reference (**Phase 13, 2026-06-07:** เดิม soft-fill bg-subtle → กลับด้านเป็นขาว default ให้ pending card เด่นสะอาด), padding space-4/space-5; `.bl-card--done` (approved = ขาว + title หรี่), `.bl-card--group` ขอบเขียว. `.bl-body` (flex:1 flex-col gap space-3) = `.bl-head`(`.bl-head-text`[`.bl-title` 15/700 1-line + `.bl-desc` 13/muted 1-line] space-between `.bl-due`[`.bl-due-label` subtle + `.bl-due-value` 700]) + `.bl-foot`(border-top divider; `.bl-author`[`.bl-avatar` 26px initial circle + `.bl-author-text`] space-between `.bl-foot-right`[`.bl-foot-pax` + `.bl-status` + `.bl-actions`]). Checkbox: `.bl-sel-check` align-self center / `.bl-sel-slot` 18px. Selection = inset 3px rail (`.bl-selected` fg / `.bl-notify-selected` blue) ไม่ดัน layout. `.bl-grp-collapse` = `flex-basis:100%` (sibling ของ `.bl-body` — ดันลงเต็มแถวล่าง แทน `grid-column` เดิม). Entrance stagger `@keyframes blCardIn` delay `calc(--va-i * 40ms)` (gated `prefers-reduced-motion:no-preference`). ลบ dead `.bl-licon/.bl-content/.bl-title-row/.bl-meta/.bl-status-text` (คง `.bl-meta-ico/.bl-meta-dot` ใช้ใน group sub + pax). ลบ Phase 2 duplicate `.bl-card` hover/selection block (1078-1103). Mobile ≤767px: `.bl-head` เป็น column (due ลงล่าง) + ซ่อน `.bl-action-secondary`. **Phase 11.1 (2026-06-06):** ลบ section 5 `.va-collapsed-bar` (+`:hover` +`circle-check`) ทิ้ง — booking list แสดงเสมอ. **Phase 14 bl-card 3-line (2026-06-07):** single booking row redesign — ลบ dead `.bl-author/.bl-avatar/.bl-author-text/.bl-foot-pax` (group ไม่ใช้); เพิ่ม `.bl-trip` row (`.bl-trip-purpose` muted max-45% truncate + `.bl-trip-arrow` subtle + `.bl-trip-dest` fg/600 truncate = purpose→dest), `.bl-badges` (flex:1 overflow-x:auto scrollbar hidden — มือถือ badge scroll บรรทัดเดียว) + `.bl-badge` (flex-shrink:0) + `.bl-badge-adhoc` (purple subtle `--vc-purple-bg/-border`); scoped `.bl-card:not(.bl-card--group) .bl-head{align-items:center}` + `.bl-title{flex:1;min-width:0}`; mobile `.bl-head{flex-direction:column}` → rescope `.bl-card--group .bl-head` (single คง row ชื่อ|status). `.bl-head/.bl-foot/.bl-due/.bl-foot-right/.bl-meta-ico` คงไว้ (group ใช้ร่วม). **Phase 14.1 group align (2026-06-07):** group row ใช้โครง 3-line เดียวกับ single → `.bl-head` align-items flex-start→center (เลิก scope `:not(.bl-card--group)` — รวมเข้า base) + `.bl-title` flex:1 1 auto/min-width:0 เข้า base; ลบ dead `.bl-head-text/.bl-desc/.bl-due/.bl-due-label/.bl-due-value/.bl-foot-right` (single+group เลิกใช้ทั้งคู่); mobile ลบ `.bl-card--group .bl-head{flex-direction:column}` + `.bl-due` rules (group head เป็น row แล้ว). `.bl-meta-ico/.bl-meta-dot` คงไว้ (group sub). **Phase 15 (2026-06-07):** trip card cost badges — ลบ dead `.va-trip-amount`; เพิ่ม `.va-trip-badges` (flex wrap gap) + `.va-trip-badge` (nowrap) + `.va-trip-badge--ot` (blue subtle `--vc-blue-bg/-border`). **Phase 16 inline calendar (2026-06-08):** เพิ่ม `.va-cal*` block (popover ปฏิทินเดือน คลิกเดียวเลือกวัน) — `.va-cal` (absolute top:100%+6px right:0, 280px, border + box-shadow + fade-in `vaCalIn`), `.va-cal-head/-title/-nav`, `.va-cal-grid` (7-col), `.va-cal-dow-cell` (หัววัน, --sun แดง/--sat ฟ้า), `.va-cal-cell` (aspect 1:1 radius-full; `--active` วงกลมทึบ `--vc-primary`; `--today` ring; `--sun/--sat` สี), `.va-cal-dot` (accent dot บอกวันมีงาน). **Phase 16.1 clip-fix (2026-06-08):** `.va-week-card` base = `overflow:hidden` (clip มุม); เพิ่ม `.va-week-card:has(.va-cal:not([hidden])){overflow:visible}` ปลด clip เฉพาะตอนปฏิทินเปิด (history Phase 3 "overflow visible" ถูก override โดย Phase 12 ที่ตั้ง hidden — :has() คือ fix จริงตอนนี้). **Phase 16.2 jitter-fix (2026-06-08):** เพิ่ม `display:flow-root` ใน `.va-week-card` (BFC ถาวร) — กัน margin-bottom ของ `.va-week-bar` (`mb-2`) collapse ออกนอก card ตอน `:has()` สลับ overflow hidden→visible (BFC จาก overflow:hidden หายตอน visible → card หด ~8px = กระตุก). **Phase 5 micro-interactions (2026-06-12):** `.bl-card` hover lift (`translateY(-1px)` + transform transition); `@keyframes pendingDotPulse` → `.vc-badge-warning.vc-badge-dot::before` pulse 2.4s (pending dot พัลส์เบาๆ); `.bl-actions .vc-btn:active` scale(0.93) press; `prefers-reduced-motion` guard ครอบทั้ง 3. |
| `fuel_admin.css` | **Vercel namespace** — fuel page only. Page-specific only now (Phase 2 moved KPI+filter → `components/`; Phase 3 moved badge+empty-state → `components/badge.css` + `components/empty_state.css`; **Phase 7 (2026-05-22)** moved §22 pivot table → `components/pivot.css`). Sections: page shell, header, card, btn, table, list+collapse+meta-grid, form input/segmented radio/modal Bootstrap-override skin/history scroll table. §22 = comment pointer only. |
| `vehicle/css/vehicle_mileage.css` | mileage admin page only (renamed จาก `mileage_admin.css` ใน prefix migration 2026-06-07). **Phase 3.3 (2026-05-14):** stripped from 1095 → ~35 lines — removed ~30 dead class blocks (`.mlg-page-header/-kpi-*/-btn-*/-missing-pill/-panel-pill/-badge*/-month-current-pill/-panel*/-breakdown-*/-bd-*/-month-pager/-pager-*/-month-page*/-month-row*/-mr-*/-filter*/-summary except summary-box/-table*/-col-check/-col-id/-num/-edit-btn/-empty*/-modal-eyebrow` + duplicate `ds-alert*` + most `.mlg-modal/-info-grid/-info-item/-state-title/-summary-box/-preview/-timestamp-box/-refuel-box` styling that template inlines via `var(--vc-*)`) → page now inherits `vc-modal/vc-table/vc-card/vc-filter-bar/vc-kpi/vc-badge/vc-empty` from `fuel_admin.css` + `components/`. Page-only: `.mlg-row/-row-check/-col-dest/-complete-row/-refuel-box .form-check`. **Phase 5.6 (2026-05-17):** เพิ่ม summary-strip block — `.mlg-summary-strip/-mode/-mode--hidden/-item/-icon/-clear` + `@media (max-width: 767.98px)` (wrap items 100%, hide `.vc-dot-sep`, dashed bottom border, full-width clear button). **Phase 5.7 (2026-05-17):** เพิ่ม classes รองรับคอลัมน์ใหม่ — `.mlg-col-bk` (mono BK-id), `.mlg-group-badge` (`+N` chip), `.mlg-budget-label/-budget-sub` (2-line budget cell, ellipsis ที่ 140px), `.mlg-row--group` (subtle bg + ไม่ hover-flash), `.mlg-group-label` (icon+text "งานร่วม"), `.mlg-refuel-link` (no underline), `.mlg-date-cell` (ย้าย inline style ของ date separator มา CSS). **Phase 9.1 (2026-05-22):** ลบ block `.mlg-filter-bar/-section/-section-title/-section-body/-filter-date/-num/-booker/-actions-row` (Phase 5.9 retired) → แทนด้วย **toolbar pattern**: `.mlg-toolbar` (column gap), `.mlg-toolbar-row` (flex-wrap), `.mlg-toolbar-left/-right` (left auto, right `margin-left:auto`), `.mlg-date-preset` (min-width 130), `.mlg-date-range/-input/-sep` (hideable date range group); `.mlg-status-chips` = segmented control (1px border, 3px padding, 8px radius, inline-flex) + `.mlg-chip` (5×12 padding, hover bg-subtle) + `.mlg-chip.is-active` (bg=fg, color=bg invert); `.mlg-adv-btn.is-active` (border+color = accent) + `.mlg-adv-dot` (6px absolute top-right indicator); `.mlg-adv-sheet` (border card 8px radius) + `.mlg-adv-grid` (3-col → 2-col ≤991.98px → 1-col ≤767.98px) + `.mlg-adv-actions` (right-aligned, dashed top border); `.mlg-range-pair/-sep` (cost min/max inline pair). **Phase 16.3 (2026-06-08):** ลบ `.mlg-date-input`; เพิ่ม `.mlg-datepick` (relative anchor) + `.mlg-date-btn` (inline-flex icon+label, min-width 150, `background-image:none` ลบ caret ของ vc-filter-select, `[aria-expanded=true]` border=fg) + `.mlg-date-btn-label` (flex truncate, muted ตอนยังไม่เลือก) + `.mlg-date-btn--filled` (label = fg); mobile `.mlg-datepick{flex:1}` + `.mlg-date-btn{width:100%}` แทน rule `.mlg-date-input` เดิม. Calendar popover style reuse `.va-cal*` จาก vehicle_admin.css. **Phase 16.4 (2026-06-08):** เพิ่ม `#mlgResults` (transition opacity) + `#mlgResults.is-loading` (opacity 0.55 + pointer-events:none) — dim ระหว่าง AJAX fetch. **Phase 17 adv-filter popover (2026-06-08):** เพิ่ม `.mlg-adv` (relative inline-flex anchor); `.mlg-adv-sheet` จาก border card (8px radius) → **popover absolute** (top calc(100%+4px), right:0, width 560px + max-width calc(100vw−2×space-4), z-index 1000, box-shadow ผ่าน DESIGN-OVERRIDE — mirror `.vc-dd-panel`); `.mlg-adv-dot` background `accent`→`--vc-red`; `.mlg-adv-grid` 3-col→**2-col** (ลบ media 991 ที่ซ้ำ, เหลือ 1-col ≤767); เพิ่ม `.mlg-adv-col-full` (grid-column 1/-1) + force `width:100%` ให้ `.vc-filter-select`/`.vc-dd`/`.vc-dd-trigger`/`.vc-ac`/`.vc-ac>input` ใน grid. |
| `vehicle_cost.css` | **`/admin/cost` only** — OT page. Pure `--vc-*` (Phase 3.7). Classes: `.cost-header/-title/-subtitle/-header-actions`, `.cost-rate-banner/-banner-title/-rate-pills/-rate-pill`, `.vc-tabs/.vc-tab/.vc-tab-count` (tab bar), `.cost-slot-tag/-morning/-evening/-night/-slot-rate` (time band chips), `.cost-table-footer/-meta/-total`, `.cost-slot-row/-row-field/-row-remove/-row-hint`, `.cost-total-box/-label/-hours/-amount`, `.cost-slot-add-btn`, `.cost-range-chip`, `.cost-action-group` (inline-flex gap-1 for row actions), `.cost-print-*` (receipt @media print). **Phase 5.10 (2026-05-18):** เพิ่ม `.cost-rate-row` (6-col grid label/day/start/end/rate/×, vc-bg-subtle bg) / `.cost-rate-row-field` / `.cost-rate-row-day` / `.cost-rate-row-remove` (32×32 icon button) / `.cost-rate-row.is-removed` (opacity .4 + pointer-events none for soft-deleted existing rows) + mobile ≤575px breakpoint; rate banner: `.cost-rate-pill--day` (amber bg/border, `--vc-amber-bg/-border/--vc-amber`) + `.cost-rate-pill-day` (label chip "อาทิตย์" prefix). **2026-06-09 (manual OT):** redesign `.cost-slot-row` → mirror `.cost-rate-row` (flat transparent input, hover/focus border) + เพิ่ม `.cost-slot-head`/`.cost-slot-head-spacer` (column header เหมือน thead, label sr-only desktop, stacked ≤575px); เพิ่ม datepicker `.budget-datepick`/`.budget-date-btn`/`.budget-date-label`(+`--filled`)/`.va-cal--start`/`.va-cal--end` (va-cal core มาจาก `vehicle_admin.css`). |
| `maintenance.css` | **`/maintenance` only** (2026-05-17) — `vc-scope` page. Classes: `.maintenance-header/-header-actions`, `.maintenance-resolved-note` (truncate with tooltip). ใช้ `--vc-*` ทั้งหมด, zero HEX. **Redesign (2026-06-16):** ลบ `.maintenance-title/.maintenance-subtitle` (header ใช้ utility class); + custom DataTables pagination scoped `#maintenanceTable_wrapper` (pill วงกลม accent, prev/next chevron `::before`) + `.maintenance-goto/-label/-select/-btn` (copy #2 ของ pattern — ถ้าหน้า #3 ใช้ → extract shared component). |
| `repair.css` | **`/repair` only** (2026-05-14) — `vc-scope` page. Classes: `.repair-header/-title/-subtitle/-header-actions`, `.repair-kpi-5` (5-col grid, responsive), `.repair-modal-icon(--warning/--blue/--success)`, `.repair-modal-title/-subtitle`, `.repair-form-section-label`, `.repair-info-box/-info-avatar/.repair-dashed`, `.repair-upload-zone`, `.resolved-note-cell`, `.repair-flash-wrap`, `.repair-kpi-card`, `.repair-time-sub`, `.repair-category-sub`, `.repair-reporter-name`, `.repair-select`, `.repair-input-lg`, `.repair-textarea`, `.repair-required`, `.repair-note-hint`. **Phase 3.4 (2026-05-15):** เปลี่ยน `--ds-*` ทั้งหมด → `--vc-*`; แทน HEX `#FFFBEB/#FDE68A` → `--vc-amber-bg/border`, `#EFF6FF/#BFDBFE` → `--vc-blue-bg/border`, `#F0FDF4/#BBF7D0` → `--vc-green-bg/border`. ไม่มี `--ds-*` เหลือ. **Redesign (2026-06-16):** ลบ `.repair-title/.repair-subtitle` (header ใช้ utility class แทน); เพิ่ม custom DataTables pagination scoped `#repairTable_wrapper` (pill วงกลม สี accent, prev/next chevron ผ่าน `::before`) + `.repair-goto/-label/-select/-btn` (go-to-page). |
| `vehicle_approver.css` *(เดิม approver_inbox.css)* | **`/vehicle/approver` only** (Phase 3.8, 2026-05-15; mobile redesign Phase 5.11, 2026-05-18) — ใช้ `--vc-*` ทั้งหมด. Classes: `.approver-wrap` (max-width 560px center), `.approver-flash`, **budget block** `.budget-card/-card-row/-card-left/-card-icon/-card-name/-card-amount/-card-total/-card-used/-card-sep/-progress/-bar`, **tabs (pill)** `.inbox-tabs/.inbox-tab(.active)`, **card** `.approver-card`, **header** `.ac-header/-header-row/-header-right/-ref/-meta/-meta-sep/-chevron(.rotated)/-body`, **actions** `.ac-action-row/-action-form/.btn-approve-action/.btn-reject-action`, **reject form** `.ac-reject-wrap/-reject-input/-reject-cancel/.btn-reject-confirm`. **Detail-card redesign (2026-06-10):** ลบ field-grid block (`.ac-fields/-field/-field-full/-field-label/-field-value/-reject-reason`); เพิ่ม **fuel badge** `.ac-fuel-badge` (amber tone, mono tnum), **detail card** `.ac-detail-card`(`:not(:last-child)` margin-bottom)`/-detail__head/__requester/__pax/__datetime/__date/__time/__trip/__purpose/__dest/__arrow/__pickup` (เลียน `driver-detail-card`, `--vc-bg-subtle` bg), **reject note** `.ac-reject-note` (red box แทน field). Mobile breakpoint ≤380px (datetime gap + compact buttons). |
| `room.css` | **`/room` only** (2026-05-17) — `vc-scope` page. Classes: `.room-legend`, `.room-badge(--small/--large)` + `.room-dot`, `.event-card.room-small/.event-card.room-large` (override calendar event color per room), `.room-list-dot(--small/--large)`, `.room-detail-dot(--small/--large)`, `.room-mobile-empty`. Reuse `vehicle.css` calendar grid. **Toolbar/grid-head (2026-06-16):** เพิ่ม `.cal-toolbar` (grid `1fr auto 1fr`), `.cal-toolbar-left` (flex), `.cal-month-label` (`<h6>` reset margin + tabular-nums จัดกลาง), `.calendar-grid-head/-header-cell` (พื้น `--vc-bg-subtle`) — copy/unscope จาก `vehicle_zendenta.css` เพราะ room.html ไม่โหลดไฟล์นั้น (ไม่มี `body.zd`) ทำให้ toolbar style หาย. Zero HEX (tokens only). |
| `manage_fleet.css` | **`/admin/manage-fleet` only** (Phase 6.2, 2026-05-17) — `vc-scope` page. Classes: `.mf-header/-title/-subtitle`, `.mf-grid` (2fr/1fr → 1fr ≤1199px), `.mf-col-right`, `.mf-plate/-name-line(-primary/-sub)/-cap/-odo-unit`, `.mf-username-chip/-dept-chip`, `.mf-actions/-icon-btn(--danger)` (28×28, scale 0.94 active 80ms), `.mf-stagger` (row enter 40ms cascade, `cubic-bezier(0.23,1,0.32,1)`), `.mf-driver-row(-main/-name/-meta/-jobs)`, `.mf-approver-row(-name/-meta)`, `.mf-readonly-note`, `.mf-switch-row`, `.mf-hist-total(-label/-value/-unit)`, `.mf-spin/-hist-loading/-distance-pos`. Custom easings `--mf-ease-out/-in-out` scoped on `.vc-scope`. Modal dialog enter `translateY(8px) scale(0.97)` → `0/1` 200ms ease-out (Emil: never `scale(0)`). Respects `prefers-reduced-motion` (animation off, opacity-only). Zero HEX. |
| `budget_manage.css` | **`/admin/budget` only** (Phase 5.8, 2026-05-18 — renamed from `budget_admin.css`) — `vc-scope` page. Classes: `.budget-header/-title/-subtitle/-header-actions`, stack helpers `.budget-flash-stack/-kpi-strip/-grid/-grid--last/-empty-block`, `.budget-filter-month` (calendar chip), `.vc-section-hdr(-icon[.is-purple]/-title/-count/-amount/-amount-label/-actions)` (`:first-of-type` looser top; **Phase 2 (2026-05-21):** `.vc-section-hdr-amount` mono/tnum 14px/600 + `.vc-section-hdr-amount-label` xs/400 muted — รับ amount จาก KPI cells ที่ลด), `.vc-bcard(-head/-name/-actions/-date/-pct-hero/-pct[.is-warn/-danger]/-pct-label/-amounts/-rem[.is-success/-warn/-danger]/-approver)` (**Phase 3:** `.vc-bcard-row` retired → `.vc-bcard-pct-hero` + `.vc-bcard-pct-label`; **Phase 4 (2026-05-21):** `.is-purple` modifier ลบครบ (sec-hdr-icon/bcard-name/progress); `.vc-bcard` padding space-5 + asymmetric child margin-top + animation `bm-fade-up` + `animation-delay: var(--bm-delay, 0ms)` + hover bg-subtle; `.vc-bcard-pct` text-2xl/tracking-tight; `.vc-bcard-pct-label` 11px uppercase tracking-wide; `.vc-progress > span` baseline `--vc-fg` (monochrome) + animation `bm-progress-fill`; new `.vc-kpi-cell--signal` (bg-subtle); new `.budget-modal-enter` (scoped modal entrance); page-scoped CSS tokens under `.vc-scope`: `--bm-ease-out` (cubic-bezier 0.16,1,0.3,1) + `--bm-dur-fast/-base/-slow` (180/240/480ms); keyframes `bm-fade-up/-progress-fill/-fade-in`; `@media prefers-reduced-motion` block. **Note:** `--bm-*` tokens defined under `.vc-scope` global selector แต่ load เฉพาะ budget_manage.css → ไม่ leak ไปหน้าอื่น) + **`.vc-bcard--inactive`** (Phase 5.9 — opacity 0.65 + repeating diagonal stripe), `.vc-progress[.is-warn/-danger]` (`.is-purple` CSS rule ลบใน Phase 4a; `.vc-btn-purple` + `.vc-badge-purple` blocks ลบใน Phase 8, 2026-05-22), `.vc-dropdown(-menu[.is-open]/-item[.is-danger]/-divider/-form)` (no shadow; Phase 5.9 — danger tone + form-wrapped POST item), `.budget-refund-scroll/-refund-hint` + `[data-pick-booking].is-picked`, **`.budget-personal-line`** (Phase 5, 2026-05-22 — was `.budget-personal-row` card; rewrite block §10 → inline strip: drop border/bg/radius/padding, ใช้ `padding-block` + `border-bottom` separator; `.meta` flex baseline inline; label sm lowercase + value sm mono fg + meta-end `margin-left:auto`; icon 16px green retained), **`.budget-modal-mono/-meta/-meta-row/-notice`** (Phase 6, 2026-05-22 — `-alert/-alert--flush` retired; `.budget-modal-meta` = `<dl>` flex column gap-2 + `border-block` Stripe-style + child `<dt>` 11px uppercase tracking-wide muted + `<dd>` sm/600/fg; `.budget-modal-notice` = flex top-border line w/ lucide icon + `<strong>` fg), **`.budget-pivot-card/-summary-content/-empty`** (Phase 7, 2026-05-22 §12.5 — page-local tweaks สำหรับ pivot section; base styles `.vc-pivot-*` มาจาก `components/pivot.css` shared component), `.budget-empty-icon`. **Phase 2 redesign (2026-05-22):** rename `.budget-kpi-strip` rule → `.budget-summary` margin helper; §2 `.budget-grid/-grid--last` margin rules retired (card grid → table); new §13 **`.budget-table-wrap/.budget-table/.budget-row[--inactive]/-name/-sub/-pct-cell/-pct/-rem-over/-tags/-approver[--missing]/.budget-personal-paid-at/.vc-progress--mini`** — table view สำหรับ central/dept/personal sections (vc-table base + custom row name w/ secondary date line + inline mini progress + dropdown col + inactive row dashed stripe). new §2B **`.budget-summary*`** block — `.budget-summary` (vc-card ผอม padding-5/6) + `-head/-title` (uppercase 13px muted) + `-amount/-used/-sep/-cap` (hero 2.25rem mono + 1.25rem cap) + `-bar/--ok/--warn/--danger` (4px progress) + `-meta/-pct/-pct--{tone}/-remain/-remain--danger/-meta-sep/-meta-dot` + `-signals/-signal/-signal--warn/--danger/-signal-amount` (footer pills, border-top); §10 `.budget-personal-line` retired (cleared block ← comment placeholder); new §12 **`.budget-section*`** block — `.budget-section[open]` `<details>` accordion (border 8px radius + border-strong on open) + `-summary` (cursor pointer flex justify-between, list-style none, hover bg-subtle) + `-label/-chev[rotate 90deg on open]/-icon/-title/-count` + `-stats/-amount/-amount-dot/-amount-warn/-pct[/-pct--warn/-pct--danger]` + `-body` (padding bottom + border-top + animation bm-fade-up) + `-actions` (flex justify-end); responsive ≤575px stacks header actions + section stats. Zero HEX (one purple `#6420A8` hover only — Vercel purple-700). **2026-06-08 redesign (§17–19):** §17 `.budget-summary-stack/-seg[--central\|--dept]/-legend/-legend-item[--aside]/-legend-dot[--central\|--dept\|--free\|--personal]/-legend-val` (stacked KPI bar แทน single `.budget-summary-bar`; central = `--vc-accent`, dept = indigo tint `#818cf8`, free = `--vc-border-strong`, personal = `--vc-green`); §18 `.budget-pivot-head/-title/-hint/-rows/-cat[open]/-cat-summary/-cat-label/-cat-chevron/-cat-icon/-cat-name/-cat-count/-cat-figures/-cat-cap/-cat-badge[--ok\|--warn\|--danger]` (pivot default = 3 สรุปแถว + กดขยาย matrix รายเดือน); §19 `.budget-bin-form/-bin-btn` (soft-delete trash แดง = `toggle_active`) + `.budget-row-sub--approver/--approver-missing` (approver ย้ายเป็น sub-line ใต้ชื่อกอง). HEX exception: `#818cf8` (indigo-400 tint สำหรับ dept segment/dot — derived จาก accent). **DNA redesign (2026-06-17) — ไฟล์จริงชื่อ `vehicle/css/vehicle_budget.css`** (row label `budget_manage.css` = ชื่อเก่า stale): §20B seg/legend สีใหม่ central=`--vc-accent`/dept=`--vc-amber`/**remain**=`--vc-green` (เลิก `--personal`); **§22** ใหม่ — `.budget-tabbar` (tab+toolbar บรรทัดเดียว, underline ย้ายมาที่นี่) · `.budget-toolbar` · `.budget-month-menu` (max-height 320 scroll) · `.budget-summary-icon` monochrome (`--vc-icon`/`--vc-icon-bg`) · `.bcard-section-head[--off]/-dot[--on green/--off กลวง]/-label/-count/-line` · `.bcard-grid` (auto-fill 280px) · `.bcard[:has(.vc-dropdown.is-open)]/-icon/-id/-name/-amount/-amount-cap/-pct[.is-ok/-warn/-danger]/-menu/-divider/-meta/-meta-row/-meta-faint/-meta-missing` · `.bcard--off` (bg `--vc-sidebar-bg` + dashed + disable tone) · `.bcard-activate`. radius 6px (`--vc-radius-sm`), no shadow, border `#f0f0f0`. |
| `dashboard.css` | **`/dashboard` only** (2026-05-17) — `vc-scope` landing page. Classes: `.dash-header/-title/-subtitle`, `.dash-section/-section-label`, `.dash-grid-{2,3,4}` (responsive: 4→2→1, 3→1, 2→1), `.dash-mine(-icon/-body/-title/-desc/-chev)` (linkable my-ticket row with chevron translateX hover), `.dash-svc(-head/-icon/-title/-desc/-foot/-cta)` (service card with arrow translateX hover), `.dash-kpi(-label/-value(-muted)/-meta)` (admin KPI), `.dash-alert` (amber banner), `.dash-superadmin(-info/-icon/-title/-desc)` (black inverted card + inverted vc-btn), `.dash-recent-card .dash-recent-sub/-time`, **`.dash-req-title`** (icon+text cell), **`.dash-today(-row/-time/-icon/-body/-title)`** (วันนี้ของฉัน timeline — 2026-06-16), `.dash-fade.d-{1..5}` (stagger entrance — 60ms delay, `cubic-bezier(0.23,1,0.32,1)`, 280ms, scale 0.995 + translateY 6px; `prefers-reduced-motion: opacity-only`). Zero HEX (tokens only). No shadow (border-hover on interactive). **2026-06-16:** `.dash-kpi*`/`.dash-alert`/`.dash-mine*` = dead (Action Hub ลบ markup; ยังไม่ purge). |
| `notification.css` | notification panel + toast. **Icon color + timeline (2026-06-15):** icon สีตามสถานะ ntype — `.notif-cat-icon.notif-icon-{success\|info\|warning\|danger}` = color `--vc-{green\|primary\|amber\|red}` + tint bg/border `--vc-{green\|blue\|amber\|red}-bg/-border` (info→blue); โครงสร้าง/text ใช้ `--vc-fg`, เน้น unread ด้วย `--vc-accent` (dot/text). icon uniform ทุกระดับ: container 28px + glyph 14px + `stroke-width 1.75` (inner เลิก inline `22px` → ใช้ base 28px). `.notif-inner` = timeline 2 บรรทัด (title เด่น `.notif-inner-title` + subtitle clamp 1 บรรทัด `.notif-inner-msg` + `.notif-inner-time` ชิดขวา); connector แนวตั้ง (`::before` left:14px = กลาง icon-node, **เฉพาะช่องว่าง top:41/bottom:-5 ไม่ชน icon, สี `--vc-fg`** ให้เห็นชัด — 2026-06-15) + `.notif-cat-icon` z-index:1; `.notif-timeline--notifs` expanded padding-left **30px** → inner icon-node/text align ใต้ parent icon column (x=44/68) |
| `vercel.css` | **Vercel-style refresh Phase 6.0** (2026-05-20) — global via `_header.html` link. Class prefix `vrc-` (กัน Bootstrap `.btn-primary` + namespace `vc-*`). Uses existing `--vc-*` tokens. **Phase 6.0 (_header)**: `.vrc-topbar` (56px sticky bar, bg-subtle), `.vrc-topbar-title` (**DNA 2026-06-17:** หัวเรื่องใหญ่ 19px/700 ใน topbar แทน breadcrumb เดิม `.vrc-topbar-crumb*` ที่ลบแล้ว — รับ `page_title or page_section`), `.vrc-topbar-btn` 32×32 icon button + descendant rule `.vrc-topbar .ds-btn-icon` ให้ existing notif/user buttons กลมกลืนโดยไม่ต้องแก้ markup, `.vrc-topbar-dot` red dot indicator. **Calendar popover (2026-05-20)**: เพิ่มบล็อก `.popover.vc-cal-pop*` — no shadow, border-only, no header, 240px × max-height 320px scroll, arrow recolor → `--vc-border/--vc-bg`; row grid `.vc-cal-pop-row` (`auto 44px 1fr` columns, hover `--vc-bg-subtle`, last-child no border), `.vc-cal-pop-dot` 6px + variants `--approved/--pending/--approver/--rejected/--group` mapped ไป `--vc-{green|amber|blue|red}` tokens, `.vc-cal-pop-time` mono 12px tabular-nums, `.vc-cal-pop-dest` 13px muted ellipsis + `.vc-cal-pop-dest-icon` 11px (สำหรับ group users icon). **Roadmap**: หลัง _header เสถียร → port sidebar/button variants/calendar จาก `docs/design/vehicle-warm-mockup.html`. **Hero banner (2026-06-13)**: เพิ่มบล็อก `.vrc-hero` (ใต้ `.vrc-page-subtitle`) — `position:relative` flex บนพื้น `--vc-accent-light` + border `--vc-accent-border` + radius-lg, `min-height:136px`; `.vrc-hero-text` (z-2, max-width 60%), `.vrc-hero-title` (text-xl/700 `--vc-accent-dark`), `.vrc-hero-sub` (text-base `--vc-accent`), `.vrc-hero-img` (absolute right/bottom, height 156px, bleed); `@media(max-width:1100px)` ย่อรูป 122px. ใช้กับ `vehicle/vehicle.html` (desktop only). |
| `main.css`, `util.css` | **orphan ตั้งแต่ 2026-06-16** — login.html เลิกโหลด main.css แล้ว (ดู `auth/css/login.css`). util.css ยังถูก include ที่อื่นไหมให้เช็กก่อนลบ |
| `auth/css/login.css` | **หน้า login** (2026-06-16) — Sneat-style card, `--vc-*` tokens เท่านั้น, no shadow→border. คลาส `login-card/login-brand/login-field/login-input/login-toggle/login-row/login-btn/login-flash/login-dots` |
| `auth/js/login.js` | **หน้า login** (2026-06-16) — IIFE toggle แสดง/ซ่อน password (สลับ icon-eye / icon-eye-off) |

**Core JS modules** (`app/static/core/js/`, Phase 4.0 — 2026-05-15; ย้ายจาก `js/core/` ขั้น 5, 2026-06-07):
| File | Exports |
|------|---------|
| `core/icons.js` | `initIcons(scope?)` — guarded `lucide.createIcons()`; ส่ง `Element` เพื่อจำกัด scope (modal-only re-init); `bindModalReinit()` — re-render on `shown.bs.modal` (auto-scope ไป `e.target`) |
| `core/format.js` | `thb(n)`, `km(n)`, `number(n)`, `thaiDate(d, {abbr})`, `thaiTime(d)` — Thai BE year + locale formatting |
| `core/http.js` | `get(url, params)`, `post(url, data)`, `del(url)` — auto JSON parse, CSRF from `<meta name="csrf-token">`, throws `HttpError` on non-2xx |

**ที่ยังไม่สร้าง** (จะเพิ่มเมื่อ feature module ต้องการ): `core/modal.js`, `core/toast.js`, `core/form.js`

**Per-page JS:**
| File | โหลดใน |
|------|--------|
| `pages/vehicle.js` | vehicle templates (รวม modals ทั้งหมด). **Phase 3.6 (2026-05-15):** `STATUS_BADGE` map → `vc-badge vc-badge-{warning|blue|success|danger|neutral} vc-badge-dot`; ลบ `badge rounded-pill` wrapper + inline `style="font-size:..."`. **Phase 4.7 (2026-05-15):** legacy `vehicle.js` (790 lines) → `pages/vehicle.js` ES module (`type="module"`); ลบ DOMContentLoaded wrapper (module deferred); expose `openEventDetail`/`openEditBookingModal`/`openMoreEvents`/`openBookingModal` ผ่าน `Object.assign(window, …)` + `eventDetailModal`/`moreEventsModal` ผ่าน `Object.defineProperty` getter (late binding); ย้าย `?pay=<id>` deep-link IIFE จาก `vehicle.html` เข้า module. **Phase 2 user-facing (2026-05-17):** `initFlatpickr()` เพิ่ม `updateDuration()` closure (compute end−start, bind `change` ทั้ง bkStart/bkEnd, output "ระยะเวลา X ชม. Y นาที" ใน `#bk_duration_preview`). **Phase 3 user-facing (2026-05-17):** `import { initIcons, bindModalReinit } from '../core/icons.js'` + `bindModalReinit()` module-top; `STATUS_ICON` map: FA strings → Lucide names (clock/send/circle-check/circle-x); 4 จุดที่ `ds-status-dot` → `vc-status-dot` (group avatar / single dot / detail header / members); FA `<i class="fa-...">` → `<i data-lucide="..." class="vc-icon-sm">` (users/${iconName}/circle-check/user); `updateMobileList` ปลาย: `initIcons(content)`; `openEventDetail` header icon: `outerHTML` swap (รักษา `id="detailHeaderIcon"` + bindModalReinit แปลง Lucide → svg ตอน shown.bs.modal); ลบ `headerDot.style.cssText='width:44px;...'` → ใช้ class `.bk-detail-header-dot`. **Phase 4 user-facing (2026-05-17):** cleanup ครบทุกอย่าง — `var(--ds-text-heading|text-muted|border)` 14 จุด → `var(--vc-fg|fg-muted|border)`; HEX 8 จุด (popover `#f3f4f6`/`#2563EB`, EVENT_CARD_STYLE 5 statuses) → tokens; ~17 FA/BI icons → Lucide (empty state calendar-x, group card truck/user/clock/pencil/chevron-down/arrow-up-right, single card user/clock/map-pin/pencil, openMoreEvents user, members list users/arrow-right/map-pin, footer actions pencil/trash-2, popover users); inline-HEX buttons → `vc-btn` variants; "งานรวม" badge → `vc-badge vc-badge-blue`; `createCell` ARIA (role=gridcell + aria-label + aria-current). เพิ่ม module-top `shown.bs.popover` listener + keydown handler (← →/T). **Phase 9 (2026-05-22):** `detailActions` block ใช้ `e.canCancel` แทน `(e.isOwner && e.isPending)`; ปุ่ม "ยกเลิก" POST ไป `e.cancelUrl` (เปลี่ยนจาก `deleteUrl`); confirm dialog ขยายเป็น "งบจะถูกคืน + แจ้ง Admin/Approver/Driver/ผู้ร่วมเดินทาง"; ปุ่ม "แก้ไข" ยังจำกัด `e.isOwner && e.isPending` (approved booking แก้เองไม่ได้). **Detail modal premium redesign (2026-05-23):** new `_esc()` helper (HTML-escape for innerHTML safety); `_plateLabel()` rewritten — return HTML (`<span class="bk-detail-plate-code">{plate}</span><span class="bk-detail-plate-brand">{brand}</span>`) แทน plain text → `detailPlate.innerHTML` (เปลี่ยนจาก `.textContent`); `detailMembersList` template restructured จาก flat rows w/ `border-bottom` → `.bk-detail-member` card tiles + `style="--bk-i:${idx}"` (CSS var stagger animation); members container ใช้ `.bk-detail-members` (was `px-4 pb-2`); ลบ L760-761 JS footer collapse (`actDiv.closest('.card-footer').style.display`) — CSS `.bk-detail-footer:has(#detailActions:empty){display:none}` ทำแทน. **Book modal date format (2026-05-23):** `initFlatpickr()` แปลง `#bk_date` native date input → flatpickr instance + `altInput:true` (visible Thai display "วันจันทร์ที่ 4 พฤษภาคม 2569" — Buddhist year +543 + Thai day/month full names) + `dateFormat:'Y-m-d'` (hidden value คงเดิมสำหรับ form submit + L155-158 `.value` reads); custom `formatDate` callback branch ตาม marker `'thai_be'` (altFormat) — fallback `flatpickr.formatDate()` static method สำหรับ Y-m-d; `disableMobile:true` (force flatpickr ทุก device); `locale: flatpickr.l10ns.th` (Thai locale โหลดอยู่แล้วใน vehicle.html L178); `openBookingModal()` L802 `bkDate.value = ds` → `bkDate._flatpickr.setDate(ds, true)` (sync altInput display เมื่อเปิด modal จาก calendar cell) + guard `bkDate._flatpickr` กัน init ซ้ำ. **Edit modal mirror book (2026-05-23):** extract 2 helpers `_initThaiDatePicker(input)` + `_bindTimeDuration(startEl, endEl, previewEl)` — shared by `initFlatpickr()` (book) + `initFlatpickrInModal()` (edit); ลบ legacy datetime flatpickr ใน `initFlatpickrInModal()` ทั้งหมด — แทนด้วย `_initThaiDatePicker('#edit_date')` + `_bindTimeDuration('#edit_start_time','#edit_end_time','#edit_duration_preview')`; `openEditBookingModal()` rewritten — populate `edit_start_time.value=e.time` + `edit_end_time.value=e.timeEnd` (แยกจาก e.date) + `editDate._flatpickr.setDate(e.date, true)` ใน setTimeout, ลบ legacy `editStartDatetime._flatpickr.setDate(\`${e.date}T${e.time}\`)`; pre-set `.is-valid` บนทุก `[required]` field ที่มีค่า (mirror book modal `shown.bs.modal` valid-icon refresh); dispatch `change` event บน `edit_end_time` เพื่อ trigger duration preview. `editBookingForm` submit handler L182-185 rewritten — `e.preventDefault()` + check validity → compose `editStartDatetime.value = edit_date + 'T' + edit_start_time` + same สำหรับ end (mirror bookingForm L155-159 pattern); เพิ่ม valid-icon listeners (`input`/`change` toggle `is-valid` ตาม `checkValidity()`) สำหรับทุก `#editBookingForm [required]`; `hidden.bs.modal` cleanup เพิ่ม `.is-valid` removal (mirror book pattern). **Mobile Phase A+B (2026-05-23):** month-nav listeners refactor — extract `_prevMonth/_nextMonth/_gotoToday` arrow funcs + dual-bind ผ่าน `['prevMonthBtn','prevMonthBtnMobile'].forEach(id => document.getElementById(id)?.addEventListener('click', _prevMonth))` (และคู่ next/today ทำนองเดียวกัน) — desktop + mobile toolbar share handler ตัวเดียวกัน; `renderCalendar()` เพิ่ม `_monthLabel` const → write ทั้ง `currentMonthLabel` + `currentMonthLabelMobile` (เช็ก existence ก่อน write); `updateMobileList()` เพิ่ม `_dayCount` (= `mockEvents.filter(e=>e.date===ds).length`) + populate `#mobileDaybarCount` ด้วย `{N} รายการ` (empty string เมื่อ 0 → CSS `:empty` ซ่อน). **Mobile Phase C (2026-05-23):** `updateMobileList()` markup refactor ครบ — เปลี่ยน Bootstrap `card`/`card-body` markup → `.vrc-m-evt*` premium event-cards; empty state จาก inline `<div class="text-center py-5">` → `.vrc-m-empty` 3-element circle-icon + title + sub copy; wrap list ทั้งหมดใน `<div class="vrc-m-list">`. Helpers ใหม่ใน scope `dotKey(s)/statusKey(s)` (handle `'completed' → 'completed'` variant, else fall back `STATUS_DOT[s]\|\|'approved'`). **Group card**: `vrc-m-evt vrc-m-evt--group` ใช้ purple `--vc-accent` dot + status pill "{N} งานรวม" (`--group` variant) + `.vrc-m-evt-toggle` ฝังใน `.vrc-m-evt-head` (เดิมเป็น ghost btn คู่ขนานกับปุ่ม pencil) — ลบปุ่ม pencil "ดูรายละเอียดกลุ่ม" ออก (group head ใช้ data-bs-toggle collapse; รายการ sub-row คลิกเปิด detail). Sub-rows = `.vrc-m-evt-sub-row` button (time prefix + booker + meta), แทน nested card-in-card pattern. **Single card**: flat `.vrc-m-evt` — title = "{time}–{timeEnd} · {dest}" (fallback ถ้าไม่มี dest), meta = booker · {pax} ท่าน · car short, status pill ขวา (`vrc-m-evt-status--{statusKey}` + `STATUS_LABEL[status]`); **ลบปุ่ม edit pencil inline บน single card** — tap ทั้งใบ → openEventDetail() (edit อยู่ใน detail modal). ใช้ plain text + `.sep` separator แทน Lucide icons (user/clock/map-pin) ใน meta — ลด visual noise, premium feel |
| `pages/vehicle-admin.js` | admin dashboard (Phase 4.3, 2026-05-15) — ES module (`type="module"`); imports `initIcons` จาก `core/icons.js`. **Phase 5 redesign (2026-05-24):** `STATUS_LICON` → `STATUS_RAIL` map (status → rail color class); rewrite `renderSingleRow(b, idx=0)` + `renderGroupRow(grpName, members, idx=0)` ใหม่ — 3-zone markup `<span.bl-rail>` + `<div.bl-content>` (title + `.bl-dept-tag` from `b.dept`) + `<div.bl-right>` (`.bl-time` mono "HH:MM–HH:MM" + `.bl-actions`); group row icon-only ghost buttons (ungroup/edit/chevron); `style="--va-i:${idx}"` per row stagger; `renderBefore()` map callback pass `(item, i)` idx; populate `#beforeCount` "${cnt.all} รายการ". **Phase 3 redesign (2026-05-24):** เพิ่ม `renderWeekMeta()` (smart range text DD–DD MMM YYYY — compress same-month/year vs cross-month vs cross-year; h2 day line "วันXที่ DD MMM YYYY" + auto-append `.va-week-today-tag` ถ้าวันนี้); `_alignWeekStartTo(date)` helper (Monday-based snap); popover funcs `openWeekPicker()`/`closeWeekPicker()` (toggle hidden + aria-expanded + auto-focus input); `bindWeekControls()` bind ครั้งเดียว: `#weekTodayBtn`/`#weekPickerBtn` toggle/`#weekPickerInput` change → parse YYYY-MM-DD + sync weekStart + close + renderAll, document click outside, Esc + return focus; `renderWeekNav()` cells get inline `style.setProperty('--va-i', i)` สำหรับ stagger; `shiftWeek(dir)` เรียก `renderWeekMeta()` + force-reflow + add `.va-week-strip--switching` (skip reduce-motion); `renderAll()` แทน inline `#selDateHeading` set ด้วย `renderWeekMeta()` call; เรียก `bindWeekControls()` ก่อน first `renderAll()`. **Phase 1-2 redesign (2026-05-24):** rename helper `setKpi`/`_kpiRaf` → `setStat`/`_statRaf` (count-up animation logic ยังเดิม 600ms ease-out cubic); call sites ใน `renderBefore()` 4 → 3 (`setStat('statPending'/'statApprover'/'statApproved', cnt.*)` — ตัด rejected). booking + trip list rendering, vehicle status grid, 4 modal handlers (assign/swap/repair/revert), week navigator + date picker, header stat counters, bulk merge + notify selectors. Exposes to `window.*` (20+ funcs: shiftWeek, openAssignModal, openSwapModal, fixDone, submitAssign, etc.) สำหรับ legacy `onclick=""` ใน template + JS-rendered HTML. **Phase 3.2:** emits `vc-badge-{warning\|blue\|success\|danger\|neutral\|solid} vc-badge-dot` + `vc-empty`. **Phase 5.4 polish (2026-05-16):** `renderSingleRow()` rewritten → spacious card layout (`.bl-body` flex-col, `.bl-row-head` w/ booker+dept+badge, `.bl-row-meta` w/ time+dest+pax chips, `.bl-row-assigned` shown when approved/waiting_approver/forwarded & has vehicle/driver, `.bl-row-actions` right-aligned with `stopPropagation`); `renderGroupRow` action class rename `.bl-actions` → `.bl-row-actions`; remove redundant `mb-2` (uses `.va-list` flex+gap). **Phase 6.1 (2026-05-17):** new helpers `ACTIVE_STATUSES` Set + `driverDayCount(driverId, dateStr, excludeBookingId)` + `findConflict(b, 'vehicleId'\|'driverId', resourceId)` + `updateConflictWarnings()`; `renderWeekNav()` count-based dot class (`--sm/--md/--lg`) + title tooltip; `openAssignModal()` driver dropdown suffix "• N งานวันนี้" + Thai placeholder "— เลือกคนขับ —"; `checkAssignReady()` now also calls `updateConflictWarnings()` (vehicle/driver select onchange both trigger); `updateModalBudget()` rewritten — tone classes `va-budget--{ok\|warn\|danger}` (remPct based: ≥20% ok, ≥10% warn, <10%/rem≤0 danger), shows `#modalBudgetWarn` text "งบเหลือน้อย (N% · X บ.)" or "งบหมดแล้ว". **Token fix Phase 5.1 followup**: `--ds-success/warning/danger` literals in `updateModalBudget()` → CSS-driven via tone classes (no inline color). **Phase 11 task-card redesign (2026-06-06):** rewrite `renderSingleRow(b, idx=0)` + `renderGroupRow(grpName, members, idx=0)` → task-list markup `<div.bl-body>` (**ไม่มี status-icon column — เลิกใช้ `.bl-icon` ตามคำขอ user**; `const ico` + `<span.bl-icon>` ลบออกจากทั้ง 2 ฟังก์ชัน แต่ map `STATUS_ICON` คงไว้ — ยังใช้ที่ vehicle status grid). `.bl-head` = `.bl-head-text`[`.bl-title`=`b.dest` + `.bl-desc`=`b.purpose`] + `.bl-due`[label "เวลาเดินทาง" + value `start–end`]; `.bl-foot` = `.bl-author`[`.bl-avatar` initial จาก `b.booker` + `.bl-author-text` "จองโดย <strong>booker</strong> · dept"] + `.bl-foot-right`[`.bl-foot-pax` + `.bl-status` badge + `.bl-actions`]; approved row +`.bl-card--done`; group row = checkbox-slot + `.bl-body` + `.bl-grp-collapse` เป็น 3 sibling ใน `.bl-card` (flex-wrap ดัน collapse ลงเต็มแถวล่าง). ลบ markup เดิม `.bl-content/.bl-title-row/.bl-meta/.bl-meta-text/.bl-right`. `buildRowActions()` ไม่เปลี่ยน. **Phase 11.1 (2026-06-06):** ลบ collapsed-state logic ใน `renderBefore()` (block `shouldCollapse && !beforeExpanded`) + ลบ `let beforeExpanded`/`function toggleBeforeExpand()`/`function isPastOrToday()` (dead) + ลบ `toggleBeforeExpand` จาก window export + ลบ `beforeExpanded = ...` ใน `renderAll()` — booking list render เสมอ. **Phase 14 (2026-06-07):** เพิ่ม helper `budgetLabel(b)` → "ส่วนกอง-{expSub}"/"ส่วนกลาง-{expSub}"/"ส่วนตัว"/null; rewrite `renderSingleRow()` เป็น 3-line — `.bl-head`(`.bl-title`=ชื่อผู้จอง + status badge) + `.bl-trip`(purpose→dest + `arrow-right` icon) + `.bl-foot`(`.bl-badges`[pax neutral / งบ neutral, ยังไม่จัดสรร→warning / `.bl-badge-adhoc` ถ้า `b.isAdHoc`] | `.bl-actions`); เลิกใช้ `.bl-head-text/.bl-desc/.bl-due/.bl-author/.bl-avatar/.bl-foot-pax` ใน single (group ยังใช้). `buildRowActions()` คงเดิม. **Phase 14.1 (2026-06-07):** single เพิ่ม `timeBadge` (lucide clock, ช่วงเวลา start–end) เป็น badge แรกใน `.bl-badges` (ก่อน pax); group `renderGroupRow()` rewrite เป็น 3-line คล้าย single — `.bl-head`(`.bl-title`=vLabel รถ + `titleBadge` "N งานรวม") + `.bl-trip`(`.bl-trip-dest`="N งานในเที่ยวเดียว") + `.bl-foot`(`.bl-badges`[`grpTimeBadge` clock + `grpPaxBadge` users] | `.bl-actions` ungroup/edit/chevron) + `.bl-grp-collapse`; group เลิกใช้ `.bl-head-text/.bl-desc/.bl-due/.bl-foot-right`. **Phase 15 (2026-06-07):** เพิ่ม helper `tripBudgetTag(expType)` → "หักงบส่วนกลาง"/"หักงบส่วนกอง"/"เรียกเก็บ"(personal); `renderTripRow()` เอา `.va-trip-amount` ออกจาก head → เพิ่ม `.va-trip-badges` row = fuelBadge (icon fuel + `fmtBaht(total)` + tag; personal→`vc-badge-warning` else neutral) + otBadge (icon clock + "OT +N ชม. · ฿X (tag)"; แสดงเฉพาะ `otAmount>0`; personal→warning else `.va-trip-badge--ot`). **Phase 16 inline calendar (2026-06-08):** เพิ่ม state `calCursor` (เดือนที่ปฏิทินแสดง) + helper `renderCalendar()` (วาด month grid Sunday-first; highlight selDate/today; dot นับ bookings ต่อวัน) + `shiftCalMonth(dir)`; rewrite `openWeekPicker()` (set calCursor=เดือนของ selDate → renderCalendar → show); rewrite `bindWeekControls()` — ลบ `#weekPickerInput` change handler → bind `#calPrevBtn`/`#calNextBtn` (shiftCalMonth) + event delegation บน `#calDays` (คลิกวัน → set selDate + `_alignWeekStartTo` + close + renderAll คลิกเดียว). **2026-06-12:** `updateExpSubDropdown()` — approver row แสดงเสมอเมื่อ department เลือก: ถ้าไม่มี approver → โชว์ "ยังไม่ได้ตั้งผู้อนุมัติ" สี `var(--vc-warning)` + `display:flex` แทนซ่อน row (`display:none`). |
| `vehicle/js/vehicle_mileage.js` | admin mileage page (Phase 4.5, 2026-05-15; renamed จาก `pages/mileage-admin.js` ใน prefix migration 2026-06-07) — ES module (`type="module"`); modal 3-state (start/end/complete), realtime cost preview, checkbox summary, export-link sync. Exposes `openMileage()`, `goEditEnd()`, `clearSelection()` ไป `window.*` สำหรับ legacy `onclick=""` ใน template (3 จุด). `window.MLG_FUEL_PRICE` injection คงเดิม. **Phase 9.1 (2026-05-22):** เพิ่ม 3 IIFE สำหรับ toolbar pattern — (1) `bindStatusChips()`: click `.mlg-chip` → set `#statusFilter` value + `form.submit()` (auto-submit pattern); (2) `bindAdvToggle()`: `#advFilterBtn` toggle `[hidden]` บน `#advSheet` + sync `aria-expanded`; (3) `bindDatePreset()`: `#datePreset` change → set `date_start/date_end/show_all` ตาม preset (month=clear all, 7d/30d=ISO date today − N, all=show_all=1, custom=reveal `#dateRangeGroup`) + auto-submit ยกเว้น custom (รอ user กด "นำไปใช้"). Initial preset detect จาก current values (`show_all=1`→all, both dates empty→month, อื่น→custom). **Phase 16.3 (2026-06-08):** เพิ่ม IIFE `bindDateRangePickers()` — 2 calendar popover (start/end) แทน native date input; loop `#dateRangeGroup [data-datepick]`, render month grid (Sunday-first, TH เดือน/พ.ศ.), คลิกวัน → set hidden input + clear `showAllInput` + `form.submit()`; prev/next เลื่อนเดือน, click-outside + Esc ปิด (เปิดได้ทีละตัว). ใช้ `.va-cal*` class จาก vehicle_admin.css (CSS reuse, JS แยก scope). **Phase 16.4 AJAX filter (2026-06-08):** กรองไม่ reload — `runFilter(push=true)` fetch `buildFilterURL()` (GET, FormData→querystring ตัด empty) → `DOMParser` parse `#mlgResults` → swap innerHTML + `mlgInitIcons(results)` + `bindResults()` + `pushState` + `syncExportLink`; race guard ด้วย `_mlgReqToken` (ทิ้งผล stale); error → fallback `location.href`. Selection/summary refactor: `$checkAll/$modeAll/$modeSel/$strip` เป็น `let` + `bindResults()` (re-grab + rebind row-check/checkAll/row-click หลังทุก swap). chips/preset/datepicker เปลี่ยน `form.submit()` → `runFilter()`; chip set `is-active` เอง (ไม่ reload). `form` submit intercept (preventDefault→runFilter) สำหรับปุ่ม "นำไปใช้". `popstate` → `location.reload()` (server render ตาม URL). "ล้างทั้งหมด" ยังเป็น full nav (`<a href>`). **Phase 17 adv-filter popover (2026-06-08):** `bindAdvToggle()` rewrite — popover open/close ผ่าน `setOpen()` helper; `btn` click `stopPropagation` + toggle; เพิ่ม document click handler ปิดเมื่อคลิกนอก (`sheet.contains`/`btn.contains` = ไม่ปิด — รวม `.vc-dd` ข้างใน) + Esc close + return focus. `form` submit handler เพิ่มปิด `#advSheet` (set `hidden` + reset `aria-expanded`) ก่อน `runFilter()` — แก้ปัญหา popover ค้างหลังกด "นำไปใช้" (AJAX swap แค่ `#mlgResults` ซึ่งอยู่นอก popover). **Phase 17.1 (2026-06-08):** `bindBudgetFilter()` เพิ่ม ref `filterBudgetTypeWrap` + toggle class `mlg-adv-col-full` บนงบ wrap ใน `updateBudgetSubDropdown()` — ไม่มี sub (ว่าง/personal) → add (งบเต็ม 2col); central/department → remove (งบครึ่ง + sub ข้าง). |
| `pages/fuel-admin.js` | fuel page (Phase 4.6, 2026-05-15) — ES module (`type="module"`); imports `initIcons`/`bindModalReinit` จาก `core/icons.js`. 5-modal controller (bill/reimb/reserve/price/budget), checkbox→merge, kebab→edit, lucide re-init on shown.bs.modal, `wireFilterBar` (auto-submit GET on select change). ลบ IIFE + DOMContentLoaded guard (module deferred). ไม่มี `onclick=""` → ไม่ต้อง window expose. **Toolbar iteration 2 (2026-06-17, ไฟล์จริง = `vehicle/js/vehicle_fuel.js`):** +`wireFilterPopover()` — ปุ่ม `#filterToggle` toggle `form.fuel-filter-panel#filterForm` (`position:fixed` คำนวณตำแหน่งจาก `getBoundingClientRect`; close on outside-click; reposition on scroll/resize); `wireFilterBar` เดิมคง (selects ใน popover auto-submit). |
| `pages/maintenance.js` | maintenance page (2026-05-17) — legacy script (ไม่ใช่ ES module เพราะต้องรอ jQuery+DataTable global); DataTable init (langUrl จาก `#pageData[data-dt-lang]`), auto-open form modal เมื่อ `data-edit-mode="true"`, modal:รับงาน/ปิดงาน handlers (populate จาก `data-ticket-*`), delete confirmation, Export Excel. ไม่มี `onclick=""` → ไม่ต้อง window expose. **Redesign (2026-06-16):** `reinitIcons()` (re-render Lucide รายการในหน้า 2+) + `renderGotoPage(table)` (go-to-page control) เรียกทุก DataTable `draw`; เพิ่ม `pageLength: 10`. |
| `pages/repair.js` | repair page (Phase 4.2, 2026-05-15) — ES module (`type="module"`); imports `initIcons`/`bindModalReinit` from `core/icons.js`. ลบ `$(document).ready()` wrapper (module deferred). DataTable init + lucide re-init on draw, modal:รับงาน/ปิดงาน handlers, auto-open edit modal, tooltips, upload zone. jQuery+bootstrap+DataTable ยังใช้ผ่าน global. **Redesign (2026-06-16):** `renderGotoPage(table)` สร้าง go-to-page control (label+select+ปุ่ม "ไป") append เข้า `.dataTables_paginate` แล้ว refresh ทุก draw (DataTables ล้าง innerHTML ทุก redraw → re-attach). |
| `pages/notification.js` | ทุกหน้าที่มี notification panel — โหลดจาก `_header.html` (Phase 4.9, 2026-05-16). ES module (`type="module"`); polling `/api/notifications` ทุก 30 วิ, dropdown panel (**hybrid feed, 2026-06-15:** booking → group-by-booking (collapsed, click expand เป็น timeline `.notif-inner`) + non-booking → solo `data.items[]`, merge-sort by `ts_ms` ผ่าน `buildFeed`. `renderGroup`/`renderInnerNotif`/`toggleGroup`/`state.expanded` **ใช้งานจริง** — doc เก่าเขียนว่าลบเป็น flat feed = ผิด, แก้ 2026-06-15. `renderInnerNotif` = 2 บรรทัด: title (`n.title` จาก backend) + subtitle (`innerSubtitle()` ตัด prefix "คำขอ/ทริป #N" ออก) + time ขวา), 3 tabs all/unread/payment + sticky payment, toast desktop only (event สำคัญ + ภายใน 45s). Admin/approver เห็นเฉพาะ notification ของตัวเอง (ไม่มี synthetic group injection อีกแล้ว). **Title freeze (2026-06-16):** `n.title` มาจาก `Notification.title` (backend freeze ตอนสร้าง, fallback `_notif_title`); sticky payment card (`renderPaymentCard`) + toast ใช้ `n.title` เป็นหัวด้วย (fallback `booking_title`/`ntypeLabel`). |
| `pages/ot-admin.js` | vehicle_cost.html (Phase 4.8, 2026-05-16) — ES module (`type="module"`); imports `initIcons` จาก `core/icons.js`. Edit modal slot row builder + recompute, print receipt (single/all), filter auto-submit. Pure data-attr delegation (no `onclick=""`) → ไม่ต้อง window expose. ลบ IIFE + DOMContentLoaded wrapper (module deferred). **Phase 5.10 (2026-05-18):** เพิ่ม `buildRateRow()` builder สำหรับ rate config modal (`#rateConfigContainer` + `#addRateBtn`) ที่ include `cfg_day[]` `<select>` (ทุกวัน + TH_DAYS 7 ตัว, default ""=NULL) + delegated click handler `.js-rate-remove` (existing row [`data-cfg-id`] → confirm + append hidden `<input name="cfg_delete[]" value="...">` to form + grey out via `.is-removed`; new row → drop DOM); call `initIcons()` after append. **2026-06-09 (manual OT):** slot logic เป็น scope-based (`SLOT_SCOPES` edit/add: `recomputeScope(container)` + `addSlotRow(container, slot)`) ใช้ร่วม 2 modal; `openAddModal()` (reset + default วันนี้ + 1 ช่วงว่าง) ผูกปุ่ม `#addOtBtn`; `#addOtForm` submit AJAX → `CREATE_URL` (JSON, alert ถ้า fail) → `runFilter`; refreshData อ่าน `createUrl`. Port **va-cal datepicker IIFE** จาก `vehicle_budget.js` (`[data-datepick]` → `.va-cal` popover, hidden ISO input, sync label ตอน `shown.bs.modal`, `data-datepick-required` ตรวจตอน submit ใน capture phase → form handler เช็ก `e.defaultPrevented`). **2026-06-14 (filter งบ):** IIFE `bindBudgetFilter` — cascade `#filterBudgetType`→`#filterBudgetSub` (อ่าน `window.EXPENSE_CATS` + `window.COST_FILTER_SUB`, show/hide `#filterBudgetSubWrap`); change select ใน filterForm → `runFilter` เดิม → กรอง OT ตามงบ (derive จาก booking ผ่าน `_apply_budget_filter` ฝั่ง view). ไฟล์จริง = `vehicle/js/vehicle_ot.js` |
| `pages/approver-inbox.js` | approver_inbox.html (Phase 4.1, 2026-05-15) — ES module (`type="module"`); functions: `switchTab()`, `showRejectForm(id)`, `hideRejectForm(id)`, chevron rotation. Exposes to `window.*` สำหรับ legacy `onclick=""` ใน template |
| `pages/driver-home.js` *(ไฟล์จริง `vehicle/js/vehicle_driver.js`)* | vehicle_driver.html (Phase 4.4, 2026-05-15) — ES module (`type="module"`); imports `initIcons` จาก `core/icons.js`. Tab switching (`.driver-tabs__btn`), accordion (`[data-card-toggle]`), `actual_start/end` timestamp on submit (`[data-driver-form] [data-actual-now]`), upload zone visual feedback (`[data-upload-input]`). ไม่มี `onclick=""` → ไม่ต้อง window expose. **2026-06-09 (card redesign req1+2):** หน้านี้โหลด `core/js/dropdown.js` (classic, ต่อท้าย module) เพิ่ม → enhance `<select data-dropdown>` ใน `.driver-emergency` (เปลี่ยนรถ) เป็น vc-dd. **2026-06-09 (ad-hoc collapse req3):** modal open/close + combo logic เดิม → **collapse** — `openAdhoc/closeAdhoc` toggle `[data-adhoc-panel]` + reset form/upload/hint; `[data-adhoc-cancel]` หุบ; ad-hoc vehicle change (`[data-adhoc-vehicle]`) อัปเดต hint เลขไมล์ล่าสุด + `min` จาก `option[data-last-odo]`. _หมายเหตุ: เคยลอง auto-OCR (tesseract.js) แต่ตัดทิ้ง 2026-06-09 — tesseract default อ่านหน้าปัด 7-segment เพี้ยน; เก็บ upload รูป + กรอกเลขเอง. `[data-odo-input]` ยังใช้โดย ad-hoc vehicle hint (set `min`)._ |
| `pages/budget-admin.js` | budget_manage.html (Phase 4.10, 2026-05-16; Phase 5.8, 2026-05-18) — ES module (`type="module"`); imports `initIcons`/`bindModalReinit` จาก `core/icons.js`. Dropdown action menus (data-attr delegation), 3 modal data-bind wirings (topUp/adjust/refund), refund row picker. **Phase 5.8:** ย้าย `setBudgetModal` `show.bs.modal` handler จาก inline `<script>` ใน template → module (swap datalist central/dept, approver pre-select, retitle+relabel, `approverRow.hidden` toggle, `initIcons(modal)` after innerHTML swap). ลบ IIFE wrapper + local `initLucide` + duplicate lucide CDN จาก template. ไม่มี `onclick=""` → ไม่ต้อง window expose. **UX writer Phase 1 (2026-05-22):** `setBudgetModal` handler รับ edit-mode detection (`isEdit = !!(deptName && amount)`) → swap title (`แก้เพดาน<group> — <dept>` vs `ตั้ง<group>ใหม่`), notice text (`#sbNoticeText`), submit text (`#sbSubmitText` = `อัปเดตเพดาน` vs `บันทึกงบ`). เพิ่ม document-level `submit` listener สำหรับ `form[data-confirm-toggle]` → `confirm()` dialog ก่อน toggle_active POST (message ใช้ `data-dept-name`/`data-to-active`, ปิดงบเตือน "block booking ใหม่"). **Phase 2E round 2 (2026-05-22):** เพิ่ม submit listener `form[data-confirm-pay]` (personal mileage รับเงิน) → confirm() แสดงชื่อผู้จอง+amount → AJAX fetch (route return JSON, ใช้ form POST ตรงๆ จะได้ raw JSON page) → location.reload(); btn loader state (`กำลังบันทึก...`) + restore on error. **Phase 3E round 3 (2026-05-22):** เพิ่ม click listener `[data-personal-filter]` (segmented pill tabs ใน sectionPersonal) → toggle `.is-active` + set `hidden` ตาม `data-personal-row` (paid/unpaid/all). Client-side filter (ไม่ reload) เพราะ data set เล็ก. **2026-06-06 (งบช่วงเวลา):** + `wireModal('extendBudgetModal', {budget_id:'bid', dept_label:'dept', start_date:'start', end_date:'end'})` — pre-fill modal "นำงบจากคลังกลับมาใช้". |
| `pages/room.js` | room.html (2026-05-17) — ES module (`type="module"`); imports `initIcons`/`bindModalReinit` จาก `core/icons.js`. Calendar render (vc-scope grid) + mobile list + 3 modal flow (book/edit/detail) + flatpickr (edit modal). `roomKind(room)` → 'small'/'large' จากชื่อห้อง (เล็ก/ใหญ่). Exposes `openEventDetail`/`openEditBookingModal`/`openBookingModal` + `eventDetailModal` getter ไป `window.*` สำหรับ inline `onclick=""` ใน JS-rendered HTML. Pattern ถอดมาจาก `pages/vehicle.js` (ตัด groups/drivers/vehicles ออก). **2026-06-15:** เพิ่ม mobile month nav listeners (`prevMonthBtnMobile`/`nextMonthBtnMobile`/`todayBtnMobile`); `renderCalendar()` sync `#currentMonthLabelMobile`; `updateMobileList()` update `#mobileDaybarCount` (N รายการ หรือว่างถ้า 0). |
| `pages/manage-fleet.js` | admin_manage_fleet.html (Phase 6.2, 2026-05-17) — plain IIFE (no ES module — uses legacy `defer` script tag + Bootstrap Modal globals). Bind 5 modals on `show.bs.modal` (edit/delete vehicle, edit/delete driver, vehicle history), fetch `/api/vehicle/{id}/history` (loading/content swap). Refresh Lucide icons on `shown.bs.modal` (catches dynamically inserted icon `<i>`). |
| `pages/dashboard.js` | dashboard.html (2026-05-17) — ES module (`type="module"`); imports `initIcons`/`bindModalReinit` จาก `core/icons.js`. Live clock (`#liveClock`) — `Intl.DateTimeFormat` th-TH weekday+date + time, refresh 30s. ไม่มี `onclick=""` → ไม่ต้อง window expose. |
| `main.js` | **orphan ตั้งแต่ 2026-06-16** — login.html เลิกใช้ (ย้ายไป `auth/js/login.js`). jQuery IIFE legacy form validation |

**Icon libraries:**
- Font Awesome (`fa-solid` / `fa-regular`) — global default ใช้อยู่ทุกหน้า
- Lucide Icons (line, stroke 1.5px) — โหลด global ใน `_header.html` (CDN unpkg). ใช้: `<i data-lucide="fuel"></i>`. หลัง DOM update เรียก `window.lucide.createIcons()`. ใช้สำหรับ Vercel namespace (Phase 2 fuel page)

---

> Patterns ที่ซ้ำซาก (booking status, telegram, in-app notify, budget mutation) → ดู CLAUDE.md § Gotchas
> Maintenance Protocol → ดู CLAUDE.md
