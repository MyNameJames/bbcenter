# INDEX — Templates + Design System

> Part ของ INDEX.md แยก เพื่อ token budget — [กลับ hub](INDEX.md)
> **อัปเดตล่าสุด:** 2026-08-07
>
> **2026-07-31 — ประวัติแยกออกไปแล้ว:** ไฟล์นี้เคยโต 220KB (~62.9K tok) เพราะทุกแถวสะสมประวัติ `Phase N (วันที่)` ไว้ตลอดกาล → ย้ายไป [INDEX_ui_history.md](INDEX_ui_history.md) แบบ verbatim (53 sections ไม่ตัดทิ้ง) เหลือ ~11.5K tok. แต่ละแถวมีลิงก์ `[ประวัติ →]` กลับไปหาของเดิม.
> **กฎต่อจากนี้:** แถวในไฟล์นี้ = "ไฟล์นี้คืออะไร + class/ID หลัก ณ ปัจจุบัน" เท่านั้น — ประวัติการเปลี่ยนแปลงลง history file หรือ [CHANGELOG.md](CHANGELOG.md) อย่าสะสมกลับเข้ามาอีก

---

## 🎨 Templates

> **2026-06-07 (ขั้น 4):** partials กลางย้าย → `templates/_shared/` (sidebar/header/navbar/notification_panel/notification_toast). modal instances → `<domain>/modals/` + prefix ชื่อ: `vehicle/modals/vehicle_*.html` · `room/modals/room_*.html` · `vehicle/admin/modals/fuel_*.html`. ชื่อ/path `*-modal-*.html` ในตารางด้านล่าง = **ชื่อเก่า** → map: `vehicle-modal-detail` = `vehicle/modals/vehicle_detail.html` (ตัด `vehicle-modal-` ออก, prefix `vehicle_`); เช่นเดียวกับ room/fuel. `_components/` (macro) **ไม่ย้าย**

**Shared partials** (include ทุกหน้า):
| Partial | File |
|---------|------|
| `_shared/sidebar.html` | [app/templates/_shared/sidebar.html](../../app/templates/_shared/sidebar.html) — `active_menu` keys: `dashboard` `vehicle` `repair` `room` `admin` `mileage` `fleet` `cost` `budget` `fuel` `approver` (`history` ลบ 2026-06-07). **Icons: Lucide** (`data-lucide="..."`) — load via `_header.html`. [ประวัติ →](INDEX_ui_history.md#_sharedsidebarhtml) |
| `_shared/header.html` | [app/templates/_shared/header.html](../../app/templates/_shared/header.html) [ประวัติ →](INDEX_ui_history.md#_sharedheaderhtml) |
| `_shared/notification_panel.html` | [app/templates/_shared/notification_panel.html](../../app/templates/_shared/notification_panel.html) — bell button `#notifBellBtn`: `ds-btn-icon` → `zen-iconbtn` (Zendenta 2026-06-19). dropdown panel + JS เดิมครบ. |
| `_shared/notification_toast.html` | [app/templates/_shared/notification_toast.html](../../app/templates/_shared/notification_toast.html) |
| `_shared/navbar.html` | [app/templates/_shared/navbar.html](../../app/templates/_shared/navbar.html) — legacy navbar (4 includes) |
| `_shared/sidebar2.html` + `_shared/header2.html` | **UE-redesign chrome (2026-07-11)** — port จาก `static/core/mockup-orders.html` แต่ role-based จริง (D1). include ผ่าน `_base_ue.html`. `sidebar2` = role sections + active-by-endpoint + Material Symbols icons; `header2` = topbar minimal + `notification_panel` จริง + **stub `window.lucide`** (ไม่โหลด Lucide) + โหลด `core/js/ms-icons.js`. ขยายไปหน้าอื่นใน phase ถัดไป |
| `_base_ue.html` | **UE redesign base layout (Phase 1.5, 2026-07-11)** — หน้าที่ redesign แบบ mockup-orders.html → `{% extends '_base_ue.html' %}`. ให้: head (bootstrap/fa/bi/fonts/components.css/gallery.css/ue.css) + `.ml2-frame` → include header2 + body-row(include sidebar2 + `main.ml2-content`) + flash→toast bridge + base scripts (jquery/bootstrap/`ue-motion.js`/`bb-components.js`). **blocks:** `title`·`head`·`page_title`(h1 แสดงเมื่อมีข้อความ ผ่าน `self.page_title()`)·`content`·`modals`·`scripts`. First adopter: `vehicle/admin/vehicle_mileage.html` |
| `_shared/illustrations.html` | [app/templates/_shared/illustrations.html](../../app/templates/_shared/illustrations.html) — symbol sprite (2026-08-07), `{% include %}` ครั้งเดียวต่อหน้า แล้วเรียกด้วย `<use href="#illust-anxious">`. ตอนนี้มีตัวเดียว: `illust-anxious` (empty state + confirm modal, ดู [vehicle_admin_page_contract.md](vehicle_admin_page_contract.md) §1.2/§6) |
| `_components/*.html` | Reusable Jinja macros (Phase 2 + 3) — see § Design System > Component library. Files: `kpi.html` (`kpi_cell` accepts `icon_kind='lucide'\|'fa'`, default lucide), `filter_bar.html`, `badge.html` (`badge(text, tone, dot=False, icon='', size='')`, lucide), `pill.html`, `empty_state.html` (`empty_state(title, desc, icon, compact)`, lucide, `{% call %}` for CTA), `form_group.html`, `table_shell.html`, `_modal.html`. Phase 3 aligned to flat vocab matching `/bbcenter-design` skill. **subfolder:** `_components/bb/` = `.bb-*` macro (**ue_chip** [Uber-style filter chip · `.ue-chip*` toggle+dropdown · 2026-07-11 · ดู § Design System]/table/daterange/**combo [ประวัติ →](INDEX_ui_history.md#_componentshtml) |

**Dashboard templates:**
| File | ใช้สำหรับ |
|------|----------|
| `auth/login.html` | **หน้า login** [ประวัติ →](INDEX_ui_history.md#authloginhtml) |
| `core/line_link.html` | **ผูกบัญชี LINE** (2026-06-12) — หน้า `/line/link` (`core_bp.line_link`): vc-scope shell (sidebar + header) แสดงโค้ด 6 หลัก (`User.line_link_code`) + 3 ขั้นตอนแอด Official Account; state `is_linked` → การ์ดยืนยัน. Bootstrap util + vc-* tokens (ไม่มี CSS แยก) |
| `dashboard/dashboard.html` | landing page หลัง login — **Action Hub** [ประวัติ →](INDEX_ui_history.md#dashboarddashboardhtml) |

**Repair templates:**
| File | ใช้สำหรับ |
|------|----------|
| `repair/repair.html` | ระบบแจ้งซ่อมไอที — **vc-scope shell** [ประวัติ →](INDEX_ui_history.md#repairrepairhtml) |
| `maintenance/maintenance.html` | แจ้งซ่อมอาคารสถานที่ — **vc-scope shell** [ประวัติ →](INDEX_ui_history.md#maintenancemaintenancehtml) |

**Room templates:**
| File | ใช้สำหรับ |
|------|----------|
| `room/room.html` | ปฏิทินจองห้องประชุม. **Migrate 2026-08-05 (design ทั้งหมดจาก `vehicle/vehicle.html`):** `{% extends '_base_ue.html' %}` (header2/sidebar2, เดิม standalone shell + `_shared/sidebar\|header.html`) · token `vc-*`→`bb-*` · **Icons: Material Symbols ตรงๆ** (แปลงจาก `data-lucide` ครบ, `<span class="material-symbols-rounded">`) · head โหลด `tokens.css`+`badge.css`+`vehicle/css/vehicle_calendar.css`(base grid/event-card/mobile-list/`.bk-*`, ใหม่ — แทน `vehicle.css` เดิมที่ตัดออก). **`room.css` ลบทิ้งแล้ว (2026-08-05, ต่อจาก migrate เดิมวันเดียวกัน)** — room ไม่มี CSS แยกของตัวเองอีกต่อไป, identity ห้อง (เล็ก/ใหญ่) ใช้ component กลางล้วน: `bb-badge is-info`(เล็ก)/`is-wr`(ใหญ่) แทน `room-badge`, `bb-avatar` + inline `var(--bb-info-bg)`/`var(--bb-wr-bg)` แทน `room-list-dot`/`room-detail-dot`, `.vrc-m-empty*`(`vehicle_calendar.css`, ใช้ร่วมกับ vehicle.html) แทน `room-mobile-empty`. [ประวัติ →](INDEX_ui_history.md#roomroomhtml) |
| `room/modals/room_book.html` *(เดิม room-modal-book.html)* | `#bookingModal` — **รวมจอง+แก้ไขเป็น modal เดียว (2026-08-05, ตาม `vehicle_book.html`)** `bkSetMode('create'\|'edit')` สลับปุ่ม/`form.action` (`data-create-action` เก็บ URL สร้างใหม่) — **ลบ `room_edit.html`**. header eyebrow+ชื่อใหญ่+avatar (`mindfulness`). วันที่/ช่วงเวลา = component `DateField`/`TimeRangeField` (`bk_date_field`/`bk_timerange_field`, สร้างใน `room_view.py::index()`) แทน native input เดิม |
| `room/modals/room_detail.html` *(เดิม room-modal-detail.html)* | `#eventDetailModal` — ดูรายละเอียด + แก้ไข/ยกเลิก (เฉพาะ owner). **2026-08-05:** header → eyebrow+avatar (`#detailStatusAvatar[data-kind]` สีตาม room kind เล็ก=info/ใหญ่=wr, icon `mindfulness`) ตาม `vehicle_detail.html`; แถวรายละเอียด icon `schedule`(เวลา)/`add_home`(ห้อง)/`sell`(หัวข้อ); ปุ่ม แก้ไข=`edit` ยกเลิก=`delete` (`bb-btn is-danger`) |

**Vehicle templates:**
| File | ใช้สำหรับ |
|------|----------|
| `vehicle/vehicle.html` | หน้าจองหลัก. **Icons: Material Symbols ตรงๆ** (`<span class="material-symbols-rounded">` — แปลงจาก `data-lucide` ครบ 2026-08-05). **2026-08-05:** `window.BOOKINGS` เพิ่ม `driverPhone`/`note` (ผูก `#eventDetailModal` redesign — ดู `vehicle_detail.html`). [ประวัติ →](INDEX_ui_history.md#vehiclevehiclehtml) |
| `vehicle/vehicle_edit.html` | แก้ไข booking |
| `vehicle/vehicle_approver.html` *(เดิม approver_inbox.html)* | Approver inbox — budget card + 3 tabs (รออนุมัติ/อนุมัติแล้ว/ปฏิเสธ) + accordion cards + inline reject form. [ประวัติ →](INDEX_ui_history.md#vehiclevehicle_approverhtml-เดิม-approver_inboxhtml) |
| `vehicle/admin/vehicle_mileage.html` | บันทึกเลขไมล์ (admin) — ประวัติ vc-scope/legacy ก่อน bb-* migrate → [CHANGELOG.md](CHANGELOG.md#vehicleadminvehicle_mileagehtml-บันทึกเลขไมล์-admin). **🎨 P1 ".bb-*" redesign (2026-06-28):** หน้านี้เป็น **first adopter** ของ `core/css/components.css` (`.bb-*` component library) — `<link>` เพิ่มเข้า `<head>` พร้อม Inter font (Google Fonts). Layout = Pattern P1 "List/Ledger": KPI strip (`.bb-kpi.is-ghost` × 3 + `#modeSelected` swap) → sticky toolbar `.mlg-toolbar` (`.bb-tabs/.bb-tab.is-on` row 1 + `.bb-search` row 2) → table (`.bb-table` + `.bb-th.sortable` + `.bb-sort-icon`) → status pills (`.bb-status.is-ok/.is-wr/.is-neutral` + `.bb-dot`) → action (`.bb-icon-btn`). modal shell คง Bootstrap: buttons → `.bb-btn.is-pri/.is-ghost`, inputs → `.bb-input/.bb-label/.bb-hint.is-error`. Legacy removed: `zen-tabs/zen-tab/zen-search/data-table/badge-pill.b-*/kpi-tile/kpi-num/sort-icon/vc-btn-*`. JS selectors: `.zen-tab`→`.bb-tab`, `'active'`→`'is-on'`, `.data-table`→`.bb-table`, `.sort-icon i`→`.bb-sort-icon [data-lucide]`. **Page contract apply (2026-08-07)** — `data-lucide` migrate ครบ 0 เหลือ (เขียน `material-symbols-rounded` ตรงๆ ทุกจุด — ปุ่มยกเลิกเลือก/tooltip เติมน้ำมัน/badge OT mismatch/action ตาราง/การ์ดมือถือ/`#mileageModal`), thead scoped override (`#bbMlResults .bb-table thead th` พื้นขาว+เส้นบน-ล่าง `n200`), empty state → illustration `illust-anxious` variant B (ไม่มีปุ่ม). `vehicle_mileage.js::updateOdoUI()` แก้ selector หา `#mmAvatar` icon เพิ่ม `.material-symbols-rounded` (เดิมพึ่ง `data-lucide`/`svg`/`i` ซึ่งไม่เหลือแล้วหลัง migrate — ไม่งั้นสี icon ตาม state จะไม่ swap). ดู [vehicle_admin_page_contract.md](vehicle_admin_page_contract.md). [ประวัติ →](INDEX_ui_history.md#vehicleadminvehicle_mileagehtml) |
| `vehicle/vehicle_driver.html` *(เดิม driver_home.html)* | หน้าคนขับ — **Vercel namespace** [ประวัติ →](INDEX_ui_history.md#vehiclevehicle_driverhtml-เดิม-driver_homehtml) |
| `vehicle/modals/vehicle_book.html` *(เดิม vehicle-modal-book.html, ย้าย→modals/ ขั้น 4 2026-06-07)* | `#bookingModal`. **Icons: Material Symbols ตรงๆ** (`bkSubmitBtn` แปลงจาก `data-lucide` แล้ว 2026-08-05). **2026-08-05:** ช่อง "หมายเหตุ" เดิมเป็น orphan input (`id="travelDate"` ไม่มี `name` เลย — ไม่เคยถูกส่ง/เก็บ) แก้เป็น `id="bk_note" name="note"` ผูกกับ `VehicleBooking.note` (คอลัมน์ใหม่, migration `2026-08-05_vehicle-booking-add-note.sql`) — ใช้ร่วมทั้งจอง(`book_vehicle_simple`)และแก้ไข(`edit_booking`) เพราะ `#bookingForm` เดียวกัน. [ประวัติ →](INDEX_ui_history.md#vehiclemodalsvehicle_bookhtml-เดิม-vehicle-modal-bookhtml-ย้ายmodals-ขั้น-4-2026-06-07) |
| `vehicle/vehicle-modal-edit.html` | `#editBookingModal`. [ประวัติ →](INDEX_ui_history.md#vehiclevehicle-modal-edithtml) |
| `vehicle/modals/vehicle_detail.html` *(เดิม vehicle-modal-detail.html)* | `#eventDetailModal` — **shared** modal ผูกทั้ง `vehicle.html` (user, `pages/vehicle.js` → `openEventDetail()`, CSS `vehicle_calendar.css`) และ `vehicle/admin/vehicle_admin.html` (admin, `vehicle_admin.js` → `openAdminBookingDetail()`/`openAdminEdit()`/`cancelAdminEdit()`/`saveAdminEdit()`, **ไม่โหลด CSS ของ `.bk-detail-*` เลย**). แก้ไฟล์นี้ต้องเช็กทั้ง 2 ฝั่ง. **2026-08-05 redesign เต็ม (เชื่อมข้อมูลจริงเข้า mockup ผู้ใช้):** header (eyebrow/date/`#detailStatusBadge`/`#detailTimeText`/`#detailStatusAvatar`+`#detailStatusIcon` — เลิกใช้ `.bk-detail-status*` แล้ว) → `#detailVehicleDriverSection` (รถ+คนขับ, ทะเบียน/รุ่น/ชื่อคนขับ/เบอร์+ปุ่มโทร `tel:`) **โชว์เฉพาะ status ∈ {waiting_approver, approved}** (ทั้ง user+admin ต้องเซ็ต d-none เอง) → ผู้โดยสารเป็น**การ์ดเส้นประต่อคน** (ไม่ใช่ `.bk-detail-member` bordered tile เดิม) รวม `note` ต่อคน (**คนแรกไม่มีเส้นประบน** กัน `hr.bk-divider` ของ header ซ้อนเป็น 2 เส้น) → `#detailActions`. Footer ไม่มี action ใด = **ซ่อนทั้งแถว (`d-none`)** — เลิกโชว์ปุ่ม "ปิด" fallback แล้ว (กลับกฎ 2026-08-03 เดิมของไฟล์นี้เอง — ไม่ใช่กฎจาก `modal_pattern.md`, ปิดผ่าน backdrop/Escape แทน). `VehicleBooking.note` คอลัมน์ใหม่ (migration `2026-08-05_vehicle-booking-add-note.sql`) เชื่อมกับ `bk_note` ใน `vehicle_book.html` (เดิม orphan input ไม่มี `name`). `#detailEditSection` **ยังใช้งานจริงฝั่ง admin** เพิ่ม `#editNote`. group-member logic ผูกทั้ง 2 ฝั่งแล้ว (เดิม admin ไม่มี). ลบ `.bk-detail-status*`/`-plate-*`/`-members`/`-member*`/`-footer`/`-content`/`-dialog` ออกจาก `vehicle_calendar.css` (`.bk-detail-title/-divider/-info/-row/-icon/-text/-section-label` เก็บไว้ — `room/modals/room_detail.html` ยังใช้อยู่). [ประวัติ →](INDEX_ui_history.md#vehiclevehicle-modal-detailhtml) |
| `vehicle/vehicle-modal-group.html` | *(merged into vehicle-modal-detail.html)* |
| `vehicle/vehicle-modal-more-events.html` | `#moreEventsModal`. **Phase 2 (2026-05-17):** ลบ class `shadow` + inline `border-radius:14px`; outer wrapper → `bk-modal-dialog-sm` + `bk-modal-content` (reuse จาก vehicle.css) |
| `vehicle/admin/vehicle_admin.html` | admin dashboard — ประวัติ vc-scope/legacy ก่อน bb-* migrate → [CHANGELOG.md](CHANGELOG.md#vehicleadminvehicle_adminhtml-admin-dashboard). **bb-* migration (2026-07-05):** ลบ legacy CSS ทั้งหมด (`design-system.css`/`vehicle.css`/`vehicle_fuel.css`/`vehicle_admin.css`) → เหลือ `core/css/components.css`+`gallery.css` (`bb-*`) ล้วน. Booking cards/vehicle status rows/trip rows (`vehicle_admin.js`) → `bb-card`/`bb-badge`/`bb-status`/`bb-btn`/`bb-avatar`. 4 modal inner markup → `bb-modal-*`/`bb-field`/`bb-seg`/`bb-callout` (Bootstrap `.modal.fade` shell คงเดิม). Toast → `ToastRegion()`+`bbToast()` แทน `.adm-toast`. ตาม pattern [redesign_migration_pattern.md](redesign_migration_pattern.md). **2026-08-05:** `window.BOOKINGS_DATA` เพิ่ม `driverPhone`/`note` (ผูก `#eventDetailModal` redesign — ดู `vehicle_detail.html`). [ประวัติ →](INDEX_ui_history.md#vehicleadminvehicle_adminhtml) |
| `vehicle/admin/vehicle_fleet.html` (เดิม `admin_manage_fleet.html`) | จัดการรถ + คนขับ + ตารางผู้อนุมัติประจำกอง (view-only); add/edit รถ+คนขับ รวมเป็น modal เดียวต่อโดเมน (`#addVehicleModal`/`#addDriverModal`, ดู [modal_pattern.md](modal_pattern.md)) — service/tax date (เฉพาะ edit) อยู่ใน `#avServiceSection` ภายใน `#addVehicleModal`. **`#addDriverModal` redesign (2026-08-05):** วงกลม avatar upload (`.profile-upload`) ผูก `name="avatar_image"` จริงแล้ว (เดิมไม่มี name → ไม่เคย submit ได้) + prefill รูปเดิมตอน edit; รูปบัตร ปชช. เป็น dropzone/uploaded-card toggle (`#idcardDropzone`/`#idcardUploadCard`, custom retokenize `--bb-*` ของหน้านี้เอง — ไม่ใช่ `Upload()` component ใน CHEATSHEET) คลิกการ์ดเปิด `#idcardPreviewModal` (ดูรูปเต็มจอ, overlay มืดของตัวเอง `data-bs-backdrop="false"` เพราะหน้านี้ modal อื่นไม่มี backdrop มืดอยู่แล้ว); switch "สถานะใช้งาน" ย้ายออกจาก modal → ปุ่มคลิกตรงคอลัมน์ "สถานะ" ในตาราง (`.mf-driver-toggle-active` → AJAX `admin_driver_toggle_active`); badge user account ผูก `d.linked_user` จริง (ซ่อนถ้าไม่ผูก); คอลัมน์ "งานในสัปดาห์" ผูกข้อมูลจริง + chevron สลับสัปดาห์ทำงานจริง (`.fleet-day.is-on/is-wr/is-info`) — ดู `admin_driver_week()`/`_compute_driver_week_status()` ใน INDEX_code.md. CSS ทั้งหมดอยู่ใน `<style>` inline ของเทมเพลตเอง — ⚠️ `vehicle/css/vehicle_fleet.css` มีไฟล์อยู่จริงแต่**ไม่ได้ถูกโหลดจากหน้านี้แล้ว** (pre-existing drift พบระหว่าง sync รอบนี้ ยังไม่ได้ตรวจว่าใครโหลดอยู่หรือลบทิ้งได้). JS `vehicle/js/vehicle_fleet.js`. **Page contract apply (2026-08-07)** — หน้าแรกที่ apply [vehicle_admin_page_contract.md](vehicle_admin_page_contract.md): thead ตาราง (`.fleet-table thead th`) พื้นขาว+เส้นบน-ล่าง `n200` แทน default pill `n50`; empty state (`vc-empty`→`bb-empty`) เปลี่ยนเป็น SVG illustration (`illust-anxious`, variant A 96px) แทน icon เดิม; `#deleteVehicleModal`/`#deleteDriverModal` ทิ้ง legacy `vc-modal` ทั้งก้อน แทนด้วย confirm card ใหม่ (`.mf-confirm-card` radius 20 exception, ไม่มี X, illustration 140px + ปุ่มยกเลิก/ลบแถวเดียวกึ่งกลางไม่มี icon) — JS ไม่กระทบ (ยังผูก id `dv_id`/`dv_name`/`dd_id`/`dd_name` เดิม). `driverDetailModal` ยังไม่แตะ (ตั้งใจ, ดู contract). [ประวัติ →](INDEX_ui_history.md#vehicleadminvehicle_fleethtml-เดิม-admin_manage_fleethtml) |
| `vehicle/admin/vehicle_cost.html` | จัดการค่าล่วงเวลา (OT) คนขับ — KPI (`bb-kpi` is-ghost ×2), filter bar (date range/driver GET), table + status tabs, edit modal (slot rows dynamic), rate config modal (rate rows dynamic), print receipt (`@media print`). **Shell + audit-rule migration → `_base_ue.html` (2026-08-06):** ลบ DOCTYPE/head/`_shared/sidebar.html`+`header.html`/CSS link ทั้งหมด (`design-system.css`/`vehicle_admin.css`/`vehicle.css`/`vehicle_cost.css`/`vehicle_fuel.css`) → `{% extends '_base_ue.html' %}`; status tabs → `.tab2-tab` (dual-class คู่กับ `.cost-chip` เดิมไว้เป็น JS hook), filter popover restyle ด้วย `.ue-chip`/`.ue-chip-pop` (class ล้วน ไม่ใส่ `data-ue-chip-*` กัน auto-init ชนกับ toggle เดิมใน `vehicle_ot.js`), table/badge/status/avatar/button → `.bb-*` (`bb-table`/`bb-status`/`bb-badge`/`bb-avatar`/`bb-btn`/`bb-cell-*`), `data-lucide` icon ทั้งหมด → `material-symbols-rounded` ตรง (เพิ่ม 4 entry ใน `ms-icons.js` MAP: `printer`/`receipt-text`/`rotate-ccw`/`undo-2`; อีก 3 ตัวเปลี่ยนชื่อ lucide source ให้ตรง MAP เดิมแทนเดา: `settings-2`→`settings`, `grid-2x2-check`→`check-circle`, `user-round-check`→`user`, `ellipsis-vertical`→`more-vertical`). widget ที่ **ไม่มี bb-* เทียบเท่า** (component-guide เช็คแล้ว "missing") — slot/rate row editor (`.cost-slot-row`/`.cost-rate-row`), date picker (`.budget-datepick`/`.va-cal*`, ported จาก `vehicle_admin.css`), row action overflow menu (`.cost-action-*`), receipt print sheet (`.cost-receipt-*`/`.rcpt-*`) — **คง class/id เดิมทั้งหมด** (JS hook ใน `vehicle_ot.js`) แค่ retokenize CSS `--vc-*`→`--bb-*`/px ย้ายเข้า scoped `<style>` ใน `{% block head %}` แทนไฟล์แยก. **Page contract apply (2026-08-07)** — ตาราง `#otTable`+pivot table เปลี่ยน class `table data-table` (legacy Zendenta, **orphan จริง**เพราะ `_base_ue.html` ไม่โหลด `main.css` เลย) → `.bb-table` (ได้ thead ที่ถูกต้องฟรีจาก default ของ `components.css` ไม่ต้อง override เพิ่ม); empty state → illustration `illust-anxious` variant B; ปุ่มลบ OT เปลี่ยนจาก `confirm()` เบราว์เซอร์ → `#costConfirmModal` (illustration confirm card, `data-confirm-name` บนฟอร์ม + `showCostConfirm()`/`submitCostAction()` ใหม่ใน `vehicle_ot.js` แทน inline `confirm(form.dataset.confirm)` เดิม). ดู [vehicle_admin_page_contract.md](vehicle_admin_page_contract.md) (backlog ที่ยังไม่ทำรอบนี้: 4 modal restyle pattern A, receipt hex tokenize). **Section tabs (2026-08-08) — เปลี่ยนความหมายทั้งหน้า:** 5 status-filter tabs เดิม (ทั้งหมด/ยังไม่จ่าย/จ่ายแล้ว/ผู้ใช้จ่ายเอง/ลบ, ผูก AJAX นับจำนวนที่ `#costTabs`) ถูกแทนด้วย `tab2_tabs()` จริง 5 มุมมองหน้า (ภาพรวมทั้งปี/ใบจ่ายจริง/ตั้งค่า OT/ผู้ใช้จ่ายเอง/**ทั้งหมด**, id `#costTab2Wrap`, ไม่มีเลข) สลับ panel ด้วย `bindCostSectionTabs()` ใหม่ (`vehicle_ot.js`, pattern เดียวกับ `bindFleetTabs`) — default active = **"ทั้งหมด"** ชั่วคราว (panel เดียวที่มีเนื้อหาจริง คือหน้าเดิมทั้งหมดที่ย้ายเข้ามาไม่แก้ logic). `.cost-filter` popover (คนขับ/งบ/หมวด-กอง/ช่วงวันที่ 6 ช่อง) ถูกตัดออกทั้งก้อน. ปุ่ม "ตั้งค่าอัตรา" (เปิด `rateConfigModal`) ตัดออก — `rateConfigModal` ยังอยู่ในไฟล์แต่เข้าไม่ถึงแล้ว รอ de-modal เข้า tab "ตั้งค่า OT". **แท็บ "ใบจ่ายจริง" ทำจริงแล้ว (redesign รอบ 2, 2026-08-08)** — layout `row col-lg-6/col-lg-6` (อ้างอิง Arto+ create-invoice): ซ้าย = field คนขับ/เดือน `.bb-field-box .slipmk-dd` (โครง visual = `.yp-dd` ของ `yearlyPlanModal` แต่ option ข้างในเป็น radio แบบ `.ue-chip-opt`; **เลิกใช้ `ue_chip_dd` แล้ว** — import ถูกลบออกจากไฟล์) + `.bb-table` รายการ OT (JS render จาก `/admin/ot/slip`) + ยอดรวม + `bb-callout` สรุปผู้ใช้จ่ายเอง; ขวา = `.slipmk-preview-wrap` (พื้น n50 + toolbar) ครอบ **กระดาษ A4 สัดส่วนจริง** `.slipmk-sheet` (`aspect-ratio: 210/297` + `container-type: inline-size` ผูกทุกขนาดกับ `cqw` → ย่อ/ขยายตามคอลัมน์โดยไม่ต้องใช้ JS scale) sticky. JS ใหม่: `initSlipDropdowns()` (ก็อป pattern `.yp-dd` จาก `vehicle_budget.js`) · `slipTableRow()`/`buildSlipSheet()` · ถังขยะแดงต่อแถว = `ot_toggle_no_receipt` จริง · "เพิ่มรายการ" เปิด `addOtModal` เดิม · "พิมพ์" เปิด `#receiptPreviewModal` เดิม (`buildReceiptPage()`/`openPreview()` ยังใช้ร่วมกับแท็บ "ทั้งหมด" **ห้ามลบ**). ปุ่ม `addOtBtn`/`printAllBtn` เดิม **ตัดออกจากหน้าแล้ว** (binding ใน JS ลบตาม) — `printAll()` ลบทิ้งไปก่อนหน้า (ไม่มี caller). **แท็บ "ตั้งค่า OT" ทำจริงแล้ว (2026-08-08, revise เดียวกันวัน — ตัด toggle ออก)** — แทน `rateConfigModal` (markup modal ยังค้างในไฟล์ ไม่มีปุ่มเปิด รอลบพร้อม `.cost-rate-*` ที่ไม่ใช้). โครงอ้างอิง Uber Eats "Operating hours" แต่ **ตัด auto-save** (config คุมเงิน → ต้องกดบันทึก + confirm modal) และ **ตัด drag-to-reorder** (`sort_order` ไม่มีผลกับการคิดเงิน — `build_ot_specs` วนทุก band ที่ทับ ไม่ใช่ first-match → handle จะสื่อผิด). เพิ่มที่ต้นแบบไม่มี: คอลัมน์หน่วย (hourly/flat_day) · error band ข้ามเที่ยงคืน (block ทั้ง client+server) · warning band ทับกัน (เตือนอย่างเดียว, เทียบเฉพาะแถวกลุ่มวันเดียวกัน). ลบช่วง = ถังขยะแดง (ไม่ใช่ toggle — ลองทำ toggle `is_active` ก่อนแล้ว **เปลี่ยนใจกลับเป็นลบทิ้งแบบเดิมในวันเดียวกัน** เพราะง่ายกว่าและตรงกับพฤติกรรม `rateConfigModal` เดิม): แถวใหม่ (ไม่มี `cfg_id`) ลบออกจาก DOM ตรงๆ · แถวเดิมต้องผ่าน `#rateDeleteConfirmModal` ก่อน แล้ว append hidden `cfg_delete[]` เข้า form (soft-delete `is_active=False` ฝั่ง backend — **ไม่มี UI เปิดกลับ** ต้องแก้ DB มือเหมือนของเดิม, ตั้งใจ). ใช้ `rate_configs` เดิม (active เท่านั้น) **ไม่มี** `rate_configs_all`. modal ใหม่ 4 ตัว: `#rateSaveConfirmModal` (ยืนยันบันทึก + จำนวนช่วง) · `#rateDeleteConfirmModal` (ยืนยันลบต่อแถว) · `#rateMidnightErrorModal` (แจ้งชื่อ band ที่ข้ามเที่ยงคืน แทน `alert()`, ปุ่มเดียว) · `#rateDirtyModal` (เตือนตอนสลับแท็บ/กดยกเลิกทั้งที่ยังไม่บันทึก ผ่าน `confirmDiscard()` ที่ใช้ร่วม 2 จุด — `leaveGuard` ที่ `bindCostSectionTabs` เรียก + ปุ่ม "ยกเลิก" แทน `confirm()`; `beforeunload` คุมเคสปิด/รีเฟรชแยก). มือถือ (≤575px) แถว → **card** (label โผล่, ฟิลด์เรียงลง, ถังขยะมุมขวาบน) — ต้อง override media query ซ้ำในสโคป `#costPanelRate` เพราะ id specificity ชนะ rule เดิมของ `.cost-rate-row`. **แท็บ "ผู้ใช้จ่ายเอง" ทำจริงแล้ว (2026-08-08)** — layout อ้างอิง `vehicle_mileage.html` ตรงๆ: `bb_table` (import จาก `_components/bb/table.html`) + thead override scoped `#otSelfResults .bb-table thead th` (พื้นขาว/เส้น n200/text mut, pattern เดียวกับ `#bbMlResults` ของหน้า mileage) สำหรับ desktop + การ์ด `.bb-card`/`.bb-card-body` สำหรับมือถือ (`d-none d-md-block` / `d-md-none`). query `no_receipt_ots` ใน `cost_summary()` เป็นชุดแยกจาก `ots`/`kpi` หลัก — **ไม่มี filter ใดๆ ตามคำขอ** (`no_receipt=True, is_deleted=False`, sort วันที่ล่าสุดก่อน, ไม่ผูก from_date/to_date/sel_driver ของแท็บ "ทั้งหมด"). action เดียว = ปุ่ม undo (นำกลับเข้าใบเสร็จ, `bb-btn is-sec is-icon`) เรียก `ot_toggle_no_receipt` เดิมผ่าน `TOGGLE_NO_RECEIPT_URL_TPL` เดียวกับแท็บ "ใบจ่ายจริง" แล้ว `window.location.reload()` (list นี้ไม่ผูก AJAX swap เหมือน `#costResults`). **คอลัมน์ (revise เดียวกันวัน)** — ช่วงเวลา OT (badge) / วันที่ / คนขับ (ตัด avatar ออก) / Booking / ชม. / ยอด (ทศนิยม 2 หลัก, เดิม 0 หลัก) / action — **ตัดคอลัมน์ งบ+สถานะ ออกทั้งคู่** (enrichment `_ot_budget_label()` ที่ backend ตัดตามเพราะไม่มีใครใช้แล้ว) มือถือปรับ layout ให้ตรงลำดับเดียวกับ desktop. **แท็บ "ภาพรวมทั้งปี" ทำจริงแล้ว (2026-08-08)** — ตาราง pivot คนขับ×เดือน สไตล์ `pivotMockupTable` (`vehicle_budget.html`): sticky คอลัมน์แรกด้วย inline `position:sticky;left:0;z-index:1;background:#fff` ตรงๆ, แถว "รวมทั้งหมด" `border-top:2px solid var(--bb-n200)` ท้ายตาราง — **ย้ายมาจาก `#otPivotCard` เดิมในแท็บ "ทั้งหมด"** (ข้อมูลชุดเดียวกัน `ot_pivot`/`ot_pivot_labels`/`ot_pivot_row_totals`/`ot_pivot_col_totals`/`ot_grand_hours`/`ot_grand_amount` — ไม่มี backend เปลี่ยน แค่ย้าย+restyle template) ตัดออกจากแท็บ "ทั้งหมด" แล้ว ไม่โชว์ซ้ำ 2 ที่. **แถมเจอ+แก้ orphan class bug ระหว่างย้าย** — ของเดิมใช้ `.vc-pivot-td-sticky`/`.vc-pivot-row-summary` (นิยามใน `core/css/components/pivot.css`) แต่ **`pivot.css` ไม่เคยถูกโหลดใน `_base_ue.html`เลย** → sticky column + bold summary row ไม่เคยทำงานจริงมาตลอด (bug ชนิดเดียวกับ `.data-table` ที่เจอตอน page contract 2026-08-07) แก้ด้วย inline style แทน ไม่ต้องพึ่ง CSS ไฟล์แยก. **แท็บ "ทั้งหมด" ทำใหม่ทั้งยวง (2026-08-08, หลัง "ภาพรวมทั้งปี")** — เดิมเป็น "เนื้อหาเดิมของหน้าทั้งหมด" (KPI strip/แถบอัตรา OT/ตาราง/ตารางแยกประเภทงาน) ตอนนี้เหลือแค่ตาราง OT ที่ไม่ถูกลบ **ทั้งหมดไม่มี filter เดือน/คนขับ/งบ/สถานะเลย** (ผู้ใช้เลือก "เก็บ Excel อย่างเดียว ตัดที่เหลือ" เมื่อถาม) — layout `bb_table` + thead override (ใช้ selector เดียวกับ `#otSelfResults` ด้านบน คนละ id คือ `#costResults`) + mobile card เหมือนแท็บ "ผู้ใช้จ่ายเอง" คอลัมน์เดิมครบ (วันที่/คนขับ/Booking/งบ/ช่วงเวลา OT/ชม./ยอด/สถานะ/actions เต็มชุดรวม kebab menu). **ตัดออก:** KPI strip (ยังไม่จ่าย/จ่ายแล้ว), แถบอัตรา OT อ้างอิง, ตารางแยกประเภทงาน+callout ส่วนตัวที่ยังไม่เก็บ — backend ลบ `_calc_ot_kpi()`/`_build_ot_by_expense()`/`_personal_uncollected()` ทั้ง 3 ฟังก์ชันทิ้ง (ไม่มีใครเรียกแล้ว) พร้อม import `Table, Column` ที่ไม่ได้ใช้. **เก็บไว้:** ปุ่ม Excel — แต่ `cost_export()` ยัง scope ตามเดือนปัจจุบัน (from_month/from_year) ไม่ได้ตามรายการที่เห็นทั้งหมด ตั้งใจไม่แตะ (ผู้ใช้ทราบแล้ว). `filterForm` เหลือไว้เปล่าๆ (ไม่มี field) ให้ AJAX-swap infra เดิม (`runFilter()`) ยัง refresh ตารางหลัง row action ได้โดยไม่ต้องแก้ JS. เก็บกวาดของตายไปด้วย: ลบ `bindFilterPopover()`/`bindBudgetFilter()` (dead ทั้งคู่ — DOM target ไม่มีในหน้าอยู่แล้วตั้งแต่ตัด `.cost-filter` popover รอบก่อน) และ `window.EXPENSE_CATS`/`window.COST_FILTER_SUB` script block. **คอลัมน์ตาราง "ทั้งหมด" ปรับอีกรอบ (revise เดียวกันวัน)** — จาก 8 คอลัมน์เต็ม (วันที่/คนขับ/Booking/งบ/ช่วงเวลา OT/ชม./ยอด/สถานะ/actions+kebab) เหลือ 7: **ช่วงเวลา OT (ย้ายมาคอลัมน์แรก)** / วันที่ / คนขับ (ตัด avatar) / Booking / ชม. / ยอด (ทศนิยม 2 หลัก) / action — action เหลือแค่ปุ่ม **แก้ไข** + **ลบ** (ทั้งคู่ `bb-btn is-sec is-icon`, ลบไอคอนสีแดง `--bb-dg-tx`) ตัดคอลัมน์ งบ+สถานะ ออก และตัด kebab overflow menu ทั้งก้อน (จ่าย/ไม่จ่าย toggle, ย้ายไปผู้ใช้จ่ายเอง, พิมพ์ใบเสร็จ — **ไม่มีทางเรียก 3 action นี้จากตารางนี้แล้ว**, `ot_toggle_no_receipt` ยังใช้ได้ผ่านแท็บ "ใบจ่ายจริง"/"ผู้ใช้จ่ายเอง" เท่านั้น) `.cost-action-more`/`.cost-action-menu`/`openKebab()`/`closeKebab()`/`printSingle()` **เหลือไว้เป็น dead code ในไฟล์** (ไม่มี trigger เหลือในหน้านี้แล้ว แต่ไม่ลบเพราะ `closeKebab()` ถูกเรียกแบบ defensive อยู่หลายจุดใน `vehicle_ot.js`, เสี่ยงเกินความคุ้มถ้าจะรื้อ). มือถือปรับตามเดียวกัน. ครบทั้ง 5 tab แล้ว (ไม่มี placeholder เหลือ). [ประวัติ →](INDEX_ui_history.md#vehicleadminvehicle_costhtml) |
| `vehicle/admin/vehicle_budget.html` | จัดการงบ — ประวัติ vc-scope/legacy ก่อน bb-* migrate → [CHANGELOG.md](CHANGELOG.md#vehicleadminvehicle_budgethtml-จัดการงบ). **bb-* migration (2026-07-05):** ลบ legacy CSS ทั้งหมด (รวม `vehicle_budget.css`) → `bb-*` ล้วน + เพิ่ม `<link>` ตรงถึง `core/css/components/dropdown.css` (sbApprover autocomplete พึ่ง CSS นี้ทางอ้อมผ่าน `design-system.css` เดิม). Summary card/tab bar/pivot table/personal section/5 modal → `bb-card`/`bb-tabs`/`bb-badge`/`bb-modal-*` (pivot table sticky-col+heat ไม่มี bb-* เทียบเท่า → ใช้ `table data-table` + inline token แทน). datepicker (`va-cal`) reskin เป็น `.bb-dp`/`.bb-cal-*` แต่ JS logic เดิมไม่แตะ. ตาม pattern [redesign_migration_pattern.md](redesign_migration_pattern.md). **card→table redesign (2026-08-03):** tab "งบที่ใช้อยู่"/"งบปิดแล้ว" เปลี่ยนจาก `budget_card`/`budget_card_off` macro (card grid) เป็นตาราง `bb-table` เดียว (คอลัมน์ ประเภท ขึ้นหน้าสุด เป็น `bb-badge is-ok`(ส่วนกลาง)/`is-info`(ส่วนกอง), ลบคอลัมน์ avatar, action ย้ายเข้า `bb-ml-dd` dropdown ในแถว) — macro `budget_card`/`budget_card_off`/`bcard_section_head` ถูกลบ (dead code หลัง redesign) · tab ปิดแล้วเพิ่ม action icon bin ลบงบ (mockup ยังไม่ผูก logic). **Flexible yearly plan (v2.28, 2026-08-06):** เพิ่ม chip "ปี" (`ddPlanYear`, นำหน้า chip "งบ" — filter server-side ผ่าน `?plan_year=`, overlap ปีปฏิทินกับ `start_date`/`end_date` จริง) · ตัด chip "กอง" (`ddPivotDept`) ออก (ผู้ใช้ตัดสินใจ ไม่จำเป็น) เหลือ "ประเภทงบ" filter อย่างเดียว · เพิ่ม tab ใหม่ "รายชื่องบใหญ่" (`data-budget-panel="plans"`) list ทุก `VehicleBudgetYearlyPlan` + radio "ตั้งเป็นค่าเริ่มต้น" (`data-set-default-radio`, disable ถ้า plan ไม่ครอบวันนี้) · เก็บกวาด `yearlyPlanModal` WIP ที่ค้าง (field ชื่องบซ้ำกับ field เงินก้อน, ช่องวันที่แยกวัน/เดือน/ปีซ้ำ 6 ช่องที่ไม่ได้ผูก logic — ลบทิ้งทั้งหมด) → เพิ่ม field "ชื่องบ" จริง (`name="name"`) ผูก DB, ลบข้อความ "สร้างงบประจำปีได้ปีละ 1 ครั้ง" (ผิด ขัดกับทิศทางใหม่). สเปกเต็ม → [2026-08-06_budget-flexible-plan.md](doc/2026-08-06_budget-flexible-plan.md). **Merge setBudget+topUp modal (2026-08-06):** `#budgetTopUpModal` (action=`top_up`) ถูกลบ — รวมเข้า `#setBudgetModal` เดียว (ทั้งคู่แก้ `budget_amount` field เดียวกัน ต่างกันแค่ absolute/delta) ด้วยกลไกจาก [modal_pattern.md](modal_pattern.md) (hidden `action`+`budget_id` สลับผ่าน JS `show.bs.modal`) + segmented control `.bb-tabs#sbModeTabs` (ซ่อนตอนสร้างงบใหม่จาก toolbar เพราะไม่มีอะไรให้ top-up) แทนสร้าง component ใหม่ — คงโครง `.bb-modal-head/-body/-foot` เดิม (มีปุ่มปิด X) ไม่ตาม literal recipe ของ modal_pattern.md ที่มาจาก context `#bookingModal` คนละหน้า. ปุ่ม dropdown แถว "แก้เพดานงบ"/"เพิ่มงบ (top-up)" ทั้งคู่ชี้ modal เดียวกันแล้ว (ต่าง `data-sb-mode` เลือก tab default). ล็อก → [2026-08-06_merge-setbudget-topup-modal.md](doc/2026-08-06_merge-setbudget-topup-modal.md). **Action column simplify + งบหลัก (v2.29, 2026-08-06 ต่อเนื่อง):** tab "รายชื่องบใหญ่" เปลี่ยนชื่อเป็น **"งบหลัก"** — ตัด header row (ชื่อ+badge+ปุ่ม "ตั้งก้อนงบใหม่") ออกทั้งหมด (สร้างก้อนงบใหม่เหลือทางเดียวคือปุ่ม "ตั้งงบใหม่" โซนบนสุดของหน้า), คอลัมน์ "ช่วงเวลา" เปลี่ยนจาก `d/m/y` ตัวเลขล้วน → รูปแบบไทย `1 มี.ค. 69 - 28 ก.พ. 70` (ผ่าน `_fmt_date_th()`, เพิ่ม `start_date_th`/`end_date_th` ใน `_build_plan_list_rows()`), เพิ่มคอลัมน์ action (icon แก้ไข) ก่อนคอลัมน์ radio "ตั้งเป็นค่าเริ่มต้น" — แต่ละแถวมี `data-plan-*` ครบ (id/name/total/central/start/end/badge) เปิด `yearlyPlanModal` แก้ plan ของแถวนั้นตรงๆ. ตาราง "งบที่ใช้อยู่"/"งบปิดแล้ว" — action column ย่อจาก `bb-ml-dd` dropdown (4 item) → 2 ปุ่มเห็นตรงๆ (icon แก้ไข + toggle เปิด/ปิด `material-symbols-rounded` `toggle_on`/`toggle_off`); "งบปิดแล้ว" ได้ icon ลบจริงเพิ่ม (เดิม mockup) ผูก `delete_budget()` ใหม่ (`app/services/vehicle/budget_service.py`) แทนที่ `extendBudgetModal` (บังคับตั้ง period ใหม่ทุกครั้งที่เปิด — ตัดออก, ปุ่ม/modal ยังอยู่ในไฟล์แต่ไม่มีอะไร trigger แล้ว รอ cleanup แยก). chip "ปี" (`ddPlanYear`) เลิก navigate — filter ตัวเลือกใน chip "งบ" (`ddYearlyPlan`) ฝั่ง client แทน (`data-plan-year-start`/`-end` บน `.ue-chip-opt` ที่เขียนมือแทนผ่าน `ue_chip_opt` macro); label ของ `ddYearlyPlan` แต่ละตัวเลือกเหลือแค่ชื่องบ (ตัดช่วงวันที่ต่อท้ายออก) ยัง navigate ไป `?plan_id=` เหมือนเดิม (ทำหน้าที่ filter จริง). **Toggle switch (v2.29 ต่อเนื่อง):** ปุ่ม toggle เปิด/ปิดงบ (ทั้ง 2 tab) เปลี่ยนจาก icon `toggle_on`/`toggle_off` → switch จริง (`.bb-switch-btn`, pill+knob เลื่อน) — CSS inline ใน `{% block head %}` ของไฟล์นี้เอง (one-off เฉพาะหน้า ตามคำขอผู้ใช้ ยังไม่ทำเป็น component กลาง ไม่มีใน CHEATSHEET.md) สีเปิด = `--bb-ok` (เขียว semantic "success" ที่มีอยู่แล้ว — ผู้ใช้ยืนยันเอาเขียวจริง แม้ guideline v2.1 จะห้าม `--bb-accent` เป็นพื้นที่ใหญ่ ก็ตาม เป็นข้อยกเว้นเฉพาะจุดที่ตกลงแล้ว ไม่ใช่ token ตัวที่ guideline ห้าม) `<button type="submit">` เดิมยังอยู่ครบ (data-confirm-toggle ไม่เปลี่ยน) แค่เปลี่ยนหน้าตา. **v2.30 (2026-08-07, bug fix + feature):** แท็บ "งบหลัก" เพิ่ม icon delete ข้าง edit ต่อแถว (ซ่อนถ้า `row.used > 0`) ผูก `action=delete_plan` → `budget_svc.delete_yearly_plan()` (ฟอร์มใหม่ `data-confirm-delete-plan`) — ลบก้อนงบพร้อม cascade งบย่อยทิ้งถาวร อนุญาตเฉพาะตอนใช้ไป 0 บาททั้งก้อน. **v2.30 ต่อเนื่อง (เพิ่มเติมรอบ 2):** `#pivotMockupTable` (แท็บ "ภาพรวมทั้งปี") เพิ่ม tbody แถว "เงินรับคืน" ต่อจากแถว "รวมทั้งหมด" (ผูก `pivot.personal`/`pivot.summary.personal.used` เดิมที่มีอยู่แล้วแต่ไม่เคยโชว์ในตารางนี้). แท็บ "เงินรับคืน" (`sectionPersonal`) ตัดแถวหัว (ชื่อ+จำนวนรายการ+฿รับแล้ว/ค้าง) และ `<nav class="bb-tabs">` filter (ทั้งหมด/ค้างรับ/รับแล้ว) ออกทั้งคู่ตามคำขอ — เรียงลำดับด้วย sort ฝั่ง controller แทน (ค้างรับขึ้นก่อนเสมอ). `yearlyPlanModal` (`#ypTotal`/`#ypCentral`/`#ypDeptPreview`) เปลี่ยนจาก `type="number"` → `type="text" inputmode="numeric"` + comma mask ระหว่างพิมพ์ (`bindMoneyInputFormat`, `vehicle_budget.js`) — ค่าที่ submit ถูก strip comma ออกก่อนเสมอ (`stripComma` ใน submit handler) backend ก็ strip ซ้ำอีกชั้น (`_handle_set_yearly_plan`) กันเผื่อ JS ไม่ทำงาน. **v2.30 ต่อเนื่อง (รอบ 3):** คอลัมน์ "ก้อนงบ" ในตาราง "งบที่ใช้อยู่"/"งบปิดแล้ว" เปลี่ยนจากโชว์ label ทั่วไป `ปีงบ {fiscal_year+543}` เสมอ → โชว์ `_plan.name` จริงถ้ามี (fallback เป็น `ปีงบ {fiscal_year+543}` เฉพาะ plan ที่ไม่ได้ตั้งชื่อ) ตรงกับ pattern ที่ตาราง "งบหลัก" ใช้อยู่แล้ว. |
| `vehicle/admin/budget_personal.html` | personal reimbursement |
| `vehicle/admin/admin_fuel.html` | **Phase 2.3–2.7 + 3 + 4.1/4.3** — Vercel shell, 6 KPI cells (raw), filter bar (year/month/vehicle/driver GET — raw), Bills data table (Excel export link, anchor `#billsCard`), Reimbursements accordion, **Pivot รถ×เดือน** (heatmap, sticky col, footer sum, **drill-down → Bills filter year+vehicle+month**), **5 modals (bill/reimb/reserve/price/budget)** + JS controller. **Phase 3:** empty states use `empty_state` macro; KPI/filter/table kept raw as canonical reference for `/bbcenter-design`. [ประวัติ →](INDEX_ui_history.md#vehicleadminadmin_fuelhtml) |
| `vehicle/admin/fuel-modal-bill.html` | Bill create/edit/delete modal — date/vehicle/driver/amount/payment radio segmented/mileage/note. `#fuelBillModal` |
| `vehicle/admin/fuel-modal-reimbursement.html` | Reimbursement create/edit modal — bill list summary + เลขใบเบิก/แหล่ง/วันส่ง/note. `#fuelReimbModal` |
| `vehicle/admin/fuel-modal-reserve.html` | Reserve adjust modal — current summary + signed change + note (required) + history 20. `#fuelReserveModal` |
| `vehicle/admin/fuel-modal-price.html` | Fuel price modal — add new + history with delete. `#fuelPriceModal` |
| `vehicle/admin/fuel-modal-budget.html` | Annual budget modal — single number input + summary. `#fuelBudgetModal` |

**กฎสำคัญ:** modal ห้ามมี inline `<script>` — JS อยู่ใน `pages/vehicle.js` ทั้งหมด

---

## 🎨 Design System

> 🎨 **กฎ design = [design_guideline.md](design_guideline.md) (canonical เดียว · v2.1 2026-07-21 "ink คือโครง เขียวคือสัญญาณ").** ส่วนนี้คือ **file map ของ asset เก่า (`--vc-*` legacy)** เท่านั้น — ใช้ดูว่าไฟล์ CSS/JS อยู่ไหน ไม่ใช่กฎ design. design ใหม่/redesign → ยึด guideline
>
> ✅ **v2.1 (Batch 0-3 · 2026-07-21) — `components.css` = canonical ตัวเดียว:** [ประวัติ →](INDEX_ui_history.md#design-system-v21-batch-0-3)

> **2026-06-07 (ขั้น 5):** asset ย้าย → `static/<domain>/{css,js}/` + `static/core/` (shared). ชื่อ `css/X.css` / `js/pages/X.js` ใน 2 ตารางด้านล่าง = **เก่า** → mapping:
> - **core/** (shared): `design-system` `tokens` `main` `util` `vercel` `notification` `components/*` (css) · `icons` `format` `http` `main` `notification` (js)
> - **vehicle/** (prefix `vehicle_`): `vehicle.css`(base) · `fuel_admin`→`vehicle_fuel` · `budget_manage`→`vehicle_budget` · `mileage_admin`→`vehicle_mileage` · `manage_fleet`→`vehicle_fleet` · `driver`→`vehicle_driver` · `approver_inbox`→`vehicle_approver` · `vehicle_admin`/`vehicle_cost` คงชื่อ (js: `-`→`_`, `ot-admin`→`vehicle_ot`). หมายเหตุ: `activity_timeline`/`vehicle_history` css+js ลบ 2026-06-07 (dead)
> - **repair/room/maintenance/dashboard/**: `<domain>.css` + `<domain>.js` · images = shared คง `static/images/`

**Token source (single):** [app/static/core/css/tokens.css](../../app/static/core/css/tokens.css) — 2026-05-14 split out (Phase 1)
- `--vc-*` **canonical** (Vercel-Black + Indigo accent) — ใช้ใน code ทุกที่ เพิ่ม `--vc-z-*`/`--vc-sidebar-width`/`--vc-header-height` ใน Phase 5.1; **2026-06-10:** font หลัก + ปุ่ม primary เปลี่ยนจากดำ `#111827` → กรมท่า/navy `#1C2E4A` (`--vc-fg` + `--vc-primary`, hover `#26405F`) — กระทบ text หลัก, `ds-btn-primary`, `vc-btn-primary`, sidebar logo box ทั้งระบบ; **Phase 3 vehicle user-facing (2026-05-17)** เพิ่ม `--vc-purple-bg` (`rgba(121,40,202,.10)`) + `--vc-purple-border` (`rgba(121,40,202,.25)`) ใต้ `--vc-purple` สำหรับ `vc-status-dot--group`
- `--ds-*` **RETIRED** (Phase 5.1, 2026-05-16) — ลบ Part A ออกครบแล้ว; `var(--ds-*)` ต้องไม่มีใน codebase; ถ้าเจอ = bug (vehicle user-facing Phase 4 2026-05-17 — `pages/vehicle.js` cleared 14 จุด; เหลือ `pages/vehicle-admin.js` L344/L369 ใน admin page)
- `@import`-ed by `design-system.css` + `components.css` (ทุกหน้าทั้ง 2 ระบบ legacy/bb-* โหลดผ่าน transitively). **scrollbar-hide + modal-open/backdrop fix consolidate (2026-07-21):** ย้ายมารวมที่นี่จุดเดียว (เดิมซ้ำ 3 ไฟล์: tokens/components/design-system — `components.css` มี scrollbar-hide แต่ขาด modal fix ทำให้เห็น gap ตอนเปิด modal บนหน้า bb-* เช่น `vehicle_mileage.html`); ไฟล์อื่นแก้ไม่ต้องก็อปซ้ำ ให้ `@import` จากที่นี่

**Component entry:** [app/static/core/css/design-system.css](../../app/static/core/css/design-system.css) — typography + components + `.vc-mono/-caption/-icon*/-scope` utilities (no token defs anymore). `@import`s `tokens.css` + every file in `components/`. [ประวัติ →](INDEX_ui_history.md#design-systemcss-component-entry)

**Component library (Phase 2, 2026-05-14):**
| Component | CSS | Macro | Notes |
|-----------|-----|-------|-------|
| KPI strip | [components/kpi.css](../../app/static/core/css/components/kpi.css) | [_components/kpi.html](../../app/templates/_components/kpi.html) | `kpi_group(cols=3\|4\|6)` + `kpi_cell(label,value,unit,icon,meta,tone)`. Tones: muted/success/danger/blue/warn. Extracted from `fuel_admin.css`+`budget_admin.css`. (4-col added 2026-05-18 for admin_fuel KPI group A. `--purple` retired Phase 8, 2026-05-22.) |
| Filter bar | [components/filter_bar.css](../../app/static/core/css/components/filter_bar.css) | [_components/filter_bar.html](../../app/templates/_components/filter_bar.html) | `filter_bar()` wraps a `<form>`; `filter_select`/`filter_date` for fields. Extracted from `fuel_admin.css §21`. |
| Badge | [components/badge.css](../../app/static/core/css/components/badge.css) | [_components/badge.html](../../app/templates/_components/badge.html) | `.vc-badge` + tones `-neutral/-warning/-blue/-success/-danger/-solid`, `.vc-badge-dot` left dot, `.vc-badge-xs` small. `-solid` = inverted black-fg/white-bg for strong emphasis (re-added Phase 3.2 for "X งานรวม" pill). (Page-local `.vc-badge-purple` ลบ Phase 8, 2026-05-22 — markup zero usage.) Phase 3 aligned to flat vocab matching `/bbcenter-design` skill. |
| Pill | [components/pill.css](../../app/static/core/css/components/pill.css) | [_components/pill.html](../../app/templates/_components/pill.html) | Rounded chip for filter tabs/segmented. `.is-active`, tones `accent/success/danger`. |
| Empty state | [components/empty_state.css](../../app/static/core/css/components/empty_state.css) | [_components/empty_state.html](../../app/templates/_components/empty_state.html) | `empty_state(title, desc, icon, compact)` — lucide icon. Use `{% call %}…{% endcall %}` for CTA button. Phase 3 aligned to flat vocab (`vc-empty-title/-desc/-icon`). |
| Form group | [components/form_group.css](../../app/static/core/css/components/form_group.css) | [_components/form_group.html](../../app/templates/_components/form_group.html) | `form_input/form_select/form_group(call)`. States `.has-error/.is-disabled`. |
| Table shell | [components/table_shell.css](../../app/static/core/css/components/table_shell.css) | [_components/table_shell.html](../../app/templates/_components/table_shell.html) | `.vc-table-shell` wrapper + `.vc-table` skin. |
| Modal | [components/modal_shell.css](../../app/static/core/css/components/modal_shell.css) | [_components/_modal.html](../../app/templates/_components/_modal.html) | Bootstrap-based macro (predates Phase 2) + new tokenized helpers (`.vc-modal-section/-divider`). |
| Pivot table | [components/pivot.css](../../app/static/core/css/components/pivot.css) | (no macro — used inline as `<details class="vc-card vc-pivot-wrap">`) | `.vc-pivot-wrap/-summary/-scroll/-table/-th-sticky/-td-sticky/-cell/-cell-empty/-total-col/-link` + `--cell-heat` (0-100 heat scale, accent blue at low opacity via `rgba(var(--vc-accent-rgb), …)`, 2026-06-16; เดิม indigo). Collapsed via `<details>` + `vc-collapse-chevron` rotation. [ประวัติ →](INDEX_ui_history.md#pivot-table) |
| Dropdown + Autocomplete | [components/dropdown.css](../../app/static/core/css/components/dropdown.css) | (no macro — JS hook `data-dropdown` / `data-autocomplete`; JS: [core/js/dropdown.js](../../app/static/core/js/dropdown.js)) | [ประวัติ →](INDEX_ui_history.md#dropdown-autocomplete) |
| UE foundation CSS | [core/css/ue.css](../../app/static/core/css/ue.css) | (no macro — โหลดผ่าน `_base_ue.html` หลัง components.css) | [ประวัติ →](INDEX_ui_history.md#ue-foundation-css) |
| UE motion JS | (no CSS) | (no macro — JS: [core/js/ue-motion.js](../../app/static/core/js/ue-motion.js)) | **Phase 1.5 (2026-07-11)** — `window.ueMotion` (pattern เดียวกับ `bbToast`): `countUp(el,target,{format,duration})` · `staggerRows(scope,{rows,dots,step,cap})` · `showSkeleton(container,{count})` + `REDUCE`/`sleep`/`SKEL_MIN_MS`. generic/parameterized — page JS เรียกใช้ (mileage: `countUp(elC,c,{format:fmt})` ฯลฯ). classic script โหลด "ก่อน" page JS (ผ่าน `_base_ue.html`). |
| UE Chip (Uber-style filter chip) | [core/css/ue.css](../../app/static/core/css/ue.css) § CHIP | [_components/bb/ue_chip.html](../../app/templates/_components/bb/ue_chip.html) | [ประวัติ →](INDEX_ui_history.md#ue-chip-uber-style-filter-chip) |
| Combo + Daterange popover (shared behavior) | (scoped ใน `components.css`/`ue.css`) | [_components/bb/combo.html](../../app/templates/_components/bb/combo.html) / [_components/bb/daterange.html](../../app/templates/_components/bb/daterange.html) | [ประวัติ →](INDEX_ui_history.md#combo-daterange-popover-shared-behavior) |
| MS icon transform (Lucide→Material Symbols) | (no CSS — sizing via page CSS) | (no macro — JS: [core/js/ms-icons.js](../../app/static/core/js/ms-icons.js)) | [ประวัติ →](INDEX_ui_history.md#ms-icon-transform-lucidematerial-symbols) |
| Sidebar | [components/sidebar.css](../../app/static/core/css/components/sidebar.css) | (template: `_sidebar.html`) | `.sidebar` + `.sb-brand/-logo(-icon,-text)/-close/-nav/-section-label/-item(.active)/-icon/-label/-badge/-footer/-logout`. Defines `--sidebar-width`. [ประวัติ →](INDEX_ui_history.md#sidebar) |

Total: 9 component CSS files + 8 macros (7 new in Phase 2 + 1 pre-existing `_modal.html`).

**Highlights:**
- `--vc-accent` `#4059e6` (indigo, 2026-06-17 redesign; เดิม `#014198` sila5) = `--vc-primary` — active states + focus ring + selected/today (legacy alias `--ds-accent` ยังมีใน tokens.css Part A — ห้ามอ้างใน code ใหม่)
- `--vc-accent-rgb` `64, 89, 230` (2026-06-17) — channel triplet สำหรับ `rgba(var(--vc-accent-rgb), …)` ที่ต้องคุม opacity (pivot heat-tint, link hover bg). **Indigo drift cleanup (2026-06-16):** rebrand 2026-06-14 แก้แค่ token แต่ component CSS ยัง hardcode indigo เก่า — ไล่แก้ครบ: `pivot.css` (heat-tint rgba `79,70,229`→`var(--vc-accent-rgb)` + drop fallback `#4F46E5`), `vehicle_budget.css` (central→`--vc-accent`, dept `#818cf8`→`--vc-blue-mid`), `vehicle_mileage.css` + `vehicle.css` (drop indigo fallback, focus ring→`--vc-accent-ring`). grep zero-drift ผ่าน
- `--vc-primary` `#4059e6` (indigo, 2026-06-17 redesign; เดิม `#014198` sila5) — primary CTA · `--vc-blue` unify เข้า brand เฉดเดียว · text = navy `#162334` (`--vc-fg`) · `--vc-border` `#f0f0f0` · `--vc-radius-md` 8px (card) · redesign ใช้ 6px (`--vc-radius-sm` = rounded-2) · **+token** `--vc-icon` `#9999b0` / `--vc-icon-bg` `#f0f0f0` / `--vc-sidebar-bg` `#fafcfc` (2026-06-19 Zendenta; เดิม `#fafbfc`) / `--vc-sidebar-active` `#e6ecfa`
- Shadow: ไม่มี (border only) — ยกเว้น modal
- **`--bb-n700` `#4A4A4A`** (components.css `:root`, 2026-07-22) — เติมช่องว่างใน neutral scale เดิม (`n50/100/200/300/500`) สำหรับ text บนพื้น tint (`n50`/`n100`) ที่เข้มกว่า `mut`; adopter แรก = `.ue-chip` base text
- Font: ไทย = Sarabun (`--vc-font-sans`, โหลด global ใน `_header.html`); [ประวัติ →](INDEX_ui_history.md#font-stack-sarabun-inter)
- `.vc-scope` = opt-in body class for pages using `--vc-*` foundation
- **🎨 `main.css` = Zendenta layer** — [core/css/main.css](../../app/static/core/css/main.css) rewrite จาก legacy login CSS → design layer. โหลดผ่าน `_shared/header.html` **หลัง** `design-system.css` = override. ครอบ app layout `.main-content` · `.sidebar-*` · `.zen-card` · `.kpi-tile` · `.data-table`. [ประวัติ →](INDEX_ui_history.md#maincss)

- **🖼️ `/dev/components` = visual reference (canonical):** render component จริงทุกตัวผ่าน `{{ component(obj) }}` (drift ไม่ได้; static `components-gallery.html` retired 2026-07-19). **rule: อยากได้ component → เปิด `/dev/components` ก่อน · มีแล้ว copy · ไม่มี แจ้งกลับเพื่อเพิ่ม** (ห้ามสร้าง `.bb-*` ใหม่เองมั่ว). ต้อง sync ทุกครั้งที่เพิ่ม/แก้ component ใน components.css
- **🎨 `components.css` = Component library (canonical)** — [core/css/components.css](../../app/static/core/css/components.css) prefix `.bb-*` + token `--bb-*`. `:root` ของทั้งระบบอยู่ที่ไฟล์นี้ที่เดียว. **2026-08-05:** เพิ่ม `.bb-daypop*` (day-cell overflow popover "+N รายการ" ของ `vehicle.html` — ย้าย+rename มาจาก `.vc-cal-pop*` เดิมใน `vercel.css`, ลบ block เดิมทิ้งแล้ว; token `--vc-*`→`--bb-*`; แก้บั๊ก dot สี `waiting_approver` ซ้ำ `pending`; แถวทั้งแถวเปลี่ยนสี+ตัวอักษรตามสถานะแทน dot เดี่ยว; arrow สีขาวคงที่ + `z-index` อยู่ใต้ `.popover-body`. คนละ namespace กับ `.bb-mcal-pop` — ห้ามใช้ชื่อซ้ำ) [ประวัติ →](INDEX_ui_history.md#componentscss)

**Per-page CSS:**
| File | ใช้กับ |
|------|--------|
| `vehicle.css` | หน้า user vehicle + history (calendar ถูกลบใน Phase 0.5). โหลดโดย `room.html`/`vehicle_driver.html`/`vehicle_approver.html`/`admin_fuel.html`/`vehicle_budget_personal.html`/`dashboard.html`/`repair.html`/`maintenance.html`/`line_link.html` — **ไม่ใช่ `vehicle.html`** (หน้านั้นโหลด `vehicle_calendar.css` แทน — ดูแถวถัดไป). **`vehicle_cost.html` เอาออกจาก list นี้แล้ว (2026-08-06)** — เลิกโหลดหลัง migrate ไป `_base_ue.html`. [ประวัติ →](INDEX_ui_history.md#vehiclecss) |
| `vehicle/css/vehicle_calendar.css` | **`vehicle.html` only** (ปฏิทิน user-facing) — `--bb-*` ล้วน, โหลดท้ายสุดเสมอ. **2026-08-05:** `.vrc-m-evt-toggle i` (selector ผูก tag `<i>` เดิม) → `.vrc-m-evt-toggle .material-symbols-rounded` (icon เปลี่ยนจาก `<i data-lucide>` เป็น `<span>` ตรงๆ ใน `pages/vehicle.js` — กันแอนิเมชัน chevron หมุนตอน expand/collapse พัง). **2026-08-03:** `.mobile-indicator.is-empty` (visibility:hidden slot กัน calendar-cell มือถือสูงไม่เท่ากัน) · `.vrc-m-evt*` (card/dot/status) ลบทิ้ง → `.vrc-m-list` ใช้ `bb-card`/`bb-avatar`/`bb-subtext`/`bb-status`/`bb-badge` จาก `core/css/components.css` แทน (เหลือแค่ `.vrc-m-evt-toggle`/`.vrc-m-evt-sub-row`/`.vrc-m-evt-sub-title`/`.vrc-m-evt-sub-meta` เดิมสำหรับ group collapse — `.vrc-m-evt-sub-time`/`.vrc-m-evt-sub-body` ลบเพิ่ม (dead หลัง sub-row เปลี่ยน markup ให้ตรง adminPreviewCards)) · `.bk-detail-footer:has(#detailActions:empty)` + `.bk-detail-content .bk-close` ลบ (ดู `vehicle-modal-detail.html`) · **redesign เต็มรอบ (2026-08-05 ต่อเนื่อง):** ลบเพิ่ม `.bk-detail-status*`/`-plate-*`/`-members`/`-member*`/`-footer`/`-content`/`-dialog` ทั้งชุด (dead หลัง `vehicle_detail.html` เปลี่ยนไปใช้ inline-style) — **เก็บ** `.bk-detail-title/-divider/-info/-row/-icon/-text/-section-label` ไว้เพราะ `room/modals/room_detail.html` ยังใช้ (shared CSS ข้าม domain, เช็กแล้วด้วย grep ก่อนลบ) |
| `vehicle_zendenta.css` *(ใหม่ 2026-06-14)* | **`vehicle.html` only** — vehicle theme (scope `body.zd`, link ต่อท้าย). **calendar restyle "ภาพ 2"**: `.cal-toolbar` grid `1fr auto 1fr` → `#currentMonthLabel` กลางเสมอ (+ `.cal-month-label`); `.calendar-grid-head` พื้นใส + `.calendar-header-cell`; `.date-number` มุมขวาบน (`align-self:flex-end`); **today = เลขปกติ** (ลบวงกลม base), **selected = เลขวงกลมน้ำเงินทึบ** (วงกลมเดียว); `.calendar-cell.other-month` opacity .5; `.event-card` pill. [ประวัติ →](INDEX_ui_history.md#vehicle_zendentacss-ใหม่-2026-06-14) |
| `vehicle_driver.css` *(เดิม driver.css)* | **`/driver` only** [ประวัติ →](INDEX_ui_history.md#vehicle_drivercss-เดิม-drivercss) |
| `vehicle_admin.css` | admin dashboard + budget pages. [ประวัติ →](INDEX_ui_history.md#vehicle_admincss) |
| `fuel_admin.css` | **Vercel namespace** — fuel page only. Page-specific only now (Phase 2 moved KPI+filter → `components/`; Phase 3 moved badge+empty-state → `components/badge.css` + `components/empty_state.css`; **Phase 7 (2026-05-22)** moved §22 pivot table → `components/pivot.css`). Sections: page shell, header, card, btn, table, list+collapse+meta-grid, form input/segmented radio/modal Bootstrap-override skin/history scroll table. §22 = comment pointer only. |
| `vehicle/css/vehicle_mileage.css` | **RETIRED** — ไม่มี template โหลดแล้ว (superseded โดย bb-* migration ของ `vehicle_mileage.html`, ยืนยันด้วย grep 2026-07-05) — ประวัติเต็ม → [CHANGELOG.md](CHANGELOG.md#vehiclecssvehicle_mileagecss-retired) |
| `vehicle_cost.css` | **ORPHAN (2026-08-06)** — ไม่มี template โหลดแล้ว หลัง `vehicle_cost.html` migrate ไป `_base_ue.html` (audit-rule pass ย้าย rule ที่ยังต้องใช้ — slot/rate row editor, date picker, action menu, receipt print — เข้า scoped `<style>` ใน template เอง retokenize `--bb-*` แล้ว ที่เหลือแทนด้วย `.bb-*`). ไฟล์เดิม (Pure `--vc-*`, Phase 3.7) เก็บไว้บนดิสก์เฉยๆ ยังไม่ลบ. [ประวัติ →](INDEX_ui_history.md#vehicle_costcss) |
| `maintenance.css` | **`/maintenance` only** (2026-05-17) — `vc-scope` page. Classes: `.maintenance-header/-header-actions`, `.maintenance-resolved-note` (truncate with tooltip). ใช้ `--vc-*` ทั้งหมด, zero HEX. **Redesign (2026-06-16):** ลบ `.maintenance-title/.maintenance-subtitle` (header ใช้ utility class); + custom DataTables pagination scoped `#maintenanceTable_wrapper` (pill วงกลม accent, prev/next chevron `::before`) + `.maintenance-goto/-label/-select/-btn` (copy #2 ของ pattern — ถ้าหน้า #3 ใช้ → extract shared component). |
| `repair.css` | **`/repair` only** [ประวัติ →](INDEX_ui_history.md#repaircss) |
| `vehicle_approver.css` *(เดิม approver_inbox.css)* | **`/vehicle/approver` only** [ประวัติ →](INDEX_ui_history.md#vehicle_approvercss-เดิม-approver_inboxcss) |
| `room.css` | **ลบแล้ว (2026-08-05)** — room ไม่มี CSS แยกของตัวเองอีกต่อไป, override ที่เหลือ (badge/avatar สีตาม room kind) ใช้ component กลางใน `components.css` (`bb-badge is-info/is-wr`, `bb-avatar` + inline token) แทนทั้งหมด. base grid/mobile-list/`.bk-*` มาจาก `vehicle/css/vehicle_calendar.css` (โหลดคู่กับ `vehicle.html`) [ประวัติ →](INDEX_ui_history.md#roomcss) |
| `manage_fleet.css` (ไฟล์จริง `vehicle_fleet.css`) | **`/admin/manage-fleet` only** [ประวัติ →](INDEX_ui_history.md#manage_fleetcss-ไฟล์จริง-vehicle_fleetcss) |
| `budget_manage.css` | **RETIRED** — ไม่มี template โหลดแล้ว (superseded โดย bb-* migration ของ `vehicle_budget.html`, ยืนยันด้วย grep 2026-07-05) — ประวัติเต็ม → [CHANGELOG.md](CHANGELOG.md#budget_managecss-retired) |
| `dashboard.css` | **`/dashboard` only** [ประวัติ →](INDEX_ui_history.md#dashboardcss) |
| `notification.css` | notification panel + toast. [ประวัติ →](INDEX_ui_history.md#notificationcss) |
| `vercel.css` | **Vercel-style refresh Phase 6.0** — **2026-08-05:** `.vc-cal-pop*` (day-cell overflow popover) ลบทิ้งทั้งหมด → ย้ายไป `core/css/components.css` เป็น `.bb-daypop*` (ดูแถว components.css ด้านบน) [ประวัติ →](INDEX_ui_history.md#vercelcss) |
| `util.css` | **orphan ตั้งแต่ 2026-06-16** — ยังถูก include ที่อื่นไหมให้เช็กก่อนลบ. (`main.css` **กลับมาใช้แล้ว 2026-06-19** = Zendenta layer โหลดทุกหน้าผ่าน `_header.html` — ดู § Design System บรรทัด main.css entry) |
| `auth/css/login.css` | **หน้า login** (2026-06-16) — Sneat-style card, `--vc-*` tokens เท่านั้น, no shadow→border. คลาส `login-card/login-brand/login-field/login-input/login-toggle/login-btn/login-flash/login-dots` (`login-row/login-remember/login-forgot` ลบ 2026-06-17) |
| `auth/js/login.js` | **หน้า login** (2026-06-16) — IIFE toggle แสดง/ซ่อน password (สลับ icon-eye / icon-eye-off) |

**Core JS modules** (`app/static/core/js/`, Phase 4.0 — 2026-05-15; ย้ายจาก `js/core/` ขั้น 5, 2026-06-07):
| File | Exports |
|------|---------|
| `core/icons.js` | `initIcons(scope?)` — guarded `lucide.createIcons()`; ส่ง `Element` เพื่อจำกัด scope (modal-only re-init); `bindModalReinit()` — re-render on `shown.bs.modal` (auto-scope ไป `e.target`) |
| `core/format.js` | `thb(n)`, `km(n)`, `number(n)`, `thaiDate(d, {abbr})`, `thaiTime(d)` — Thai BE year + locale formatting |
| `core/http.js` | `get(url, params)`, `post(url, data)`, `del(url)` — auto JSON parse, CSRF from `<meta name="csrf-token">`, throws `HttpError` on non-2xx |

**ที่ยังไม่สร้าง** (จะเพิ่มเมื่อ feature module ต้องการ): `core/modal.js`, `core/toast.js`, `core/form.js`

**Per-page JS:**
| File | โหลดใน |
|------|--------|
| `pages/vehicle.js` | vehicle templates (รวม modals ทั้งหมด). **2026-08-05:** `buildDesktopEventCards()` — popover "+N รายการ" ตอนนี้แสดง**ครบทุกรายการ**ของวันนั้น (เดิมแสดงแค่ส่วนเกินหลัง 2 chip แรก); ป้าย trigger ยังนับตามส่วนเกินเหมือนเดิม (ไม่เปลี่ยน); class เปลี่ยนจาก `vc-cal-pop*` → `bb-daypop*` (ดู `components.css`) · icon ทั้งไฟล์แปลงจาก Lucide (`data-lucide`) → Material Symbols ตรงๆ (`<span class="material-symbols-rounded">`) ครบทุกจุด active ตาม MAP ใน `ms-icons.js` (เลิกพึ่ง shim). **2026-08-03:** `createCell()` render `.mobile-indicator` slot เสมอ (`.is-empty` เมื่อ 0 event) · `updateMobileList()` การ์ด bb-card (`STATUS_TONE`/`BB_STATUS_TONE`/`BB_STATUS_ICON` consts ใหม่ต่อ status) ตรงกับ `adminPreviewCards` (`ptCardSingle`/`ptCardGroup` ใน `vehicle_admin.js`) ทุกจุดยกเว้น budget/checkbox/ปุ่ม action — title=ชื่อผู้จอง, subtext=เส้นทาง (`pickup → dest`), meta=`_mlTripMeta()` (pax+เวลา+duration ตาม `ptTripMeta`), บรรทัดคนขับ·ทะเบียน (`_mlPlate()`) แทน budget line · helper ใหม่ `_mlDurLabel()`/`_mlPlate()`/`_mlTripMeta()` (ก่อนฟังก์ชัน `updateMobileList()`) · `openEventDetail()` footer เพิ่มปุ่ม "ปิด" fallback เมื่อไม่มี action **(กลับแล้ว — ดู entry ถัดไป)**. **`openEventDetail()` redesign เต็ม (2026-08-05 ต่อเนื่อง):** ผูกข้อมูลจริงเข้า header/`#detailVehicleDriverSection` (`VEHICLE_ASSIGNED_STATUSES = ['waiting_approver','approved']`)/สมาชิกผู้โดยสาร (การ์ดเส้นประ, คนแรกไม่มีเส้นประบนกันซ้อนกับ `hr.bk-divider`) แทน stub เดิมที่ comment ทิ้งไว้ (ดู `vehicle_detail.html`); ปุ่มโทรผูก `tel:${driverPhone}`; footer ไม่มี action = ซ่อนทั้งแถว (`actDiv.classList.toggle('d-none', ...)`) **แทนโชว์ปุ่ม "ปิด" แบบ 2026-08-03**; `_plateLabel()` ลบทิ้ง (ไม่ใช้ format เดิมแล้ว); `openEditBookingModal()`/`openDuplicateModal()` เพิ่ม prefill `bk_note`. [ประวัติ →](INDEX_ui_history.md#pagesvehiclejs) |
| `pages/vehicle-admin.js` *(ไฟล์จริง `vehicle/js/vehicle_admin.js`)* | admin dashboard (Phase 4.3, 2026-05-15) — ES module (`type="module"`); imports `initIcons` จาก `core/icons.js`. **2026-08-03:** `openAdminBookingDetail()` เพิ่มปุ่ม "ปิด" fallback ใน footer เมื่อ `!canEdit` (booking completed/cancelled) — คู่กับการตัดปุ่ม X ออกจาก `#eventDetailModal` ที่ share กับ `pages/vehicle.js` (ดู `vehicle-modal-detail.html`) **(กลับแล้ว — ดู entry ถัดไป)**. **`openAdminBookingDetail()` เขียนใหม่ทั้งหมด (2026-08-05):** ของเดิมพังอยู่ (อ้าง id ที่ template ลบไปแล้ว: `detailStatusPill`/`detailPlate`/`detailDriverLine`/`detailDriver`) — เขียนใหม่ให้ตรง design เดียวกับ `pages/vehicle.js::openEventDetail()` ทุกจุด (`PT_TONE`/`STATUS_BADGE`/`STATUS_ICON` เดิมนำมาใช้, ไม่ต้องสร้างใหม่) รวม **group-member logic ที่ admin เดิมไม่มี** (`_adminSortByTime()` ใหม่, `tripGroup` filter/sort เหมือน user); `STATUS_CSS_KEY` ลบทิ้ง (ไม่ใช้แล้ว); footer ไม่มีปุ่มแก้ไข = ซ่อนทั้งแถวแทนโชว์ปุ่ม "ปิด"; `openAdminEdit()`/`saveAdminEdit()` เพิ่ม `editNote`/`note`. [ประวัติ →](INDEX_ui_history.md#pagesvehicle-adminjs) |
| `vehicle/js/vehicle_mileage.js` | admin mileage page (Phase 4.5, 2026-05-15; renamed จาก `pages/mileage-admin.js` ใน prefix migration 2026-06-07) — ES module (`type="module"`); modal 3-state (start/end/complete), realtime cost preview, checkbox summary, export-link sync. Exposes `openMileage()`, `goEditEnd()`, `clearSelection()` ไป `window.*` สำหรับ legacy `onclick=""` ใน template (3 จุด). `window.MLG_FUEL_PRICE` injection คงเดิม. [ประวัติ →](INDEX_ui_history.md#vehiclejsvehicle_mileagejs) |
| `pages/fuel-admin.js` | fuel page (Phase 4.6, 2026-05-15) — ES module (`type="module"`); imports `initIcons`/`bindModalReinit` จาก `core/icons.js`. 5-modal controller (bill/reimb/reserve/price/budget), checkbox→merge, kebab→edit, lucide re-init on shown.bs.modal, `wireFilterBar` (auto-submit GET on select change). ลบ IIFE + DOMContentLoaded guard (module deferred). ไม่มี `onclick=""` → ไม่ต้อง window expose. [ประวัติ →](INDEX_ui_history.md#pagesfuel-adminjs) |
| `pages/maintenance.js` | maintenance page (2026-05-17) — legacy script (ไม่ใช่ ES module เพราะต้องรอ jQuery+DataTable global); DataTable init (langUrl จาก `#pageData[data-dt-lang]`), auto-open form modal เมื่อ `data-edit-mode="true"`, modal:รับงาน/ปิดงาน handlers (populate จาก `data-ticket-*`), delete confirmation, Export Excel. ไม่มี `onclick=""` → ไม่ต้อง window expose. **Redesign (2026-06-16):** `reinitIcons()` (re-render Lucide รายการในหน้า 2+) + `renderGotoPage(table)` (go-to-page control) เรียกทุก DataTable `draw`; เพิ่ม `pageLength: 10`. |
| `pages/repair.js` | repair page (Phase 4.2, 2026-05-15) — ES module (`type="module"`); imports `initIcons`/`bindModalReinit` from `core/icons.js`. ลบ `$(document).ready()` wrapper (module deferred). DataTable init + lucide re-init on draw, modal:รับงาน/ปิดงาน handlers, auto-open edit modal, tooltips, upload zone. jQuery+bootstrap+DataTable ยังใช้ผ่าน global. **Redesign (2026-06-16):** `renderGotoPage(table)` สร้าง go-to-page control (label+select+ปุ่ม "ไป") append เข้า `.dataTables_paginate` แล้ว refresh ทุก draw (DataTables ล้าง innerHTML ทุก redraw → re-attach). |
| `pages/notification.js` | ทุกหน้าที่มี notification panel — โหลดจาก `_header.html` (Phase 4.9, 2026-05-16). ES module (`type="module"`); polling `/api/notifications` ทุก 30 วิ, dropdown panel ( [ประวัติ →](INDEX_ui_history.md#pagesnotificationjs) |
| `pages/ot-admin.js` | vehicle_cost.html (Phase 4.8, 2026-05-16) — ES module (`type="module"`); imports `initIcons` จาก `core/icons.js`. Edit modal slot row builder + recompute, print receipt (single/all), filter auto-submit. Pure data-attr delegation (no `onclick=""`) → ไม่ต้อง window expose. ลบ IIFE + DOMContentLoaded wrapper (module deferred). [ประวัติ →](INDEX_ui_history.md#pagesot-adminjs) |
| `pages/approver-inbox.js` | approver_inbox.html (Phase 4.1, 2026-05-15) — ES module (`type="module"`); functions: `switchTab()`, `showRejectForm(id)`, `hideRejectForm(id)`, chevron rotation. Exposes to `window.*` สำหรับ legacy `onclick=""` ใน template |
| `pages/driver-home.js` *(ไฟล์จริง `vehicle/js/vehicle_driver.js`)* | vehicle_driver.html (Phase 4.4, 2026-05-15) — ES module (`type="module"`); imports `initIcons` จาก `core/icons.js`. Tab switching (`.driver-tabs__btn`), accordion (`[data-card-toggle]`), `actual_start/end` timestamp on submit (`[data-driver-form] [data-actual-now]`), upload zone visual feedback (`[data-upload-input]`). ไม่มี `onclick=""` → ไม่ต้อง window expose. [ประวัติ →](INDEX_ui_history.md#pagesdriver-homejs-ไฟล์จริง-vehiclejsvehicle_driverjs) |
| `pages/budget-admin.js` *(ไฟล์จริง `vehicle/js/vehicle_budget.js`)* | budget_manage.html (Phase 4.10, 2026-05-16; Phase 5.8, 2026-05-18) — ES module (`type="module"`); imports `initIcons`/`bindModalReinit` จาก `core/icons.js`. Dropdown action menus (data-attr delegation), 3 modal data-bind wirings (topUp/adjust/refund), refund row picker. **Phase 5.8:** ย้าย `setBudgetModal` `show.bs.modal` handler จาก inline `<script>` ใน template → module (swap datalist central/dept, approver pre-select, retitle+relabel, `approverRow.hidden` toggle, `initIcons(modal)` after innerHTML swap). ลบ IIFE wrapper + local `initLucide` + duplicate lucide CDN จาก template. ไม่มี `onclick=""` → ไม่ต้อง window expose. **v2.28 (2026-08-06):** เพิ่ม `initPlanYearChip()` (chip "ปี" → navigate `?plan_year=`, ล้าง `?plan_id=` เดิม) · `initPivotFilter()` ตัด `ddPivotDept` ออก (เหลือกรองแค่ประเภทงบ) · listener ใหม่ `[data-set-default-radio]` (auto-submit ฟอร์มแถวเมื่อเลือก "ตั้งเป็นค่าเริ่มต้น" ในแท็บ "รายชื่องบใหญ่") · `initYearlyPlanPreview()` แก้ `ypDeptPreview.textContent` → `.value` (element เปลี่ยนจาก `<strong>` เป็น `<input>` ตาม WIP cleanup). **Bug fix (2026-08-06 ต่อเนื่อง):** `initYearlyPlanModalMode()` ใหม่ — `yearlyPlanModal` ใช้ร่วม 3 ปุ่ม (ตั้งงบใหม่/แก้ไขก้อนเงิน/ตั้งก้อนงบใหม่) แต่ field ทั้งหมด render จาก plan ที่กำลังดูตรงๆ ทำให้ปุ่ม "สร้างใหม่" ดันไปแก้ไข plan เดิม (ของเก่าหาย) — แก้ด้วย `data-plan-mode="create"/"edit"` บนปุ่ม + listener เคลียร์/คืนค่าฟอร์มจาก snapshot ตอน `show.bs.modal`; ฟอร์ม "ตั้งเป็นค่าเริ่มต้น" เพิ่ม hidden `year`/`month` (ขาดไปทำ POST-redirect 500 เพราะ `int('')`). **Merge setBudget+topUp (2026-08-06):** ลบ `wireModal('budgetTopUpModal', ...)`; `setBudgetModal` handler ขยาย — อ่าน `btn.dataset.bid` เพิ่ม (`hasBudget`), เพิ่ม `sbApplyMode(mode)` toggle `sbSetFields`/`sbTopUpFields` (`d-none`) + hidden `sbAction`/`sbBudgetId` + retitle ตาม mode, listener คลิกบน `#sbModeTabs [data-sb-mode]`. แก้ title icon ที่ inject ผ่าน string จาก `<i data-lucide>` → `<span class="material-symbols-rounded">` ตรงๆ (ห้าม lucide shim) — `initIcons(setBudgetModal)` เดิมไม่ต้องเรียกซ้ำแล้ว. **v2.29 (2026-08-06 ต่อเนื่อง):** action column ย่อจาก dropdown → 2 ปุ่มเห็นตรงๆ (edit icon + toggle) ทั้ง "งบที่ใช้อยู่"/"งบปิดแล้ว" — ตัด trigger "เพิ่มงบ (top-up)" แยกออก (ยังกดถึงได้ผ่าน `sbModeTabs` ในตัว `setBudgetModal` เดิม) และตัด "ปรับยอดใช้ไป" ออกจาก UI ทั้งหมด (ฟังก์ชัน/`budgetAdjustModal` ยังอยู่ แค่ไม่มีทางเข้า) · เพิ่ม listener `form[data-confirm-delete]` (confirm ก่อนลบถาวร) · `initYearlyPlanModalMode()` เปลี่ยนจาก snapshot เดียวตอน page-load → อ่าน `data-plan-*` จากปุ่มที่กดตรงๆ (wireModal-style) รองรับ icon แก้ไขรายแถวในแท็บ "งบหลัก" (เดิม "รายชื่องบใหญ่") แก้ plan ไหนก็ได้ ไม่ใช่แค่ plan ที่กำลังดูอยู่ · `initPlanYearChip()` เลิก navigate ไป `?plan_year=` เปลี่ยนเป็น filter ฝั่ง client ล้วน (ซ่อน/โชว์ `.ue-chip-opt` ใน `#ddYearlyPlan` ตาม `data-plan-year-start/end`). **Bug fix (2026-08-07):** `initYearlyPlanChip`/`initPlanYearChip`/`initPivotFilter` ทั้ง 3 ตัวเคย query checked input ด้วย `dd.querySelector(...)` ตอน event `ue-chip:change` ยิง — แต่ `initUeChipDd` (bb-components.js) portal popover ไป `document.body` ตอนเปิด (กัน overflow clip) และ event ยิงตอน dropdown ยังเปิดอยู่ (คลิก checkbox/radio ก่อนปิด panel) ทำให้ input หลุดออกจาก `dd` subtree ไปแล้ว query จึงหาไม่เจอเสมอ = chip กด filter แล้วไม่ทำอะไรเลยทั้ง 3 ตัว. แก้โดย capture `pop` (`[data-ue-chip-pop]`) ไว้ตอน init ครั้งเดียว แล้ว query จาก reference นั้นแทน `dd` เสมอ (node reference ใช้ query ได้ไม่ว่าจะอยู่ตรงไหนใน document ปัจจุบัน). เพิ่ม `document.addEventListener('submit', ...)` สำหรับ `form[data-confirm-delete-plan]` (confirm ก่อนลบก้อนงบ + งบย่อย cascade). **v2.30 ต่อเนื่อง:** ลบ listener `[data-personal-filter]` ทิ้ง (dead code หลังตัด filter nav ใน `sectionPersonal` ออกจาก template) · เพิ่ม `fmtComma`/`parseComma`/`stripComma`/`bindMoneyInputFormat` (helper ใหม่ระดับ module — comma thousand-separator mask สำหรับ `#ypTotal`/`#ypCentral`, คง caret position ระหว่างพิมพ์) `initYearlyPlanModalMode` เรียก `bindMoneyInputFormat` ตอน setup + prefill ตอน edit ผ่าน `fmtComma()` + submit handler strip comma ก่อนปล่อยให้ native submit ทำงาน; `initYearlyPlanPreview` เปลี่ยนมา parse ผ่าน `parseComma()`. [ประวัติ →](INDEX_ui_history.md#pagesbudget-adminjs) |
| `pages/room.js` | room.html — ES module (`type="module"`). **Migrate 2026-08-05 (ตาม `pages/vehicle.js`):** เลิก import `core/icons.js` (ไม่ต้องมี — Material Symbols เป็น `<span>` static ไม่ต้อง JS render เหมือน Lucide SVG); ตัด flatpickr (`initFlatpickrInEditModal`) → date/time ผ่าน `DateField`/`TimeRangeField` component (`bkSetDate`/`bkSetTimeRange`/`bkDateFieldEl`/`bkTimeRangeEl`, event `bb-datefield:change`/`bb-timerangefield:change` จัดการใน `bb-components.js`); **book+edit รวม `#bookingForm` เดียว** `bkSetMode('create'\|'edit')` แทน `editBookingModal` แยก. `roomKind(room)` → 'small'/'large' จากชื่อห้อง (เล็ก/ใหญ่) ไม่เปลี่ยน. Exposes `openEventDetail`/`openEditBookingModal`/`openBookingModal`/`openDuplicateModal` + `eventDetailModal` getter ไป `window.*` (รวม `openDuplicateModal` — ใช้โดยปุ่ม "ทำซ้ำ" ใน `dashboard.html`, `?copy_from=` deep-link จาก `auth_view.py`). [ประวัติ →](INDEX_ui_history.md#pagesroomjs) |
| `pages/manage-fleet.js` (ไฟล์จริง `vehicle_fleet.js`) | vehicle_fleet.html (Phase 6.2, 2026-05-17) — plain IIFE (no ES module — uses legacy `defer` script tag + Bootstrap Modal globals). Bind modals on `show.bs.modal` (add/edit vehicle รวม 1 modal, delete vehicle, add/edit driver รวม 1 modal, delete driver, driver detail, vehicle history, **`#idcardPreviewModal` เพิ่ม 2026-08-05**), fetch `/api/vehicle/{id}/history` (loading/content swap). **เพิ่ม 2026-08-05:** avatar circle preview/remove (`setAvatarPreview`/`bindAvatarUpload` — ย้าย+แก้บั๊กจาก inline `<script>` เดิมในเทมเพลตที่ลบทิ้งแล้ว, เดิม `reader` ถูกอ้างนอก scope + input ไม่มี `name=`) · id-card dropzone/uploaded-card toggle + preview modal (`setIdcardState`/`bindIdcardUpload`/`bindIdcardPreview`, blob URL ผ่าน `URL.createObjectURL` + revoke กัน memory leak) · driver status toggle คลิกจากตาราง (`document` click delegation `.mf-driver-toggle-active` → fetch `admin_driver_toggle_active`) · "งานในสัปดาห์" chevron นำทางจริง (`bindFleetWeekNav()` → fetch `admin_driver_week`; **ห้ามใช้ `d.toISOString()` หลัง `setDate()`** — แปลงเป็น UTC ก่อน slice ทำให้วันเพี้ยนไป 1 วันถ้า browser timezone +7 (ICT) ต้อง build date string จาก local `getFullYear/getMonth/getDate` เอง, ดู `toIsoDate()`). Refresh Lucide icons on `shown.bs.modal` (catches dynamically inserted icon `<i>`). [ประวัติ →](INDEX_ui_history.md#pagesmanage-fleetjs-ไฟล์จริง-vehicle_fleetjs) |
| `pages/dashboard.js` | dashboard.html (2026-05-17) — ES module (`type="module"`); imports `initIcons`/`bindModalReinit` จาก `core/icons.js`. Live clock (`#liveClock`) — `Intl.DateTimeFormat` th-TH weekday+date + time, refresh 30s. ไม่มี `onclick=""` → ไม่ต้อง window expose. |
| `main.js` | **orphan ตั้งแต่ 2026-06-16** — login.html เลิกใช้ (ย้ายไป `auth/js/login.js`). jQuery IIFE legacy form validation |

**Icon libraries:**
- Font Awesome (`fa-solid` / `fa-regular`) — global default ใช้อยู่ทุกหน้า
- Lucide Icons (line, stroke 1.5px) — โหลด global ใน `_header.html` (CDN unpkg). ใช้: `<i data-lucide="fuel"></i>`. หลัง DOM update เรียก `window.lucide.createIcons()`. ใช้สำหรับ Vercel namespace (Phase 2 fuel page)

---

> Patterns ที่ซ้ำซาก (booking status, telegram, in-app notify, budget mutation) → ดู CLAUDE.md § Gotchas
> Maintenance Protocol → ดู CLAUDE.md
